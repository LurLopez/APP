/**
 * @fileoverview Estado compartido del módulo EmpresaMetricsState.
 */

(function (window) {

window.EmpresaMetricsState = {
  metricsChartState: null,
  compareSearchDebounceTimer: null,
  chartPendingSeriesId: null,
};

})(window);
