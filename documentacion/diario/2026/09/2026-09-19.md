## 14:02 — Resumen: Traducción automática del análisis nuevo a los dos idiomas

Se reportó que, tras hacer un análisis en castellano, al verlo en inglés aparecía el diálogo de confirmación («Este análisis ya existe en otro idioma… consume cupo») en lugar de estar ya traducido; se esperaba que un análisis nuevo quedase disponible en ambos idiomas automáticamente.

### Resultado

- **Traducción automática en segundo plano**: nuevo servicio `src/services/translation/autoTranslate.service.js`. Tras guardar un análisis nuevo se dispara `scheduleLanguageVariants(...)`, que traduce y guarda la variante del otro idioma sin bloquear la respuesta y **sin consumir cupo del usuario** (la variante queda cacheada, con la misma visibilidad que el original).
- **Un único proceso por filing**: `inFlight` deduplica por `ticker:accession:idioma`; si el usuario pide el otro idioma mientras la traducción automática sigue en curso, `joinAutoTranslation` espera al mismo proceso y sirve la variante recién guardada (sin 409 ni coste). Añadido `findDoneAnalysisByFilename` al repositorio para localizar la variante por nombre de archivo.
- **Puntos de enganche**: análisis de filing por ticker/accession (`filing.analysis.controller.js`), subida manual de PDF (`analysis.upload.controller.js`, variante privada) y regeneración del administrador (`adminReports.controller.js`). El flujo confirmado por el usuario (409 `LANGUAGE_VARIANT_REQUIRED` → `confirmLanguage`) sigue consumiendo cupo como hasta ahora.
- **Historial**: la tabla de análisis muestra un badge con el idioma de cada informe (ES/EN) para distinguir las variantes (`analisisHistory.js` + estilo `.analysis-type-pill.language`).
- **Tests**: nuevo `tests/unit/autoTranslate.test.js` (7 pruebas: idiomas pendientes, clave estable, traducción de la variante que falta, no repetir existentes, propagación de visibilidad/usuario y join de la traducción en curso). Suite completa **216/216**.

## 14:20 — Resumen: Corregido el 500 al analizar un filing (variable fuera de ámbito)

Se reportó que al pulsar «Analizar» la página mostraba «No se pudo verificar el documento / Internal server error» aunque el análisis sí se generaba y se podía ver después.

### Resultado

- **Causa**: en `filing.analysis.controller.js` la constante `options` se declaraba dentro del bloque `try` de generación y se usaba fuera de él en `scheduleLanguageVariants` (`options.filename`, `options.isPublic`), lo que lanzaba `ReferenceError: options is not defined` y el middleware global devolvía el 500 genérico. El análisis se había guardado antes del error, de ahí que luego apareciera en el histórico.
- **Corrección**: `filename` se calcula antes del `try` y la planificación de la traducción usa ese valor y `isPublic: true` en lugar de `options`. Reproducido y verificado con el servidor real (puerto 3100): análisis nuevo de MO responde **HTTP 200** y genera la variante EN automáticamente (`opencode · traducción`); petición concurrente en el otro idioma servida sin 409. Suite **216/216**.


## 14:38 — Resumen: Cambios en la dirección: ocultar campos sin información y traducir sus etiquetas

Se reportó que en la sección «Cambios en la dirección» aparecían textos sin traducir (etiquetas de campo en español en un análisis en inglés) y líneas de relleno tipo «Trayectoria previa: No public information available» cuando el informe no aportaba el dato; se pidió ignorar por completo esos campos vacíos.

### Resultado

- **Ocultar rellenos de «sin información»**: nuevo detector `isNoInfoValue` (ES/EN: «No public information available», «No se dispone de información pública verificada», «No consta», «N/A», «None», «—»…), exportado desde `src/services/reportExport/executiveChanges.js` y replicado en el frontend (`analisisTablesReport.js`). `getExecutiveChanges` ahora descarta esos valores, elimina bloques de directivo que se quedan vacíos y omite la sección completa si ningún relevo tiene datos reales; también aplica en el pipeline de generación (`cleanReportText` de `annualConclusionCeoRepurchases.js`) y en el PDF (`pdfRepurchasesDrawer.js`), exportadores HTML/DOCX/ODT, Markdown SEO y HTML SSR.
- **Traducción de etiquetas**: `renderCeoFieldLine` pasa las etiquetas de los campos por `tr(...)` (usan el idioma del informe), corrigiendo la mezcla de español/inglés; los títulos de reserva también se traducen.
- **Prompts y esquema**: `analystAnnualSystemPrompt.js` y `analystExtractionPrompt.js` prohíben frases de relleno (se omiten los campos o se usa `null`); el ejemplo de `analystAnnualSchema.js` ya no contiene «No se dispone de información…».
- **Tests**: 2 pruebas nuevas en `fallbacksResilience.test.js`; suite completa **218/218**.

## 14:41 — Resumen: Corregidos los botones «Ajustado» de las tarjetas PER y Payout

Se reportó que los checkboxes «Ajustado» de las tarjetas PER y Payout (pestaña Valoración) no respondían al pulsarlos.

### Resultado

- **Causa**: `setPeAdjusted` (`empresaValuationChartData.js`) actualizaba la variable léxica global `valPeAdjusted` (declarada con `let` en `empresa.js`), pero `renderValuation` lee `window.valPeAdjusted`, que solo se escribía una vez en el arranque (`empresaBoot.js`). Al re-renderizar se usaba el valor antiguo y se volvía a marcar la casilla: el clic parecía no hacer nada.
- **Corrección**: `setPeAdjusted` sincroniza también `window.valPeAdjusted`; cache-busting de `empresaValuationChartData.js` a `?v=3` en `index.html` y `empresa.html`.
- **Verificación** (Chrome headless con CDP sobre `/empresa/KO`): al desmarcar, PER 26,74x → 27,75x, BPA 3,30 → 3,18 $ y payout 62,42 % → 64,78 %; los tres checkboxes (PER, Payout y gráfico) se sincronizan y al volver a marcar se restauran los valores ajustados. Suite **218/218**.


## 15:05 — Resumen: Corregido «Inicia sesión» en Mis análisis con la sesión iniciada

Se reportó que con la sesión iniciada (usuario visible en la barra lateral y botón «Salir») el panel «Mis análisis» seguía mostrando «Inicia sesión para guardar tus análisis y consultarlos aquí» y el botón «Iniciar sesión».

### Resultado

- **Causa**: `AS.currentUser` (bandera que decide el estado del historial) solo se actualizaba al recibir el evento `auth:change`, pero ese evento se dispara al resolver `/api/auth/me` y podía ocurrir antes de que `analisis.js` registrase su listener (o antes de que `analisisInit.js` publicase `window.setAuthenticated`), por lo que la sesión ya cargada se perdía. Al navegar a la sección Análisis, `empresaShell` llama a `fetchAnalyses()`, que al ver `AS.currentUser === false` pinta el aviso de inicio de sesión.
- **Corrección**: `analisisInit.js` incorpora `syncAuthState()`, que usa el patrón ya establecido en watchlists/portfolio (`AuthModule.isReady()` / `whenReady()`): si la sesión ya está cargada sincroniza el usuario al inicializar; si aún no lo está, espera a `whenReady`. Además, `init()` ahora también se ejecuta si el script llega con el DOM ya cargado. Cache-buster `analisisInit.js?v=2` en `index.html` y `empresa.html`.
- **Verificación**: smoke tests con `vm` sobre `analisisInit.js` para los tres caminos (sesión lista, `whenReady` tardío y anónimo): `AS.currentUser` y `fetchAnalyses()` correctos en todos. Suite completa **218/218**.

## 17:54 — Resumen: Medias móviles en el gráfico «Evolución del múltiplo por sesión» (Valoración)

Se pidió que el gráfico de evolución de múltiplos (pestaña Valoración) tuviese medias móviles de 100, 500 y 1000 sesiones y personalizadas, igual que el gráfico de cotización.

### Resultado

- **UI equivalente al gráfico de cotización**: chips `MA 100 / MA 500 / MA 1000` activables, botón `+ MA` con popover para activar/desactivar, editar sesiones (2–5000), eliminar y añadir ventanas personalizadas (presets 100/500/1000). Nuevo módulo `public/js/empresa/empresaValuationChartMa.js` (config propia en `localStorage`, clave `cifra_val_chart_ma_config_v1`, por defecto las tres activas); marcado añadido a `empresa.html` e `index.html` y cableado en `wireValuationChartInteractions`. Las MA se ocultan al seleccionar «Deuda Neta/EBITDA» (gráfico de barras anual).
- **Cálculo con lookback completo**: las medias se calculan en cliente sobre el histórico completo descargado. Para que la línea arranque desde el borde izquierdo con ventanas largas, `/api/screener/company/:ticker/valuation` acepta `?buffer=<días>` (`getValuationSeries` amplía el rango de precios, máx. 7305 días) y devuelve `rangeFrom`; el cliente filtra los puntos del rango visible y calcula las medias sobre la serie extendida. Al añadir una ventana mayor de lo descargado se recarga solo con el buffer necesario (misma heurística que el gráfico de cotización: `sesiones × 1,6 + 60` días).
- **Render y hover**: líneas discontinuas con el color de cada MA incluidas en la escala del gráfico (no se recortan), puntos de cada media y valores `MA n: x,xx×` en el tooltip; la curva más cercana al cursor tiñe el badge del eje y el indicador de cabecera, igual que en cotización. La comparación con clic derecho usa la misma escala.
- **Verificación**: 16/16 comprobaciones con Chrome headless/CDP sobre `/empresa/KHC` (chips, 3 líneas, buffer y filtrado, lookback en el borde en 5Y y 1Y, hover con puntos y valores, alta/baja de MA personalizada, recarga al añadir MA 2000, cambio de métrica y de rango, gráfico de barras) y capturas revisadas; suite completa **223/223**. Cache-busters actualizados (`empresaValuationChartData/Hover/Render/Chart` y `empresa.js?v=81`).

## 17:40 — Resumen: Calendario de vencimientos de deuda incorrecto en KO (rangos "due 2024-2093")

Se reportó que el gráfico del calendario de deuda del informe KO 2023-10K mostraba importes erróneos (21.982M + 12.888M en 2024 y 684M en 2028, con total de 35.928M) cuando el texto del análisis citaba los vencimientos reales (1.960M/1.070M/1.810M/4.616M/2.770M). Se pidió corregirlo garantizando que el resto de informes siguiera funcionando.

### Resultado

- **Causa**: la nota de deuda de KO agrupa emisiones por rangos amplios ("U.S. dollar notes due 2024-2093", "Euro notes due 2024-2041") y `buildMaturityScheduleFromDebtTable` asignaba cada agregado al primer año del rango. La tabla año a año real ("maturities of long-term debt for the five years succeeding December 31, 2023") no se parseaba.
- **Fix determinista**: nuevo parser de la tabla explícita "maturities of long-term debt" en `buildMaturityScheduleFromFilingText` (filas "2024 $1,960", "2025 $1,070"…, con "Thereafter" para después del año 5), marcado `authoritative`. `buildMaturityScheduleFromDebtTable` ahora descarta las filas con rango multi-año (antes las asignaba al primer año) y devuelve null si lo descartado supera lo aprovechable. `recoverAnnualMaturities` prefiere la tabla año a año cuando el calendario actual falta o trae rangos amplios (`hasWideRangeLabels`, ≥5 años); los rangos cortos (TAP "2026-2027", PEP "2020-2021") mantienen el comportamiento previo.
- **Prompts**: la pasada focalizada, la extracción y el prompt anual priorizan la tabla explícita año a año y prohíben asignar al primer año los rangos de 5+ años.
- **Datos ya guardados**: nuevo `scripts/backfill-debt-maturities.js` (dry-run por defecto, `--apply` para guardar) que reescribe el calendario de análisis anuales desde el PDF del filing. Aplicado a 7 análisis de KO (ids 763, 771, 762, 761, 758, 306, 273); verificado en vivo: /informe/KO/2021-10K … /2025-10K ya muestran los vencimientos oficiales. Resto de informes sin cambios (solo se parchean calendarios con rangos amplios o etiquetas de clasificación de balance).
- **Tests**: 5 pruebas nuevas (KO 10-K FY2023, rangos amplios vs cortos, arrendamientos operativos, fila Thereafter); suite completa **223/223**.

## 18:05 — Resumen: Revisión y optimización integral de SEO y GEO (Generative Engine Optimization)

Se realizó una auditoría completa y optimización final de SEO técnico y GEO (motores generativos e IA) para todo el sitio (`cifraresearch.com`), asegurando coherencia bilingüe (ES/EN), etiquetado semántico enriquecido y compatibilidad con rastreadores de IA modernos.

### Resultado

- **Rastreadores y robots.txt**:
  - `public/robots.txt`: ampliadas las exclusiones `Disallow` para cubrir todas las rutas privadas en español e inglés (`/calendario`, `/admin/reportes`, `/en/seguimiento`, `/en/cartera`, `/en/calendario`, `/en/analisis`, `/en/análisis`, `/en/alertas`, `/en/alertas-precio`, `/en/novedades`, `/en/reportes`, `/en/admin/`, `/en/logout`).
  - Añadidas directivas explícitas `Allow` para las rutas públicas en inglés (`/en`, `/en/empresa`, `/en/guias`, `/en/legal`, `/en/informe/`).
  - Incorporados User-Agents de rastreadores y motores de IA de 2026: `GoogleOther`, `GoogleOther-Image`, `GoogleOther-Video`, `DeepSeekBot`, `DuckAssistBot`, `YouBot`, `searchgpt`, `Perplexity-User`, `cohere-training-data-crawler`.
- **GEO y llms.txt**:
  - `public/llms.txt` y endpoints dinámicos (`/llms.txt`, `/llms-full.txt` en `sitemapAndLlms.service.js`): actualizadas las 9 guías financieras completas (añadidas las dos faltantes: `como-analiza-la-ia-por-sectores` y `que-es-el-bpa-ajustado`), URLs y títulos localizados en inglés, e indexación de endpoints en Markdown estructurado (`/empresa/<TICKER>.md`, `/informe/<TICKER>/<PERIODO>.md`, `/en/empresa/<TICKER>.md`, `/en/informe/<TICKER>/<PERIODO>.md`).
- **Tablas de datos para IA y bots**:
  - En `src/services/seo/botContent.service.js` y `src/services/seo/markdownSeo.service.js`: añadida la métrica de «Beneficio operativo» / «Operating income» a las tablas anuales SSR y Markdown, aumentando la riqueza de datos fundamentales consumidos por LLMs.
- **Hreflang y Canonical en HTML**:
  - Inyectadas etiquetas `<link rel="alternate" hreflang="en" ...>` en `public/index.html`, `public/empresa.html`, y en todas las plantillas de guías y páginas legales (`src/content/guias/*.html`, `src/content/legal/*.html`).
  - Corregido bug de duplicidad de `x-default` en `src/services/seo/reportSeo.build.service.js` eliminando los tags hreflang de portada previos antes de inyectar las etiquetas específicas del informe.
- **Localización de JSON-LD y Schemas**:
  - `src/services/seo/pageRenderer.service.js`: `serveStandalone` ahora localiza dinámicamente schemas JSON-LD para guías y legales servidas en `/en/` (`inLanguage: 'en'`, títulos/descripciones en inglés, URLs canónicas en `/en/`, BreadcrumbList adaptada y serialización segura con `safeJsonForScript`). `serveHtml` localiza además el atributo `og:image:alt`.
  - `src/services/seo/companyMeta.service.js`: reemplazado `JSON.stringify` directo por `jsonLdScript(...)` seguro para evitar inyección y rotura de etiquetas HTML en datos de empresas.
  - `src/services/seo/featuredCompanies.service.js`: fallback robusto con mapeo a benchmark cuando el nombre de la empresa no está disponible en la base de datos.
- **Documentación y Tests**:
  - Actualizado `documentacion/seo.md` con las especificaciones actualizadas (guías, bots GEO, endpoints multilingüe).
  - Actualizado `tests/unit/seoJsonLd.test.js` con tests para localización de JSON-LD, cobertura de robots.txt y verificación de llms.txt. Suite completa en verde: **226/226**.

## 18:50 — Resumen: Corrección del análisis 10-K de Adobe (ADBE) y protección contra PDFs con fuentes corruptas

Se reportó que el informe anual de Adobe (`ADBE-2025-K.pdf`) presentaba resultados deficientes (comparativas del año anterior a 0, notas aludiendo erróneamente a un 8-K, descuadre de 8.657M en asignación de capital, omisión de recompras de acciones y datos anómalos de deuda neta), a diferencia de las empresas de consumo defensivo.

### Resultado

- **Detección de fuente corrupta en SEC EDGAR**: la SEC incluía en el índice de Adobe un PDF «no oficial» (`adbe10kfy25unofficialpdf.pdf`) con codificación de glifos embebidos sin CMaps estándar ToUnicode. `findFilingPdfUrl` seleccionaba ese PDF en lugar de descartarlo; al extraer el texto con `pdf-parse`, se obtenían caracteres no imprimibles y desplazados (`)"\u001d(\u0019\u0018...`), provocando que el LLM recibiera texto ilegible y alucinara la falta de estados financieros.
- **Validación obligatoria de legibilidad de PDFs**: nueva función [`isReadablePdfText`](file:///home/lur/Lur/APP/src/services/pdf.service.js) que verifica la ausencia de fuentes corruptas (proporción de caracteres de control ≤ 5 %) y la presencia de terminología contable estándar. Integrada en [`analyzePdf`](file:///home/lur/Lur/APP/src/services/analysis.service.js), [`getFilingPdfPath`](file:///home/lur/Lur/APP/src/services/edgar/filingDocuments.js) y [`getFilingContentBuffer`](file:///home/lur/Lur/APP/src/services/edgar/filingDocuments.js).
- **Exclusión de PDFs no oficiales de EDGAR**: [`findFilingPdfUrl`](file:///home/lur/Lur/APP/src/services/edgar/filingDocuments.js) ahora descarta archivos con `unofficial` y exige coincidencia exacta de nombre base con el documento principal; ante PDFs corruptos o no coincidentes, renderiza siempre el HTML oficial de EDGAR con Chromium headless (`generateFilingPdf`).
- **Corrección de escala de deuda neta negativa (caja neta)**: en [`buildEdgarDebtHistory`](file:///home/lur/Lur/APP/src/agents/analyst/analystRunSteps.js), la condición `netDebt > 1e6` impedía dividir por $10^6$ los saldos de deuda neta negativos (empresas con posición de caja neta, muy habituales en tecnología como Adobe). Se actualizó a `Math.abs(netDebt) > 1e6`.
- **Soporte de ticker y fallback EDGAR**: añadido `ADBE` a `WELL_KNOWN_ORIGINS` y `KNOWN_TICKER_OVERRIDES` para resolución instantánea y resistencia frente a límites de tasa (HTTP 429) de la SEC.
- **Regeneración de ADBE 2025-K**: regenerado el informe completo (ID 778) y actualizada la copia local en `/home/lur/Documentos/ADBE-2025-K.pdf` con todos los bloques completos (ventas +10,53 %, FCF 9.852M$, recompras de 11.281M$, cambios directivos, calendario de deuda y gráfico histórico de 10 años).
- **Tests**: 2 pruebas añadidas en [`tests/unit/sectorTechnology.test.js`](file:///home/lur/Lur/APP/tests/unit/sectorTechnology.test.js); suite completa pasando **233/233**.

## 19:40 — Resumen: Ajuste analítico de Stock Options / Compensación en Acciones (SBC al 120 %) en Cash Flow

Se implementó el criterio conservador de deducción del **120 %** de la compensación basada en acciones (Stock-Based Compensation / SBC) en la columna **Ajustado** del bloque 2 (Cash Flow). Aunque en US-GAAP el SBC se añade de vuelta al flujo operativo por no requerir desembolso inmediato de efectivo, representa una dilución patrimonial directa del accionista y suele concederse con descuento sobre el precio de mercado, por lo que bajo una visión analítica conservadora se resta el 120 % de dicha partida ($\text{Ajuste SBC} = -1,20 \times \text{SBC}$).

### Cambios realizados

- **Extracción de SBC y fallbacks**:
  - Actualizado [`analystExtractionSchema.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionSchema.js) y [`analystExtractionPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionPrompt.js) para extraer `stockCompensation` en `cashFlow` y en `facts` (`stockCompensationQuarter`, `stockCompensationYtd`).
  - Implementada función de extracción regex [`extractStockCompensation`](file:///home/lur/Lur/APP/src/agents/analyst/financialParsersExtract.js) exportada en [`financialParsers.js`](file:///home/lur/Lur/APP/src/agents/analyst/financialParsers.js).
  - Integrados fallbacks en [`analystRunSteps.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystRunSteps.js) tanto desde el texto del filing como desde los facts XBRL oficiales de SEC EDGAR (`values.stockCompensation`).
- **Cálculo y cascada de Cash Flow**:
  - En [`normalizeCashFlowBlock`](file:///home/lur/Lur/APP/src/agents/analyst/analystCashCapitalProcessor.js), se calcula la deducción $\text{sbcAdjustment} = -1,20 \times \text{SBC}$.
  - Se aplica sobre el Cash Flow Ajustado y recalcula en cascada el FCF Ajustado, el FCF/Acción Ajustado y el remanente Libre.
  - La celda de Cash Flow Ajustado conserva el resalte visual (`cashFlowAdjustedNote = '*2'`).
  - Se genera la nota explicativa (`*2` o `*3`, según si aplica o no ajuste fiscal previo) detallando la partida reportada, el 120 % deducido y la frase de conciliación matemática completa con los ajustes de circulante, impuestos y stock options.
  - Actualizado [`buildCashFlowAdjustmentChain`](file:///home/lur/Lur/APP/src/agents/analyst/analystCashCapitalProcessor.js) y el diccionario [`public/locales/en.json`](file:///home/lur/Lur/APP/public/locales/en.json) con soporte bilingüe completo (ES/EN).
- **Reglas del Analista**:
  - Documentada la regla en [`src/agents/knowledge/general.md`](file:///home/lur/Lur/APP/src/agents/knowledge/general.md) (Bloque 2, punto 5) y en [`src/agents/knowledge/tecnologia/sector.md`](file:///home/lur/Lur/APP/src/agents/knowledge/tecnologia/sector.md) (punto 2), respetando estrictamente los números de versión de las reglas.
- **Regeneración y Verificación**:
  - Regenerado el análisis de Adobe 2025 Form 10-K (análisis ID 780): Cash Flow Ajustado $7.211,9\text{M}$ (frente a $10.031\text{M}$ normal), FCF Ajustado $7.032,9\text{M}$ ($17,03\text{ \$}$/acción vs $23,85\text{ \$}$ normal), con desglose exacto: $10031\text{M} - 662,8\text{M}\text{ (circulante)} + 174,1\text{M}\text{ (impuestos)} - 2330,4\text{M}\text{ (stock options al 120 \%)} = 7211,9\text{M}$.
  - Actualizado el archivo PDF en `/home/lur/Documentos/ADBE-2025-K.pdf`.
  - Añadidas pruebas unitarias en [`tests/unit/sectorTechnology.test.js`](file:///home/lur/Lur/APP/tests/unit/sectorTechnology.test.js); suite completa pasando **240/240**.

## 20:05 — Resumen: Calendario de vencimientos de deuda incorrecto en Adobe (tabla con columna "Due Date")

Se reportó que el calendario de deuda del informe ADBE 2025-10K mostraba un único vencimiento erróneo de **1.500M$ en 2025** (deuda ya amortizada en febrero de 2025) mientras el texto afirmaba que el 10-K no desglosaba los vencimientos. Se pidió corregirlo sin afectar al resto de informes.

### Resultado

- **Causa 1 (extracción)**: la nota `NOTE 17. DEBT` de Adobe lista cada tramo con columnas "Issuance Date" / "Due Date" en mes y año, sin día ("1.90% 2025 Notes February 2020 February 2025 2.07% $ — $ 500"). `extractDebtFilingText` no reconocía ese encabezado y caía en la mención de riesgo "debt obligations" del Item 1A; `buildMaturityScheduleFromFilingText` no tenía parser para ese formato.
- **Causa 2 (XBRL)**: `buildDebtMaturitiesFromFacts` infiere un calendario parcial de `LongTermDebtCurrent`; en Adobe el último saldo anual era el del ejercicio 2024 (1.499M$ a vencer en 2025) y `buildAnnualEdgarData` lo aceptaba para el informe de FY2025, pintando como futuro un vencimiento ya pagado.
- **Fix determinista**:
  - Nuevo parser `parseMonthYearMaturityRows` en [`debtMaturityFallback.js`](file:///home/lur/Lur/APP/src/agents/analyst/debtMaturityFallback.js): lee el año de la fecha de vencimiento (la última del tramo) y el importe de la columna del ejercicio actual; si el saldo actual es "—" el tramo ya se amortizó y se omite (las notas de 2025 de Adobe no se pintan). Cupón preferente del nombre del tramo (2,15 % para las 2027) y los importes posteriores al año 5 van a `maturityAfterFive`.
  - Nueva ventana de extracción en [`filingExtractor.js`](file:///home/lur/Lur/APP/src/agents/analyst/filingExtractor.js) para encabezados "Issuance Date … Due Date".
  - Guard en [`analystRunSteps.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystRunSteps.js): el calendario parcial (`partial: true`) del XBRL solo se acepta si su `baseYear` coincide con el ejercicio del informe (se exportó `buildAnnualEdgarData` para testearlo).
- **Backfill**: [`scripts/backfill-debt-maturities.js`](file:///home/lur/Lur/APP/scripts/backfill-debt-maturities.js) ampliado (calendarios parciales de EDGAR y clasificaciones de balance, con sustitución determinista desde la nota, corrección de la frase obsoleta sobre el calendario y regeneración de PDF/HTML/DOCX/ODT; nuevo filtro `--ids=`). Aplicado a 7 análisis de Adobe (776-781) y a KDP 265; verificado en vivo: el calendario muestra **2027: 1.350M$** (850 + 500), **2028: 800M$**, **2029: 750M$**, **3.250M$ después del año 5** y tipo medio estimado del 3,98 %. Actualizado `/home/lur/Documentos/ADBE-2025-K.pdf`. PG, KO y CAG se descartaron tras comprobar que sus calendarios guardados ya eran correctos (PG usa años fiscales oficiales).
- **Tests**: 3 pruebas nuevas (nota real de Adobe, extracción de la ventana y descarte del calendario parcial del XBRL); suite completa **243/243**.

## 20:20 — Resumen: "Cambios en la dirección" del 10-K de Adobe mostraba una bio de 2007/2017

Se preguntó por qué el informe ADBE 2025-10K mostraba en «2: Cambios en la dirección» un cambio de enero de 2017 (nombramiento de Narayen como CEO en diciembre de 2007 y como Chair en enero de 2017).

### Resultado

- **Causa**: la extracción determinista [`extractExecutiveChangesFromText`](file:///home/lur/Lur/APP/src/agents/analyst/financialParsersExtract.js) capturó la bio de los directivos del 10-K («appointed our Chief Executive Officer» de 2007) como si fuera un relevo del ejercicio, y `applyExecutiveChangesFallback` no filtraba por antigüedad.
- **Filtro de relevos históricos**: nueva `isStaleExecutiveChange(change, fiscalYear)`, que solo usa las fechas declaradas del cambio (`announcementDate`/`effectiveDate`) para no confundir la antigüedad del directivo saliente con la fecha del relevo (caso Campbell's: la sucesión de Mick Beekhuizen menciona «desde 2019» y debe conservarse). El extractor determinista recorre las siguientes coincidencias hasta encontrar un relevo vigente y `applyExecutiveChangesFallback` (que ahora recibe `fiscalYear`) descarta también los históricos que traiga la IA, eliminando la clave si no queda ninguno.
- **Backfill**: nuevo [`scripts/backfill-executive-changes.js`](file:///home/lur/Lur/APP/scripts/backfill-executive-changes.js) (dry-run por defecto, `--apply`). Aplicado a Adobe 778-782: sección eliminada y numeración recalculada (Deuda pasa a «3: Deuda»), formatos regenerados y `/home/lur/Documentos/ADBE-2025-K.pdf` actualizado desde el análisis 782. Los cambios de CPB (588, 591, 753) se conservan intactos.
- **Tests**: 4 pruebas nuevas (bio de Adobe, filtro de fechas, descarte de históricos de la IA y conservación de relevos recientes); suite completa **247/247**.

## 20:40 — Resumen: Stock Options (SBC) al importe íntegro en el Cash Flow Ajustado (sin el 120 %)

Se pidió que en la partida de stock options del FCF se reste únicamente su importe, sin el 20 % adicional del criterio anterior.

### Resultado

- **Código**: [`normalizeCashFlowBlock`](file:///home/lur/Lur/APP/src/agents/analyst/analystCashCapitalProcessor.js) ajusta ahora `sbcAdjustment = -SBC` (antes `-1,20 × SBC`) y recalcula en cascada FCF, FCF/Acción y Libre; la etiqueta de la cadena pasa a `stock options` y la nota explicativa cuantifica «el importe íntegro de esta partida (-{sbcAdjustment}M)».
- **Reglas**: [`general.md`](file:///home/lur/Lur/APP/src/agents/knowledge/general.md) (Bloque 2, punto 5) y [`tecnologia/sector.md`](file:///home/lur/Lur/APP/src/agents/knowledge/tecnologia/sector.md) (punto 2) actualizadas al criterio del importe íntegro, respetando los números de versión de las reglas.
- **Guías**: hub, guía del 10-Q, Datos Financieros y Análisis IA corregidas (ya no recomiendan ajustar/un 20 % adicional).
- **i18n**: diccionario [`en.json`](file:///home/lur/Lur/APP/public/locales/en.json) con las claves nuevas (nota SBC, `stock options`, fragmentos de guías) y `_sources.json` alineado con los textos vigentes.
- **Datos guardados**: regenerado el análisis de Adobe 2025 Form 10-K (ID 783). La nota queda `*3: ... importe íntegro de esta partida (-1942M) ... -1942M (stock options)` y el FCF Ajustado en 7.421,3M$; los análisis 780-782 conservan el criterio antiguo. Actualizado `/home/lur/Documentos/ADBE-2025-K.pdf` y verificado en vivo `/informe/ADBE/2025-10K`. Traducida la variante inglesa actualizada (ID 784), que sustituye a la antigua 777 en la versión EN del informe.
- **Tests**: [`sectorTechnology.test.js`](file:///home/lur/Lur/APP/tests/unit/sectorTechnology.test.js) actualizado a los nuevos importes; suite completa **247/247**.

## 21:15 — Resumen: Corrección de volúmenes en tecnología (Nvidia) y coordinación de color en notas de Cash Flow (*2 y *3)

El usuario reportó que en el análisis de Nvidia el sistema tomaba volúmenes planos (0%), lo cual no tiene sentido para una empresa tecnológica de alto crecimiento, y que las notas de Stock Options e Impuestos deben tener el mismo color que la casilla de Cash Flow Ajustado que ambas modifican (`*2` y `*3` con el mismo color).

### Diagnóstico

1. **Volúmenes planos en Tecnología**:
   - `EXTRACTION_PROMPT` y los prompts de análisis asumían la hipótesis de consumo defensivo («si el informe no proporciona volumen, volumeGrowth es obligatoriamente 0 y la inflación sectorial es 3 %»).
   - En empresas tecnológicas como NVIDIA, no existen unidades físicas de volumen homogéneo (cajas, hectolitros); según la regla 5 de [`tecnologia/sector.md`](file:///home/lur/Lur/APP/src/agents/knowledge/tecnologia/sector.md), el multiplicador del circulante teórico es el **crecimiento de ventas** ($\text{Crecimiento de ventas}$), que en NVIDIA superó el +114 %. Asumir 0 % de volumen y 3 % de inflación desvirtuaba el cálculo teórico del Working Capital.
   - `buildWorkingCapitalDataFallback` en [`capitalAllocationHelpers.js`](file:///home/lur/Lur/APP/src/agents/analyst/capitalAllocationHelpers.js) aplicaba siempre la fórmula `(inflación + volumen)` con `3% + 0%` sin discriminar el sector.
2. **Color de resalte en Cash Flow (`*2` y `*3`)**:
   - Tanto la normalización fiscal (`*2`) como la deducción de Stock Options / SBC (`*3`) modifican la misma celda de la tabla (`Cash Flow` en columna `Ajustado`).
   - La celda se resaltaba con el color de la nota 2 (`highlight-c2`, morado en tema oscuro / naranja corporativo). Sin embargo, `renderNotes` coloreaba la nota `*3` con `highlight-c3` (verde), rompiendo la correspondencia visual con la casilla ajustada.

### Cambios realizados

- **Capital circulante en Tecnología (`capitalAllocationHelpers.js` y `analystAgentCore.js`)**:
  - `buildWorkingCapitalDataFallback` detecta el sector tecnológico (por parámetro, por `extracted.sector` o resolviendo el ticker mediante [`resolveSectorByTicker`](file:///home/lur/Lur/APP/src/agents/sectorAgent.js)).
  - Para tecnología calcula la tasa real de crecimiento de ventas del periodo (`resolveSalesGrowth`) y aplica la fórmula sectorial:
    $$\text{WC} = (\text{Cuentas por pagar} - \text{Inventarios} - \text{Cuentas por cobrar}) \times (\text{crecimiento de ventas})$$
  - Redacción bilingüe en notas explicativas con `(crecimiento de ventas)` / `(sales growth)` en lugar de `(inflación + volumen) = (3% + 0%)`.
  - Actualizado [`analystExtractionPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionPrompt.js), [`analystSystemPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystSystemPrompt.js), [`analystAnnualSystemPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystAnnualSystemPrompt.js) y [`tecnologia/sector.md`](file:///home/lur/Lur/APP/src/agents/knowledge/tecnologia/sector.md) (conservando estrictamente la versión 1).
  - Añadidas claves de traducción en [`public/locales/en.json`](file:///home/lur/Lur/APP/public/locales/en.json).
- **Coordinación de color en Cash Flow**:
  - `renderNotes` en [`analisisTablesCore.js`](file:///home/lur/Lur/APP/public/js/analisis/analisisTablesCore.js), `renderNotesSsr` en [`reportSsrHtml.js`](file:///home/lur/Lur/APP/src/services/seo/reportSsrHtml.js), `buildNotes` en [`reportSections.js`](file:///home/lur/Lur/APP/src/services/reportExport/reportSections.js) y `drawNotes` en [`pdfStyles.js`](file:///home/lur/Lur/APP/src/services/report/pdfStyles.js) reciben `{ isCashFlow: true }`.
  - En el bloque de Cash Flow, la nota `*3` (SBC) adopta el mismo color oficial que la nota `*2` (`highlight-c2`), alineándose perfectamente con la casilla de Cash Flow Ajustado.
- **Pruebas**:
  - Añadidas pruebas unitarias en [`sectorTechnology.test.js`](file:///home/lur/Lur/APP/tests/unit/sectorTechnology.test.js) que validan el cálculo del WC para NVIDIA con crecimiento de ventas del 114,2 % (sin volumen plano) y la asignación coordinada del color `highlight-c2` para `*2` y `*3`.
  - Suite completa ejecutada: **249/249 pruebas pasando**.



## 21:35 — Corrección: el análisis con IA aceptaba empresas de sectores no admitidos

Se solicitó investigar por qué al pulsar «Analizar con IA» el sistema generaba análisis para cualquier empresa, cuando solo deben admitirse tecnología y software y consumo defensivo.

### Diagnóstico

- El portero de sector (`SectorAgent`) aceptaba de forma determinista cualquier empresa cuyo código SIC de SEC EDGAR cayera en rangos excesivamente amplios del clasificador [`companyProfileData.js`](file:///home/lur/Lur/APP/src/services/edgar/companyProfileData.js):
  - `2830-2836` (farmacéuticas: Pfizer, JNJ, Merck, AbbVie, Bristol-Myers, Abbott…) se traducía como `Consumo defensivo`.
  - Todo `73xx` (servicios empresariales: staffing como Manpower, consultoría, publicidad, redes de pago como Visa/Mastercard/PayPal) se traducía como `Servicios informáticos` y se admitía como tecnología.
  - Todo `36xx` (equipos eléctricos y conglomerados como GE) se traducía como `Electrónica` y se admitía como tecnología.
- Los análisis de MAN y PYPL en la BD (creados durante las pruebas del usuario) confirmaron la fuga.

### Resultado

- Nuevo clasificador determinista por rangos SIC en [`sectorAgent.js`](file:///home/lur/Lur/APP/src/agents/sectorAgent.js): `resolveSectorBySic` solo admite consumo defensivo (`2000-2199` alimentación/bebidas/tabaco y `2840-2844` hogar e higiene personal) y tecnología (`3570-3579` hardware, `3660-3679` semiconductores/componentes y `7370-7379` software/servicios informáticos). El resto pasa al clasificador IA, que rechaza los sectores no admitidos.
- Corregido `profileSector` en [`companyProfileData.js`](file:///home/lur/Lur/APP/src/services/edgar/companyProfileData.js): farmacéuticas → `Química y farmacéutica`; `73xx` no informático → `Servicios empresariales`; `36xx` → `Equipos eléctricos`; `357x` → `Informática`; `366x/367x` → `Tecnología`. `getCompanyOrigin` devuelve también el SIC. Corregidos los sectores de AAPL, JNJ, PFE, ABBV y V en `WELL_KNOWN_ORIGINS`.
- Endurecido el prompt del verificador de sector: excluye expresamente redes y procesadores de pagos, conglomerados y servicios empresariales no tecnológicos (consultoría, staffing, publicidad, outsourcing, RR. HH.).
- Eliminados 4 análisis de prueba (MAN y PYPL, ES/EN) generados por el fallo, con sus PDF/exportaciones.
- Verificado con EDGAR real: PFE, MAN y V quedan rechazados (`UNSUPPORTED_SECTOR`) y ONTO (semiconductores) se admite como tecnología. Pruebas añadidas en [`sectorTechnology.test.js`](file:///home/lur/Lur/APP/tests/unit/sectorTechnology.test.js); suite completa: **251/251 pasando**.



## 22:00 — Gráfico de deuda: la caja neta (deuda neta negativa) se dibuja hacia abajo

Se solicitó que, en el gráfico «Evolución de la deuda: normal vs neta», cuando la deuda neta es negativa (caja neta) la barra baje por debajo de la línea cero en lugar de quedar aplastada contra la base.

### Resultado

- Escala con eje divergente (mínimo negativo → cero → máximo positivo) y línea cero resaltada:
  - [`analisisChartsDividends.js`](file:///home/lur/Lur/APP/public/js/analisis/analisisChartsDividends.js) (`renderDebtHistoryChart`, web).
  - [`svgRenderers.js`](file:///home/lur/Lur/APP/src/services/reportExport/svgRenderers.js) (`renderDebtHistorySvg`, informe HTML/PDF/ODT/DOCX), usando el `minVal` que ya calculaba `buildDebtHistoryModel`.
- Las barras de deuda neta negativa parten del cero hacia abajo, con su etiqueta (`-N M`) debajo del extremo; los años y las variaciones se desplazan para no solaparse cuando hay caja neta. La deuda normal sigue dibujándose hacia arriba.
- Verificado con datos reales del informe afectado (positivos y negativos, incluido un valor extremo) en los dos renderizadores, sin `NaN`/`undefined`; suite completa: **251/251 pasando**.


## 22:15 — NVIDIA 10-K: recuperar la fila de marketable securities y el guidance del 8-K

Se solicitó corregir el análisis de NVIDIA: la fila «Inversiones a corto plazo» (marketable securities) no mostraba importe y no aparecía el guidance oficial de la compañía.

### Resultado

- **Guidance ausente (causa raíz):** los documentos del 8-K de resultados de NVIDIA se llaman `q4fy26pr.htm` (pr = press release) y `q4fy26cfocommentary.htm`, y [`classifyEarningsDocument`](file:///home/lur/Lur/APP/src/services/edgar/filingPresentations.js) no los reconocía, así que nunca se descargaba la sección complementaria y la extracción concluía «Sin guidance cuantitativo publicado».
  - Se ampliaron los patrones de nombre (`pr`/`er` como abreviatura de comunicado, `commentary`, `shareholder letter`).
  - Nuevo respaldo `pickEarningsDocumentsFallback`: si ningún documento del 8-K encaja con los patrones y el 8-K es de resultados (Item 2.02), se toman sus anexos ofimáticos como comunicado. Añadidas claves de traducción no aplican; pruebas nuevas en [`filingPresentations.test.js`](file:///home/lur/Lur/APP/tests/unit/filingPresentations.test.js).
  - Prompts ([`analystExtractionPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionPrompt.js) y [`analystAnnualSystemPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystAnnualSystemPrompt.js)): cuando la compañía solo publica guidance **trimestral** (caso NVIDIA: ingresos, márgenes, opex y tasa fiscal del trimestre siguiente), debe extraerse y pintarse etiquetado con su trimestre, sin declarar «sin guidance».
- **Marketable securities:** [`extractCapitalCashFlowFacts`](file:///home/lur/Lur/APP/src/agents/analyst/financialParsersExtract.js) ahora suma las líneas separadas de ventas y vencimientos (NVIDIA: 15.157 + 11.226) sin doble contaje cuando van juntas, y las compras/ventas se normalizan en valor absoluto en [`analystRunSteps.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystRunSteps.js) y [`capitalAllocationHelpers.js`](file:///home/lur/Lur/APP/src/agents/analyst/capitalAllocationHelpers.js): las compras van entre paréntesis en el estado de flujos y antes podían invertir el neto (`Inversiones a corto plazo` = ventas − compras = −14.233M).
- **Compatibilidad:** no se modificó el esquema del informe ni el renderizado, por lo que los análisis anteriores siguen funcionando igual (verificado con el análisis 792 y con `visibleCapitalRows`/`renderHorizon`).
- **Verificación real:** pipeline anual completo (sin guardar) sobre NVDA 10-K + 8-K: el outlook ya incluye la tabla de guidance Q1 FY2027E (Revenue 78.000M$ ±2 %, márgenes, opex y tasas fiscales FY2027) y la asignación de capital muestra `Inversiones a corto plazo = −14.233`. Suite completa: **256/256 pasando**.


## 22:38 — Circulante: media histórica del peso sobre el flujo operativo cuando no hay volúmenes

Se solicitó sustituir la estimación teórica del capital circulante por la media de los últimos 10 ejercicios del peso que el circulante ha supuesto sobre el flujo operativo sin circulante, aplicable también al consumo defensivo cuando el informe no publica volumen.

### Resultado

- **Nuevo método (`capitalAllocationHelpers.js`)**: con la serie histórica (hasta 10 ejercicios) se calcula por año `peso = ΔWC / (CFO − ΔWC)` (ejemplo del usuario: ΔWC −20.000M y CFO 80.000M ⇒ base 100.000M y peso −20 %) y se aplica la media al flujo del periodo sin circulante. Se activa cuando no hay dato de volumen: siempre en tecnología y software, y en consumo defensivo si el informe no lo desglosa; si hay volumen reportado, se mantiene `(inflación + volumen)`. Se descartan los ejercicios en que el peso supera el ±100 % (denominadores pequeños que desvirtúan la media: KHC 2023-24 con +152 %/+201 %).
- **Serie histórica (`analystRunSteps.js`)**: `buildWorkingCapitalHistory` construye los puntos año a año desde la serie anual de EDGAR (CFO y `workingCapitalChange`) y se adjunta a la extracción en los 10-K. La variación de circulante del ejercicio analizado se toma de esa misma serie para que la comparación sea homogénea; el parser determinista de la sección «Changes in operating assets and liabilities» (`extractAnnualWorkingCapitalChange`) queda como respaldo, y también se conserva el método de saldo de inicio/`g/(1+g)` para empresas con menos de 3 ejercicios de historia.
- **Nota del informe**: se detalla la media, los pesos de cada ejercicio y el desvío contra lo reportado. Verificado con datos reales: NVDA (media −15,4 % × 126.345M = −19.492M frente a −23.627M reportados), KHC (−24,9 % × 4.481M = −1.115M frente a −19M) y TAP (−0,9 % × 1.931M = −16,5M frente a −147M).
- **Guías actualizadas** (`que-es-un-informe-10-q.html` y `guiasAnalisisIaContent.js`) con las dos fórmulas (volumen reportado vs. media histórica) y sus traducciones al inglés.
- **Compatibilidad**: los informes ya guardados no se regeneran; el cambio solo afecta a análisis nuevos. Pruebas nuevas en [`sectorTechnology.test.js`](file:///home/lur/Lur/APP/tests/unit/sectorTechnology.test.js) y [`cashCapitalReconciliation.test.js`](file:///home/lur/Lur/APP/tests/unit/cashCapitalReconciliation.test.js); suite completa: **259/259 pasando**.


## 22:42 — Circulante: peso agregado (media ponderada) en lugar de media simple de porcentajes

Se verificó el resultado de KHC del método anterior: la media simple de porcentajes anuales es inestable (media 10 años +15,4 %, sin outliers −24,9 %, mediana −3,7 %), porque hay ejercicios con liberaciones de 2.500M sobre bases de 1.300M (+152 %/+201 %) y un año con CFO de 501M (−82,9 %).

### Resultado

- [`capitalAllocationHelpers.js`](file:///home/lur/Lur/APP/src/agents/analyst/capitalAllocationHelpers.js) calcula ya el **peso agregado** `ΣΔWC / Σ(CFO − ΔWC)` de los últimos 10 ejercicios (media ponderada por tamaño), sin filtros arbitrarios: KHC −6,1 %, NVDA −17,8 %, TAP −2,1 % (antes: filtrada −24,9 %, −15,4 %, −0,9 %). La nota del informe refleja el peso agregado y sigue listando los ratios anuales.
- La serie se corta siempre a los **10 ejercicios más recientes** (`buildWorkingCapitalHistory(edgarResults, 10)`); el mínimo de 3 solo aplica si la empresa tiene menos historia disponible.
- Prueba unitaria reescrita con 10 ejercicios; suite completa: **259/259 pasando**.


## 22:46 — Comparativa del circulante en 10 empresas de consumo defensivo

Se solicitó comparar el peso agregado histórico (10 ejercicios) con la fórmula usada en consumo defensivo sin volumen (inflación 3 % + volumen 0 %) en 10 empresas.

### Resultado

- Script reproducible [`scripts/qa/compare-working-capital.js`](file:///home/lur/Lur/APP/scripts/qa/compare-working-capital.js) (EDGAR + `buildWorkingCapitalHistory`) y resultados en [`comparacion/circulante-10-empresas-consumo-defensivo.md`](file:///home/lur/Lur/APP/comparacion/circulante-10-empresas-consumo-defensivo.md).
- Error absoluto medio frente a la variación reportada: fórmula 3 % = **1.028M**; peso agregado = **881M**. El histórico se acerca más en 6 de 10 casos (KO, PEP, PG, CL, CPB, TAP); el 3 % gana donde la base de circulante es mínima o positiva (KMB, GIS, KHC, MKC), porque el peso agregado predice una liberación que ese ejercicio no ocurrió.
