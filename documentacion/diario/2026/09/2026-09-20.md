## 13:58 — Resumen: Política sectorial de amortizaciones: tecnología solo ajusta el deterioro de fondo de comercio

Se decidió que en tecnología la amortización recurrente de intangibles de vida finita no se sume de vuelta en la columna Ajustado (es desgaste real del activo, misma lógica que el SBC), mientras que el deterioro de fondo de comercio/marcas/activos no recurrentes sí se sigue sumando. Consumo defensivo mantiene el criterio actual: suma de vuelta amortización y deterioros.

### Resultado

- **Nueva política sectorial** en `src/agents/analyst/sectorPolicy.js` (`intangibleAmortizationAddBack`, `goodwillImpairmentAddBack`) y directiva generada para el prompt (`buildSectorPolicyDirective`).
- **Prompts** (`analystSystemPrompt.js` y `analystAnnualSystemPrompt.js`): nuevo placeholder `{SECTOR_POLICY}` que `structureReport` rellena por sector; las instrucciones de deterioros/amortizaciones dejan de ser universales y remiten a la política del sector. En tecnología la amortización recurrente no se suma y no genera nota.
- **Extracción** (`analystExtractionPrompt.js`): `impairments*` queda restringido a deterioros no recurrentes (goodwill, marcas, activos) y `intangiblesAmortization*` a la amortización ordinaria, para que el ajuste determinista no mezcle ambas.
- **Procesador determinista** (`analystSalesProcessor.js`): recibe el sector; en tecnología detecta y revierte la amortización sumada por el modelo en Beneficio Operativo, EBT y Beneficio Neto (conservando el criterio fiscal implícito y los deterioros reales) y limpia el resaltado si no hay deterioro. Consumo defensivo no se toca.
- **Reglas** (`knowledge/tecnologia/sector.md`): reglas 1 y 3 reescritas con la excepción explícita a la regla general de Nivel 1.
- **Verificación**: 3 pruebas nuevas en `tests/unit/sectorTechnology.test.js` (directiva, reversión pura y reversión conservando deterioro). Suite completa **262/262**.
- **Pendiente/aviso**: los números de versión de los `.md` no se modifican (regla del proyecto); si se quiere regenerar análisis de tecnología ya cacheados habrá que subir la versión de `tecnologia/sector.md`. Las series derivadas del perfil (`rederiveStatements.calculateNormalizedNetIncomeAndEps`, BPA diluido ajustado/beneficio neto ajustado de la ficha de empresa) siguen sumando la amortización de intangibles en todos los sectores; queda pendiente alinearlas con la política si se desea coherencia total.
## 13:59 — Volumen real en consumo defensivo: extraerlo del 8-K y no asumir 0 %

Se señaló que el volumen no es 0 %: la app venía generando «(3% + 0%)» porque el prompt de extracción caía al 0 por defecto sin buscar el dato en la sección complementaria.

### Resultado

- Revisados los comunicados reales de 10 empresas: KO publica el volumen como titular («Global Unit Case Volume Grew 5 %»), KHC como volume/mix orgánico (−2,6 %), TAP como brand volume consolidado (≈ −4 %); PEP/PG/CL solo por segmentos o cualitativo y GIS/MKC/CPB sin cifra consolidada.
- [`analystExtractionPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionPrompt.js): en consumo defensivo ahora se exige buscar el volumen en el informe y en la sección complementaria del 8-K (unit case volume, organic volume growth, volume/mix), usar el total o el más representativo con su signo, y solo usar 0 % si no existe ningún dato cuantitativo.
- Comparativa actualizada con volumen real (fórmula `inflación + volumen` vs peso agregado histórico) en [`comparacion/circulante-10-empresas-consumo-defensivo.md`](file:///home/lur/Lur/APP/comparacion/circulante-10-empresas-consumo-defensivo.md): aun con volumen real, la fórmula da valores casi nulos frente a la media histórica (KO: 8 % → −145M frente a −1.484M). Suite: **262/262 pasando**.
## 14:35 — Circulante: el peso agregado histórico (10 ejercicios) pasa a ser el método único y configurable por .env

Se decidió usar siempre la media de los últimos 10 ejercicios del peso del circulante sobre el flujo operativo sin circulante para estimar el WC teórico, también cuando el informe publica volumen; la fórmula anterior (`(CxP − Inv − CxC) × (inflación + volumen)`) deja de aparecer en toda la cara visible, pero queda disponible para revertirla cambiando un solo parámetro del `.env`. Además, la tabla de estados de la ficha de empresa pasa a mostrar cuánto debería haber sido el cambio de circulante cada año según esa media.

### Resultado

- **Interruptor `WC_METHOD`** (`.env` y `.env.example`): `historical` (por defecto) usa siempre el peso agregado `ΣΔWC / Σ(CFO − ΔWC)` de los 10 ejercicios; `formula` restaura el comportamiento anterior (consumo defensivo con volumen reportado → `(inflación + volumen)`; el resto → histórico si lo hay). Implementado en [`capitalAllocationHelpers.js`](file:///home/lur/Lur/APP/src/agents/analyst/capitalAllocationHelpers.js), que lee la variable en cada llamada (cambiable sin reiniciar procesos que recarguen .env).
- **Fórmula antigua fuera de la cara visible**: prompts (`analystSystemPrompt.js`, `analystAnnualSystemPrompt.js`, `analystExtractionPrompt.js` y sus ejemplos), `analystAnnualSchema.js`, `auditorPrompt.js`, `languageDirective.js` (nota WC en inglés), conocimiento (`prompts/consumo-defensivo.md`, `knowledge/consumo-defensivo/sector.md`, `knowledge/general.md`, `knowledge/anual/general.md`, `knowledge/tecnologia/sector.md`) y guías (`guiasAnalisisIaContent.js`, `que-es-un-informe-10-q.html`) describen solo el peso agregado histórico; el código de la fórmula se conserva únicamente como respaldo (empresas con menos de 3 ejercicios o `WC_METHOD=formula`).
- **Nueva fila en la ficha de empresa**: `Nota: Circulante teórico (media 10 años)` justo bajo `Nota: Cambio en el capital circulante` ([`statementDisplay.js`](file:///home/lur/Lur/APP/src/services/edgar/statementDisplay.js) + cálculo en [`empresaStatementsValues.js`](file:///home/lur/Lur/APP/public/js/empresa/empresaStatementsValues.js), `workingCapitalTheoreticalValue`): por cada ejercicio, ratio agregado de los 10 más recientes con datos × flujo del propio año (CFO − ΔWC); solo en la serie anual (en trimestral se oculta automáticamente) y con hint explicativo traducido. Verificado con KHC real: ratio −6,1 % y teórico −275M en 2025, idéntico al análisis IA.
- **Traducciones**: claves nuevas/actualizadas en `public/locales/en.json` y libro de fuentes `_sources.json` (todos los fragmentos nuevos traducidos al inglés; eliminadas las claves obsoletas de la fórmula); `DICTIONARY_VERSION` 10 → 11 para forzar recarga.
- **Pruebas**: `sectorTechnology.test.js` actualizada (consumo defensivo con volumen usa el histórico) y nueva prueba de `WC_METHOD=formula`. Suite completa: **263/263 pasando**.
- **Nota**: los análisis ya guardados no se regeneran; solo afecta a análisis nuevos.
## 14:49 — Resumen: Botón «Ocultar todos / Mostrar todos» en la leyenda del gráfico de métricas

Se pidió añadir una opción en la leyenda del gráfico de métricas para ocultar o mostrar de una vez todas las series.

### Resultado

- **Botón conmutable en la leyenda** (`empresaMetricsChartRender.js`): se renderiza al final de los chips solo cuando hay más de una serie; muestra «Ocultar todos» si hay alguna visible y «Mostrar todos» cuando todas están ocultas (`aria-pressed` acorde).
- **Lógica** (`empresaMetricsChartCore.js`): `toggleAllSeriesVisibility()` lee las series visibles en la leyenda, oculta todas (o muestra todas si ya estaban todas ocultas), persiste en `localStorage` y reutiliza el render existente; evento de cambio de series ocultas igual que el toggle individual del ojo.
- **Listener** (`empresaMetricsChartListeners.js`): clic en `.metrics-legend-toggle-all` dentro del contenedor de la leyenda.
- **Estilos** (`styles.css`): píldora compacta a juego con los chips de la leyenda.
- **Traducciones**: claves «Ocultar todos» / «Mostrar todos» en `_sources.json` y `en.json` (Hide all / Show all).
- **i18n**: `scripts/i18n/extract.js` excluye ahora `sectorPolicy.js` de la extracción (es un generador de prompt interno, como el resto de ficheros de prompts); se regeneró `_sources.json` con la extracción pendiente del árbol de trabajo. Suite completa: **263/263 pasando** (incluye i18n).
- **Caché**: bump de `?v=` en `empresa.html` e `index.html` para Core/Render/Listeners (`81 → 82`).
## 14:54 — Circulante en 10-Q: media histórica con prorrateo (Q1 1/4, Q2 2/4, Q3 3/4)

Se reportó que los análisis trimestrales seguían usando la fórmula `(inflación + volumen)` y se pidió que también usen la media de los últimos 10 años, prorrateando el teórico anual: Q1 1/4, Q2 2/4, Q3 3/4 (cada trimestre suelto, 1/4).

### Resultado

- **Causa**: `buildWorkingCapitalHistory` solo se adjuntaba a la extracción en los 10-K, así que en los 10-Q no había serie y el sistema caía a la fórmula de respaldo.
- **Serie histórica en 10-Q** ([`analystRunSteps.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystRunSteps.js)): `buildWorkingCapitalHistory` se adjunta ahora también en los trimestrales (la sobreescritura del ΔWC reportado sigue siendo exclusiva del anual).
- **Prorrateo** ([`capitalAllocationHelpers.js`](file:///home/lur/Lur/APP/src/agents/analyst/capitalAllocationHelpers.js)): en horizontes < 12 meses el peso agregado se aplica al flujo ANUAL del último ejercicio completo de la serie (ancla) y el teórico resultante se prorratea por meses/12 (Q1 → 1/4, Q2 → 2/4, Q3 → 3/4); la columna de últimos 3 meses usa siempre 1/4. El 10-K mantiene el método verificado (peso × base del propio ejercicio).
- **Nota del informe**: nueva redacción bilingüe que desglosa el ancla y el prorrateo (`{ratio}% × {baseAnual}M (flujo anual de {year}) = {annual}M en todo el año -> en {months} meses = {ytd}M`), con claves nuevas en `en.json`/`_sources.json`.
- **Guías**: añadida la mención del prorrateo trimestral en `guiasAnalisisIaContent.js` y `que-es-un-informe-10-q.html`, con sus traducciones.
- **Verificación real (KHC 2026 Q2)**: peso agregado −6,1 % × base anual 2025 (4.481M) = −274,6M anual → YTD 6 meses −137,3M (2/4) y trimestre −68,6M (1/4); desviaciones y Cash Flow ajustado calculados coherentemente (2088M − 539,3M = 1548,7M YTD).
- **Pruebas**: nueva prueba del prorrateo Q1/Q2/Q3 en `sectorTechnology.test.js`. Suite completa: **264/264 pasando**.

## 14:59 — Resumen: Tecnología: el deterioro de intangibles no se ajusta; el de fondo de comercio sí

Se pidió que, en el sector de tecnología, ni en 10-K ni en 10-Q se sume de vuelta el deterioro de intangibles (allí el producto intangible pierde valor de verdad cada año), manteniendo el ajuste cuando se trate de fondo de comercio (*goodwill*).

### Resultado

- **Extracción desglosada** ([`analystExtractionPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionPrompt.js) y [`analystExtractionSchema.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionSchema.js)): nuevos hechos `goodwillImpairmentQuarter/PrevQuarter/Ytd/PrevYtd` (solo fondo de comercio) e `intangibleImpairmentQuarter/PrevQuarter/Ytd/PrevYtd` (intangibles, marcas u otros activos); `impairments*` pasa a ser el total (suma exacta de ambos) y solo se usa si no hay desglose. Las nuevas claves se normalizan también por unidad en [`financialParsersFormat.js`](file:///home/lur/Lur/APP/src/agents/analyst/financialParsersFormat.js).
- **Política sectorial** ([`sectorPolicy.js`](file:///home/lur/Lur/APP/src/agents/analyst/sectorPolicy.js)): nuevo flag `intangibleImpairmentAddBack` (tecnología `false`, consumo defensivo `true`), helpers `resolveImpairmentAddBack`, `resolveExcludedIntangibleImpairment` y `resolveDiscardedImpairment`, y directiva de prompt reescrita (goodwill se ajusta; intangibles/marcas/activos permanecen como coste en tecnología, también en «Anterior Ajustado»).
- **Procesador determinista** ([`analystSalesProcessor.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystSalesProcessor.js)): el ajuste de deterioros usa la parte que la política permite; se revierte de Beneficio Operativo/EBT/Neto cualquier deterioro de intangibles, amortización o total agregado sin desglose que el modelo hubiera sumado (incluida la columna Anterior Ajustado), se recalcula el % Ajustado y se limpia el resaltado. Notas nuevas: «deterioro de fondo de comercio» cuando solo se ajusta esa parte y nota informativa de que el deterioro de intangibles se mantiene como coste. El fallback fiscal de [`dividendHistoryBuilders.js`](file:///home/lur/Lur/APP/src/agents/analyst/dividendHistoryBuilders.js) y el paso de sector por [`analystCashCapitalProcessor.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystCashCapitalProcessor.js)/[`analystRunSteps.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystRunSteps.js) quedan alineados.
- **Prompts y reglas**: instrucciones de deterioros reescritas en [`analystSystemPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystSystemPrompt.js) y [`analystAnnualSystemPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystAnnualSystemPrompt.js); regla 3 de [`knowledge/tecnologia/sector.md`](file:///home/lur/Lur/APP/src/agents/knowledge/tecnologia/sector.md) reescrita con la excepción; [`auditorPrompt.js`](file:///home/lur/Lur/APP/src/agents/auditor/auditorPrompt.js) actualizado para no marcar como error la no reversión en tecnología.
- **i18n**: tres claves nuevas en `public/locales/en.json` y `_sources.json` (deterioro de fondo de comercio actual/previo y nota de deterioro que permanece como coste).
- **Bug latente corregido** ([`analystSalesProcessor.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystSalesProcessor.js)): `formatReported(null)` devolvía `0M` y podía borrar el comparativo anterior cuando la extracción no traía `quarter`/`ytd`; ahora devuelve `null`.
- **Verificación**: pruebas de `sectorTechnology.test.js` actualizadas y ampliadas (política, reversión sin desglose, conservación del goodwill con desglose, reversión en el comparativo anterior) y simulaciones 3M/YTD con desglose real. Suite completa: **265/265 pasando**.
- **Pendiente/aviso**: los análisis ya guardados no se regeneran (solo afecta a análisis nuevos). La serie derivada del perfil (`rederiveStatements.calculateNormalizedNetIncomeAndEps`) sigue sumando la amortización de intangibles en todos los sectores (pendiente ya anotado en la entrada de las 13:58).
## 15:17 — Nuevo sector Consumo Discrecional: solo 10 subsectores admitidos

Se preguntó cuál era el siguiente sector más parecido a los ya implementados. Tras la propuesta, se decidió implementar **Consumo Discrecional limitado exclusivamente a 10 subsectores**: mejora del hogar y distribución de materiales; grandes almacenes, descuento y off-price; ropa, calzado y complementos; hogar, muebles, electrodomésticos y ocio; retail especializado; comercio electrónico; concesionarios y usados; restaurantes; vivienda y materiales; y autopartes. Quedan **fuera** (se rechazan) fabricantes de automóviles, hoteles, cruceros, casinos, agencias de viaje, educación y servicios de consumo.

### Resultado

- **Reglas del sector** ([`consumo-discrecional/sector.md`](file:///home/lur/Lur/APP/src/agents/knowledge/consumo-discrecional/sector.md), versión 1): alcance explícito de los 10 subsectores, política de ajustes igual que consumo defensivo (amortización y deterioros se suman de vuelta, impuestos al 23 %), y novedades propias del sector: ventas comparables (comps/same-store sales) en lugar de volumen, arrendamientos operativos fuera de la Deuda Balance, circulante típicamente negativo y estacionalidad trimestral, e-commerce con ingresos diferidos, sale-leaseback y riesgo promocional del inventario.
- **Clasificador** ([`sectorAgent.js`](file:///home/lur/Lur/APP/src/agents/sectorAgent.js)): `KNOWN_CONSUMER_DISCRETIONARY_TICKERS` (~150 tickers agrupados por subsector), nuevos rangos SIC deterministas (retail 52xx-59xx seleccionados, restaurantes 5812-5813, e-commerce 5961, ropa 2300-2399/3021/3140s, hogar 2510s/3630s/3651/3940s, vivienda 1520-1539/2451-2452, autopartes 3714; excluidos 3711 autos, 7011 hoteles, 7900s ocio, 8200s educación), etiquetas de sector admitidas y prompt del clasificador IA reescrito con el alcance exacto; mensaje de error actualizado.
- **Integración**: slug en [`versionRegistry.js`](file:///home/lur/Lur/APP/src/agents/versionRegistry.js), carga de reglas en [`filingExtractor.js`](file:///home/lur/Lur/APP/src/agents/analyst/filingExtractor.js), política sectorial explícita en [`sectorPolicy.js`](file:///home/lur/Lur/APP/src/agents/analyst/sectorPolicy.js) (mismos add-back que defensivo), etiqueta `Consumo discrecional` en [`companyProfileData.js`](file:///home/lur/Lur/APP/src/services/edgar/companyProfileData.js) y detección mock en [`mock.provider.js`](file:///home/lur/Lur/APP/src/services/ai/providers/mock.provider.js).
- **Extracción**: [`analystExtractionPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionPrompt.js) indica que en consumo discrecional no hay volumen físico y que `volumeGrowth` debe reflejar el crecimiento de ventas comparables (o el total) como dato contextual de respaldo.
- **SEO EN**: [`empresaFormatting.js`](file:///home/lur/Lur/APP/public/js/empresa/empresaFormatting.js) traduce «Consumo discrecional» a *consumer discretionary* en las metaetiquetas inglesas.
- **Pruebas**: nuevo [`sectorConsumerDiscretionary.test.js`](file:///home/lur/Lur/APP/tests/unit/sectorConsumerDiscretionary.test.js) (9 pruebas: tickers admitidos y excluidos, SIC, versión, reglas, agente, política, perfil y mock). Suite completa: **280/280 pasando**.
- **Pendiente/aviso**: los textos de la interfaz y las guías (`index.html`, `empresa.html`, `guiasAvisoContent.js`) aún dicen que solo se analiza consumo defensivo (venían desactualizados desde la tecnología); los números de versión de los `.md` no se tocan (regla del proyecto); los análisis guardados no se regeneran; y para empresas con menos de 3 ejercicios el respaldo del WC sigue usando `inflación + volumen` (caso residual de nuevas cotizadas).

## 15:16 — Resumen: Normalización fiscal determinista (±20 %) en la tabla de Ventas

Se reportó con el informe anual de Adobe (ADBE) que la nota fiscal detectaba la desviación fuera del umbral («justo fuera del ±20 %»), calculaba el Beneficio Neto Ajustado normalizado (6.725,18M) pero la tabla seguía mostrando la cifra reportada (7.130M), con un párrafo contradictorio en la nota y una nota *1 duplicada del deterioro del año anterior.

### Resultado

- **Enforcement determinista** en [`analystSalesProcessor.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystSalesProcessor.js): tras los ajustes de deterioros, si el tipo efectivo reportado (impuesto reportado / EBT normal) se desvía más del ±20 % del 23 % de referencia, se fuerza `Beneficio Neto Ajustado = EBT Ajustado × 0,77` (menos la NCI implícita si la hay), se marca `isAdjusted: true` con `adjustedNote` de la nota fiscal, se recalcula el `% Ajustado` y se reescribe la nota *2 con el cierre exacto de la tabla. La regla se aplica también a la columna «Anterior Ajustado» cuando su impuesto supera el umbral. La desviación se mide sobre el EBT normal para que los deterioros/amortizaciones sumados de vuelta no la distorsionen (mantiene los casos de `sectorTechnology.test.js`).
- **Nota fiscal coherente**: la nota *2 se sustituye (o se crea si faltaba) con la redacción determinista y bilingüe que cierra dígito a dígito con la celda; se elimina el caso de notas contradictorias tipo «se normaliza… no obstante se mantiene la cifra reportada».
- **Nota de deterioro del año anterior**: la detección de duplicados pasa a ser insensible a mayúsculas y reconoce el ejercicio anterior por año (evita el `*1: El año anterior tuvieron un impairment…` duplicado de ADBE); al crearla usa el siguiente índice en vez de `*1` fijo.
- **Auditor** ([`deterministicChecks.js`](file:///home/lur/Lur/APP/src/agents/auditor/deterministicChecks.js)): la comprobación `notas.beneficio_neto` usaba `[^.]*?(?=\n|$)` y nunca casaba con notas de una sola línea acabadas en punto; ahora compara el último término de la igualdad de la frase fiscal con la celda.
- **i18n**: nuevas claves de la nota fiscal en `en.json`/`_sources.json`; se regeneró `_sources.json` y se tradujeron con `scripts/i18n/translate.js` las claves pendientes del árbol de trabajo.
- **Verificación**: nueva suite [`taxNormalization.test.js`](file:///home/lur/Lur/APP/tests/unit/taxNormalization.test.js) con el caso ADBE (6.725,18M), margen dentro del umbral, techo +20 %, columna anterior, nota en inglés y detección del auditor. Suite completa: **280/280 pasando**.
- **Pendiente/aviso**: los análisis ya guardados (incluido el ADBE del reporte) no se regeneran; el cambio aplica a análisis nuevos.

## 15:23 — Resumen: Paridad de cálculos entre consumo discrecional y consumo defensivo

Ante el nuevo sector de consumo discrecional se pidió que los cálculos sean exactamente los mismos que en consumo defensivo.

### Resultado

- **Auditoría del pipeline**: el cálculo determinista solo distingue tecnología del resto (`buildWorkingCapitalDataFallback`, `normalizeSalesBlock`, `normalizeCashFlowBlock`, `getTaxNormalizationData`); el sector entra únicamente por `getSectorPolicy(sector)` y las reglas de conocimiento. Consumo discrecional ya estaba cableado (sectorAgent, filingExtractor, versionRegistry, companyProfileData, prompt de extracción y `knowledge/consumo-discrecional/sector.md`).
- **Política única compartida** ([`sectorPolicy.js`](file:///home/lur/Lur/APP/src/agents/analyst/sectorPolicy.js)): nueva constante `CONSUMER_ADD_BACK_POLICY` referenciada por `defensive_consumer` y `consumer_discretionary` (misma identidad de objeto), de modo que no puedan divergir por edición independiente.
- **Prueba de paridad** ([`sectorCalculationParity.test.js`](file:///home/lur/Lur/APP/tests/unit/sectorCalculationParity.test.js)): con un horizonte sintético con amortización, deterioros y desviación fiscal, `normalizeSalesBlock` + `normalizeCashFlowBlock` + `normalizeCapitalBlock` producen resultados idénticos (deepEqual) en ambos sectores de consumo; se verifica también que la directiva de prompt solo difiere en el nombre del sector y que tecnología sí calcula distinto (la paridad no es vacía).
- **Verificación**: suite completa **283/283 pasando**.

## 15:30 — Resumen: Consumo discrecional habilitado para 10-Q y 10-K (y pendientes en .md)

Se pidió habilitar los análisis trimestrales y anuales del nuevo sector de consumo discrecional y dejar en un `.md` los subsectores pendientes de desarrollar.

### Resultado

- **Verificación del pipeline**: con `HD`/`MCD` el sector se clasifica como `consumer_discretionary`, `loadKnowledgeRules` carga el `general.md` trimestral o `anual/general.md` + `consumo-discrecional/sector.md` según el formulario, el versionado resuelve igual en 10-Q y 10-K y la directiva de la política sectorial es la de consumo defensivo. La web (subida de PDF y buscador SEC) ya ejecuta ambos formularios sin cambios adicionales.
- **Pregeneración en lote** ([`analyze-consumer.core.js`](file:///home/lur/Lur/APP/scripts/analyze-consumer.core.js) y [`analyze-consumer.worker.js`](file:///home/lur/Lur/APP/scripts/analyze-consumer.worker.js)): nueva opción `--sector=discretionary` (`WORKER_SECTOR`) que usa la lista curada de `KNOWN_CONSUMER_DISCRETIONARY_TICKERS` (161 empresas) y mantiene `staples` por defecto; nuevo script `npm run analyze:discretionary`. Se corrigió un bug preexistente del refactor: el worker usaba `FROM_YEAR`/`YEARS_LABEL` sin importarlos (fallaba al arrancar); ahora se exportan desde el core. Verificado con `--list` en ambos sectores.
- **Pendientes documentados** ([`documentacion/PENDIENTE-CONSUMO-DISCRECIONAL.md`](file:///home/lur/Lur/APP/documentacion/PENDIENTE-CONSUMO-DISCRECIONAL.md)): estado de lo habilitado, los 10 subsectores admitidos con tickers representativos y lo que falta por subsector (crear `subsectores/<slug>/subsector.md` de Nivel 3 + `ejemplos/`), más los puntos transversales (comps/tráfico/ticket, arrendamientos operativos, sale-leaseback, inventario, estacionalidad, e-commerce, versión anual específica, universo del worker con market caps y textos de portada/SEO).
- **Pruebas**: nueva suite [`sectorConsumerDiscretionaryForms.test.js`](file:///home/lur/Lur/APP/tests/unit/sectorConsumerDiscretionaryForms.test.js) (reglas 10-Q/10-K, versionado igual en ambos, `isAnalysisOutdated` y directiva de cálculo). Suite completa: **287/287 pasando**.
## 17:35 — Resumen: Consumo discrecional: no inventar la deuda asumida con inversiones financieras (AMZN 2026-Q2)

Se reportó que el informe generado para AMZN 2026-Q2 no cuadraba: la asignación de capital acumulada mostraba «No cuadra: quedan -50953M» por una fila «Deuda asumida (no-cash) -50758M» que atribuía a compras del semestre una deuda asumida que no existió. Se pidió arreglarlo solo en consumo discrecional, sin afectar a consumo defensivo ni a tecnología.

### Resultado

- **Causa**: el heurístico de `buildCapitalAllocationFromBalance` infería deuda asumida cuando había una partida de adquisiciones material (en AMZN, la línea "Acquisitions, net of cash acquired, non-marketable investments, and other": 5.000M/10.000M en preferentes de Anthropic y 13.700M en OpenAI) y la variación de deuda del balance (63.246M) superaba el flujo de deuda extraído por la IA (12.488M). El ratio del 80 % pasaba el umbral del 85 % y pintaba la fila; sin ella el total queda en -195M, dentro del margen.
- **Política sectorial de deuda asumida** ([`sectorPolicy.js`](file:///home/lur/Lur/APP/src/agents/analyst/sectorPolicy.js)): en consumo discrecional se exige descripción de compra de negocio (`isBusinessAcquisitionDescription`), se prioriza la composición XBRL del sistema (`systemDebtCash`) sobre el parser de texto y se limita la divergencia al 50 % de la variación de deuda. Consumo defensivo y tecnología mantienen el criterio clásico (85 %), verificado con el caso KDP.
- **Cálculo** ([`capitalAllocationHelpers.js`](file:///home/lur/Lur/APP/src/agents/analyst/capitalAllocationHelpers.js)): `buildCapitalAllocationFromBalance` recibe el sector (se pasa desde `analystAgentCore.js`) y aplica la política; las cifras de Libre, caja y deuda no cambian.
- **Reglas y extracción**: [`knowledge/consumo-discrecional/sector.md`](file:///home/lur/Lur/APP/src/agents/knowledge/consumo-discrecional/sector.md) distingue compras de negocio de inversiones financieras, prohíbe la deuda asumida sin empresa adquirida identificada y aclara que las revalorizaciones no realizadas no son resultado recurrente; [`analystExtractionPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionPrompt.js) describe las líneas mixtas de inversión y exige identificar las inversiones, sin inventar un negocio adquirido.
- **Verificación end-to-end**: análisis real del 10-Q de AMZN con el agente (provider opencode-go) → ambos horizontes cuadran (-262M y -325M, «Más o menos cuadra»), sin fila de deuda asumida y con la nota *1 describiendo Anthropic/OpenAI como inversiones. Suite completa: **290/290 pasando** (3 pruebas nuevas en `sectorConsumerDiscretionary.test.js`: caso AMZN, cuadre sin la fila y separación de políticas).
- **Pendiente/aviso**: los análisis ya guardados no se regeneran (solo afecta a análisis nuevos); la paridad de add-back del bloque Ventas entre los dos consumos se mantiene intacta.
## 17:49 — Resumen: Beneficio Bruto siempre desde la extracción (ventas − coste de ventas), nunca copiado de Ventas

Se detectó en un análisis nuevo de AMZN 2026-Q2 que el Beneficio Bruto mostraba 200.606M, idéntico a las Ventas, en los dos horizontes y en el comparativo (167.702M en lugar de 86.893M). La cuenta de resultados de Amazon no publica la línea "Gross profit", y el dato real es 104.828M = 200.606 − 95.778 (coste de ventas). Se pidió garantizar que los datos sean correctos sin romper los casos que ya funcionaban.

### Resultado

- **Extracción** ([`analystExtractionSchema.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionSchema.js) y [`analystExtractionPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionPrompt.js)): nuevo campo `cogs` por periodo y comparativo (trimestre y acumulado) e instrucción explícita: Beneficio Bruto = ventas netas − coste de ventas ("Cost of sales"/"Cost of revenue"), con el ejemplo de Amazon (200.606 − 95.778 = 104.828; 167.702 − 80.809 = 86.893; semestre 382.125 − 183.241 = 198.884); prohibido usar las ventas como Beneficio Bruto o mezclar columnas de periodos distintos.
- **Cálculo determinista** ([`analystSalesProcessor.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystSalesProcessor.js)): el Beneficio Bruto se calcula como ventas − coste de ventas cuando la extracción trae `cogs` (eliminado el respaldo que devolvía `rep.sales`); y las cifras explícitas de la extracción pasan a imponer las filas Normal y Anterior Normal del informe, sincronizando la columna Ajustado solo si era una copia (los ajustes sectoriales se aplican después). Los fallbacks entre métricas siguen rellenando huecos, pero ya no sobrescriben datos del informe.
- **Pruebas**: nueva suite [`grossProfitNormalization.test.js`](file:///home/lur/Lur/APP/tests/unit/grossProfitNormalization.test.js) (4 pruebas con las cifras reales de AMZN Q2/semestre, grossProfit sin cogs y fila operativa alterada por el modelo). Suite completa: **294/294 pasando**, incluidos los casos de consumo defensivo y tecnología (ADBE, KDP, CAG) que ya funcionaban.
- **Verificación end-to-end**: análisis nuevo del 10-Q de AMZN → Beneficio Bruto 104.828M / 86.893M (+20,64 %) y 198.884M / 165.584M (+20,11 %); la asignación de capital cuadra en ambos horizontes y mantiene la corrección de la deuda asumida.
- **Pendiente/aviso**: los análisis ya guardados no se regeneran; AMZN 2026-Q2 debe reanalizarse para verse corregido en la web.
## 18:03 — Resumen: Las partidas no operativas no recurrentes se excluyen del EBT y del Beneficio Neto Ajustado (Anthropic en AMZN)

Se reportó que, con el Beneficio Bruto ya corregido, el EBT y el Beneficio Neto seguían fallando: la columna Ajustado era idéntica a la Normal e incluía los 50.486M de revalorización no realizada de las inversiones en Anthropic (el propio modelo reconocía en nota que no lo había ajustado). Se pidió corregirlo garantizando que los casos anteriores sigan funcionando.

### Resultado

- **Extracción** ([`analystExtractionSchema.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionSchema.js), [`analystExtractionPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionPrompt.js) y [`financialParsersFormat.js`](file:///home/lur/Lur/APP/src/agents/analyst/financialParsersFormat.js)): nuevos hechos `nonOperatingGainsQuarter/PrevQuarter/Ytd/PrevYtd` (partidas no operativas extraordinarias incluidas en EBT: revalorizaciones no realizadas, plusvalías puntuales) y `nonOperatingGainsDescription`. Ejemplo real AMZN: 50.486M en Q2 y 62.814M acumulado (upward adjustments de Anthropic); se excluyen intereses y dividendos recurrentes.
- **Cálculo determinista** ([`analystSalesProcessor.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystSalesProcessor.js)): si la partida es material (≥ 50M y ≥ 5 % del EBT normal), se resta del EBT Ajustado y del Beneficio Neto Ajustado (efecto después de impuestos al 23 % normalizado); el EBT es la casilla de origen (resalte y nota) y el Beneficio Neto cambia en cascada. Las notas del modelo que decían que no se había ajustado se eliminan y la nota fiscal se reescribe para no contradecir la tabla; las notas quedan ordenadas por llamada (*1, *2, *3).
- **Efecto en cascada en Cash Flow**: el ajuste fiscal del flujo de caja usa ya el EBT recurrente (AMZN Q2: 6.985,3M normalizados frente a 2.655M pagados → -4.330,3M en lugar de -15.942,1M).
- **i18n**: 4 claves nuevas extraídas y traducidas (`_sources.json` + `en.json`) y `DICTIONARY_VERSION` 11 → 12.
- **Pruebas**: dos pruebas nuevas en [`taxNormalization.test.js`](file:///home/lur/Lur/APP/tests/unit/taxNormalization.test.js) (caso AMZN con tres notas del modelo y caso de nota única que mezclaba impuestos y partida no recurrente). Suite completa: **296/296 pasando**; los casos de consumo defensivo y tecnología no cambian salvo que la extracción traiga estas partidas.
- **Verificación end-to-end**: análisis nuevo del 10-Q de AMZN → EBT Ajustado 30.371M (80.857 − 50.486) y Beneficio Neto Ajustado 23.772,78M (62.647 − 50.486 × 0,77), con notas *1 y *2 coherentes (`% A.` +45,62 % y +48,03 %); el Beneficio Bruto, la asignación de capital y la deuda asumida siguen correctos.
- **Pendiente/aviso**: los análisis ya guardados no se regeneran; AMZN 2026-Q2 debe reanalizarse para verse corregido en la web.

## 18:16 — Resumen: Plan del nuevo sector Cíclicas (`cyclical`) a partir del diseño conversado

Se diseñó (sin implementar) el cuarto sector de análisis para empresas cíclicas: químicas, materias primas/minería, metales, transporte, ETTs, maquinaria…, centrado en deuda, sostenibilidad del dividendo, posición en el ciclo y márgenes. Se decidió que los 10-Q sigan el proceso estándar (sin la sección nueva) y que el 10-K sume una sección de posición en el ciclo y valoración normalizada. Queda pendiente cerrar la lista de subsectores admitidos antes de codificar.

### Resultado

- **Documento creado** ([`PLAN-CICLICAS.md`](file:///home/lur/Lur/APP/documentacion/PLAN-CICLICAS.md)): decisiones de diseño, política contable (sin ajuste de intangibles; deterioro de goodwill con nota), clasificación con precedencia sobre los sectores ya curados, sección anual (media móvil, gráfico de margen operativo ajustado con mediana 10 años, tabla de beneficio normalizado con intereses de la deuda actual, comprobaciones de reversión/supervivencia/dilución y alerta de margen operativo TTM < 0 %), ficheros afectados, 5 fases, pruebas previstas y riesgos.
- **Decisiones clave registradas**: sin agente clasificador nuevo (cuarto valor en `sectorAgent`); cotización en vivo y beneficio normalizado guardado; cálculos deterministas con la IA solo interpretando; la sección debe salir también en el PDF; lenguaje condicional sin predicciones.
- **Pendiente de decidir**: lista definitiva de subsectores (¿automotrices? ¿energía?), precedencia definitiva con consumo discrecional y mínimo de histórico (propuesto: 7 años).

## 18:31 — Resumen: La asignación de capital anual explica la deuda no monetaria con el flujo XBRL de EDGAR (MCD 2025)

Se reportó que la asignación de capital de McDonald's (10-K 2025) no cuadraba: tabla Libre 2.071M + Desinversiones 476M − Adquisiciones 354M − Recompras 2.056M + Caja 311M + Deuda 1.549M = +1.997M sin explicar. Se pidió investigar la causa, arreglarla y no romper los demás casos.

### Resultado

- **Causa**: la fila `Deuda` es la variación del balance (38.424M → 39.973M, +1.549M) por regla, pero el flujo neto de deuda con caja del ejercicio fue de solo −72M (corto plazo +6M; largo plazo +4.724M − 4.802M). Los +1.621M restantes son movimientos no monetarios (efecto divisa y ajustes de valor razonable de la deuda en EUR/JPY, entre otros) que no tenían explicación determinista. El parser de texto de deuda daba 2.963M (leía columnas cruzadas), así que no servía.
- **Flujo de deuda anual desde EDGAR** ([`analystRunSteps.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystRunSteps.js)): en los 10-K se carga el flujo neto de deuda XBRL del ejercicio (`debtIssued + debtPaid`) en `systemDebtCash.ytd`, la misma fuente homogénea que usa el balance.
- **Deuda no monetaria anual** ([`capitalAllocationHelpers.js`](file:///home/lur/Lur/APP/src/agents/analyst/capitalAllocationHelpers.js)): nuevo residuo anual = Δdeuda balance − flujo de deuda EDGAR − deuda asumida ya detectada; si es material (≥ 50M y ≥ 10 % del delta) se explica bajo la tabla como «deuda no monetaria» aunque supere la variación del balance. La lógica trimestral con banda (KHC, TAP, PEP) queda intacta y `analystAgentCore.js` pasa `isAnnual`.
- **Pruebas**: dos pruebas nuevas en [`cashCapitalReconciliation.test.js`](file:///home/lur/Lur/APP/tests/unit/cashCapitalReconciliation.test.js) (MCD 2025: noCashDebt 1.627M; cuadre «Más o menos cuadra» con resto +370M y sin inventar el movimiento si falta EDGAR). Suite completa: **298/298 pasando**.
- **Verificación end-to-end**: análisis nuevo del 10-K de MCD → «Más o menos cuadra. … deuda no monetaria (…) -1627M. Con ellos, el resto sin explicar sería +64M, dentro del margen razonable» (esa pasada extrajo recompras 2.016M y desinversiones 130M).
- **Pendiente/aviso**: los análisis ya guardados no se regeneran; MCD 2025 debe reanalizarse para verse corregido en la web.

## 18:39 — Resumen: La sección anual de deuda muestra los intereses que paga la empresa (MCD 2025)

Se reportó que en la sección de deuda del 10-K de McDonald's no aparecía qué intereses paga: el texto afirmaba que la nota «no desglosa los tipos cupón por tramo, por lo que no es posible calcular un tipo de interés medio ponderado exacto», cuando el propio 10-K publica «Weighted-average annual interest rate of total debt» (4,0 % en 2025 y 2024; 3,7 % en 2023) y el estado de flujos el «Interest paid». Se pidió arreglarlo sin romper lo demás.

### Resultado

- **Extracción** ([`analystExtractionPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionPrompt.js) y [`analystExtractionSchema.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionSchema.js)): nuevos campos anuales de deuda `allDebtAverageRate` y `allDebtAverageRateSource` (tabla «Debt highlights» del MD&A o «weighted-average effective rate» de la nota, con el ejemplo real MCD 4,0 %), `interestExpense` e `interestPaid` (gasto y pago en efectivo de intereses, ejemplo MCD 1.582M/1.555M).
- **Respaldo EDGAR** ([`analystRunSteps.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystRunSteps.js)): `buildAnnualEdgarData` expone `edgarInterestExpense` y `edgarInterestPaid` del ejercicio desde XBRL.
- **Sección de deuda** ([`annualConclusionSections.js`](file:///home/lur/Lur/APP/src/agents/analyst/annualConclusionSections.js)): si no hay tipo publicado, se estima con el gasto por intereses sobre la deuda media (marcado como estimado y con su fuente); si existe tipo, se elimina del texto la frase «no desglosa / no es posible calcular» y se añaden de forma determinista el tipo medio y las frases de gasto y pago de intereses (sin duplicar lo que ya diga el texto). El banner del gráfico muestra el tipo automáticamente (web `analisisChartsDebt.js` y export SVG/PDF).
- **i18n**: 6 claves nuevas extraídas y traducidas (`_sources.json` + `en.json`); `DICTIONARY_VERSION` 12 → 13.
- **Pruebas**: nueva suite [`debtInterestDisclosure.test.js`](file:///home/lur/Lur/APP/tests/unit/debtInterestDisclosure.test.js) (4 pruebas: tipo publicado, estimado, sin datos y datos de extracción sin EDGAR). Suite completa: **302/302 pasando**, incluidas las de refinanciación y calendario de deuda (Pepsi).
- **Verificación end-to-end**: análisis nuevo del 10-K de MCD → texto «El tipo de interés medio de toda la deuda es del **4,0 %** según el MD&A. El gasto por intereses del ejercicio fue de 1.582M. Los intereses pagados en efectivo ascendieron a 1.555M.»; `allDebtAverageRate = 4` (no estimado, fuente «MD&A Debt highlights») y calendario de vencimientos intacto.
- **Pendiente/aviso**: los análisis ya guardados no se regeneran; MCD 2025 debe reanalizarse para verse corregido en la web.

## 19:26 — Resumen: Agente verificador-analisis y script de promoción a producción

Se solicitó un agente de opencode (solo este proyecto) para verificar análisis uno a uno: el usuario analiza un filing en local, el agente corrige solo ese informe y, tras el visto bueno, lo sube a producción con el sello de verificado (icono de «Revisado por un humano»).

### Resultado

- **Agente `.opencode/agent/verificador-analisis.md`** (mode primary, solo proyecto): flujo localizar → estudiar informe vs filing → corregir solo ese análisis (report JSONB + formatos) → visto bueno → promoción → diario. Reglas: no toca código/prompts/pipeline, no regenera otros análisis, no commitea y no sube nada sin OK explícito.
- **Script `scripts/promote-analysis.js`**: promueve un análisis local a `cifra_prod` con `is_public=true` e `is_reviewed=true` (`reviewed_at`/`reviewed_by`), regenera y copia PDF/HTML/DOCX/ODT por scp, reinicia `cifra-prod` para invalidar la caché SEO y verifica el resultado. Idempotente por `pdf_url` (INSERT o UPDATE en un bloque `DO`), soporta `--dry-run`, `--with-translations` y `--ticker/--accession`; obtiene la `DATABASE_URL` del `.env` remoto con Node (el `.env` no es sourceable por `MAIL_FROM`).
- **Validación**: `node --check` correcto; `--dry-run` real con el análisis 810 (MCD 10-K) sin tocar producción; upsert probado contra la BD local en transacción con ROLLBACK (fila insertada con `is_reviewed=t`, `reviewed_by=53`, sin rastro). Pendiente: probar una promoción real cuando el usuario lo pida.

## 19:32 — Resumen: Agente auditor-informe (auditoría de informes contra el filing)

Se solicitó un agente de opencode (solo este proyecto) que audite un informe de Cifra contra el filing original: recibe el PDF del filing y el del informe, los lee, verifica cifras, cuadres y coherencia, y devuelve una nota de 1 a 10 con los fallos y su evidencia.

### Resultado

- **Agente `.opencode/agent/auditor-informe.md`** (mode primary, solo proyecto, solo chat): extrae el texto de ambos PDFs (`pdftotext`), reutiliza la rúbrica del auditor interno (`src/agents/auditor/auditorPrompt.js`), audita bloque a bloque (Ventas, Cash Flow, Asignación de Capital, Notas, Conclusión, Formato) y responde con nota, veredicto, fallos (gravedad, ubicación, esperado vs mostrado, evidencia, impacto), aciertos, dudas y cifras contrastadas.
- **Permisos**: lectura (incluida externa) y bash para extracción; sin escritura (`edit: deny`) y sin diario, porque no modifica nada.

## 19:38 — Resumen: Agente explicador-codigo y sección documentacion/explicacion-codigo

Se solicitó un agente (solo este proyecto) que enseñe el código y responda preguntas sobre él, con una sección de documentación HTML dividida en Frontend, Backend, API/Datos y Análisis IA donde apuntar las preguntas y respuestas cuando el usuario lo pida.

### Resultado

- **Agente `.opencode/agent/explicador-codigo.md`** (mode primary, solo proyecto): responde leyendo el código real (citas `ruta:línea`), con mapa de categorías y regla de apuntar solo cuando el usuario lo pide; permisos de lectura y escritura limitada a la documentación.
- **Estructura `documentacion/explicacion-codigo/`**: `index.html` (portada con las 4 tarjetas y contadores) + `frontend.html`, `backend.html`, `api-datos.html` y `analisis-ia.html`, con el tema oscuro de `ARQUITECTURA.html`, navegación cruzada entre las 5 páginas, contadores (`#qa-count` en cada página y `#count-*` en el índice) y plantilla comentada de Q&A.
- **Verificación**: enlaces correctos entre las 5 páginas y balance de etiquetas validado con parser HTML; los tres agentes nuevos de la sesión (`verificador-analisis`, `auditor-informe`, `explicador-codigo`) quedan en `.opencode/agent/` (solo proyecto) y sin `prompt:` en el frontmatter.

## 19:02 — Resumen: Beneficio Bruto con costes directos funcionales en restaurantes (MCD 2025)

Se reportó que en el informe anual de McDonald's el Beneficio Bruto aparecía idéntico a las Ventas (26.885M y 25.920M en el comparativo). McDonald's no publica la línea "Gross profit" ni un único "Cost of sales": presenta gastos operativos funcionales, por lo que la extracción no traía ni `cogs` ni `grossProfit` y el modelo copiaba las ventas.

### Resultado

- **Extracción** ([`analystExtractionPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionPrompt.js)): nueva regla para empresas con gastos funcionales sin coste de ventas: usar como coste de ventas los costes directos del negocio (McDonald's: "Franchised restaurants-occupancy expenses" + "Company-owned and operated restaurant expenses"). Ejemplo real MCD 2025: 2.618 + (3.006 + 2.905 + 2.358 + 564) = 11.451 de `cogs` y Beneficio Bruto 15.434M; 2024: 11.210 y 14.710M.
- **Cálculo determinista** ([`analystSalesProcessor.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystSalesProcessor.js)): un `grossProfit` idéntico a las ventas se descarta como fuente; nueva guarda anti-copia que, si no hay coste ni beneficio bruto en la extracción y la fila del modelo copió las Ventas, deja la fila sin dato (`—`) en vez de publicar un margen del 100 % falso (periodo actual y comparativo).
- **Pruebas**: dos pruebas nuevas en [`grossProfitNormalization.test.js`](file:///home/lur/Lur/APP/tests/unit/grossProfitNormalization.test.js) (MCD con costes directos y guarda anti-copia sin fuente). Suite completa: **304/304 pasando**.
- **Verificación end-to-end**: extracción del 10-K de MCD → `cogs` 11.451/11.210 y Beneficio Bruto 15.434/14.710; informe completo → Beneficio Bruto 15.434M / 14.710M (+4,92 %), resto de filas y sección de deuda correctas.
- **Pendiente/aviso**: los análisis ya guardados no se regeneran; MCD 2025 debe reanalizarse para verse corregido en la web.

## 19:14 — Resumen: La sección de deuda muestra los tipos que paga la empresa por divisa/categoría (MCD 2025)

Se preguntó por qué no aparecían los intereses de cada vencimiento en la sección de deuda. La nota de MCD no publica el cupón de cada vencimiento (solo el importe agregado por año y los tipos medios efectivos por divisa/tipo), y esos tipos por divisa no llegaban a la extracción: ni la tabla "Interest rates / Amounts outstanding / Maturity dates" ni la del MD&A ("Debt highlights": tipo medio 4,0 %) estaban en el texto que recibe el modelo.

### Resultado

- **Extractor de secciones** ([`filingExtractor.js`](file:///home/lur/Lur/APP/src/agents/analyst/filingExtractor.js)): dos ventanas nuevas en `extractKeyFilingSections` — tabla de tipos y saldos por divisa y tabla de ratios del MD&A con el tipo medio publicado — y otra en `extractDebtFilingText`; admiten `skipIndexCheck` porque la fila de cabecera va separada por tabuladores y el heurístico la confundía con un índice. Verificado: `buildAnalysisText` ya incluye ambas tablas.
- **Extracción** ([`analystExtractionPrompt.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionPrompt.js) y [`analystExtractionSchema.js`](file:///home/lur/Lur/APP/src/agents/analyst/analystExtractionSchema.js)): nuevo campo `debt.rateBuckets` con la lista de tipos medios efectivos por divisa/categoría (ejemplo real MCD con los ocho tramos).
- **Sección de deuda** ([`annualConclusionSections.js`](file:///home/lur/Lur/APP/src/agents/analyst/annualConclusionSections.js)): añade los tipos por divisa al texto; si el modelo ya los enumera, solo añade la aclaración de que la nota no desglosa el cupón de cada vencimiento (sin duplicar). El tipo medio publicado (4,0 %) manda sobre la estimación.
- **i18n**: 3 claves nuevas extraídas y traducidas; `DICTIONARY_VERSION` 14 → 15.
- **Pruebas**: 4 pruebas nuevas (2 en [`debtMaturityFallback.test.js`](file:///home/lur/Lur/APP/tests/unit/debtMaturityFallback.test.js) para los extractores y 2 en [`debtInterestDisclosure.test.js`](file:///home/lur/Lur/APP/tests/unit/debtInterestDisclosure.test.js) para el texto). Suite completa: **309/309 pasando**.
- **Verificación end-to-end**: extracción del 10-K de MCD (texto real de pdf-parse) → `rateBuckets` con USD 4,4 %, EUR 2,6 %, etc. y `allDebtAverageRate` 4,0 %; informe → texto con el 4,0 %, los tipos por divisa, 1.582M de gasto y 1.555M pagados, y la aclaración de que no hay cupón por vencimiento (MCD no lo publica).
- **Pendiente/aviso**: los análisis ya guardados no se regeneran; MCD 2025 debe reanalizarse para verse corregido en la web.
