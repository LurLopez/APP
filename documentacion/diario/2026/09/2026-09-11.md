# 2026-09-11

## 00:17 — Resumen: Optimización integral de SEO y GEO (llms-full.txt, endpoints Markdown, guía 8-K y catálogo benchmark)

Se solicitó mejorar el SEO y el GEO de la página.

### Resultado

- **Generative Engine Optimization (GEO) y estándar llms.txt**:
  - Implementado `GET /llms-full.txt` dinámico con caché (30 min): integra la metodología de análisis de Cifra (los dos horizontes y tres pilares), el texto íntegro de las 7 guías educativas, el glosario de métricas y el directorio de empresas.
  - Actualizado `public/llms.txt` con enlaces bidireccionales HTML y Markdown para cada empresa y referencia cruzada a `llms-full.txt`.
  - Añadido soporte de negociación de contenido y endpoints nativos en Markdown: `GET /empresa/:ticker.md` y `GET /informe/:id.md` (o cabecera HTTP `Accept: text/markdown`), permitiendo a ChatGPT, Perplexity, Claude y otros agentes LLM ingerir la información financiera estructurada en formato puro y sin ruido HTML.
- **Rastreo y robots.txt**:
  - Ampliado `public/robots.txt` con directivas explícitas de acceso público a `/llms.txt` y `/llms-full.txt`, y soporte para 17 rastreadores y motores de búsqueda generativos (GPTBot, OAI-SearchBot, ChatGPT-User, PerplexityBot, ClaudeBot, Claude-Web, anthropic-ai, Google-Extended, Applebot, Applebot-Extended, Meta-ExternalAgent, Meta-ExternalFetcher, Amazonbot, cohere-ai, Diffbot, CCBot, Bytespider), manteniendo el bloqueo de áreas privadas (`/api/`, `/cartera`, `/seguimiento`, etc.).
- **Nueva guía educativa sobre el Formulario 8-K**:
  - Creada `src/content/guias/que-es-un-informe-8-k.html`: explica el Item 2.02 de resultados, los anexos de notas de prensa (ex-99.1) y presentaciones a inversores (ex-99.2), la tabla comparativa con el 10-Q y 10-K, y por qué el guidance de la dirección se publica en el 8-K.
  - Enriquecida con metadatos completos y JSON-LD estructurado (`Article`, `BreadcrumbList`, `FAQPage`).
  - Integrada en el hub de guías (`src/content/guias/hub.html`), sitemap, `GUIDES` en `seo.service.js` y en los manifiestos LLM.
- **Catálogo benchmark de consumo defensivo e indexación en Sitemap**:
  - Definido `BENCHMARK_CONSUMER_DEFENSIVE` con 28 compañías líderes del sector en EE. UU. (KO, PEP, PG, COST, WMT, MDLZ, PM, MO, CL, KHC, GIS, STZ, HSY, KR, ADM, EL, TAP, CAG, TSN, CLX, CHD, SJM, MKC, CPB, HRL, KVUE, DG, DLTR).
  - El `sitemap.xml` dinámico ahora indexa 41 URLs (frente a las 17 anteriores), incorporando todas las empresas del catálogo con prioridad 0.8 y frecuencia semanal, namespace `xmlns:xhtml` y etiquetas `<xhtml:link rel="alternate" hreflang="es" />`.
  - El directorio de empresas `/empresa` expone ahora todas las compañías con enlaces directos a sus fichas web y versiones Markdown.
- **SSR enriquecido y limpieza de datos estructurados**:
  - Añadido **Resumen Financiero Ejecutivo** en el bloque SSR de cada empresa (`getCompanyBotContent`), detallando ventas, beneficio neto, flujo de caja libre, margen FCF (%) y BPA diluido del último ejercicio reportado ante la SEC.
  - Añadida sección de preguntas frecuentes (FAQ) visible en HTML para cada empresa.
  - Resuelto el conflicto de Schema.org en `injectCompanyMeta`: se sustituye limpiamente la etiqueta estática para que cada ficha de empresa declare únicamente su `@graph` específico con `Corporation` (con CIK, bolsa, sector, industria, `knowsAbout` y `sameAs`), `WebPage`, `BreadcrumbList` y `FAQPage` dinámico.
  - Añadidos meta keywords, rating general y links alternativos de descubrimiento LLM en `public/index.html` y `public/empresa.html`.
- **Verificación**:
  - Comprobación de sintaxis de todos los ficheros con `node --check`.
  - Pruebas HTTP automáticas de todos los endpoints: 200 en `/robots.txt`, `/llms.txt`, `/llms-full.txt`, `/sitemap.xml` (41 URLs), `/`, `/empresa`, `/empresa/KO`, `/empresa/KO.md`, `/guias/que-es-un-informe-8-k`, `/informe/58` y `/informe/58.md`.
  - Validación con `JSON.parse` de los esquemas JSON-LD de todas las páginas sin errores.
  - Verificación de la negociación de contenido con `Accept: text/markdown`.
  - Comprobación de integridad con `git diff --check`.

## 01:05 — Resumen: Distinción de Presentaciones (PDF) vs Comunicados (8-K) y soporte de diapositivas de Conagra Brands

El usuario reportó un problema con las presentaciones en la tabla de informes trimestrales: al pulsar "Presentación" en empresas como Conagra Brands (CAG), se abría la nota de prensa legal en HTML de la SEC (Exhibit 99.1 del 8-K) en lugar del deck de diapositivas clásico en PDF (como el de Molson Coors: `Molson-Coors-Q2-2026-Earnings-Deck-FINAL.pdf`), y consultó si era posible acceder mediante enlace directo a las presentaciones oficiales en PDF.

### Diagnóstico

1. **Confusión en la interfaz**: Si el backend no localizaba el archivo PDF del deck (`docType: 'presentation'`), recurría al primer documento disponible (`presentations[0]`), que era el comunicado de prensa de resultados (`docType: 'release'`, HTML EX-99.1), y lo etiquetaba de forma equívoca como "Presentación".
2. **Bloqueo de crawler en la web de Conagra**: Conagra Brands aloja sus presentaciones de diapositivas en su propio portal Drupal (`conagrabrands.com/files/events/...`) protegido por Cloudflare Turnstile, lo cual bloqueaba el crawler headless Chrome del backend. Conagra no adjunta las diapositivas como anexo en la SEC (solo la nota de prensa EX-99.1).
3. **Filtrado de anexos SEC**: En anexos 8-K, expresiones regulares capturaban falsos positivos por números de accession que contenían "992" y no descartaban archivos de índice SEC.

### Resultado

1. **Diferenciación visual estricta en UI (`public/empresa.js`, `public/styles.css`)**:
   - Separación nítida de dos acciones independientes:
     - **`Presentación`** (píldora ámbar `.filing-action-presentation`): enlace directo al PDF del deck / diapositivas de resultados (IR / SEC Exhibit 99.2).
     - **`Comunicado`** (píldora gris pizarra `.filing-action-release`): enlace directo a la nota de prensa / comunicado oficial de resultados en SEC EDGAR (Exhibit 99.1).
   - Si la empresa dispone de ambos documentos (como Molson Coors o Conagra Brands), se muestran los dos botones en la fila.
   - Si la empresa no publica presentación en diapositivas (solo comunicado, ej. trimestres de Coca-Cola), se muestra únicamente el botón "Comunicado", evitando que el usuario sea engañado con un botón "Presentación" que abra un texto legal en HTML.
2. **Generador determinista de decks para Conagra Brands (`src/services/edgar.service.js`)**:
   - Nueva función `getCagDeckForFiling(filing)` que calcula el trimestre fiscal y año de Conagra y genera de forma instantánea el enlace directo al PDF oficial en `https://www.conagrabrands.com/files/events/${filedAt}/Q${q}FY${fy}-Earnings-Slides`.
   - Bypass de rastreo Chrome para CAG, evitando esperas innecesarias ante el reto Turnstile.
3. **Robustez en la clasificación de anexos EDGAR (`classifyEarningsDocument`)**:
   - Soporte para Exhibit 99.2+ (`/(?:^|[^a-z0-9]|x)(?:ex|exhibit)[-_.]?99(?:[._-][2-9]|d[2-9])(?![0-9])/i`) clasificado como `presentation`.
   - Exclusión estricta de páginas de índice SEC (`lower.includes('index')`).
   - Detección mejorada de retos Cloudflare Turnstile en español e inglés en `irPageErrorType`.
4. **Caché y versiones**:
   - Actualizados `public/empresa.html` y `public/index.html` a `styles.css?v=95` y `empresa.js?v=62`.
5. **Verificación**:
   - CAG: FY 2026 enlaza directamente `https://www.conagrabrands.com/files/events/2026-07-15/Q4FY26-Earnings-Slides` (Presentación) y `tmb-20260715xex99d1.htm` (Comunicado).
   - TAP: Q2 2026 enlaza directamente `https://s27.q4cdn.com/826627388/files/doc_financials/2026/q2/Molson-Coors-Q2-2026-Earnings-Deck-FINAL.pdf` (Presentación) y `tapex99120260630-earningsr.htm` (Comunicado).
   - KO: clasifica correctamente comunicados sin falsos positivos de índice.
   - PYPL: detectado subdominio `investor.pypl.com` en Q4 Inc. Q2 2026, Q1 2026, FY 2025 y Q3 2025 ahora cuentan con enlaces directos a sus presentaciones PDF en `q4cdn.com` (`PYPL-2Q-26-Earnings-Presentation.pdf`, etc.) junto a sus comunicados 8-K.

## 01:50 — Resumen: Ingesta del Outlook / Guidance de la Presentación y Comunicado 8-K en la IA

El usuario solicitó dejar de lado los detalles de presentación visual en la interfaz web y priorizar que la información del comunicado y la presentación llegue íntegramente a la IA para que los análisis anuales (10-K) dispongan del outlook cuantitativo para el siguiente ejercicio.

### Diagnóstico

1. **Pérdida del Guidance por truncado ciego en `trimPresentationText`**: Los comunicados de resultados (8-K) suelen tener entre 50.000 y 90.000 caracteres. El helper anterior recortaba los primeros 8.000 caracteres y los últimos 6.000, eliminando todo el contenido intermedio. En empresas como Molson Coors (TAP), el apartado `2026 OUTLOOK` figuraba en el carácter 28.248, quedando suprimido por completo antes de llegar a la IA.
2. **Presupuesto insuficiente y falta de priorización en `analystAgent.js`**: `buildAnalysisText` limitaba la sección complementaria a 25.000 caracteres. Cuando se descargaba primero un deck de diapositivas de IR (ej. 14.000 caracteres), la nota de prensa 8-K con el guidance detallado quedaba truncada o excluida.
3. **Normalización defensiva rota en el agente**: El fallback de `outlook.secSnippet` buscaba en `rawAnn.outlookSecTable` en vez de `rawAnn.outlook.secTable`, y `text` caía a una frase genérica si el modelo omitía la narrativa del outlook.
4. **Rutas desincronizadas**: La regeneración administrativa (`adminReports.routes.js`) y el worker continuo (`scripts/analyze-defensive-consumer.js`) no pasaban `presentationText` ni `formType`.

### Resultado

1. **Extractor inteligente de secciones relevantes (`extractRelevantPresentationSections` en `analysis.service.js`)**:
   - Búsqueda dirigida de encabezados y bloques de guidance con año o periodo fiscal (`2026 OUTLOOK`, `FULL YEAR 2026 GUIDANCE`, `FISCAL OUTLOOK`, `TARGETS`).
   - Captura generosa de 4.500 caracteres por bloque de guidance, garantizando la extracción íntegra de tablas de métricas, rangos de ventas, EBT, BPA, FCF, CAPEX e intereses.
   - Inclusión del resumen ejecutivo (primeros 3.500 caracteres) y notas clave de asignación de capital / recompras / planes de ahorro.
2. **Priorización de documentos y ampliación de presupuesto**:
   - `buildPresentationText` detecta qué documento contiene el guidance y lo sitúa en primer lugar (`processed.sort`), etiquetándolo con `[CONTIENE SECCIÓN DE GUIDANCE / OUTLOOK]`.
   - Presupuesto ampliado a 55.000 caracteres en `analystAgent.js` (`presentationBudget`), cubriendo tanto el deck visual como el comunicado 8-K.
3. **Instrucciones rigurosas y normalización corregida**:
   - `EXTRACTION_PROMPT` y `ANNUAL_REPORT_SYSTEM_PROMPT` refuerzan que el documento complementario es la fuente primaria del outlook si el 10-K no lo trae, prohibiendo tajantemente inventar cifras o copiar ejemplos del schema.
   - Corregida la normalización en `analystAgent.js`: `rawAnn.outlook.secTable` se inyecta en `result.conclusion.outlook.secSnippet`, y se mapean automáticamente `guidanceSales`, `guidanceEbt`, `guidanceEps`, `guidanceFcf`, `guidanceCapex` y `guidanceNetInterest`.
   - Parser JSON robusto (`cleanJsonText` en `modelProvider.js`) para extraer estructuras válidas entre `{}` y `[]` ante respuestas conversacionales de los modelos.
4. **Cableado universal de presentaciones**:
   - Añadida descarga y envío de `presentationText` en `src/api/routes/adminReports.routes.js` (`POST /reports/:id/regenerate`) y en `scripts/analyze-defensive-consumer.js`.
   - Descarga concurrente (`Promise.allSettled`) en `getPresentationBuffers` (`src/services/edgar.service.js`).
5. **Verificación en base de datos**:
   - **Molson Coors (TAP, ID 59)**: regenerado con el deck y comunicado 8-K. Extrae para 2026: Ventas Flat ± 1 %, EBT -15 % a -18 %, BPA -11 % a -15 %, FCF 1.100 M$ ± 10 %, CAPEX 650 M$ ± 5 %, Intereses 260 M$ ± 5 %, Tasa impositiva 22-24 %, Plan de ahorro de costes de 450 M$ a 3 años y tabla oficial `secSnippet` con 8 filas completas.
   - **Kraft Heinz (KHC, ID 60)**: regenerado con el Business Update y nota de prensa. Extrae para 2026: Ventas orgánicas -3,5 % a -1,5 %, Beneficio operativo divisa constante -18 % a -14 %, BPA ajustado 1,98 $ a 2,10 $, FCF conversion ~100 %, Intereses ~940 M$, Inversión comercial de 600 M$ y tabla oficial `secSnippet` con 8 filas completas. Eliminadas las alucinaciones previas.

## 02:15 — Resumen: Carga progresiva e instantánea de Informes Trimestrales, Presentaciones y Comunicados

El usuario reportó que al abrir la pestaña de "Informes trimestrales" la pantalla se quedaba en blanco durante demasiado tiempo debido a la búsqueda síncrona de presentaciones y comunicados de resultados (8-K). Solicitó que primero se cargue de inmediato todo el listado de informes con sus acciones principales (Analizar con IA, Ver análisis, Vista previa, Descargar) y que las presentaciones y comunicados se carguen posteriormente en segundo plano, mostrando un indicador visible de carga.

### Diagnóstico

1. **Bloqueo síncrono en backend (`GET /api/screener/company/:ticker/filings`)**: La ruta esperaba a que `getFilingsWithPresentations(ticker)` explorara secuencialmente sitios web de relación con inversores (IR) y descargara índices 8-K de la SEC para 16 informes. Si había sitios lentos o caídos con timeouts de 12 segundos, la petición HTTP tardaba entre 10 y 25 segundos antes de devolver un solo byte.
2. **Pantalla en blanco en frontend (`public/empresa.js`)**: Durante esa espera prolongada, `#filings-table tbody` permanecía vacío y `#filings-count` en `—`, dejando la sección de Informes trimestrales completamente en blanco sin spinner ni feedback visual.
3. **Falta de caché en memoria de presentaciones**: Las presentaciones descubiertas no se persistían en memoria, por lo que cada navegación repetía el rastreo completo.

### Resultado

1. **Desacoplamiento de endpoints en backend (`src/api/routes/screener.routes.js`)**:
   - `GET /api/screener/company/:ticker/filings`: Devuelve de forma casi instantánea (< 150 ms) la lista completa de filings (10-Q y 10-K), estados de análisis de IA previos, valoraciones por estrellas y links oficiales. Incluye presentaciones si ya están en caché de memoria e informa de `presentationsPending: true/false`.
   - Nuevo `GET /api/screener/company/:ticker/filings/presentations`: Endpoint dedicado para resolver en segundo plano las presentaciones (decks IR) y comunicados (8-K Item 2.02) de los filings recientes.
2. **Caché en memoria y concurrencia optimizada (`src/services/edgar.service.js`)**:
   - Nueva caché `filingPresentationsCache` (TTL 24 h) y deduplicación de peticiones concurrentes en vuelo (`filingPresentationsInFlight`).
   - Sondas de sitios IR en paralelo (`Promise.all`) con timeout ajustado a 6 segundos en lugar de 12 segundos secuenciales.
   - Nueva función exportada `getFilingsPresentationsMap(ticker)`.
3. **Carga en 2 fases e indicadores visuales de progreso (`public/empresa.js`, `public/styles.css`, `public/empresa.html`, `public/index.html`)**:
   - **Feedback inmediato**: Al pulsar en Informes trimestrales, se renderiza al instante la cabecera y una fila con spinner: `Cargando informes trimestrales de SEC EDGAR…`, eliminando cualquier pantalla en blanco.
   - **Fase 1 (Ultrarrápida, ~100-300 ms)**: La tabla completa se pinta al instante con los 40 filings, fechas, formulario, "Vista previa", "Descargar", botón "Analizar con IA" / "Ver análisis con IA ✨" y estrellas. El usuario puede analizar o inspeccionar cualquier informe inmediatamente.
   - **Indicadores de carga**: Mientras se buscan presentaciones para los trimestres recientes, se muestra un indicador en la cabecera (`Cargando presentaciones y comunicados…`) y una píldora discreta en cada fila afectada (`Cargando…` con mini spinner de 24 px de alto, idéntica altura a los botones para evitar cualquier salto o desplazamiento de layout / CLS = 0).
   - **Fase 2 (Segundo plano)**: Al resolver `loadFilingsPresentations()`, las píldoras de carga se sustituyen limpiamente por los botones correspondientes (`Presentación` en ámbar y/o `Comunicado` en gris pizarra), o se retiran si el periodo no dispone de ellos. El indicador de cabecera se oculta suavemente.
   - **Control de concurrencia y navegación**: Integrado `AbortController` para cancelar consultas de presentaciones si el usuario cambia de empresa.
   - Incrementadas versiones de assets a `styles.css?v=96` y `empresa.js?v=62`.
4. **Verificación**:
   - `node --check` superado sin errores en `edgar.service.js`, `screener.routes.js` y `empresa.js`.
   - TAP: `/filings` responde en 328 ms (frente a >15 s previo), `/filings/presentations` en 188 ms y recarga con caché en 0 ms.
   - KO: `/filings` responde en 37 ms con comunicados 8-K clasificados.
   - KHC y GIS: filings en ~250-400 ms y presentaciones en ~150 ms.
   - Comprobado en Chrome Headless: renderizado perfecto de filas 10-Q/10-K, botones "Analizar con IA", "Vista previa", "Descargar", "Presentación" y "Comunicado".

## Corrección del etiquetado de periodo fiscal (Q1-Q3 y FY) en informes 10-Q y 10-K

### Contexto y Problema

El usuario reportó que en la tabla de informes trimestrales de empresas como Campbell's Co (`CPB`), la columna de periodo mostraba etiquetas incoherentes:
1. Indicaba `Q2 2026` cuando el periodo que cubre (hasta `2026-05-03`) correspondía en realidad al `Q3 2026`.
2. Mostraba un `Q4 2025` justo después del informe anual `10-K FY 2025`, lo cual carece de sentido financiero y normativo ante la SEC.

### Diagnóstico

1. **Cálculo naive por mes de calendario**: La función `filingPeriodLabel` calculaba el trimestre con `Math.min(4, Math.ceil(Number(reportDate.slice(5, 7)) / 3))` a partir del mes de calendario de la fecha de reporte (`reportDate`).
2. **Años fiscales desplazados**: Empresas como Campbell's (`CPB`, cierre en agosto `0803`), Apple (`AAPL`, cierre en septiembre), Walmart/Nvidia (`WMT`/`NVDA`, cierre en enero) o Nike/FedEx (`NKE`/`FDX`, cierre en mayo) no coinciden con el año natural (diciembre).
   - Para CPB, el ejercicio fiscal 2025 concluyó el `2025-08-03` con su `10-K`.
   - El periodo cerrado el `2025-11-02` (noviembre) era en realidad el **Q1 del año fiscal 2026**, pero el código calculaba mes 11 → `Math.ceil(11/3) = 4` → `Q4 2025`.
   - El periodo cerrado el `2026-05-03` (mayo) era el **Q3 del año fiscal 2026**, pero calculaba mes 5 → `Math.ceil(5/3) = 2` → `Q2 2026`.
3. **Normativa SEC sobre Formulario 10-Q**: En la normativa de la SEC estadounidense, **un formulario 10-Q nunca es Q4**. El cuarto trimestre se integra y presenta exclusivamente mediante el informe anual `10-K` (FY). Por tanto, un 10-Q siempre es Q1, Q2 o Q3.

### Solución Implementada

1. **Cálculo fiscal con `fiscalYearEnd` (`src/services/edgar.service.js`)**:
   - Se implementaron las funciones `getFiscalPeriodInfo(formType, reportDate, fiscalYearEnd)` y `filingPeriodLabel(formType, reportDate, fiscalYearEnd)`.
   - Se utiliza el dato oficial `submissions.fiscalYearEnd` devuelto por la SEC (ej. `'0803'` para CPB, `'0131'` para WMT, `'0926'` para AAPL, `'1231'` para año natural).
   - Para formularios `10-K` / `10-K/A`, se asigna trimestre `'FY'` y año fiscal correspondiente.
   - Para formularios `10-Q` / `10-Q/A`, se calcula la distancia respecto al cierre del ejercicio fiscal: se clasifica estrictamente en `Q1`, `Q2` o `Q3` y se determina el año fiscal al que pertenece el periodo (ej. periodo `2025-11-02` de CPB pertenece al año fiscal `2026` → `Q1 2026`).
2. **Metadatos adicionales en `getCompanyFilings` y `getCompanySeoProfile`**:
   - Cada objeto filing incluye ahora de forma explícita `periodLabel` (`Q3 2026`, `FY 2025`), `quarter` (`'Q1'`, `'Q2'`, `'Q3'`, `'FY'`) y `fiscalYear` (`2026`, `2025`).
3. **Emparejamiento de presentaciones de resultados (IR Decks)**:
   - Se actualizó `getIrDeckForFiling` para buscar presentaciones usando tanto el año fiscal como el de calendario (`q${quarter}${fiscalYear}`, `q${quarter}fy${fiscalYear}` además de `q${quarter}${year}`), resolviendo fallos de detección de diapositivas en empresas con año fiscal no natural.
4. **Consolidación en SEO (`src/services/seo.service.js`)**:
   - Eliminada la copia duplicada local con cálculo de mes de calendario y unificada la importación de `filingPeriodLabel` desde `edgar.service.js`.

### Verificación

- Verificado en endpoint real `GET /api/screener/company/CPB/filings` de `http://localhost:3000`:
  - `2026-05-03` (10-Q) → `Q3 2026` (antes: `Q2 2026`)
  - `2026-02-01` (10-Q) → `Q2 2026` (antes: `Q1 2026`)
  - `2025-11-02` (10-Q) → `Q1 2026` (antes: `Q4 2025`)
  - `2025-08-03` (10-K) → `FY 2025`
  - `2025-04-27` (10-Q) → `Q3 2025` (antes: `Q2 2025`)
  - `2025-01-26` (10-Q) → `Q2 2025` (antes: `Q1 2025`)
  - `2024-10-27` (10-Q) → `Q1 2025` (antes: `Q4 2024`)
  - `2024-07-28` (10-K) → `FY 2024`
- Verificado adicionalmente contra facts oficiales de la SEC en más de 15 compañías con años fiscales variados (AAPL, MSFT, NVDA, WMT, NKE, FDX, COST, ADBE, CRM, INTU, PYPL, GOOGL, AMZN, META), confirmando coincidencia exacta en trimestres fiscales y eliminación total de etiquetas `Q4` en formularios 10-Q.

## 10:40 — Resumen: Separación del Calendario Financiero a pestaña independiente con gestión de empresas y protección de cartera

El usuario solicitó desacoplar el calendario financiero que se encontraba anidado como subpestaña dentro de "Cartera" para convertirlo en una pestaña principal independiente ubicada exactamente entre "Cartera" y "Análisis". La funcionalidad del calendario se mantiene íntegra, agregando la visualización de las empresas que se están siguiendo, con capacidad para añadir nuevas o eliminar existentes, bajo la regla estricta de que las empresas con posición en cartera no se pueden eliminar.

### Diagnóstico y Arquitectura

1. **Dependencia de la Cartera**: El calendario estaba condicionado a la existencia de transacciones en la cartera del usuario. Usuarios sin compras activas o que deseaban monitorizar dividendos y fechas de resultados (10-Q/10-K) de empresas seguidas no podían aprovechar la vista.
2. **Navegación e Historial**: Se requería una nueva ruta `/calendario` en la barra lateral de navegación entre Cartera y Análisis (`data-section="calendario"`), con sincronización en `pushState`, metadatos SEO y carga de vista global sin ticker activo.
3. **Gestión de Empresas Seguidas y Protección de Cartera**:
   - Era necesario exponer la lista unificada de empresas seguidas en el calendario (tanto de cartera como de seguimiento personalizado).
   - Permitir añadir nuevas empresas mediante un buscador predictivo conectado a `/api/screener/search`.
   - Proteger a doble nivel (frontend y backend) que las posiciones activas de la cartera no puedan eliminarse del calendario.

### Resultado

1. **Backend y API (`src/services/portfolio.service.js`, `src/api/routes/watchlists.routes.js`, `src/services/seo.service.js`)**:
   - `buildPortfolioCalendarEvents`: Deduplica y ordena las empresas seguidas situando primero las que tienen posición en cartera (`isPortfolio: true`). Devuelve `{ events, companies }`.
   - `getPortfolio`: Retorna `calendarCompanies` junto a `calendarEvents` y `dividends`.
   - `DELETE /api/watchlists/calendar/items/:ticker`: Verifica en base de datos si el usuario mantiene acciones (`shares > 0`) en su cartera para dicho ticker. En caso afirmativo, deniega la eliminación con HTTP 400 (`'No se pueden eliminar del calendario las empresas con posición en tu cartera.'`).
   - `src/services/seo.service.js`: Añadido `/calendario` a las rutas privadas (`PRIVATE_PATHS`).
2. **Navegación y Estructura HTML (`public/index.html`, `public/empresa.html`)**:
   - Añadido enlace de navegación `<a class="nav-link" href="#" data-section="calendario">` situado estrictamente entre "Cartera" y "Análisis".
   - Añadida sección contenedora `<section class="company-section portfolio-page-section calendar-page-section" id="section-calendario" hidden>` con contenedor `#calendar-section`.
   - Retirada la subpestaña "Calendario" de la barra interna de Cartera (`portfolioTabsHtml`).
3. **Panel de Gestión de Empresas Seguidas (`public/portfolio.js`, `public/styles.css`)**:
   - Diseñado el componente `calendarCompaniesPanelHtml()` que muestra una tarjeta superior con:
     - Buscador predictivo con debounce (250 ms) para añadir cualquier empresa por nombre o ticker usando `/api/screener/search`.
     - Chips de empresas seguidas con logo, ticker, nombre completo, píldora de origen (`💼 Cartera` vs `📅 Seguimiento`).
     - Candado visual `🔒` con tooltip explicativo para empresas de la cartera (sin botón de borrado).
     - Botón de eliminación `✕` accesible para empresas en seguimiento que no pertenecen a la cartera.
   - Implementadas funciones `renderCalendarSection(root)` y `mountCalendarSection(root, options)` para inicializar y gestionar el ciclo de vida del calendario de forma totalmente autónoma.
4. **Enrutamiento e Integración Frontend (`public/empresa.js`, `public/app.js`, `public/watchlists.js`)**:
   - Soporte para la ruta `/calendario`, títulos dinámicos y navegación sin recarga.
   - Exportado `getCalendarTickers` en `Watchlists` y cableado reactivo de adición y eliminación con refresco automático de eventos y panel.
   - Actualización de versiones de caché (`styles.css?v=97`, `watchlists.js?v=10`, `portfolio.js?v=63`, `empresa.js?v=63`).
5. **Verificación**:
   - `node --check` superado en todos los archivos modificados.
   - Comprobación en base de datos con usuarios reales (ej. Usuario 3 con posiciones en CAG, TAP, PYPL, KHC y KO en seguimiento):
     - Detección correcta de `isPortfolio: true` y bloqueo de eliminación garantizado en backend y frontend.
     - Operaciones de adición (`POST /api/watchlists/calendar/items`) y eliminación (`DELETE /api/watchlists/calendar/items/:ticker`) validadas con éxito.

## 11:15 — Resumen: Formato enriquecido en Outlook anual (negritas clave y tabla de guidance de 4 columnas con año anterior y cifras absolutas)

El usuario solicitó dos ajustes específicos en la sección de Outlook / Guidance de los informes anuales generados por la IA:
1. Resaltar en **negrita** los números y datos más importantes de la narrativa del outlook.
2. En la tabla de guidance de la SEC (`secSnippet` / `secTable`): cuando se indique "flat" o porcentajes relativos, añadir al lado a cuánto equivale en cifra monetaria/absoluta proyectada; y poner siempre al lado el valor real que se obtuvo el año pasado para todas las métricas (ventas, EBT, BPA, Free Cash Flow, CAPEX, etc.).

### Diagnóstico

1. **Tablas de 2 columnas sin contexto histórico ni equivalencia monetaria**: El guidance oficial de la SEC reporta variaciones porcentuales (ej. "flat +/- 1 %" o "-15 % a -18 %") sin reflejar de forma inmediata la magnitud en millones o dólares ni comparar directamente contra el ejercicio que acaba de cerrar.
2. **Texto plano en interfaz y exportaciones**: La narrativa del outlook carecía de procesado markdown en pantalla y en exportación PDF, haciendo pesada la lectura rápida de cifras clave de FCF, EBT y ahorros.

### Resultado

1. **Formalización en Knowledge Base y Schemas del Agente (`src/agents/knowledge/anual/general.md`, `src/agents/analystAgent.js`)**:
   - Establecida regla imperativa de formatear con markdown bold (`**...**`) todos los importes, porcentajes, rangos y conceptos prioritarios.
   - Definida la estructura canónica de 4 columnas para `secTable`: `["Métrica", "[AÑO-1] (Año anterior)", "Guidance [AÑO]E*", "Cifra Proyectada [AÑO]E"]`.
2. **Motor determinista de enriquecimiento histórico y proyección (`withOutlookComparison`)**:
   - Implementada y exportada la función helper `withOutlookComparison(snippet, report)` en `src/agents/analystAgent.js`, reutilizada en `public/analisis.js`, `src/services/reportExport.service.js` y `src/services/report.service.js`.
   - Deduce automáticamente el año anterior (`[AÑO-1]`) a partir de `report.horizons[0]`.
   - Mapea de forma inteligente las cifras reales del ejercicio precedente para Ventas (`sales`), EBT (`ebt`), BPA (`eps`), FCF (`fcf`), CAPEX (`capex`) e Intereses netos (`netInterestExpense`).
   - Calcula matemáticamente los rangos en valores monetarios proyectados cuando el guidance expresa "flat" o variaciones porcentuales (ej. `11.834M` con `flat +/- 1 %` -> `~11.716M a 11.952M`).
   - Transforma de forma retrocompatible cualquier tabla SEC previa de 2 columnas a las 4 columnas canónicas.
3. **Formateador visual y tipografía en Frontend (`public/analisis.js`, `public/styles.css`)**:
   - Función `formatAnnualRichText(text)` que procesa markdown bold y detecta automáticamente expresiones porcentuales y monetarias complejas, saneando anidaciones de etiquetas.
   - Renderizado de tabla SEC con clases semánticas: columna de año anterior con fondo sutil (`.sec-col-prev`), guidance oficial destacado (`.sec-col-guidance`) y cifra proyectada resaltada (`.sec-col-proj`).
4. **Exportaciones Multi-formato (PDF, Word, HTML)**:
   - `src/services/report.service.js`: Renderizado con `PDFKit` mediante `drawPdfFormattedText` (soporte de `Helvetica-Bold`) y tabla SEC de 4 columnas calibrada con anchos específicos y alineación numérica a la derecha.
   - `src/services/reportExport.service.js`: Integración de tabla de 4 columnas con estilos diferenciados y negritas en Word (`docx`) y HTML descargable.
5. **Actualización de Base de Datos y Verificación**:
   - Migrados y enriquecidos los análisis anuales existentes de Molson Coors (`TAP`, ID 59) y Kraft Heinz (`KHC`, ID 60) en PostgreSQL.
   - Verificada la generación sin errores de PDF, Word y HTML, y la visualización correcta en navegador.

## 11:45 — Resumen: Configuración y edición personalizada de eventos visibles en el Calendario Financiero

El usuario solicitó que el calendario ofrezca la opción de editar y configurar qué eventos financieros se desean visualizar y cuáles ocultar, permitiendo seleccionar de forma independiente los dividendos (pagos), los resultados empresariales (10-Q/10-K) y las fechas del ex-dividendo.

### Diagnóstico

1. **Filtros anteriores mutuamente excluyentes (tipo radio)**: El calendario únicamente permitía seleccionar `'all'`, `'earnings'`, `'exdiv'` o `'payout'`, impidiendo combinaciones personalizadas (por ejemplo, querer ver tanto Resultados como Pagos de dividendo pero ocultar las fechas Ex-Dividend).
2. **Falta de persistencia**: Cualquier selección de filtro se perdía al navegar a otra sección o recargar la página.
3. **Ausencia de panel de personalización**: No existía una interfaz explícita donde el usuario pudiera revisar y configurar detalladamente qué tipos de eventos y orígenes de empresas mostrar u ocultar.

### Resultado

1. **Modelo de Visibilidad Multivariable y Persistente (`public/portfolio.js`)**:
   - Sustituido el selector único `calendarFilter` por un estado estructurado de visibilidad con persistencia en `localStorage` (`cifra_calendar_visibility_v1`):
     - `earnings`: Resultados e informes SEC (10-Q / 10-K).
     - `exdiv`: Fechas Ex-Dividend (corte de cupón).
     - `payout`: Pagos y cobros de dividendos en efectivo.
     - `portfolio`: Eventos de empresas en cartera.
     - `watchlist`: Eventos de empresas en seguimiento.
   - Funciones `loadCalendarVisibility()`, `saveCalendarVisibility()` y `hasCustomCalendarFilters()`.
2. **Modal de Configuración "Editar qué mostrar" (`calendarConfigModalHtml`)**:
   - Nuevo botón en la barra de herramientas del calendario: `Editar qué mostrar` (con icono de controles y punto de aviso si hay filtros activos).
   - Modal interactivo con interruptores (checkboxes accesibles), iconos y descripciones claras para cada categoría:
     - **Resultados empresariales**: Publicación oficial de cuentas trimestrales y anuales en la SEC con análisis IA.
     - **Fechas Ex-Dividend**: Día límite con derecho a percibir el dividendo anunciado.
     - **Pagos de dividendos**: Día de liquidación o abono en cuenta del importe en efectivo.
     - **Empresas en cartera**: Mostrar eventos de posiciones compradas.
     - **Empresas en seguimiento**: Mostrar eventos de empresas seguidas en el calendario.
   - Accesos directos predefinidos (*presets*): "Mostrar todo", "Solo Dividendos (Ex-Div + Cobro)" y "Solo Resultados SEC".
   - Acciones de "Restablecer por defecto" y "Guardar y ver calendario".
3. **Filtros Rápidos en Toolbar y KPIs Interactivos**:
   - Botones rápidos en la cabecera: cada botón (`Resultados`, `Ex-Dividend`, `Cobro`) funciona como un interruptor independiente con estilo visual activo/inactivo (texto tachado/atenuado cuando está deshabilitado).
   - Tarjetas KPI interactivas: cada tarjeta muestra un distintivo de estado (`Visible` / `Oculto`) y permite alternar la categoría con un solo clic. Si un tipo de evento está oculto, la tarjeta se atenúa visualmente (`.kpi-dimmed`).
   - La tarjeta general de eventos del mes muestra el total de eventos visibles y aclara si hay filtros aplicados.
4. **Estados Vacíos y Banners Informativos**:
   - Si se ocultan todos los eventos de un mes mediante la configuración pero existen eventos en dicho periodo, se muestra un banner/estado vacío intuitivo explicando la causa y ofreciendo botones directos para "Mostrar todo" o "Editar qué mostrar".
5. **Estilos y Caché (`public/styles.css`, `public/index.html`, `public/empresa.html`)**:
   - Añadidas reglas CSS para el modal de configuración, tarjetas KPI interactivas, botones de filtro atenuados, banner de advertencia y botones predefinidos.
   - Incrementadas las versiones de caché a `styles.css?v=98` y `portfolio.js?v=64`.
6. **Verificación**:
   - Sintaxis validada con `node --check`.
   - Pruebas de filtrado en base de datos real con 290 eventos (192 resultados, 49 ex-dividendos, 49 cobros, 248 de cartera y 42 de seguimiento) comprobando el funcionamiento de todas las combinaciones y presets.

## 11:05 — Resumen: Corrección de caché estática y renderizado de negrita y tablas de 4 columnas en PDF

El usuario reportó que al descargar el informe en PDF, el documento seguía visualizándose con el formato anterior (tabla antigua de 2 columnas y sin las negritas solicitadas).

### Diagnóstico

1. **Caché estática obsoleta en disco (`uploads/generated/`)**: El endpoint `GET /api/reports/:file` (`src/api/routes/analysis.routes.js`) solo comprobaba si el archivo existía y si su tamaño era inferior a 6.000 bytes. Al existir previamente en disco archivos de ~14 KB generados horas antes, el servidor enviaba el archivo estático antiguo sin regenerarlo contra los nuevos datos y lógica de la base de datos.
2. **Corte y paginación rota en tablas SEC (`drawPdfSecSnippet`)**: En `src/services/report.service.js`, la tabla comenzaba a renderizarse incluso si apenas quedaban 50 puntos de margen al pie de página, dejando la cabecera y una única fila aislada en la página 2 y saltando el resto a la página 3 sin repetir la barra de cabecera.
3. **Parseo de negritas markdown en PDF**: El reemplazo ciego con expresiones regulares causaba anidación de asteriscos (`****28,7M****`) y dejaba caracteres `**` visibles cuando el texto ya incluía negritas.

### Resultado

1. **Invalidador y regenerador reactivo al vuelo (`src/api/routes/analysis.routes.js`)**:
   - El endpoint compara ahora la fecha de modificación (`mtime`) del archivo en disco contra la fecha de última modificación de los servicios de renderizado (`report.service.js` y `reportExport.service.js`) y contra la fecha de creación/actualización del análisis en BD.
   - Si el archivo en disco es anterior al código del motor o a la base de datos, se regeneran instantáneamente al vuelo el PDF, DOCX, HTML y ODT y se sobreescribe el disco.
2. **Motor de formateo robusto para PDF (`parseMarkdownAndNumbers` en `src/services/report.service.js`)**:
   - Tokenizador que respeta las negritas `**...**` explícitas del modelo y resalta automáticamente cifras monetarias, rangos y porcentajes sin colisiones ni etiquetas rotas.
3. **Paginación profesional y repetición de cabeceras en tablas PDF**:
   - Validación de espacio mínimo (~110 pt): si no cabe la cabecera y al menos 4 filas, la tabla inicia limpiamente en la página siguiente.
   - Repetición automática de la fila de cabecera oscura (`Métrica`, `[AÑO-1] (Año anterior)`, `Guidance [AÑO]E*`, `Cifra Proyectada [AÑO]E`) si la tabla se extiende a través de varias páginas.
4. **Regeneración total en disco**:
   - Ejecutado script de actualización para los 47 análisis almacenados en base de datos (`uploads/generated/*.pdf`, `*.html`, `*.docx`, `*.odt`).
   - Verificado con `curl` y `pdftotext` que tanto Molson Coors (ID 59) como Kraft Heinz (ID 60) devuelven el PDF con las 4 columnas completas y las negritas renderizadas.

## 11:25 — Resumen: Configuración y edición individualizada de eventos del calendario por empresa mediante icono de lápiz (✏️)

El usuario solicitó que cada ficha/chip de empresa en el panel de seguimiento del calendario disponga de un icono de lápiz (`✏️`) para poder editar de forma individual qué eventos específicos mostrar u ocultar para esa compañía en particular (dividendos, resultados empresariales 10-Q/10-K y fecha del ex-dividendo).

### Diagnóstico

1. **Configuración únicamente global**: La configuración previa permitía filtrar categorías a nivel de todo el calendario (por ejemplo, ocultar todos los ex-dividendos de todas las empresas), pero no permitía al usuario personalizar qué eventos ver de una compañía concreta (por ejemplo, querer ver los dividendos y ex-dividendos de Coca-Cola pero ocultar sus resultados 10-Q, o viceversa).
2. **Acceso directo en los chips de empresa**: Los chips de las empresas seguidas mostraban el candado de cartera (`🔒`) o el aspa de eliminación (`✕`), pero carecían de un botón de edición contextual para ajustar sus eventos específicos.

### Resultado

1. **Botón de lápiz (`✏️`) en cada chip de empresa (`public/portfolio.js`, `public/styles.css`)**:
   - Incorporado el botón `.pf-cal-chip-edit-btn` con icono SVG de lápiz en todos los chips de empresas (tanto en las de cartera permanente como en las de seguimiento).
   - Detección de configuración personalizada: si una empresa tiene filtros individuales activos, el botón de lápiz se resalta en azul (`.is-custom`) y muestra un punto indicador discreto (`.pf-cal-chip-custom-dot`).
   - Añadido botón de acceso rápido "Editar qué mostrar" en la cabecera del panel de empresas.
2. **Modal dedicado de edición por empresa (`calendarCompanyEditModalHtml`)**:
   - Al pulsar el lápiz de cualquier empresa (ej. `KO`, `CAG`, `TAP`), se abre un modal contextual con el logo oficial, ticker, nombre completo y distintivo de origen (`💼 En Cartera` vs `📅 En Seguimiento`).
   - Interruptores independientes con explicaciones detalladas:
     - **Dividendos (Pagos y cobros)**: Fechas estimadas o confirmadas de cobro del dividendo en cuenta.
     - **Resultados empresariales**: Presentación de cuentas 10-Q / 10-K ante la SEC con análisis interactivo de IA.
     - **Fecha del Ex-Dividendo**: Día de corte del cupón con derecho a cobro.
   - Vistas rápidas (presets) específicas para la empresa: "Mostrar todo", "Solo Dividendos (Ex-Div + Cobro)" y "Solo Resultados SEC".
   - Acciones de "Restablecer por defecto" y "Guardar y aplicar".
3. **Persistencia y Doble Capa de Filtrado**:
   - Persistencia local en `localStorage` con la clave `cifra_calendar_company_visibility_v1`.
   - El motor de filtrado del calendario combina ambas capas:
     1. Capa global de visibilidad (`calendarVisibility`).
     2. Capa específica por empresa (`calendarCompanyVisibility[ticker]`).
   - Actualización inmediata reactiva: al modificar o restablecer la configuración de una empresa, los eventos se recalculan al instante en la cuadrícula, la vista en lista y los contadores KPI.
   - Soporte para tecla `Escape` para cerrar limpiamente tanto los modales globales como los de empresa y detalle de eventos.
4. **Caché de activos**:
   - Actualizadas referencias a `styles.css?v=99` y `portfolio.js?v=65` en `public/index.html` y `public/empresa.html`.
5. **Verificación**:
   - Sintaxis JavaScript validada con `node --check public/portfolio.js`.
   - Comprobación de ausencia de errores de formato con `git diff --check`.

## 11:35 — Resumen: Nueva sección visual y analítica de Deuda en Informes Anuales 10-K (vencimientos a 10 años, histórico dual y refinanciación con impacto en BPA)

Se solicitó implementar una mejora integral en la sección de Deuda de los Informes Anuales (Form 10-K) generados por la IA:
1. Añadir un diagrama de barras apiladas del calendario de vencimientos de deuda acotado estrictamente a los próximos 10 años (`baseYear + 1` a `baseYear + 10`), excluyendo de forma rigurosa cualquier vencimiento a plazos más largos. Cada bloque de deuda debe tener un color identificativo según su tipología e indicar su tipo de interés cupón. En cada año se debe calcular y mostrar el tipo de interés medio ponderado pagado por las deudas que vencen en ese ejercicio, y en el pie del gráfico debe figurar un banner con el tipo de interés medio ponderado total de todo el conjunto de la deuda a 10 años.
2. Incorporar un gráfico de barras dual con la evolución histórica de los últimos 10 años comparando la Deuda Normal (Total) frente a la Deuda Neta, indicando la variación anual (Δ vs año anterior en $M) con colores semánticos (verde para reducción/desapalancamiento y rojo para aumento de deuda).
3. Añadir un análisis cuantitativo de refinanciación: comparativa del tipo de interés de la deuda retirada/amortizada vs el tipo de la nueva deuda emitida, cálculo explícito del sobrecoste o ahorro neto de intereses tras impuestos (tasa fiscal normalizada del 23%) y proyección del impacto resultante en el Beneficio por Acción (BPA en $/acción) empleando las acciones en circulación (`shares`).
4. Resaltar en negrita todos los importes monetarios, tipos de interés y cifras clave, optimizando el renderizado vectorial para una exportación limpia, profesional y elegante en PDF, Word, ODT y visualización interactiva web.

### Resultado

1. **Modelado y Motores de Cálculo (`src/services/reportExport.service.js`)**:
   - `buildDebtMaturityModel(debt, reportFiscalYear)`:
     - Aplica filtro estricto desde `baseYear + 1` hasta `baseYear + 10` (excluyendo vencimientos > 10 años como 2042 o 2046).
     - Asigna paleta cromática diferenciada (`DEBT_BLOCK_PALETTE`) por tramo/tipo de instrumento (Notas Senior, bonos en divisa extranjera, líneas de crédito, etc.).
     - Calcula el tipo medio ponderado por año: $\frac{\sum (\text{importe} \times \text{tipo})}{\sum \text{importe}}$ y el tipo de interés medio ponderado total de la deuda viva a 10 años.
   - `buildDebtHistoryModel(debt, report)`:
     - Extrae la serie histórica de hasta 10 ejercicios cerrados desde `debt.debtHistory`, `report.edgarDebtHistory` o `report.annualDebtHistory`.
     - Normaliza importes a millones mediante `toMillionsVal(v)`.
     - Computa las variaciones interanuales (`deltaTotalDebt`, `deltaNetDebt`) contra el año fiscal precedente.
   - `buildDebtRefinancingModel(debt, report)`:
     - Extrae el tipo de la deuda vencida/retirada, el tipo de la nueva deuda emitida y el importe nominal refinanciado.
     - Calcula el diferencial bruto de intereses anuales ($\Delta \text{Intereses} = \text{Importe} \times (\text{Tipo}_{\text{nuevo}} - \text{Tipo}_{\text{antiguo}})$).
     - Aplica tasa impositiva corporativa estándar (23%) para obtener el impacto neto post-impuestos ($\Delta \text{Intereses}_{\text{neto}} = \Delta \text{Intereses} \times (1 - 0{,}23)$).
     - Proyecta el impacto en BPA ($\text{Impacto BPA} = \frac{-\Delta \text{Intereses}_{\text{neto}}}{\text{Acciones en circulación}}$) en $/acción (ej. ahorro de `+0,05 $/acc` o sobrecoste de `-0,09 $/acc`).
   - Generación de tablas y gráficos multi-formato:
     - `renderDebtMaturitySvg` y `renderDebtHistorySvg` para incrustación vectorial en exportaciones HTML y previsualizaciones.
     - `buildDebtMaturityTable` y `buildDebtHistoryTable` con formato nativo para documentos Word (`.docx`) y OpenDocument (`.odt`).
     - Integración en el modelo unificado de exportación `buildReportModel`.

2. **Renderizado Vectorial en PDFKit (`src/services/report.service.js`)**:
   - `drawDebtMaturityChart`: Dibuja mediante primitivas vectoriales de PDFKit el gráfico de barras apiladas con escala calibrada, bloques de colores por emisión, badges con el tipo cupón dentro de cada segmento, etiquetas de tipo medio ponderado sobre cada columna anual y banner inferior con fondo gris pizarra (`#f1f5f9`) detallando el tipo de interés medio ponderado global a 10 años y el volumen total.
   - `drawDebtHistoryChart`: Renderiza el gráfico dual de barras emparejadas (Deuda Normal en azul marino `#1e3a8a` vs Deuda Neta en cian `#0284c7`), incluyendo insignias superiores redondeadas con la variación anual en $M$ coloreadas en verde esmeralda (`#16a34a`) para reducciones o rojo carmesí (`#dc2626`) para incrementos.
   - `drawDebtRefinancingBox`: Caja de llamada (*callout*) estilizada con borde azul sutil, badges de tipos anterior y nuevo, importe refinanciado y texto destacado en negrita con el impacto estimado en el BPA por acción.
   - Integración fluida en `conclusion.debt` con soporte tipográfico de negritas mediante `drawPdfFormattedText`.

3. **Interfaz Interactiva Web (`public/analisis.js`)**:
   - Implementadas las funciones `renderDebtMaturityChart`, `renderDebtHistoryChart` y `renderDebtRefinancingCard`.
   - Genera SVGs interactivos con tooltips y leyendas de color en la Tarjeta 3: Deuda de los análisis anuales, reflejando fielmente el calendario de 10 años, la comparativa histórica dual y la tarjeta de impacto en BPA.

4. **Agente Analista y Base de Conocimiento (`src/agents/analystAgent.js`, `src/agents/knowledge/anual/general.md`)**:
   - Enriquecidos `EXTRACTION_SCHEMA`, `EXTRACTION_PROMPT`, `ANNUAL_OUTPUT_SCHEMA` y `ANNUAL_SYSTEM_PROMPT`:
     - Regla obligatoria de limitar el calendario de vencimientos exclusivamente a los próximos 10 años.
     - Regla explícita de cálculo de impacto en BPA usando acciones diluidas (`shares`).
     - Instrucción imperativa de formatear importes, tipos y métricas prioritarias en formato Markdown Bold (`**...**`).
   - Conectada la extracción histórica de 10 años de deuda desde SEC EDGAR (`getCompanyResults`) inyectándose directamente en `result.conclusion.debt.debtHistory`.
   - Formalizada en la base de conocimiento (`src/agents/knowledge/anual/general.md`) la metodología de análisis de deuda, la regla de los 10 años de vencimiento, la fórmula de los tipos medios ponderados y el impacto por acción de la refinanciación.

5. **Pruebas y Verificación**:
   - **Pruebas unitarias de modelos**: Evaluado el cálculo de modelos con datos reales de Molson Coors (`TAP`) y Kraft Heinz (`KHC`).
   - **Exclusión estricta > 10 años**: Confirmado que vencimientos lejanos (ej. notas senior de 2042 y 2046) quedan estrictamente fuera del modelo y del gráfico.
   - **Cálculo de impacto en BPA**: Verificado el comportamiento matemático tanto en reducciones de costes (+0,05 $/acc) como en sobrecostes por mayor tipo de interés (-0,09 $/acc).
   - **Generación de documentos**: Verificada la compilación limpia de PDFs sin errores de salto de página ni desbordamientos visuales, así como la correcta generación en Word (`.docx`) y HTML.
   - **Validación de sintaxis**: Superado `node --check` sin advertencias ni errores en `reportExport.service.js`, `report.service.js`, `analisis.js` y `analystAgent.js`.

## 11:55 — Resumen: Vinculación de las reglas de Ajustes (Cartera vs Seguimiento) en la visibilidad por defecto de eventos en el Calendario

El usuario reportó que al añadir una empresa que no estaba en su cartera (como Coca-Cola, `KO`, que pertenece a Seguimiento) y abrir su configuración con el lápiz (`✏️`), aparecían activados todos los tipos de eventos (Dividendos, Resultados y Ex-Dividendo), sin respetar las preferencias configuradas en el panel de **Ajustes y Preferencias** del usuario ("Al agregar a Favoritos / Seguimiento" vs "Al añadir a la Cartera").

### Diagnóstico

1. **Valores por defecto estáticos y ciegos**: `getCompanyVisibility(ticker)` asignaba por defecto `{ earnings: true, exdiv: true, payout: true }` a cualquier compañía sin discriminación, ignorando por completo si la empresa era una posición activa de la cartera o una empresa en seguimiento, y sin consultar las preferencias de usuario guardadas en base de datos (`user_preferences`).
2. **Desconexión con las reglas automáticas**: En Ajustes, el usuario configuró:
   - **Para acciones en seguimiento**: únicamente Resultados 10-Q/10-K activos; Fechas Ex-Dividend y Pagos de Dividendos desmarcados (`false`).
   - **Para acciones en cartera**: Resultados, Fechas Ex-Dividend y Pagos de Dividendos activos (`true`).
   Al añadir `KO`, debían aplicarse de inmediato las reglas de Seguimiento y no las de Cartera.

### Resultado

1. **Inyección de preferencias en el backend (`src/services/portfolio.service.js`)**:
   - `getPortfolio` ahora incluye `getUserPreferences(userId)` en su consulta concurrente y devuelve `userPreferences` en la respuesta JSON del endpoint `/api/portfolio`.
2. **Motor de visibilidad contextual (`public/portfolio.js`)**:
   - Implementadas las funciones `isCompanyInPortfolio(ticker)` y `getCompanyDefaultVisibility(ticker)`:
     - Si la empresa está en cartera (`hasPosition(ticker)` o marcada en `calendarCompanies`): se aplican `portfolioNotifyEarnings`, `portfolioNotifyExdiv` y `portfolioNotifyPayout` (los tres activos).
     - Si la empresa está en seguimiento (como `KO`): se aplican `watchlistNotifyEarnings` (activo), `watchlistNotifyExdiv` (desactivado) y `watchlistNotifyPayout` (desactivado).
   - `getCompanyVisibility(ticker)` ahora utiliza `getCompanyDefaultVisibility(ticker)` como estado base si el usuario no ha establecido un cambio manual para esa compañía.
   - Si el usuario edita una empresa y sus opciones coinciden con las de por defecto, el override se limpia automáticamente para no acumular configuraciones redundantes en `localStorage` (`cifra_calendar_company_visibility_v2`).
3. **Interfaz contextual del Modal de Edición**:
   - En el modal de cada empresa se indica explícitamente si se están aplicando las reglas automáticas por defecto de `💼 Cartera` o de `⭐ Favoritos / Seguimiento`.
   - Se añadió la píldora de preset "Por defecto (Cartera/Seguimiento)" y el botón "Restablecer por defecto (Cartera/Seguimiento)".
4. **Sincronización reactiva con el panel de Ajustes (`public/settings.js`)**:
   - `Settings` emite el evento global `settings:change` al cargar o guardar ajustes en el modal de preferencias.
   - `Portfolio` escucha `settings:change` y re-renderiza el calendario al vuelo para que cualquier cambio en las reglas generales se propague instantáneamente a todas las empresas que sigan la configuración por defecto.
5. **Caché y Verificación**:
   - Actualizadas versiones de scripts a `portfolio.js?v=66` y `settings.js?v=4` en `public/index.html` y `public/empresa.html`.
   - Verificado mediante test unitario con datos del Usuario 3 que `KO` (seguimiento) inicia con `earnings: true, exdiv: false, payout: false` y que `CAG` (cartera) inicia con los tres activos.

## 11:50 — Resumen: Rediseño del calendario de deuda a 5 años con importes visibles en el PDF y mejor cálculo del impacto de refinanciación en BPA

El usuario mostró el informe anual generado y pidió que el calendario de deuda mostrara un gráfico del próximo año hasta 5 años vista con la cantidad que vence en cada ejercicio, visible en el PDF y con estilo similar al gráfico de acciones. Mantener los tipos de interés por bloque/año y el medio total, el gráfico histórico de 10 años de deuda normal vs neta con su variación anual, el cálculo del impacto en BPA de la refinanciación y el resaltado en negrita de las cifras clave. El problema de fondo era que la IA no siempre devolvía `maturitySchedule`, por lo que el gráfico no aparecía en el PDF.

### Resultado

1. **Extracción del calendario contractual desde SEC XBRL (`src/services/edgar.service.js`)**:
   - Nueva función `buildDebtMaturitiesFromFacts` que lee los tags XBRL `LongTermDebtMaturitiesRepaymentsOfPrincipal...` (y variantes `LongTermDebtAndCapitalLeaseObligations...` y `...Rolling...`): próximos 12 meses, años 2-5 y tramo posterior al año 5.
   - Elige el ejercicio más reciente etiquetado en un 10-K y devuelve importes en millones (`years`, `afterYearFive`, `totalAmount`, `source`).
   - Respaldo parcial: si la empresa no etiqueta el calendario, usa la porción corriente de deuda a largo plazo (`LongTermDebtCurrent`) como vencimiento del próximo año.
   - `getCompanyResults` expone ahora `debtMaturities`.

2. **Agente analista (`src/agents/analystAgent.js`)**:
   - Captura `edgarDebtMaturities` y solo lo usa si su año base coincide exactamente con el ejercicio del informe (evita datos obsoletos de 10-K antiguos).
   - Si la IA no extrajo `maturitySchedule`, lo inyecta en `conclusion.debt.maturitySchedule` junto a `maturityAfterFive`.
   - Prompts de extracción y análisis actualizados al horizonte de 5 años para el calendario (el histórico de deuda sigue siendo de 10 años).

3. **Modelo y exportaciones (`src/services/reportExport.service.js`)**:
   - `buildDebtMaturityModel` reescrito: ventana fija de 5 años (año+1 a año+5, con huecos vacíos si no hay vencimientos), importes por año, tipos medios solo cuando existen datos de cupón, tramo posterior al año 5 y color naranja único cuando solo hay un tipo de deuda (paleta multicolor si hay varios tramos).
   - `buildDebtMaturityTable`, `renderDebtMaturitySvg` y `renderDebtHistorySvg` actualizados: importes en negrita sobre cada barra, banner con el tipo medio total y la deuda a amortizar (+ tramo posterior al año 5) y variación anual de deuda normal y neta bajo cada barra.

4. **PDF (`src/services/report.service.js`)**:
   - `drawDebtMaturityChart` rediseñado a 5 años con etiqueta de importe en negrita por año, tipos por bloque y por año cuando existen, y banner inferior con tipo medio total, total a amortizar y deuda posterior al año 5.
   - `drawDebtHistoryChart` corregido: las variaciones ya no usan el carácter Unicode Δ (se corrompía en Helvetica) y ahora muestra debajo de cada barra la variación de la deuda normal y de la deuda neta, en verde/rojo.
   - `drawDebtRefinancingBox` incluye también la explicación del cálculo de intereses/BPA.

5. **Cálculo de refinanciación mejorado**:
   - Análisis de texto con números en formato europeo (`2.434,1`) y nuevos patrones para el tipo nuevo ("coste estimado de refinanciación… 5 %") y el volumen ("asciende a 2.434,1M"), tanto en el servidor como en `public/analisis.js`.
   - Verificado con TAP: deuda anterior al 3,00 % → nueva al 5,00 %, 2.434,1M refinanciados, +48,7M de intereses brutos, -0,19 $/acción de impacto en BPA.

6. **Web (`public/analisis.js`) y base de conocimiento (`src/agents/knowledge/anual/general.md`)**:
   - Gráfico web del calendario reescrito a 5 años con importes y respaldo XBRL, y variaciones duales en el histórico.
   - Reglas de deuda actualizadas al calendario de 5 años y tramo posterior.

7. **Verificación**: modelos y extracción probados con datos reales de TAP, KHC, GIS, CPB, SJM y HRL; PDFs generados y revisados visualmente (gráficos a 5 años, histórico dual con ambas variaciones y caja de refinanciación con el cálculo en negrita); HTML y tablas Word/ODT comprobados sin errores.

## 12:12 — Resumen: Análisis simultáneos — una sesión de IA por análisis (OpenCode Go)

Se solicitó que varios usuarios puedan analizar informes con IA a la vez: al iniciar un segundo análisis mientras el primero seguía en curso, el segundo quedaba bloqueado hasta que terminara el primero.

### Resultado

- **Diagnóstico** (servidor aislado en el puerto 3001 + base de datos de prueba + registro de cada `fetch` a la API): el pipeline ya era asíncrono, pero **todos los análisis compartían una única sesión global** (`currentSessionId`) en el proveedor OpenCode Go. La documentación de Go exige enviar `x-opencode-session` estable **por conversación** para enrutado y caché de prompt. Con dos análisis concurrentes, las llamadas se intercalaban desde el cliente pero el backend agrupaba ambas en la misma sesión/conversación: una de ellas acabó en timeout de 180 s y un total de 222 s (el timeout de `AI_REQUEST_TIMEOUT_MS` más un reintento), lo que el usuario percibía como "bloqueo" del segundo análisis.
- **Contexto de IA por análisis** (`src/services/ai/modelProvider.js`): nuevo `AsyncLocalStorage` (`aiContext`) que propaga el `sessionId` del análisis a todas las llamadas de los agentes sin tocar la firma de cada agente.
- **Sesión por análisis** (`src/services/analysis.service.js`): `analyzeText` genera un `randomUUID()` por análisis y ejecuta todo el pipeline dentro de `aiContext.run`; log conciso `[analysis] sesión IA xxxxxxxx · archivo`.
- **Proveedor OpenCode Go** (`src/services/ai/providers/opencode-go.provider.js`): se elimina la sesión global; el orden de resolución es `options.sessionId` → `defaultSessionId` (solo si un script lo fija con `setSessionId`) → UUID nuevo por llamada. Se añade `User-Agent` propio (`AI_CLIENT_USER_AGENT`, por defecto `Cifra-FinancialAnalyzer/0.1`) tal como pide la documentación de Go para identificar al cliente.
- **Documentación** (`.env.example`): variable opcional `AI_CLIENT_USER_AGENT` comentada.
- **Verificación**:
  - Antes: dos análisis concurrentes usaban la misma sesión `95ec087f`; tiempos 33 s y 222 s (timeout + reintento).
  - Después: dos análisis concurrentes usan sesiones distintas (`325a669a` y `63bb4d27`), las llamadas se solapan en paralelo y ambos terminan en **~38 s**. La sesión se mantiene estable dentro de cada análisis (origen, sector, extracción y análisis final comparten la del análisis).
  - `node --check` de los módulos modificados y `git diff --check` correctos; servidor del usuario recargado con el cambio; base de datos y artefactos de prueba eliminados.

## 12:16 — Resumen: Corregido bloqueo del navegador al ver/regenerar el 10-K de Molson Coors (TAP)

Se solicitó arreglar que al pulsar "Regenerar" o "Ver análisis" en el último 10-K de Molson Coors la página se quedaba bloqueada, mientras que con un 10-Q funcionaba bien.

### Resultado

- **Diagnóstico**: bucle infinito en el navegador dentro de `formatAnnualRichText` (`public/analisis.js`). El texto del 10-K (`conclusion.debt.refinancingAnalysis`) contiene `"en torno al 3,0 %-3,44 %"`; los regex de resaltado generan `<strong><strong>3,0 %</strong>-3,44</strong>` y el `while (s.includes('<strong><strong>'))` no encontraba nunca el cierre `</strong></strong>` en la misma línea, por lo que el bucle no avanzaba y bloqueaba la pestaña. Solo afecta a informes anuales 10-K porque esta sección no se renderiza en los 10-Q.
- **Corrección** (`public/analisis.js`): bucle acotado (máx. 20 iteraciones) con detección de progreso; si el reemplazo no cambia la cadena, se colapsan los `<strong><strong>` duplicados y se sale. El regex interno pasa a `[\s\S]*?` para abarcar saltos de línea. La limpieza posterior (`</strong>…</strong>`) deja el HTML equilibrado.
- **Caché**: subida la versión del script a `?v=18` en `public/index.html` y `public/empresa.html` para saltar el `max-age` de 7 días de los `.js` y asegurar que el arreglo llega al navegador.
- **Segundo intento del usuario bloqueado**: tras el arreglo, el usuario seguía viendo el cuelgue porque `readTemplate` (`src/services/seo.service.js`) cachea `index.html`/`empresa.html` en memoria desde el arranque del proceso; el servidor seguía sirviendo `analisis.js?v=17` (versión con el bucle) aunque los archivos ya estaban en `v=18`. Se reinició el servidor (touch a `server.js`, `--watch`) y se verificó que `/` y `/empresa` ya sirven `v=18`. El navegador debe recargar sin caché para descargar el JS corregido.
- **Verificación**: reproducido el cuelgue con el informe real de TAP (analyses id 59) y comprobado el arreglo sobre todos los campos anuales del 10-K y del 10-Q de TAP: todos terminan en 0-1 ms y con etiquetas `<strong>` balanceadas. Además, `renderReport` completo con DOM simulado sobre los informes 10-K y 10-Q de TAP: 4 ms/0 ms sin bloqueo, y casos límite (rangos `3,0 %-3,44 %`, multilínea, miles de `<strong>` anidados) por debajo de 1 ms. `node --check public/analisis.js` correcto.

## 12:52 — Resumen: Recompras anuales completas (KHC) y calendario de deuda a 5 años garantizado en el PDF

Se solicitó que el análisis anual (10-K) hiciera bien la sección de recompras, tomando como referencia el informe de Molson Coors (TAP), y se confirmó que el calendario de deuda con gráfico de barras (próximo año a 5 años) debe aparecer siempre en el PDF.

### Resultado

1. **Respaldos deterministas SEC/XBRL en `src/agents/analystAgent.js`**:
   - `buildSharesHistoryFromEdgar`: 5 ejercicios de acciones en circulación al cierre (`sharesOutstanding`) desde la serie anual de la SEC.
   - `buildRepurchaseHistoryFromEdgar`: serie anual de recompras (`PaymentsForRepurchaseOfCommonStock`) con fecha de cierre, en millones.
   - Ambos se inyectan en `annualDetails.repurchases` antes de la estructuración, por lo que la IA redacta la narrativa y la tabla con datos reales aunque el 10-K no traiga tabla propia.
   - `extractRepurchaseProgramTerms`: extrae del texto completo autorización (`repurchase up to $X`), fecha de aprobación y vencimiento (`through ...`), y `extractRemainingAuthorization` ampliado para frases del tipo "remaining authorization under the share repurchase program of approximately $1.5 billion".
   - `buildRepurchaseSecTable`: tabla multianual canónica (columna por ejercicio con fecha de cierre, fila "Aggregate cost (in millions)" y "Remaining authorization (in millions)") como respaldo cuando la IA devuelve una tabla de un solo año.
   - `buildShareCountEvolutionText` y `buildFutureProjectionText`: texto de evolución y proyección a 5 años deterministas; sustituyen placeholders del tipo "no disponible"/"sin variación" cuando los datos reales los contradicen.
2. **Calendario de deuda siempre inyectado**:
   - El calendario XBRL (`debtMaturities`) se inyecta en `annualDetails.debt.maturityItems` antes de la estructuración, de modo que el texto del análisis es coherente con el gráfico.
   - Corregido el bug por el que `conclusion.debt.maturitySchedule: []` (array vacío, truthy) impedía el respaldo EDGAR y dejaba el gráfico sin dibujar.
3. **Corregido el bug que dejaba análisis del usuario sin `report`** (`src/api/routes/screener.routes.js`): al vincular un análisis público ya existente al historial del usuario se creaba la fila con `pdfUrl` pero sin `report`; ahora se copian también `origin`, `sector`, `report` y `model_used`. Esta era la causa de la fila 62 de KHC (usuario admin) con `report = NULL`.
4. **PDF** (`src/services/report.service.js`): `sanitize` elimina los marcadores Markdown `**` que quedaban literales en negritas dentro de bullets, watchlist y notas (p. ej. `**~1.500M$**`).
5. **Base de conocimiento** (`src/agents/knowledge/anual/general.md`) y prompts de extracción/estructuración actualizados: formato canónico de tabla multianual de recompras, prohibición de tabla de un solo año y obligación de reflejar autorización, vigencia y remanente.
6. **Reparación de los análisis de KHC**:
   - Ejecutado el pipeline completo (10-K + presentación 8-K, sesión DeepSeek directo) y actualizadas las filas 62 (usuario admin) y 60 (público) con el nuevo informe; eliminada la fila temporal del reanálisis.
   - Verificado en el PDF resultante: narrativa con programa (3.000M autorizados en noviembre de 2023, vencimiento 26 de diciembre de 2026, remanente 1.500M), gráfico de acciones de 5 años (2021-2025), tabla SEC multianual 2021-2025 con coste agregado y remanente, gráfico de vencimientos 2026-2030 con importes y banner, y outlook con guidance del 8-K intacto.
7. **Verificación técnica**: `node --check` correcto en `analystAgent.js`, `screener.routes.js` y `report.service.js`; `git diff --check` sin errores; 0 asteriscos literales en el PDF final.

## 12:58 — Resumen: "Regenerar" vuelve a llamar a la IA desde cero (bug de Express 5 en `req.query`)

Se solicitó que al pulsar "Regenerar" el informe se rehiciera desde cero, es decir, llamando de nuevo a la API de IA y no sirviendo la copia cacheada.

### Diagnóstico

- En Express 5 `req.query` es un getter sin caché que vuelve a parsear la query string en cada acceso. La ruta `POST /company/:ticker/filings/:accession/regenerate` (`src/api/routes/screener.routes.js`) hacía `req.query.force = '1'`; la mutación se aplicaba a un objeto temporal y se perdía al siguiente acceso, por lo que el handler de `/analyze` calculaba `force = false` y devolvía el análisis guardado al instante sin llamar a la IA.

### Resultado

1. **Corrección** (`src/api/routes/screener.routes.js`):
   - La regeneración se propaga mediante `res.locals.forceRegeneration = true`; el handler de `/analyze` lo incluye en la condición de `force`. Eliminada la mutación de `req.query`.
   - `preservePublic`: la comprobación previa a la regeneración ahora busca la fila pública (`userId: null`) en lugar de la del usuario; al regenerar un informe público se mantiene público y sin propietario (mismo criterio que el panel de administración), y `userId` se pasa como `null` en ese caso para que `is_public` se conserve.
2. **Verificación end-to-end**: `POST /api/screener/company/KHC/filings/0001637459-26-000009/regenerate` con sesión de administrador tardó **49,5 s** (llamada real a DeepSeek directo, tras el cambio de `.env` a `AI_PROVIDER=deepseek`), devolvió `cached: false`, `saved: true` y un análisis nuevo (id 65) con 5 años de acciones, tabla de recompras 2021-2025, remanente 1.500M, vigencia diciembre 2026, 5 años de vencimientos y outlook del 8-K.
3. **Estado de datos**: restaurada la copia pública de KHC (id 60) con el informe regenerado para no perder la caché pública compartida tras la prueba; `node --check` y `git diff --check` correctos.

## 13:37 — Resumen: Histórico de deuda a 10 años, vencimientos con tipo de interés por tramo y eliminación de la tabla de deuda redundante

Se solicitó que el gráfico de evolución de la deuda mostrara los últimos 10 años (como la referencia de TIKR), que el calendario de vencimientos dibujara una barra por cada vencimiento con su tipo de interés (y el tipo medio total abajo) y que se retirara la tabla "Debt Obligations — Contractual Maturities" cuando el gráfico ya muestra el calendario.

### Diagnóstico

1. **Histórico de solo 2 años**: la IA devolvía `conclusion.debt.debtHistory` con años sin importes (2021-2023 con `totalDebt: null`) y, al ser una lista no vacía, impedía el respaldo EDGAR; el gráfico descarta los puntos sin importe y quedaba limitado a 2024-2025 pese a existir la serie oficial de 10 años.
2. **Sin tipos por tramo**: la nota de deuda del 10-K (con los cupones de cada emisión) está en el carácter ~402.000 del texto y el contexto enviado a la extracción solo cubría hasta ~80.000 (cabecera + ventana de estados financieros), por lo que la IA nunca veía los tipos. Además el gráfico apilaba los tramos en una sola barra por año.
3. **Tabla redundante**: el "EXTRACTO OFICIAL SEC" de deuda repetía el mismo calendario que ya muestra el gráfico.

### Resultado

1. **Histórico de 10 años garantizado**:
   - `analystAgent.js`: el merge combina la serie de la IA con la oficial de EDGAR (prioridad) y recorta a los últimos 10 ejercicios.
   - `buildDebtHistoryModel` (`reportExport.service.js`) y `renderDebtHistoryChart` (`public/analisis.js`) combinan todas las fuentes (`debtHistory`, `edgarDebtHistory`, `annualDebtHistory`) por año, dan prioridad a EDGAR y rellenan huecos de deuda neta.
2. **Calendario con barras agrupadas y tipos**:
   - Los tres renderizadores (PDF `drawDebtMaturityChart`, HTML `renderDebtMaturitySvg` y web) dibujan una barra por cada vencimiento del año, con el tipo cupón rotulado dentro de la barra (o encima si no cabe), el importe total del año sobre el grupo, la leyenda de tipos si hay varios tramos y el tipo medio ponderado total en el banner inferior. Eliminada la etiqueta "Media" por año para evitar solapes.
   - `analystAgent.js` conserva los tramos detallados de la IA y solo completa con el agregado XBRL los años sin detalle, evitando doble cómputo.
   - Extracción reforzada: `EXTRACTION_PROMPT` exige un elemento por emisión (importe en USD, cupón y tipo) y `ANNUAL_SYSTEM_PROMPT` exige dos entradas cuando dos vencimientos caen el mismo año.
   - **Secciones dirigidas del filing** (`extractKeyFilingSections`): se rescatan del texto completo la nota de deuda, la tabla de vencimientos contractuales, el programa de recompras y las acciones en circulación, y se añaden al contexto de extracción (presupuesto de 80.000 caracteres rebalanceado). Verificado: la extracción de TAP ya devuelve "CAD 500M 3.44%" (364,3M) y "$2.0B 3.0%" (2.000M) como dos tramos de 2026.
3. **Tabla de deuda retirada**: cuando el gráfico de vencimientos existe, se omite el extracto SEC de "Debt Obligations" en PDF, HTML, Word/ODT y web (`table: maturityChart ? null : ...`).
4. **Tipos con 2 decimales** (`3,44 %`, `3,00 %`) en los tres renderizadores y caché de `analisis.js` subida a `v=19`.
5. **Verificación y datos**:
   - Análisis real de TAP (10-K FY2025 con presentación 8-K, DeepSeek): `debtHistory` con 10 ejercicios (2016-2025), dos barras en 2026 con 3,44 % y 3,00 %, tipos nulos solo en los años sin detalle y banner con tipo medio total del 3,07 %. PDF y HTML revisados visualmente (leyenda "Senior Notes"/"Deuda total", histórico dual de 10 años, sin tabla redundante).
   - Informe persistido en las filas 61 (usuario) y 66 (pública) de TAP con artefactos regenerados.
   - `node --check` en `analystAgent.js`, `report.service.js`, `reportExport.service.js` y `public/analisis.js`; `git diff --check` correcto. Servidor relanzado en segundo plano con la nueva configuración.

## 14:00 — Resumen: Tabla de recompras con último año resaltado, tipo medio de TODA la deuda, vencimientos apilados por emisión y refinanciación posible explicada

Se solicitó: (1) en la tabla de recompras resaltar solo la columna del último ejercicio en un color distinto; (2) que el banner del calendario muestre el tipo de interés medio de TODA la deuda y no solo el de los próximos 5 años; (3) que en el gráfico de vencimientos los tramos de un mismo año se apilen en una única barra con colores distintos y el cupón dentro de cada segmento, con el total del año encima; y (4) que la caja de refinanciación etiquete las estimaciones como "posible tipo de nueva emisión" y "posible impacto en BPA", calculando el impacto aunque no haya decisión y explicando la base del tipo estimado (mercado, emisiones recientes y valor razonable de la deuda del 10-K).

### Resultado

1. **Tabla de recompras (PDF, HTML/Word/ODT y web)**:
   - Detección de tabla de recompras y resaltado exclusivo de la columna del ejercicio más reciente (verde `#f0fdfa`/`#0d9488`); las demás columnas quedan neutras. Eliminado el resaltado por filas (amarillo/naranja) y el verde del ejercicio más antiguo, que resultaba confuso.
   - `latestYearIdx` se deduce de la cabecera con el año máximo, tanto si las columnas van de más reciente a más antigua como al contrario.
2. **Tipo de interés medio de toda la deuda**:
   - `buildDebtMaturityModel` pondera ahora todos los tramos con cupón conocido (incluidos los posteriores al año 5) y, con `rateFromSecSnippet`, usa la tabla oficial de deuda (cupón por emisión y saldo del ejercicio más reciente) cuando cubre más deuda que los tramos detallados. En TAP pasa del 3,07 % (5 años) al **3,85 %** de toda la deuda.
   - `analystAgent.js` inyecta `annualDetails.debt.allDebtAverageRate` (calculado por `computeAllDebtAverageRate`) antes de la estructuración y el prompt obliga a la IA a usar exactamente esa cifra en el texto, con lo que narrativa y banner quedan alineados.
   - Etiqueta del banner en los tres renderizadores: "Tipo de interés medio total de la deuda".
3. **Calendario con barras apiladas**:
   - Una barra por año; cada vencimiento es un segmento con color propio (`DEBT_STACK_PALETTE`), ordenados de mayor a menor importe, con el cupón a dos decimales dentro del segmento e importe total del año encima. Eliminadas la leyenda por tipo y la etiqueta "Media" anual (el tipo medio global va en el banner).
4. **Refinanciación posible**:
   - `buildDebtRefinancingModel` detecta escenarios sin decisión tomada (`possible`) y calcula igualmente volumen, sobrecoste y `epsImpact`. El volumen se toma de la narrativa o, en su defecto, de la suma de los vencimientos del primer año del calendario; el "tipo deuda actual" pasa a ser el tipo medio ponderado de esos vencimientos.
   - Etiquetas: "Tipo deuda actual", "Posible tipo nueva emisión", "Volumen a refinanciar" y "Posible impacto en BPA" (PDF y web); el badge del HTML/Word/ODT usa "Posible refinanciación".
   - El prompt exige explicar la base del tipo estimado: valor razonable de la deuda frente a valor en libros del 10-K (descuento de mercado), cupones de emisiones/refinanciaciones recientes y nivel general de tipos.
5. **Corrección de parseo numérico** (`parseSecNumber` y equivalentes en web): los importes tipo `$2,000.0` o `12,906,851` se interpretaban mal (2 y 12,9); ahora se distingue el separador decimal por posición y se tratan los millares correctamente. Esto afecta al cálculo del tipo medio de toda la deuda y al precio medio de las recompras.
6. **Verificación con TAP (10-K FY2025 + presentación, DeepSeek)**:
   - PDF revisado: tabla de recompras con la columna de diciembre de 2025 resaltada; calendario 2026 con una barra de dos segmentos (3,44 % y 3,00 %) y total $2364,3M encima; banner "Tipo de interés medio total de la deuda: 3,85 %"; caja "Posible refinanciación" con 3,07 % / 5,25 % / $2425M / -0,20 $/acc y explicación de la base del tipo.
   - Informe actualizado en las filas 61 (usuario) y 68 (pública) de TAP; `analisis.js` a `v=20`; servidor relanzado con el código nuevo; `node --check` y `git diff --check` correctos.

## 14:25 — Resumen: Importe por segmento en el calendario de deuda, WK corregido con respaldo de balance XBRL y restauración del 10-Q de TAP

Se solicitó: (1) que en la barra apilada de 2026 cada color muestre también su importe; y (2) corregir el WK del Cash Flow, que aparecía en 0 porque no se estaban tomando inventarios, cuentas por pagar ni cuentas por cobrar.

### Resultado

1. **Importe por segmento en el calendario de vencimientos** (PDF, HTML y web):
   - Cuando un año tiene varios tramos, cada segmento muestra ahora su importe (p. ej. `$364,3M` y `$2.000,0M`) y su cupón (`3,44%`, `3,00%`). Con un solo tramo se mantiene solo el cupón y el total del año encima.
   - Formato de millones con separador de miles (`fmtMillions`) aplicado también al total anual y al banner (`$2.364,3M`, `$2.367,5M`, `$3.841,6M`).
2. **WK corregido con respaldo de balance XBRL** (`src/agents/analystAgent.js`):
   - En el bloque anual de EDGAR se rellenan inventarios, cuentas por pagar, cuentas por cobrar, caja, inversiones a corto plazo y deuda total (y sus valores de inicio de ejercicio) cuando la extracción del PDF los deja vacíos o en 0. También se completa la variación de capital circulante reportada (`workingCapitalChange` de EDGAR, en millones) si la extracción no la trae.
   - TAP FY2025 pasa de `(0 - 0 - 0) × 3% = 0M` a `(1823 - 716 - 703) × 3% = 12,1M` con circulante reportado de `-147M`; el escenario ajustado muestra Cash Flow 1943,5M frente a 1784,4M normal. La asignación de capital también mejora al usar caja/deuda reales.
3. **Restauración del análisis del 10-Q Q2 2026 de TAP (fila 61)**: los scripts de reparación anteriores (que actualizaban las filas 61 y 68 del 10-K) sobrescribieron por error la fila 61, que en realidad correspondía al 10-Q Q2 2026. Se reanalizó el filing `0000024545-26-000071` con el pipeline completo y se restauró la fila 61 (formType 10-Q, periodo 2026 Q2); el 10-K FY2025 se mantiene en la fila pública 71.
4. **Verificación**: PDF del 10-K de TAP con `$364,3M`/`3,44%` y `$2.000,0M`/`3,00%` dentro de la barra de 2026, total `$2.364,3M`, banner de tipo medio de toda la deuda 3,85 % y tabla de WK con valores reales; 10-Q restaurado. `analisis.js` a `v=21`; servidor relanzado; `node --check` y `git diff --check` correctos.

## 14:50 — Resumen: Adquisiciones no materiales ocultas, CAGR en deuda y dividendos, y nueva sección de dividendos con payout sobre BPA ajustado

Se solicitó: (1) no mostrar adquisiciones/desinversiones cuando no sean importantes; (2) mostrar el CAGR de la deuda de los últimos 10 años y hacer lo mismo con el importe del dividendo; y (3) añadir una sección de dividendos cuando haya aumento/recorte, con gráfico de barras del importe y línea del payout sobre el BPA ajustado por año.

### Resultado

1. **Adquisiciones no materiales ocultas** (`buildAcquisitionsModel` en `reportExport.service.js`):
   - La sección "4: Adquisiciones" solo se muestra si las adquisiciones o desinversiones del ejercicio alcanzan los 50M$ (leídos de la asignación de capital) o si la narrativa describe una operación (no coincide con "no se realizaron / no hubo / no material").
   - Aplicado en PDF, HTML/Word/ODT y web. En TAP (22,3M de adquisiciones y 15,8M de desinversiones) la sección desaparece.
2. **CAGR del periodo en el gráfico histórico de deuda**:
   - `buildDebtHistoryModel` calcula el CAGR de la deuda normal y de la deuda neta entre el primer y el último ejercicio (hasta 10 años) y se muestra bajo el título del gráfico en PDF, HTML y web (p. ej. "CAGR del periodo (2016–2025): deuda normal -7,0 % · deuda neta -8,1 %").
3. **Nueva sección "5: Dividendos"** (solo si hay un cambio relevante, ≥ 2 %):
   - **Datos**: `buildDividendHistoryFromEdgar` construye la serie anual (dividendo por acción, importe total pagado y BPA diluido reportado) desde XBRL; la IA aporta el BPA ajustado/subyacente por año desde el 10-K y el 8-K; `mergeDividendHistory` combina ambos y el agente calcula `changeType`/`changePct`. La fusión final crea la sección `conclusion.dividends` si no la generó la IA y el cambio supera el 2 % (o si la IA ya la incluyó).
   - **Gráfico** (`buildDividendModel` + `drawDividendChart`/`renderDividendSvg`/`renderDividendChart`): barras del dividendo total ($M) por año y línea del payout sobre BPA ajustado (eje derecho), con etiquetas por punto, CAGR del importe y por acción, nota "* años con BPA reportado (sin ajustado)" y leyenda. Se degrada a tabla en Word/ODT y se integra en la tarjeta web.
   - **Prompts**: `EXTRACTION_SCHEMA`/`EXTRACTION_PROMPT` extraen `annualDetails.dividends` (histórico con dps, total y `adjustedEps`, más `changeType`/`changePct`/`changeDate`); `ANNUAL_OUTPUT_SCHEMA`/`ANNUAL_SYSTEM_PROMPT` generan `conclusion.dividends` solo cuando hay aumento, recorte, suspensión o cambio relevante.
4. **Verificación con TAP (10-K FY2025 + 8-K, DeepSeek)**:
   - Sección "5: Dividendos" con aumento del 6,8 % (1,76 $ → 1,88 $), payout 2025 del 34,7 % sobre BPA ajustado (5,42 $) y CAGR 2021–2025 del +26,3 % en importe y +28,9 % por acción.
   - "4: Adquisiciones" ya no aparece; el histórico de deuda muestra el CAGR del periodo.
   - Fila pública 74 actualizada con el informe y los artefactos regenerados; `analisis.js` a `v=22`; servidor relanzado; `node --check` y `git diff --check` correctos.

## 15:05 — Resumen: Dividendo por acción en el gráfico y numeración dinámica de la sección de dividendos

Se solicitó mostrar el dividendo por acción en las barras (en vez del importe total) y corregir la numeración de la sección de dividendos, que debe ser 4 cuando no se muestra la sección de adquisiciones.

### Resultado

1. **Barras por dividendo/acción**: en PDF, HTML y web, las barras del gráfico de dividendos usan ahora `dps` (dividendo por acción) con eje izquierdo en dólares (`$2,0`, `$1,3`...), etiqueta por barra (`1,88 $`) y leyenda "Dividendo por acción"; el payout sobre BPA ajustado se mantiene en la línea del eje derecho. La tabla de Word/ODT conserva ambas columnas (por acción y total).
2. **Numeración dinámica**: `buildDividendModel` y la vista web numeran la sección como **4: Dividendos** cuando no hay adquisiciones materiales y como **5: Dividendos** cuando sí las hay; se elimina el número que hubiera puesto la IA y se recomputa.
3. **Etiquetas del payout**: cuando el punto de payout queda cerca de la etiqueta del dividendo, la etiqueta se dibuja a la derecha del punto para evitar solapes.
4. **Verificación**: regenerada la fila pública de TAP (id 77) y revisado el PDF: sección "4: Dividendos" con barras 0,68 $/1,52 $/1,64 $/1,76 $/1,88 $, línea de payout (14,7 %/28,3 %/29,5 %/34,7 %), CAGR +26,3 % importe y +28,9 % por acción; sin sección de adquisiciones. `analisis.js` a `v=23`; servidor relanzado; `node --check` correcto.

## 15:20 — Resumen: Cada punto de la Parte II en una página independiente en el PDF

Se solicitó que cada punto de la Parte II del informe anual (Recompras, Outlook, Deuda, Adquisiciones, Dividendos y Cosas a tener en cuenta) comience en una página propia para que el PDF no quede compactado.

### Resultado

- **`buildReportPdf`** (`src/services/report.service.js`): nuevo helper `startSectionPage()` que añade página y reinicia el cursor vertical; se invoca al inicio de "2: Outlook", "3: Deuda", "4: Adquisiciones" (cuando se muestra), "Dividendos" (4/5) y "Cosas a tener en cuenta". La "Nota de Resultados" (Parte III) también comienza en página propia si no está ya al principio de una.
- La sección "1: Recompras" comparte página con la cabecera de la Parte II; el resto de puntos ocupan una página cada uno (las secciones que no aplican no dejan huecos porque el salto se realiza solo al renderizar cada bloque).
- **Verificación** en el PDF de TAP: página 2 (Parte II + Recompras), 3 (Outlook), 4 (Deuda), 5 (Dividendos), 6 (Cosas a tener en cuenta) y 7 (Nota de Resultados). Servidor relanzado; `node --check` y `git diff --check` correctos.

## 13:06 — Resumen: Ajustes reorganizados por pestañas + Apariencia (tema, modo oscuro) e idioma guardado en BD

Se solicitó organizar el modal de ajustes en secciones, añadir selector de idioma (guardado en BD aunque aún sin efecto de traducción), elegir entre dos temas de fondo (índigo actual y naranja clásico) y un modo oscuro.

### Resultado

1. **BD** (`db/schema.sql` + migración aplicada): columnas nuevas en `user_preferences` → `language TEXT DEFAULT 'es'`, `theme TEXT DEFAULT 'indigo'`, `dark_mode BOOLEAN DEFAULT false` (ALTER con `ADD COLUMN IF NOT EXISTS` tras el CREATE, orden seguro para BD existentes y nuevas).
2. **Backend** (`db/repositories/watchlistRepository.js`): `getUserPreferences`/`updateUserPreferences` devuelven y persisten los tres campos nuevos, con normalización/validación (`language ∈ es|en`, `theme ∈ indigo|naranja`, `darkMode` booleano); valores inválidos caen a defaults. Verificado end-to-end con usuario temporal: GET/PUT correctos y PUT inválido no persiste valores fuera de lista.
3. **Frontend**:
   - Modal de ajustes reorganizado en 4 pestañas con navegación lateral (vertical en escritorio, horizontal scrollable en móvil): **Apariencia** (nuevo: tarjetas de tema Índigo/Naranja con miniaturas, interruptor de modo oscuro, selector de idioma Español/English con nota "traducción próximamente"), **Perfil**, **Seguimiento** y **Cartera**. Botón de guardado global común. Cambios duplicados en `index.html` y `empresa.html` (misma estructura/ids).
   - **`public/theme.js` (nuevo)**: aplica `data-theme`/`data-mode` en `<html>`, cachea en `localStorage` (`cifra-appearance`) y sincroniza con las preferencias del servidor vía eventos `settings:change` (aplica+guarda) y `settings:preview` (previsualización en vivo al elegir tema/oscuro en el modal; se revierte al cerrar sin guardar). Snippet inline en `<head>` de ambas páginas para evitar parpadeo al recargar.
   - **`public/settings.js` (v5)**: lógica de pestañas, campos nuevos (`language`, `theme`, `darkMode`) en defaults/render/guardado, emisión de preview al cambiar tema u oscuro y revertido al cerrar.
4. **CSS** (`public/styles.css`): sistematizado el color a variables — extraídos ~40 hex hardcodeados a tokens (`--accent*`, `--surface*`, `--line*`, `--muted*`, info azul, verde/rojo/ámbar) con sustitución consciente de propiedad (color/fondo/borde); bloque `html[data-theme="naranja"]` (paleta #ff9900 clásica con topbar #242424), bloque `html[data-mode="dark"]` (superficies #14181e, textos claros, acordes suaves translúcidos) y variante naranja+oscuro; ajustes puntuales para modales de reportes con estilos inline. Hover de botón primario vía `--accent-hover` (conserva #6366f1 en índigo).
5. **Verificación**: `node --check` en JS modificado, balance de llaves CSS = 0, servidor arrancado con páginas y assets 200 (`/`, `/empresa`, `/theme.js`, `/settings.js`), modal presente en ambas páginas y migración aplicada en PostgreSQL.

## 13:45 — Resumen: Arreglo de ajustes rotos por caché de CSS y pulido del modo oscuro

Se reportó que el modal de ajustes se veía sin estilos y que el tema de color y el modo oscuro no aplicaban cambios.

### Diagnóstico

- El `<link>` de `styles.css` seguía en `?v=99`, por lo que el navegador servía la copia cacheada sin las reglas nuevas (pestañas, tarjetas de tema, interruptor); los atributos `data-theme`/`data-mode` se aplicaban pero no había CSS que los pintara.

### Resultado

1. **Caché**: `styles.css` subido a `?v=100` en `index.html` y `empresa.html`.
2. **Verificación con Chrome headless** (capturas reales): modal correcto en los 4 estados (índigo, naranja, oscuro, naranja+oscuro) y página completa (KHC) consistente en oscuro: topbar, sidebar, paneles, gráfico, mini-tarjeta de empresa y listas.
3. **CSS**: segunda pasada de variables — fondos claros residuales (`#fafcff`, `#fcfcfc`, `#f2f2f2`, etc.) a `--surface-*`; rojos/verdes/ámbar de badges a `--red-bg/--green-bg/--amber-bg` + nuevos `--red-border/--green-border/--amber-border`; textos oscuros (`#202020`, `#323a42`, `#9a9a9a`, `#8a99aa`, `#5b6a7d`) a tokens; bordes `#d3d3d3/#d6d6d6/#dfe2e6/#e2e2e2` a `--line*`; pulido de `.theme-option` (hover con elevación, `focus-visible`, título en columna) y `theme-color` del `<meta>` ahora dinámico en `theme.js`.

## 13:55 — Resumen: Arreglada la navegación entre pestañas del modal de Ajustes

Se reportó que no se podía pasar a otros apartados (Cartera, Perfil, Seguimiento) dentro del modal de ajustes.

### Diagnóstico

- `switchTab`/`initTabs` en `settings.js` consultaban `.settings-nav-item` dentro del `<form>` (`form.querySelectorAll`), pero el menú de pestañas está fuera del form (hermano de `.settings-panels`), así que nunca se encontraban ni se les adjuntaba el listener de clic.

### Resultado

1. **Corrección** (`public/settings.js`, v6): `switchTab` e `initTabs` ahora consultan sobre `modalBackdrop` (contenedor del modal completo) en lugar del form; el estado activo y el `hidden` de los paneles se aplican sobre el scope correcto.
2. **Verificación**: subido a `?v=6` en ambas páginas; simulación de clic con Chrome headless confirma que el panel de Cartera se muestra y el elemento del menú queda activo; `node --check` correcto.

## 15:45 — Resumen: Corregidos el signo del ajuste de circulante en Cash Flow y el gráfico de dividendos (2021/2022)

Se revisó el informe de TAP 2025 y se pidió: (1) que la nota del ajuste de capital circulante respete el signo negativo (la nota decía "ajuste de -159M" pero sumaba "+159,1M"); (2) que en el gráfico de dividendos se vea bien el payout de 2021 y aparezca el de 2022.

### Resultado

1. **Nota de Cash Flow coherente** (`src/agents/analystAgent.js`): nueva función `buildWcDeviationSentence` que cierra la nota con la desviación y la resta explícita (`CF ajustado = CF normal - desviación`; el sistema sobrescribe la nota del modelo). Ejemplo resultante en TAP: `Desviación del circulante reportado (-147M) frente al WK teórico (12,1M): -159,1M. El Cash Flow ajustado resta esa desviación: 1784,4M - (-159,1M) = 1943,5M.` Reglas actualizadas en `consumo-defensivo.md`, `anual/general.md` (fórmula unificada `(CxP - Inv - CxC) x (inflación + volumen)`), `consumo-defensivo/sector.md` y el prompt anual del agente.
2. **BPA ajustado histórico desde los 8-K** (`src/services/edgar.service.js`, nueva `getHistoricalUnderlyingEps`): busca el comunicado anual de resultados de cada ejercicio (8-K item 2.02 cercano al 10-K) y extrae el BPA subyacente/ajustado solo cuando el comunicado separa un bloque de año completo ("full year financial highlights/results..."), con caché de 24 h. `analystAgent` completa la serie de dividendos con esos valores (TAP 2021: 4,15 $; 2022: 4,10 $; 2023: 5,43 $ —corrige el 5,80 $ erróneo de la IA—; 2024: 5,96 $). Si no hay bloque anual claro, no se toca el dato previo (sin regresiones).
3. **Gráfico de dividendos**: el payout de 2021 (16,4 %) se dibuja a un lado de la barra, sin pisar la etiqueta del dividendo, y el de 2022 (37,1 %) ya aparece; se elimina la nota de "años con BPA reportado". Corregido en `src/services/reportExport.service.js` (PDF/HTML/Word) y `public/analisis.js` (web, `?v=24`).
4. **Nota de Asignación de Capital**: incluye ahora el movimiento de caja (`Caja balance: 969M -> 896,5M (-72,5M); la caja disminuyó: fuente de liquidez (+); fila Caja = +72,5M`) para dejar claro el signo de la fila `Caja`.
5. **Verificación**: regenerado el análisis completo de TAP contra la API (28,4 s) y revisado el PDF: nota del WK correcta, dividendos con los cinco payouts (2021-2025) y nota de capital con el detalle de caja. `node --check` en los 4 archivos JS modificados, servidor relanzado y `analisis.js` a `v=24`.

## 16:10 — Resumen: Lectura sistemática del balance y de valores negociables desde XBRL + suma correcta del capital

Se reportó que la fila `Caja` de TAP seguía leyendo mal (969M en vez de 969,3M) y que en KHC la fila `Inversiones a corto plazo` solo restaba las compras (1.724) ignorando las ventas (686), con un `En total` mal sumado (1431,3 en vez de 395). Se pidió que sea sistemático en cada análisis, no un parche puntual.

### Resultado

1. **Balance anual desde XBRL con decimales** (`src/agents/analystAgent.js`): `fillBalance` ahora **fuerza** los valores oficiales de XBRL (caja, deuda, inventarios, cuentas por pagar/cobrar, inversiones a corto plazo y sus saldos de inicio) sobre la extracción de la IA, y `toMillionsValue` conserva un decimal (969,3M en vez de 969M). `capitalAllocationData` se recalcula después de este volcado. TAP: `Caja 969,3M -> 896,5M (-72,8M)`, fila `+72,8`; `Deuda 6146,1M -> 6299,5M (+153,4M)` (antes usaba 6176,1M).
2. **Valores negociables netos**: nuevo concepto XBRL `securitiesProceeds` (`ProceedsFromSaleAndMaturityOfMarketableSecurities`, etc.) y tag `PaymentsToAcquireMarketableSecurities` añadido a las compras; el agente vuelca compras y ventas al JSON de extracción (`proceedsFromSaleOfMarketableSecurities...`) y la fila `Inversiones a corto plazo` pasa a ser el **flujo neto** ventas − compras (KHC: 686 − 1.724 = `-1038`), tanto en el camino anual como en el trimestral. Reglas actualizadas en `anual/general.md`, `sector.md` y prompts.
3. **Suma del capital robusta**: nuevo `parseLooseReportNumber` que interpreta `"1,724"` como 1.724 (separador de miles) y `"239,5"` como decimal; el `En total` y la validación de la fila `Deuda` lo usan. KHC: `1800 - 1038 - 436 - 1281 + 1350 = 395` (antes 1431,3).
4. **Notas huérfanas**: la nota de marcas/adquisiciones solo se incluye si existe una fila que la referencie (KHC ya no muestra la nota de Plasmon sin fila).
5. **Prompt de extracción**: se advierte expresamente de que en las tablas de la SEC la coma es separador de millares (`(1,724)` = 1.724 millones).
6. **Verificación**: regenerados TAP (29,0 s) y KHC (26,5 s) contra la API; filas públicas 82 (TAP) y 83 (KHC) actualizadas con informe y PDF corregidos. `node --check` correcto y servidor relanzado.

## 16:40 — Resumen: Saldos de caja etiquetados con su ejercicio en la nota de capital

Se reportó que el signo de `Caja` parecía invertido en TAP porque la nota no distinguía los saldos del ejercicio analizado de la columna comparativa del 10-K. La regla vigente del usuario es: caja ↑ = uso de capital (-), caja ↓ = fuente de liquidez (+).

### Resultado

- **Nota con ejercicios**: `buildCashMovementDetails` añade el año a cada saldo (`Caja balance: 969,3M (2024) -> 896,5M (2025) (-72,8M); la caja disminuyó: fuente de liquidez (+); fila Caja = +72,8M`). En el 10-K de TAP FY2025 la caja pasó de 969,3M (cierre 2024) a 896,5M (cierre 2025): **disminuyó 72,8M**, por lo que la fila suma `+72,8` como fuente. El `868,9 -> 969,3` (+100,4) es la comparativa de 2024 (en el análisis de 2024 la fila Caja es `-100,4`, coherente con la misma regla).
- **Verificación**: regenerados TAP (28,0 s) y KHC (24,1 s); filas 85 (TAP) y 84 (KHC) actualizadas con informe y PDF; `node --check` correcto y servidor relanzado.

## 16:55 — Resumen: La nota de caja explica la diferencia con el saldo final del estado de flujos (efectivo restringido)

Se preguntó por qué la caja del balance y el saldo final del estado de flujos ("cash, cash equivalents and restricted cash") no cuadran en KHC.

### Resultado

- **Causa**: KHC presenta en el estado de flujos "Cash, cash equivalents and restricted cash" (2.944M al cierre 2025 y 1.486M al inicio), que incluye el efectivo restringido (329M en 2025 y 152M en 2024), mientras el balance muestra solo caja y equivalentes (2.615M / 1.334M). La variación del balance es +1.281M y el "net increase" del estado de flujos +1.458M (diferencia de 177M). En TAP no hay restringido y ambos coinciden (969,3M -> 896,5M, -72,8M).
- **Nota automática**: `buildCashMovementDetails` compara ahora el neto del estado de flujos (`netChangeInCash` de XBRL) con la variación de caja del balance y, si difieren, añade la explicación: "El estado de flujos presenta un neto de 1458M porque incluye efectivo restringido y otros ajustes (+177M frente a la caja del balance)".
- **Verificación**: regenerados TAP (31,5 s) y KHC (28,7 s); filas 86 (TAP) y 87 (KHC) actualizadas con informe y PDF; `node --check` correcto y servidor relanzado.

## 17:08 — Resumen: Cifras exactas, tabla de recompras completa y tipos de interés en el gráfico de deuda

Se reportaron tres errores en el informe anual de KHC: (1) la tabla de Cash Flow mostraba cifras redondeadas (4500/800/1900) en vez de las exactas; (2) la tabla de recompras solo tenía "Aggregate cost" y "Remaining authorization", sin "Shares repurchased" ni "Average price paid"; (3) el gráfico de deuda no mostraba el tipo de interés en las barras ni en los subloques, y faltaba espacio entre los años y el banner. Se pidió expresamente que las correcciones sean generales para cualquier 10-K.

### Resultado

1. **Cifras exactas de Cash Flow** (`src/agents/analystAgent.js`): el bloque anual vuelca ahora el estado de flujos oficial de XBRL (SEC) sobre la extracción de la IA: `cashFlow.operating` (cfo), `cashFlow.capex` (valor absoluto) y `cashFlow.dividends` (dividendos comunes/preferentes). KHC pasa de `4500 / 800 / 1900` a `4462 / 801 / 1898` (FCF 3661, Libre 1763). Se añaden reglas de exactitud (prohibición de redondear y de estimar cifras reportadas) en `EXTRACTION_PROMPT`, `ANNUAL_SYSTEM_PROMPT` y `knowledge/anual/general.md`, y se filtran notas del modelo que reconozcan estimaciones de CAPEX.
2. **Tabla de recompras canónica**: nuevo concepto XBRL `buybackShares` (`StockRepurchasedDuringPeriodShares`, `TreasuryStockSharesAcquired`...) en `edgar.service.js`; `buildRepurchaseSharesHistoryFromEdgar` y `mergeRepurchaseShares` adjuntan el número exacto de títulos a la serie de costes, y `buildRepurchaseSecTable` genera las filas "Shares repurchased", "Aggregate cost (in millions)", "Average price paid (in $)" (precio = coste × 1e6 / títulos) y "Remaining authorization". `enrichRepurchaseSnippet` completa esas filas también cuando la tabla viene de la IA. Verificado con TAP: `12,906,851 / 10,907,779 / 3,454,694` con precios `$51.0 / $59.2 / $61.6`. En KHC las filas de acciones no aparecen porque su 10-K no publica títulos recomprados ni precio medio por ejercicio (solo el coste agregado): el sistema nunca las inventa.
3. **Vencimientos y tipos de deuda**: `extractKeyFilingSections` captura ahora la nota de deuda real de los dos formatos habituales ("Debt Obligations ... As of" y "Long-Term Debt: The following table") y la tabla de "aggregate principal maturities"; el merge anual usa SIEMPRE los importes oficiales de principal de XBRL y descarta los importes brutos del MD&A (Material Cash Requirements incluye intereses) y las etiquetas plurianuales ("2027-2028"). KHC pasa de `2.802 / 5.261 / …` a `1.879 / 1.893 / 1.679 / 1.009 / 915` y "después del año 5" de 21.577 a 13.568. Se calcula el tipo medio de toda la deuda con rangos de cupón (punto medio ponderado; KHC ≈ 4,76 %) o, si no hay tabla, por intereses del ejercicio sobre deuda media; se marca como estimado (`allDebtAverageRateEstimated`). El gráfico rotula cada barra/subloque con su cupón exacto o con el tipo estimado (`~`) y el banner distingue "Tipo de interés medio total" de "Tipo de interés medio estimado".
4. **Más espacio en el gráfico**: los tres renderizadores (SVG web `public/analisis.js`, HTML `reportExport.service.js` y PDF `report.service.js`) amplían la separación entre las etiquetas de año y el banner de tipos (9 → 19 px; caja del gráfico de 265 → 275 y +8 pt en el PDF).
5. **Verificación**: regenerados TAP (37,9 s, gráfico 2026 con subloques 3,44 % / 3,00 % y banner 3,85 %; tabla de recompras con las 3 filas) y KHC (50,5 s y 89,7 s; Cash Flow exacto, vencimientos oficiales, tipo estimado 4,76 % en cada barra y remanente 1.500M). HTML y PDF comprobados con captura de texto; `node --check` correcto y servidor relanzado.

## 17:20 — Resumen: Red de seguridad para la cuenta de resultados anual (filas vacías de la extracción)

Se reportó que, tras los últimos cambios, un informe de KHC mostraba la tabla de Ventas con filas enteras en "—" (Beneficio Bruto, EBT...) porque la extracción de la IA no devolvió esas líneas del estado de resultados (y en otras ocasiones redondeaba: 8.356M en vez de los 8.309M oficiales).

### Resultado

- **Cuenta de resultados oficial XBRL** (`src/agents/analystAgent.js`): en el bloque anual, tras el volcado de balance y cash flow, se completan `extracted.ytd` y `extracted.ytd.prev` con las líneas oficiales del XBRL de la SEC: ventas (`revenue`), beneficio bruto (`grossProfit`), beneficio operativo (`operatingIncome`), EBT (`ebtIncludingUnusual`/`pretaxIncome`) y beneficio neto atribuible (`netIncomeToCommonIncludingUnusual`/`netIncome`). La IA ve ya esas cifras exactas al redactar.
- **Deterioros y amortización exactos**: `facts.impairmentsYtd`/`impairmentsPrevYtd` se corrigen con la suma XBRL de `goodwillImpairment` + `assetImpairment` cuando la extracción se queda corta (KHC 2025: 9.306M en vez de 9.300M), y `intangiblesAmortization` se rellena si falta.
- **Red de seguridad en la tabla final**: en la normalización de Ventas, cualquier fila (`Ventas`, `Beneficio Bruto`, `Beneficio Operativo`, `EBT`, `Beneficio Neto`) que el modelo deje en "—" se rellena con la cifra oficial ya volcada; además, si no hay ningún ajuste documentado, la columna Ajustado hereda la Normal (Beneficio Bruto/EBT sin partidas extraordinarias).
- **Verificación**: KHC regenerado (63,3 s): Ventas 24.942/25.846 (-3,50 %), Bruto 8.309/8.968 (-7,35 %), Operativo normal -4.669 + deterioros = 4.637 ajustado, EBT -5.445/856, Neto -5.846/2.744; Cash Flow exacto 4.462/801/1.898. TAP regenerado (38,4 s): Ventas 11.140,8, Bruto 4.274,6, Operativo 1.583, gráfico de deuda con subloques 3,44 %/3,00 % y tabla de recompras con las 4 filas. `node --check` correcto y servidor relanzado.

## 17:46 — Resumen: Correcciones generales del análisis anual 10-K (comparativo, caja/deuda, recompras, outlook y tipos del gráfico)

Se reportaron cinco errores en el análisis anual de CAG y se pidió expresamente que la corrección fuese general para cualquier empresa (no para ese PDF concreto): (1) la columna "Anterior" de Ventas repetía las cifras actuales con +0,00 %; (2) Caja y Deuda aparecían a 0 en la Asignación de Capital; (3) las recompras insignificantes debían omitirse por completo; (4) faltaban datos del año anterior en la tabla del Outlook; (5) el gráfico de deuda repetía un tipo medio estimado en todas las barras cuando no había cupón por tramo.

### Resultado

- **Comparativo del año anterior (Ventas)** — causa raíz: en `edgar.service.js` los balances de un trimestre natural (`CYxxxxQ4I`) se asignaban a la fila anual por etiqueta `period`; con ejercicios no naturales (CAG cierra en mayo) se mezclaban saldos de un trimestre intermedio. Ahora la asignación es solo por fecha exacta de cierre. Además, `selectAnnualRows()` (nuevo en `analystAgent.js`) selecciona actual/anterior por `periodEnd` (no por `period`), y la normalización de Ventas sobrescribe las columnas "Anterior" con el comparativo oficial de XBRL cuando existe. CAG pasa de `11.281,6 / 11.281,6 (+0,00 %)` a `11.281,6 / 11.612,8 (-2,85 %)`, con todas las filas del estado de resultados corregidas.
- **Caja y Deuda** — con la fila anterior correcta, `buildCapitalAllocationFromBalance` ya calcula variaciones reales: CAG muestra `Caja -150` y `Deuda -799,2` (antes 0 y 0), el total cuadra (`8,8`) y la nota de deuda coincide con el gráfico y el texto (8.067,6M -> 7.268,4M; deuda neta 7.997,4M -> 7.046,4M).
- **Recompras insignificantes** — se omite la sección completa "Recompras" del informe anual cuando las recompras del ejercicio son < 50M (CAG: 15M), y la fila "Recompras" también se omite en la tabla de Asignación de Capital. Las secciones restantes se renumeran correlativamente. El histórico multianual no se usa para decidir (evita que años pasados con recompras grandes mantengan la sección).
- **Outlook (columna "Año anterior")** — la extracción guarda ya los valores del ejercicio cerrado (`priorYearSales`, `priorYearEbt`, `priorYearEps`, `priorYearFcf`, `priorYearFcfConversion`, `priorYearCapex`, `priorYearNetInterest`, `priorYearOperatingMargin`); `completeOutlookPriorColumn()` rellena las celdas vacías y `mergeOutlookRows()` recupera filas oficiales que el modelo de estructuración omita (conversión de FCF, intereses…). El gasto neto por intereses del año anterior se inyecta desde XBRL (`InterestIncomeExpenseNet` añadido a `interestExpense`) y manda sobre la IA: CAG queda en `$382.6M` (la IA había tomado el trimestre: 99,7M). Se eliminan además valores inventados del comparador anterior (D&A $705M, intereses $230M, tasa 21,5 %, CAPEX $717M).
- **Gráfico de deuda** — si un tramo no tiene cupón propio (calendario agregado de vencimientos), la barra ya no muestra ningún tipo; solo el banner conserva el tipo medio estimado. Aplicado en los tres renderizadores (`reportExport.service.js`, `public/analisis.js` y, por herencia, el PDF).
- **Verificación**: análisis anual de CAG regenerado contra el 10-K real (dos ejecuciones), comparativo de Ventas correcto, Caja/Deuda reales, sección de recompras omitida y tipos por barra suprimidos; test unitario de los helpers (`selectAnnualRows`, `completeOutlookPriorColumn`, `mergeOutlookRows`, `buildDebtMaturityModel`, `buildDividendModel`) OK; `node --check` correcto. El análisis de prueba creado en BD para verificar se eliminó.

## 18:14 — Resumen: Word (.docx), ODT y web (.html) con los gráficos y el aspecto del PDF

Se solicitó que los formatos Word, ODF y web del informe se vean tan bien como el PDF, incluyendo los gráficos exactamente como están en el PDF.

### Resultado

- **Gráficos en Word y ODT**: los 4 gráficos (acciones, calendario de vencimientos, evolución de la deuda y dividendo/payout) se generan ahora como PNG de alta resolución (1920 px) rasterizando los mismos SVG que usa el HTML con `@resvg/resvg-js`, y se incrustan como imágenes: `word/media/*.png` con `w:drawing` en DOCX y `Pictures/*.png` con `draw:frame`/`draw:image` en ODT. La tabla de vencimientos/histórico se sustituye por el gráfico (igual que en el PDF) y se mantiene como respaldo si la imagen no se puede generar.
- **Caja de refinanciación**: se renderiza en HTML, DOCX y ODT con las 4 métricas (tipo anterior/actual, tipo nueva emisión, volumen e impacto en BPA) y los textos de análisis/impacto, idéntica a la del PDF; se dejan de duplicar los badges en la sección de Deuda.
- **Markdown unificado**: nuevo `parseRichSegments` (port fiel de `parseMarkdownAndNumbers` del PDF) aplica negritas `**…**` y autodetección de cifras en HTML, DOCX y ODT; `sanitize` elimina los marcadores. Antes había ~124 asteriscos literales por informe en esos formatos.
- **Paridad estructural con el PDF**: cada sección de la Parte II empieza en página nueva; encabezado "EXTRACTO OFICIAL SEC (FORM 10-K)" con título y resumen en las tablas oficiales; nota de años con BPA reportado en el gráfico de dividendos; watchlist con "OK"; y la nota de resultados en página propia.
- **Empaquetado OOXML corregido**: se añade `word/_rels/document.xml.rels` (antes vacío) con la relación a `styles.xml` (ahora sí aplica Helvetica) y a las imágenes; `[Content_Types].xml` declara PNG. El ODT declara los namespaces `draw`/`svg`/`xlink`, el layout de página A4 y las entradas de `Pictures/` en el manifest.
- **Botón de descarga web**: se añade "Web (.html)" a las acciones del informe y a la tabla de histórico (la API ya soportaba el formato); el HTML exportado ahora se ve como el PDF (Markdown, tablas SEC y gráficos SVG).
- **Verificación**: informe anual real (Campbell's) regenerado en los 4 formatos; DOCX y ODT convertidos a PDF con LibreOffice → 8 páginas, las mismas que el PDF, con gráficos, caja de refinanciación, tablas y saltos de página revisados visualmente. 10-Q (Molson Coors) sin Parte II verificado en DOCX/ODT (2 páginas, sin regresiones). HTML exportado a PDF con Chrome headless revisado visualmente. 0 asteriscos literales en los tres formatos y `node --check` correcto en los archivos modificados.
- **Nueva dependencia**: `@resvg/resvg-js` ^2.6.2 (rasterizado SVG→PNG en servidor).

## 18:39 — Resumen: Compra del dominio cifraresearch.com (Cloudflare) y del VPS (Contabo, región UE)

Se solicitó comprar el dominio y el VPS para iniciar el despliegue de la beta.

### Resultado

- **Dominio**: `cifraresearch.com` registrado en **Cloudflare Registrar** (a precio de coste, ~10,44 $/año). Libre verificado por RDAP justo antes de la compra; la cuenta ya lo muestra en Registrations ("Your domain is on its way"). DNS gestionado por Cloudflare (nameservers obligatorios); pendiente confirmar la verificación del contacto ICANN y la autorenovación.
- **VPS**: contratado **Contabo Cloud VPS 4** (4 vCPU / 8 GB RAM / 100 GB SSD, ~6,66 €/mes, pedido #15396914) con **Ubuntu** y región **Unión Europea** (cambio respecto al plan de `lanzamiento.md`, que proponía EE. UU.). Decisión: la UE acerca la web a los usuarios hispanohablantes y facilita el RGPD, asumiendo ~100-150 ms extra por llamada a SEC/Yahoo (mitigable con caché y Cloudflare). Provisión 30 min–3 h; pendiente recibir credenciales (IP y root).
- **Nombres descartados**: `cifra.com/.net/.org/.io/.ai/.eu/.es/.co/.mx` ocupados; variantes libres evaluadas (`cifra.capital`, `cifra.report`, `cifraresearch.com`).
- **Pendiente**: configuración del servidor (usuario deploy, SSH, UFW, Node 22, PostgreSQL 16, Nginx, PM2, Certbot), DNS (`@`, `www`, `dev`), email (Cloudflare Email Routing + Resend) y despliegue CI/CD.

## 18:54 — Resumen: Aprovisionamiento del VPS (Contabo) y despliegue base en dev/prod de cifraresearch.com

Se accedió por SSH al VPS contratado y se configuró toda la base de la infraestructura.

### Resultado

- **Sistema**: Ubuntu 24.04.5 LTS actualizado, hostname `cifra`, zona horaria Europe/Madrid. Hardware real: 4 vCPU / 7,8 GB RAM / 96 GB SSD.
- **Acceso**: usuario `deploy` con sudo sin contraseña y clave SSH del equipo local (login root solo por clave); UFW activo con 22/80/443.
- **Paquetes**: Node.js 22.23.2, npm 10.9.8, PostgreSQL 16.15, Nginx 1.24, Certbot.
- **PostgreSQL**: usuario `cifra_user` (contraseña aleatoria guardada en `/root/.cifra_db_pass`, chmod 600) y bases `cifra_prod` / `cifra_dev`; migraciones ejecutadas correctamente en ambas.
- **Despliegue base**: clones del repositorio (rama `development`, commit 9342428) en `/var/www/cifra-dev` (puerto 3001 → `dev.cifraresearch.com`) y `/var/www/cifra-prod` (puerto 3000 → `cifraresearch.com`), cada uno con su `.env` (JWT_SECRET nuevo, `DATABASE_URL`, `PUBLIC_SITE_URL` y `GOOGLE_CALLBACK_URL` adaptados al dominio).
- **Decisión técnica: se descarta PM2** — el daemon fallaba con `spawn /usr/bin/node EACCES` (Node 22 en Ubuntu 24.04) aunque el arranque manual funcionaba. Sustituido por dos servicios **systemd** (`cifra-dev.service` y `cifra-prod.service`) como usuario `deploy`, con `Restart=always` y arranque automático al reiniciar el VPS. Verificado: ambos `active` y HTTP 200.
- **Nginx** como proxy inverso para `cifraresearch.com`, `www` y `dev` (gzip, `client_max_body_size 50m` para subidas PDF, timeouts de 300 s para los análisis IA); verificado con HTTP 200 en los tres hosts vía `Host` header.
- **DNS**: la zona ya está delegada en Cloudflare (`kanye/lila.ns.cloudflare.com`); pendiente añadir los registros A (`@`, `www`, `dev` → 194.163.166.21), ejecutar Certbot para el SSL y configurar backups automáticos.
- **Pendiente**: desplegar los 19 archivos locales sin commitear (los servidores tienen la última versión subida a GitHub), actualizar el callback de Google OAuth en Google Cloud y sustituir el SMTP de Gmail por Resend.

## 18:57 — Resumen: HTTPS con Let's Encrypt y backups automáticos de las bases de datos

Se añadieron los registros DNS en Cloudflare (proxied) y se completó la puesta en producción del dominio.

### Resultado

- **DNS verificado**: `cifraresearch.com`, `www` y `dev` resuelven a Cloudflare y llegan al origen (nube naranja/proxied). `www` y `dev` responden 200; `@` redirige 301 a `https://cifraresearch.com/`.
- **SSL (Certbot/Let's Encrypt)**: certificado emitido para `cifraresearch.com`, `www.cifraresearch.com` y `dev.cifraresearch.com`, desplegado en Nginx con redirección HTTP → HTTPS y renovación automática programada. Verificado el origen directo (certificado válido) y los tres hosts a través de Cloudflare.
- **Backups**: `/home/deploy/backup-db.sh` vuelca `cifra_prod` y `cifra_dev` a gzip en `/home/deploy/backups/`, conserva 14 días y registra en `backup.log`; cron diario a las 03:00 como usuario `deploy`. Probado y verificado (gzip íntegro y cabecera `PostgreSQL database dump`).
- **Pendiente**: fijar el modo SSL de Cloudflare en Full (strict), configurar el correo (`hola@cifraresearch.com` vía Cloudflare Email Routing y Resend para lo transaccional), actualizar los redirect URIs de Google OAuth y desplegar los cambios locales sin commitear.

## 19:18 — Resumen: CI/CD con GitHub Actions: dos pipelines independientes (development → dev, production → prod)

Se solicitó configurar el CI/CD de GitHub para que cada rama despliegue en su entorno: `development` actualiza `dev.cifraresearch.com` y `production` (aún inexistente) actualizará `cifraresearch.com`.

### Resultado

- **Dos workflows** (`.github/workflows/deploy-dev.yml` y `deploy-prod.yml`), con la misma estructura cada uno:
  - **CI (`check`)**: se ejecuta en push y Pull Requests; `npm ci` + `node --check` de todos los `.js` rastreados por git. Si hay error de sintaxis, se cancela el despliegue.
  - **CD (`deploy`)**: solo en push; comprueba que los secrets existan (si no, omite el paso sin fallar), entra por SSH, hace `git fetch/checkout/reset --hard` a la rama, `npm ci` (`--omit=dev` en producción), `npm run db:migrate`, `systemctl restart` del servicio correspondiente y health check con `curl` a `robots.txt`.
  - Detalles: `concurrency` por rama, `workflow_dispatch` manual y `environment: production` en el pipeline de producción (permite exigir aprobación manual en el futuro).
- **Acceso**: clave SSH dedicada `~/.ssh/cifra_actions` (pública autorizada para el usuario `deploy` del VPS; privada en el secret `VPS_SSH_KEY`). El remoto local pasó a `git@github.com:LurLopez/APP.git` y la clave pública personal se registró en GitHub. Secrets configurados: `VPS_HOST`, `VPS_USER` y `VPS_SSH_KEY`.
- **Primera ejecución verificada** (run 34653042318, intento 2 tras configurar los secrets): CI correcto y despliegue SSH correcto. `/var/www/cifra-dev` quedó en `e49a7f1` y el servicio `cifra-dev` se reinició solo (00:17:52 CEST).
- **Pendiente**: crear la rama `production` desde `development` cuando se quiera publicar, y subir los cambios locales sin commitear (19 archivos + `public/theme.js`), que se desplegarán solos al hacer push a `development`.

## 19:31 — Resumen: Correo transaccional real con Resend (dominio verificado) en dev y prod

Se solicitó dejar de enviar correos desde la cuenta personal de Gmail y usar el dominio propio.

### Resultado

- **Resend**: alta de `cifraresearch.com` en la región **EU (Irlanda)**; registros DNS creados con la auto-configuración de Cloudflare (DKIM `resend._domainkey`, SPF y MX en `send.` → Amazon SES `eu-west-1`) y dominio **verificado**. Creada API key con permiso de envío (no se guarda en el repositorio).
- **Configuración en el servidor** (`/var/www/cifra-dev/.env` y `/var/www/cifra-prod/.env`): `SMTP_HOST=smtp.resend.com`, `SMTP_PORT=465`, `SMTP_SECURE=true`, `SMTP_USER=resend`, `SMTP_PASS` con la API key, `MAIL_FROM=Cifra <hola@cifraresearch.com>` y **eliminado `MAIL_TO_OVERRIDE`** (los correos se envían ya a los destinatarios reales). Servicios `cifra-dev` y `cifra-prod` reiniciados.
- **Prueba**: `scripts/test-email.js` enviado correctamente a la cuenta personal como verificación de entrega.
- **Pendiente de correo**: activar Recepción (Cloudflare Email Routing para `hola@cifraresearch.com` → Gmail) y registro DMARC opcional (`v=DMARC1; p=none;`).

## 19:39 — Resumen: Recepción de correo en hola@cifraresearch.com activada (Cloudflare Email Routing)

Se configuró la recepción de correo del dominio.

### Resultado

- **Cloudflare Email Routing** activado para `cifraresearch.com` (MX `route1/2/3.mx.cloudflare.net`, SPF `_spf.mx.cloudflare.net` y DKIM `cf2024-1._domainkey` en el dominio raíz; no chocan con los registros de Resend, que están en `send.`).
- Regla activa: `hola@cifraresearch.com` → reenvío a `lurlopez13@gmail.com` (destino verificado).
- **Verificación**: correos de prueba entreGmail y desde el servidor (SMTP de Resend) recibidos en la bandeja de entrada, tanto el envío saliente (`Cifra <hola@cifraresearch.com>`) como el reenvío entrante. El aviso inicial "Missing email" era de la ventana de propagación de los MX.

## 19:47 — Resumen: Endurecimiento final del VPS y de Cloudflare (SSH sin contraseña y SSL Full strict)

Se completaron los ajustes de seguridad pendientes de la infraestructura.

### Resultado

- **SSH**: desactivado el login por contraseña en el VPS (`/etc/ssh/sshd_config.d/99-cifra.conf`: `PasswordAuthentication no`, `KbdInteractiveAuthentication no`, `PermitRootLogin prohibit-password`). Verificado que `root` y `deploy` siguen accediendo por clave.
- **Cloudflare SSL/TLS**: modo **Full (strict)** (el origen usa el certificado Let's Encrypt válido de Certbot), **Always Use HTTPS** y **Automatic HTTPS Rewrites** activados.
- **Verificación**: HTTPS 200 en `cifraresearch.com`, `www` y `dev`; HTTP → 301 a HTTPS en todos; origen directo con certificado válido (`verify=0`).

## 19:52 — Resumen: Mejoras SEO/GEO con el dominio real (hreflang x-default, entidad "Cifra Research" y www → dominio principal)

Se solicitó mejorar el SEO/GEO de la web una vez conocido el dominio definitivo (`cifraresearch.com`).

### Resultado

- **hreflang**: añadido `hreflang="x-default"` junto a `hreflang="es"` en `public/index.html`, `public/empresa.html` y las 8 páginas de `src/content/guias/`. `serveHtml` (`src/services/seo.service.js`) ahora **actualiza** las etiquetas hreflang con la URL canónica real de cada página (incluidas las fichas `/empresa/<TICKER>`) en lugar de inyectarlas solo cuando faltan, evitando señales cruzadas entre `/empresa` y `/empresa/<TICKER>`.
- **Entidad de marca**: `alternateName: "Cifra Research"` en los JSON-LD `WebSite`/`Organization` de las plantillas y en el `publisher` de los informes públicos, alineando la marca con el dominio.
- **Consolidación de dominio**: `www.cifraresearch.com` redirige ahora **301** a `cifraresearch.com` (bloques nginx 80 y 443, con el certificado de Let's Encrypt existente); `dev.` se mantiene independiente. Verificado desde el origen y a través de Cloudflare.
- **Documentación**: actualizados `documentacion/lanzamiento.md` (dominio real, región UE, ramas `development`/`production`, systemd en lugar de PM2), `lanzamiento.md` (resumen) y `documentacion/seo.md` (hreflang x-default, www→apex, alternateName).
- **Verificación**: 11 bloques JSON-LD válidos tras sustituir `{{SITE_URL}}`; `node --check` de `seo.service.js`; sitemap 200 con `lastmod` y URLs al dominio real; canonical y hreflang correctos en producción; prueba de la inyección dinámica sobre `/empresa/KO` (1 solo hreflang es + 1 x-default, ambos autorreferentes).
- **Pendiente**: el `hreflang x-default` y el `alternateName` requieren commit + push para desplegarse; el redirect de `www` ya está activo. Próximo paso SEO: verificación en Google Search Console y envío del sitemap.

## 19:55 — Resumen: Bloqueo de indexación del entorno development y preparación de Search Console / Analytics

Se acordó que solo producción debe indexarse y tener analítica; development queda excluido.

### Resultado

- **nginx (dev)**: añadido `add_header X-Robots-Tag "noindex, nofollow" always` y una ruta `location = /robots.txt` que devuelve `User-agent: * / Disallow: /` para `dev.cifraresearch.com`. Verificado: dev responde con cabecera noindex y robots de bloqueo total; producción intacta (sin noindex).
- **Google Search Console**: se verificará solo la propiedad de dominio `cifraresearch.com` mediante TXT en Cloudflare y se enviará el sitemap.
- **Google Analytics 4**: se creará una única propiedad para producción; el código de medición se cargará **solo** en `cifraresearch.com`/`www` (excluido `dev.`), pendiente de recibir el ID de medición para integrarlo.

## 20:05 — Resumen: Google Search Console verificado, indexación solicitada y Google Analytics 4 integrado (solo producción)

Se completó la puesta en marcha de las herramientas de Google.

### Resultado

- **Search Console**: propiedad de dominio `cifraresearch.com` verificada por TXT en Cloudflare (autorización automática desde Cloudflare). Sitemap `https://cifraresearch.com/sitemap.xml` enviado; la inspección en vivo confirma "URL disponible para Google / se puede indexar" y se ha **solicitado la indexación** de la portada. El sitemap aparece como "No se ha podido obtener" en el primer intento (estado transitorio habitual; Google reintenta solo). Comprobado que Googlebot recibe HTTP 200 con XML válido.
- **Google Analytics 4**: propiedad `Cifra Research` (zona horaria España, EUR) con flujo web `Web Cifra`; ID de medición `G-7PSC9M3B1H`.
- **Integración GA4** (`src/services/seo.service.js`): nuevo helper `withAnalytics()` que inyecta la etiqueta gtag antes de `</head>` **solo cuando `PUBLIC_SITE_URL === https://cifraresearch.com`**, aplicado a `serveHtml` (portada, empresas, informes) y `serveStandalone` (guías). Verificado por simulación: presente en producción, ausente en `dev.cifraresearch.com`. Queda pendiente de despliegue (commit + push).
- **Pendiente GDPR**: valorar un banner de consentimiento de cookies (Consent Mode) para visitantes de la UE.

## 20:12 — Resumen: Rediseño del modal de donaciones (solo PayPal y Bizum personales, sin Buy Me a Coffee/Ko-fi/cripto)

Se pidió simplificar las donaciones a PayPal y Bizum usando cuentas personales y mejorar el diseño.

### Resultado

- **Eliminados** Buy Me a Coffee, Ko-fi, las criptomonedas y el panel de "personalizar enlaces" (HTML en `public/index.html` y `public/empresa.html`; lógica en `public/donations.js`; estilos en `public/styles.css`). El módulo queda reducido a PayPal + Bizum y el selector de importe.
- **Bizum personal** por defecto: `+34 622 018 097`, con concepto dinámico `Donación Cifra (importe)`. **Pendiente**: el enlace personal de PayPal.Me (variable `PAYPAL_URL` en `donations.js`), que sustituirá al placeholder actual.
- **Diseño renovado**: botón de PayPal con degradado oficial y sombra, tarjeta de Bizum con borde azul y número monoespaciado con botón de copiar, píldoras de importe con estado activo en color de acento, badge del corazón con degradado y nota legal ("no es una entidad sin ánimo de lucro: aportes no desgravable y sin factura").
- **Verificación**: `node --check` de `donations.js`; balance de llaves CSS 2427/2427; sin referencias huérfanas a los métodos eliminados; capturas con Chrome headless en modo claro y oscuro correctas (buen contraste y alineación). Pendiente de commit + push para verse en la web.

## 20:14 — Resumen: Donaciones solo con PayPal (Bizum retirado por privacidad)

El usuario prefirió no publicar su número de teléfono, así que se deja únicamente PayPal.

### Resultado

- Retirada la tarjeta de Bizum del modal (`public/index.html` y `public/empresa.html`), su lógica en `public/donations.js` y sus estilos en `public/styles.css` (`.donation-card-box`, `.donation-box-*`, `.donation-copy-*`).
- El modal queda: título "Apoyar a Cifra", selector de importe (3/5/10/25 € o personalizado), botón de PayPal con degradado y nota legal. Verificado con captura en claro (diseño limpio y centrado); `node --check` y balance CSS 2420/2420 correctos.
- **Enlace configurado**: `PAYPAL_URL = 'https://paypal.me/cifraresearch'` en `donations.js`; el botón añade el importe seleccionado (verificado: `paypal.me/cifraresearch/5` redirige a `www.paypal.com/paypalme/cifraresearch/5`).

## 20:36 — Resumen: Despliegue a dev del commit acumulado y corrección de un proceso huérfano que bloqueaba el puerto 3001

Se autorizó el commit + push de todo lo pendiente (analista anual, ajustes/tema, SEO/GEO, correo y donaciones PayPal).

### Resultado

- **Commit `4e781a1`** ("analista anual, ajustes y tema oscuro, SEO/GEO, correo y donaciones con PayPal") subido a `development`; el pipeline **Deploy Development** ejecutó CI + despliegue con éxito. Incluye también los nuevos logos `logo-cifra-blanco.png` y `logo-cifra-blanco-plano.png` en la raíz y `public/theme.js`.
- **Incidencia**: dev seguía sirviendo el modal antiguo pese al despliegue. Causa: un **proceso huérfano** arrancado manualmente a las 23:53 (PID 5730) retenía el puerto 3001; cada reinicio de `cifra-dev` creaba un proceso que no podía enlazar el puerto y el sitio seguía respondiendo desde el proceso viejo (plantillas cacheadas en memoria). Se eliminó el huérfano y se reinició el servicio (MainPID nuevo).
- **Mejora preventiva** (`cbdf590`): los workflows `deploy-dev.yml` y `deploy-prod.yml` liberan el puerto con `sudo fuser -k <puerto>/tcp` antes de reiniciar el servicio, evitando que un huérfano bloquee futuros despliegues.
- **Verificación**: `/var/www/cifra-dev` en `cbdf590`, puerto 3001 servido por el MainPID de systemd, modal con **solo PayPal** (`donations.js?v=3`, sin BMAC/Ko-fi/Bizum), `X-Robots-Tag: noindex` en dev y GA ausente en dev (solo producción).
- **Pendiente**: probar dev y crear la rama `production` para el despliegue final en `cifraresearch.com`.

## 20:39 — Resumen: Lanzamiento en producción — rama `production` creada y cifraresearch.com actualizado

Tras validar dev ("todo ok"), se ejecutó el despliegue final.

### Resultado

- **Rama remota `production`** creada desde `development` (`git push origin development:production`, commit `cbdf590`), sin tocar la rama local de trabajo.
- **Pipeline Deploy Production** ejecutado y completado con éxito a la primera: CI (`npm ci` + `node --check`), `git reset --hard` a `production`, dependencias de producción (`--omit=dev`), migraciones, liberación del puerto 3000, reinicio de `cifra-prod` y health check.
- **Verificación en producción**:
  - `/var/www/cifra-prod` en `cbdf590`; puerto 3000 servido por el MainPID del servicio (`9864`).
  - Modal de donaciones con **solo PayPal**; **GA4 activo solo en producción** (`G-7PSC9M3B1H`); `hreflang="es"` y `x-default` por página; `alternateName: "Cifra Research"` en los schemas.
  - Rutas públicas 200: `/`, `/empresa`, `/empresa/KO`, `/guias`, `/guias/que-es-un-informe-10-q`, `/robots.txt`, `/sitemap.xml`, `/llms.txt`, `/llms-full.txt` y `/empresa/KO.md`.
- **Circuito completo operativo**: push a `development` → dev.cifraresearch.com; push/merge a `production` → cifraresearch.com.

## 18:42 — Resumen: Adquisiciones del ejercicio anterior en informes anuales (caso Campbell Soup)

Se reportó que el análisis del último 10-K de Campbell Soup (CPB) mostraba como adquisición del año una compra del ejercicio anterior: la fila "Adquisiciones" tomaba los 2.857M$ de Sovos Brands (adquirida en el ejercicio 2024) en el informe del ejercicio 2025, que no tuvo adquisiciones. Se pidió que el fallo no pueda volver a ocurrir.

### Resultado

- **Causa**: la asignación de capital anual se construía con los campos extraídos por la IA (`facts.acquisitionsYtd`, etc.) y, cuando el estado de flujos solo mostraba la línea de adquisiciones en la columna comparativa del año anterior, se copiaba esa cifra (2.857M/2.899M) como si fuera del ejercicio analizado.
- **Fuente oficial XBRL**: en el bloque anual de `src/agents/analystAgent.js` se vuelcan ahora desde el estado de flujos XBRL del ejercicio analizado `shareBuybacks`, `acquisitionsYtd`, `brandDivestitures` y `assetSalesYtd` (incluidos los 0 explícitos), de modo que nunca se cuela la columna comparativa. Para CPB 2025: adquisiciones 0, desinversiones 258M, recompras 62M; la fila "Adquisiciones" desaparece y la tabla cuadra (En total 84).
- **Narrativa coherente**: si el dato oficial confirma que no hubo adquisiciones materiales (< 50M), el texto de la sección "Adquisiciones" se reescribe para confirmarlo (y describe la desinversión material si la hubo), y la nota de la partida elimina cualquier mención a la adquisición del año anterior. Se elimina también la redundancia "por la venta de Venta del negocio..." al componer notas (`cleanAssetDescription`).
- **10-Q**: en `getPreviousQuarterCashFlow` (`src/services/edgar.service.js`) los campos de adquisiciones/desinversiones/venta de activos/recompras de la asignación trimestral distinguen ahora "sin dato" (null) de "0 oficial", y el analista prioriza el XBRL sobre la IA cuando existe dato oficial.
- **Prompts**: se añade la REGLA DEL PERIODO en `EXTRACTION_PROMPT` (las partidas del cash flow se toman siempre del ejercicio analizado; el comparativo no cuenta) y una regla en `ANNUAL_SYSTEM_PROMPT` para no presentar compras del año anterior como del año analizado.
- **Verificación**: CPB regenerado contra el 10-K real (dos pasadas): nota "*1: Desinversiones: ... noosa ... Pop Secret ..." sin Sovos, sección "4: Adquisiciones: No se realizaron adquisiciones materiales durante el ejercicio. Se completó la desinversión de negocio de yogur noosa ..."; `node --check` y tests de helpers OK; servidor relanzado en `:3000`.

## 19:10 — Resumen: Confirmación y alineación del dominio oficial de producción `cifraresearch.com`

El usuario confirmó que la plataforma está completamente subida a producción en el dominio oficial **`cifraresearch.com`**.

### Resultado

1. **Verificación en vivo**:
   - Comprobada respuesta HTTP/2 200 directa a través de Cloudflare SSL en `https://cifraresearch.com`.
   - Verificados y funcionando en producción: `https://cifraresearch.com/robots.txt` (con enlace canónico al sitemap), `https://cifraresearch.com/sitemap.xml` (41 URLs indexadas), el motor GEO `/llms-full.txt` (49.7 KB con metodología completa) y el endpoint Markdown dinámico `/empresa/KO.md` con resumen financiero ejecutivo y tablas contables.
2. **Alineación de código y fallbacks (`config/index.js`, `src/services/email.service.js`, `public/llms.txt`, `.env.example`)**:
   - Sustituido el antiguo dominio fallback de desarrollo `cifra.app` por el dominio canónico de producción `https://cifraresearch.com`.
   - Actualizado `MAIL_FROM` por defecto a `Cifra <no-reply@cifraresearch.com>`.
   - Actualizado el correo de contacto oficial a `contacto@cifraresearch.com` en `public/llms.txt`.
3. **Validación técnica**:
   - Superado `node --check` sin advertencias ni errores en `config/index.js` y `src/services/email.service.js`.

## 20:50 — Resumen: Blindaje de la Caché Global Compartida de Análisis (0 tokens para usuarios posteriores)

Se blindó la mecánica central de coste y escalabilidad de Cifra: **cuando cualquier persona analiza un filing oficial con IA, el informe resultante queda permanentemente público y cacheado**, de modo que cualquier visitante posterior lo visualiza al instante (en ~50 ms) sin coste de tokens ni llamadas redundantes a la API de IA.

### Diagnóstico
- En `db/repositories/analysisRepository.js`, `createAnalysis` contenía la condición `[userId, Boolean(isPublic && userId === null), ...]`, lo que provocaba que cuando un usuario con sesión iniciada analizaba un filing, `is_public` se forzaba siempre a `false`.
- Como consecuencia, los visitantes anónimos o nuevos usuarios no encontraban el análisis previo (`findLatestDoneAnalysis` y `getAnalyzedAccessionsWithRatings` filtraban por `is_public = true OR user_id = `) y el sistema volvía a ejecutar la llamada a la IA desde cero, consumiendo saldo y tiempo innecesariamente.
- En la base de datos existían 45 informes completados marcados como privados (`is_public = false`) que no se compartían ni se indexaban en el sitemap.

### Resultado
1. **Caché compartida universal (`db/repositories/analysisRepository.js` y `src/api/routes/screener.routes.js`)**:
   - `createAnalysis`: eliminado el requisito `&& userId === null`; ahora respeta el valor de `isPublic` directamente, permitiendo que un análisis tenga propietario (`userId`) y sea simultáneamente público (`is_public = true`).
   - `screener.routes.js`: las peticiones de análisis de filings SEC desde el Screener fijan siempre `isPublic: true`. Al vincular un análisis existente a un nuevo usuario, también se mantiene `isPublic: true`.
2. **Base de Datos y Sitemap**:
   - Ejecutada actualización masiva en PostgreSQL: `UPDATE analyses SET is_public = true WHERE accession IS NOT NULL AND status = 'done' AND report IS NOT NULL`.
   - Se liberaron 45 análisis existentes como públicos compartidos (total: 49 análisis públicos).
   - El sitemap dinámico (`getSitemapXml()`) saltó automáticamente de **41 URLs a 87 URLs indexables** con prioridad alta.
3. **Verificación**:
   - Confirmado mediante tests que `findLatestDoneAnalysis` y `getAnalyzedAccessionsWithRatings` devuelven el informe y la insignia "Ver análisis con IA ✨" para peticiones anónimas (`userId: null`) en empresas como `CPB`, `CAG` y `TAP`.
   - Validación de sintaxis con `node --check` en `analysisRepository.js` y `screener.routes.js`.

## 21:05 — Resumen: Cuenta de administrador secreta, registro obligatorio con cupo de 3 análisis IA/día, aviso de registro en datos financieros y Novedades v0.0.1

Se solicitó: (1) crear una cuenta de administrador con usuario y contraseña aleatorios, guardados solo en los `.env` (local y servidor) y nunca en GitHub; (2) exigir registro para analizar informes nuevos con IA, con máximo de 3 análisis diarios por usuario, sin que leer análisis ya existentes consuma cupo; (3) impedir que una cuenta creada con Google obtenga el rol de administrador (solo la cuenta secreta puede serlo); (4) dejar Novedades únicamente con la versión 0.0.1 "Generación de la web básica"; y (5) que las celdas financieras bloqueadas para visitantes no muestren "PRO" (apariencia de pago) sino una invitación a registrarse.

### Resultado

1. **Credenciales de administrador secretas**:
   - Generados usuario y contraseña aleatorios (`ADMIN_USERNAME=cifra_336950ebb9`, contraseña de 28 caracteres) y guardados solo en el `.env` local y en `/var/www/cifra-prod/.env` del servidor (con copia `.env.bak-admin-*` previa). `.env` sigue en `.gitignore`; en `.env.example` quedan únicamente marcadores de ejemplo.
   - `ensureAdminUser` refuerza la exclusividad: al arrancar, solo la cuenta configurada en el `.env` conserva el rol `admin`; cualquier otro administrador previo (incluidas cuentas de Google) se degrada a `user`.
   - Eliminada de `db/schema.sql` la promoción automática por email (`UPDATE users SET role='admin' WHERE email IN (...)`) causante de que la cuenta de Google del propietario entrara como administradora.
   - `loginOrRegisterGoogle` bloquea el acceso con Google a la cuenta de administración (`ADMIN_GOOGLE_BLOCKED`, mensaje específico en el modal).
2. **Registro obligatorio y cupo diario de 3 análisis IA**:
   - Nueva tabla `ai_generation_usage` (`db/schema.sql`), repositorio `usageRepository.js` y servicio `aiQuota.service.js`: cupo diario por usuario (`DAILY_AI_ANALYSES_LIMIT=3`, día natural en horario de Madrid). Los fallos devuelven el cupo consumido y el admin es ilimitado.
   - `POST /api/upload` exige sesión (`requireAuth`) y consume cupo; `POST /api/screener/company/:ticker/filings/:accession/analyze` responde `AUTH_REQUIRED` (401) solo cuando hay que generar un informe nuevo, y `DAILY_LIMIT_REACHED` (429) al superar el límite.
   - Leer análisis ya existentes (propios o públicos de la caché global) sigue siendo gratis para todos, incluso sin sesión o con el cupo agotado.
   - Frontend (`public/analisis.js`): sin sesión abre el registro, guarda la acción pendiente y la reintenta automáticamente al iniciar sesión; al completar muestra "Te quedan X análisis con IA hoy". `public/portfolio.js` recibe el mismo tratamiento.
3. **Datos financieros sin apariencia de pago**:
   - Las celdas bloqueadas para visitantes ya no muestran la píldora `PRO`: ahora aparece un botón `Regístrate` (`.register-pill`) que abre el registro gratuito.
4. **Novedades**:
   - `public/novedades.js` contiene solo `v0.0.1 — Generación de la web básica`; badge superior actualizado en `index.html` y `empresa.html`; versiones de caché incrementadas (styles v103, auth v11, analisis v25, portfolio v67, novedades v5, empresa v64).
5. **Verificación local**:
   - `node --check` correcto en todos los ficheros modificados; migración aplicada (`ai_generation_usage`).
   - Servidor en modo `mock`: subida y análisis anónimos → 401 `AUTH_REQUIRED`; usuario registrado → 3 generaciones con cupo decreciente y 4.ª con 429; lecturas cacheadas (usuario al límite y visitante anónimo) → 200 `cached:true` sin consumir; intentos fallidos → 0 filas de cupo (devolución correcta).
   - Confirmado que solo `cifra_336950ebb9` tiene rol `admin` y que el login con Google para el correo de administración queda bloqueado (`ADMIN_GOOGLE_BLOCKED`).
6. **Despliegue a producción**:
   - Commit `e7610c0` subido a `development` y `production`; pipelines ejecutados correctamente.
   - Verificado en `/var/www/cifra-prod` (commit `e7610c0`, servicio activo, tabla `ai_generation_usage` creada) y en `cifraresearch.com`: subida anónima → 401; lectura anónima de un análisis público existente (KHC) → 200 `cached`; filing nuevo sin caché → 401 `AUTH_REQUIRED`; login del administrador secreto → rol `admin`; Novedades sirviendo `v0.0.1`.
   - Actualizados `documentacion/PROYECTO.md` y `documentacion/PROYECTO-detalle.md` para reflejar el administrador secreto, el cupo diario y el estado de producción.

## 21:05 — Resumen: Implementadas Señales E-E-A-T en Schema.org (isBasedOn y citation hacia SEC EDGAR)

Se solicitó implementar el punto 2 de las mejoras de SEO/GEO: **añadir señales E-E-A-T oficiales mediante la propiedad `isBasedOn` y `citation` hacia SEC EDGAR** en todos los esquemas estructurados Schema.org (JSON-LD) de la plataforma.

### Resultado

1. **Fichas de Empresa (`buildCompanyJsonLd` en `src/services/seo.service.js`)**:
   - En la entidad `Corporation`: añadido `isBasedOn` y `citation` apuntando a la ficha oficial CIK de la compañía en EDGAR (ej. `https://www.sec.gov/edgar/browse/?CIK=21344`).
   - En la entidad `WebPage`: añadido `isBasedOn` de tipo `DataFeed` estructurado con nombre oficial *"SEC EDGAR (Electronic Data Gathering, Analysis, and Retrieval system)"* y proveedor *"U.S. Securities and Exchange Commission"* (`GovernmentOrganization`).
2. **Informes Públicos de Resultados (`buildReportJsonLd` en `src/services/seo.service.js`)**:
   - En la entidad `Article`: enriquecido el objeto `isBasedOn` (tipo `DigitalDocument`) con enlace directo al filing oficial de la SEC y acreditación formal a la SEC como proveedor gubernamental.
3. **Guías Educativas (`src/content/guias/*.html`)**:
   - Inyectadas propiedades `isBasedOn: "https://www.sec.gov/edgar"` y `citation: "https://www.sec.gov/edgar"` en las 8 guías del portal.
4. **Verificación**:
   - Verificado mediante script de prueba con datos reales de Coca-Cola (KO) y Campbell Soup (CPB) que el JSON-LD generado es sintácticamente válido y contiene los nodos de procedencia oficial.

## 21:08 — Resumen: Renombrada la marca a "Cifra" y nuevo logo (diferenciación de TIKR)

Se solicitó eliminar "Terminal" del nombre y cambiar el logotipo para reducir el parecido visual con TIKR.

### Resultado

1. **Marca**: "Terminal Cifra" → "Cifra" en la topbar y el modal de autenticación (`index.html`, `empresa.html`), títulos de pestaña (`public/app.js`), las 8 guías de `src/content/guias/`, la plantilla de guías de `seo.service.js`, los correos de `email.service.js` y comentarios de `analisis.js`, `priceAlerts.js` y `forum.js`.
2. **Logo**: sustituido el triángulo `▲` por una marca propia de barras ascendentes en SVG (`fill="currentColor"`) en topbar, modal y guías; adaptadas las reglas `.brand-mark`, `.cifra-symbol` y `.guia-brand-mark` en `styles.css` y `guia.css` para dimensionar SVG (20/25/17/27 px y 1.1rem).
3. **Assets**: rediseñados `favicon.svg`, `logo-cifra.png` (512×512), `favicon-32.png` (32×32), `apple-touch-icon.png` (180×180) y `og-cifra.png` (1200×630) con las barras esmeralda sobre fondo `#0f172a`, regenerados con `rsvg-convert`.
4. **Caché**: `styles.css?v=103` → `?v=104` en `index.html` y `empresa.html`.
5. **Verificación**: `node --check` correcto en todos los JS tocados; sin restos de "Terminal" ni del triángulo en `public/` ni `src/`. El backup `backup/paleta-naranja-2026-09-10/` se conserva intacto y el tema naranja opcional sigue disponible (pendiente decidir si se elimina).

## 21:10 — Resumen: Cache-busting de favicons tras el cambio de logo

El navegador seguía mostrando el triángulo antiguo en la pestaña pese a estar regenerados los assets, por caché de favicon del navegador.

### Resultado

- Añadido `?v=2` a los enlaces de `favicon.svg`, `favicon-32.png` y `apple-touch-icon.png` en `index.html`, `empresa.html`, las 8 guías, la plantilla de `seo.service.js` y la página 404; también a los iconos de `manifest.webmanifest` (`logo-cifra.png?v=2`).
- `node --check` de `seo.service.js` correcto. Para ver el nuevo icono hay que recargar la pestaña (mejor cerrarla y abrirla) y, en producción, desplegar los cambios.

## 21:10 — Resumen: Botón "Copiar enlace" en informes y sincronización de URL en el navegador

El usuario consultó cómo obtener la URL pública de un análisis recién generado (ejemplo de TAP) desde la vista `/analisis`.

### Diagnóstico
- Al finalizar un análisis o abrirlo desde el histórico, la vista SPA mantenía la URL estática `/analisis` en la barra del navegador sin exponer al usuario la dirección canónica permanente (`/informe/:id`) ni proveer un botón directo para copiarla.

### Resultado
1. **Frontend (`public/index.html`, `public/empresa.html`, `public/analisis.js` v26)**:
   - Añadido el botón **`🔗 Copiar enlace`** (`#copy-report-url`) en la barra de acciones del informe generado (junto a los descargables PDF, Word, ODT y HTML).
   - Al hacer clic, copia automáticamente la URL canónica (ej. `https://dev.cifraresearch.com/informe/:id`) al portapapeles y emite un toast de confirmación.
   - En `finishAnalysis` y `viewHistoryAnalysis`, el navegador actualiza dinámicamente su URL en la barra de direcciones mediante `history.replaceState(null, '', '/informe/' + currentAnalysisId)`, permitiendo al usuario copiar la dirección directamente desde el navegador. Al pulsar "Analizar otro informe", se restablece a `/analisis`.
2. **Caché de cliente**:
   - Incrementada la versión de `analisis.js` a `?v=26` en ambos HTML.

## 21:19 — Resumen: Paginación de "Mis análisis" (10 por página), arreglo del logo solapado al contraer el menú y limpieza del aviso de Novedades

El usuario pidió tres cambios: limitar la tabla de análisis guardados a 10 por página con navegación entre páginas, evitar el solapamiento del logo de Cifra con el botón de expandir cuando el menú lateral está contraído, y eliminar el aviso "¿Actualizando la plataforma?" de la sección Novedades.

### Resultado

1. **Paginación en "Mis análisis" (`public/analisis.js`)**:
   - Añadido estado `historyPage` y constante `HISTORY_PAGE_SIZE = 10`.
   - `renderHistory` ahora ordena, calcula el total de páginas, recorta el rango visible y lo pinta; la página se reinicia al buscar/filtrar (`fetchAnalyses`), al ordenar por columna y al limpiar filtros.
   - Nueva función `renderHistoryPagination` que muestra "Mostrando X–Y de Z" y botones Anterior / números de página (con elipsis si hay muchas) / Siguiente, deshabilitando los extremos; se oculta si hay 10 análisis o menos.
   - Delegación de clic en `#history-pagination` para cambiar de página y desplazar la vista al inicio de la tabla.
   - Añadido el contenedor `<div class="history-pagination" id="history-pagination" hidden>` en `index.html` y `empresa.html`.
2. **Logo solapado con el botón de expandir (`public/styles.css`)**:
   - En estado `sidebar-collapsed`, el área de marca ahora centra el botón de expandir con `justify-content: center` y sin padding, y se oculta `.brand-lockup` (logo + texto) para evitar que ambos iconos se solapen en los 68 px del menú.
   - En móvil (`max-width: 900px`) se restaura la visibilidad del logo, ya que el menú se comporta como panel deslizante.
3. **Aviso eliminado de Novedades (`public/novedades.js`, `public/styles.css`)**:
   - Retirado el bloque `.novedades-footer-note` ("¿Actualizando la plataforma? Edita `public/novedades.js`…") y sus estilos asociados.
4. **Caché de cliente**:
   - `styles.css?v=104` → `?v=105`, `analisis.js?v=26` → `?v=27` y `novedades.js?v=5` → `?v=6` en `index.html` y `empresa.html`.
5. **Verificación**:
   - `node --check` correcto en `analisis.js` y `novedades.js`.
   - Probado con Chrome Headless: menú contraído sin solapamiento (solo botón de expandir centrado); con 23 análisis simulados la tabla muestra 10 filas por página, "Mostrando 1–10 de 23", páginas 1/2/3, y los botones Anterior/Siguiente se deshabilitan en los extremos (en la última página solo 3 filas); la sección Novedades ya no muestra el aviso.
   - Nota: `seo.service.js` cachea las plantillas HTML en memoria, por lo que los cambios de `index.html`/`empresa.html` requieren reiniciar el servidor para verse en local.

## 21:35 — Resumen: Renderizado completo y estilizado de la aplicación Cifra en URLs públicas `/informe/:id`

El usuario reportó que al acceder a una URL directa como `http://localhost:3000/informe/25`, el diseño se mostraba como un artículo de blog oscuro y plano (`guia.css`), en lugar del diseño elegante e interactivo de la aplicación Cifra (`#section-analisis > #result-preview` con `styles.css`, tablas con cabeceras oscuras, números coloreados, desglose en horizontes, valoración con estrellas, reporte de incidencias y botones de descarga).

### Diagnóstico
- `src/middleware/seo.middleware.js` delegaba las peticiones a `/informe/:id` en `getPublicReportHtml(id)` de `seo.service.js`.
- `buildReportPage` envolvía los datos en la plantilla de guías (`guia-wrap`, `guia.css`), que es un layout estrecho y monocromático pensado para artículos educativos y no para la interfaz de la aplicación.
- No existía hidratación en `analisis.js` ni resolución de la sección en `empresa.js` cuando la URL comenzaba por `/informe/`.

### Resultado
1. **Backend (`src/services/seo.service.js`)**:
   - `buildReportPage(row)` ahora utiliza como plantilla base `public/index.html`.
   - Inyecta los metadatos SEO / OpenGraph / Twitter de forma dinámica (`title`, `description`, `canonical`, `alternate` en markdown y hreflang).
   - Genera Schema.org JSON-LD con `@graph` conteniendo `Article` (con `isBasedOn` y `citation` a SEC EDGAR) y `BreadcrumbList`.
   - Inyecta el script de hidratación `<script id="cifra-initial-report" type="application/json">` con todos los datos del informe.
   - Pre-renderiza para SSR / bots el HTML completo del informe (`renderReportSsrHtml`, `renderHorizonSsr`, `renderTableSsr`, `renderNotesSsr`) con las clases exactas de Cifra (`report-block`, `report-extras`, `table-wrap`, celdas `.pct-positive`, `.pct-negative`, `.cell-bold`, `.highlight-adjust`, notas con `.highlight-note`).
   - Activa el estado inicial del DOM para que el preview del informe sea visible de inmediato: desoculta `#company-body`, `#section-analisis` y `#result-preview`; oculta `#company-loading`, `#company-head-row` y `#sec-analysis-entry`; asigna la pestaña "Análisis" como activa.
   - Añadida la función `invalidateReportCache(id)` para limpiar la caché de informes al regenerar.
2. **Frontend (`public/analisis.js` v28, `public/empresa.js` v65)**:
   - `empresa.js`: `resolveInitialSection()` reconoce ahora las URLs que comiencen por `/informe` para seleccionar automáticamente la pestaña de análisis y ocultar la fila de cotización.
   - `empresa.js`: `getInitialCompanyTicker()` lee el ticker del informe pre-inyectado en `#cifra-initial-report` para que la navegación lateral a Perfil o Informes mantenga el contexto de la empresa analizada.
   - `analisis.js`: nueva función `loadReportData(analysis, options)` reutilizada tanto por `viewHistoryAnalysis` como por la hidratación automática de `/informe/:id`.
   - Al cargar la página, `analisis.js` lee `#cifra-initial-report` (o consulta `/api/analyses/:id` como fallback), vincula todos los botones de descarga (PDF, Word, ODT, HTML), el botón de copiar enlace, la barra de valoración por estrellas y el modal de reporte de errores.
3. **Regeneración de informes (`src/api/routes/adminReports.routes.js`)**:
   - Se llama a `invalidateReportCache(id)` tras regenerar un informe con IA para asegurar que las solicitudes públicas vean los datos nuevos inmediatamente.
4. **Verificación**:
   - `node --check` correcto en todos los ficheros JS (`seo.service.js`, `empresa.js`, `analisis.js`, `adminReports.routes.js`).
   - Verificado con `curl http://localhost:3000/informe/25`: devuelve la página completa de Cifra con título `Informe 10-Q de The Kraft Heinz Company (KHC) — 2025 Q3 results — KHC | Cifra`, `#section-analisis` visible, `#result-preview` activo, tabla de ventas, cash flow y asignación de capital con estilos, script `#cifra-initial-report` y botones de acción.
   - Verificado con `curl http://localhost:3000/informe/25.md` y `curl -H "Accept: text/markdown"`: devuelve la versión limpia en Markdown para motores de IA.
