## 11:58 — Resumen: Versionado de análisis, botón de actualización e histórico de versiones

Se solicitó que junto al botón «Ver análisis con IA» de los informes aparezca la versión del análisis guardado; si existe una versión más reciente (p. ej. consumo defensivo 0.2 frente a un análisis 0.1), mostrar un botón para regenerarlo con la nueva versión. Requisito clave: las versiones anteriores nunca se eliminan y deben poder consultarse y descargarse desde un histórico de versiones del análisis. Aclaración posterior del usuario: la decisión de regenerar es en dos niveles, dando prioridad a la versión del subsector/empresa concreta y, solo si coincide, comparando la versión general de consumo defensivo.

### Resultado

**Versionado en dos niveles** (`src/agents/sectorAgent.js`):
- Versión actual subida de `0.1` a `0.2` (sector y subsector `cerveceras`).
- `resolveVersionInfo(subsector)` devuelve la versión del subsector (si tiene reglas propias), la del sector general y la efectiva.
- `isAnalysisOutdated()` decide si se puede regenerar: primero compara la versión específica (subsector/empresa) guardada con la nueva; si la nueva es mayor o antes no había reglas específicas → regenerar; si coinciden, compara la versión general del sector usada al generar; nunca se regenera hacia una versión inferior ni cuando no hay datos para comparar. `compareVersions()` soporta versiones tipo `1.10.3`. Los 9 escenarios de decisión han sido verificados con un script de pruebas.

**Base de datos** (`db/schema.sql`, `db/repositories/analysisRepository.js`):
- Nuevas columnas `subsector` y `sector_version` en `analyses` (migración aplicada).
- `getAnalysisVersions({ ticker, accession, userId })`: todas las versiones de un mismo informe (públicas o del propio usuario), de la más reciente a la más antigua.
- El listado de filings devuelve además la versión del último análisis, el número de versiones, la versión vigente y `versionOutdated` (decisión del servidor).
- `findLatestDoneAnalysis` y `findUserAnalysis` desempatan por `id DESC` además de `created_at`.

**Guardado** (`src/services/analysis.service.js`): cada análisis persiste `version` (efectiva), `subsector` y `sectorVersion` (reglas generales usadas). El agente de sector devuelve ambas versiones.

**API** (`src/api/routes/screener.routes.js`, `analysis.routes.js`, `adminReports.routes.js`, `seo.service.js`):
- Nuevo endpoint `GET /api/screener/company/:ticker/filings/:accession/versions` con el histórico y la versión vigente.
- `POST .../analyze` acepta `{ upgrade: true }` o `?upgrade=1`: salta la caché, genera un análisis nuevo y **no elimina** las versiones anteriores. Requiere sesión y consume cupo.
- Las regeneraciones de admin (`?force`, `.../regenerate` y la del panel de administración) ya **no borran** análisis previos: crean una versión nueva y conservan el histórico.
- `GET /api/analyses/:id`, los filings y la hidratación SEO (`/informe/...`) incluyen versión y `versionOutdated`.

**Interfaz**:
- Tabla de informes de empresa (`public/empresa.js`): badge `v0.2` junto a «Ver análisis con IA ✨» y botón «Actualizar a v0.2 ✨» / «Actualizar versión ✨» cuando procede (con confirmación, aviso de consumo de cupo y conservación del histórico). Tras actualizar se recargan los informes.
- Vista de análisis (`public/analisis.js`, `index.html`, `empresa.html`, `styles.css`): badge de versión y control «Versiones (N)» con menú para ver y descargar (PDF/Word/ODT) cada versión; botón de actualización a la versión vigente. El histórico de análisis muestra la etiqueta de versión. Versiones de caché subidas: `styles.css?v=106`, `analisis.js?v=30`, `empresa.js?v=67`, `reports.js?v=4`.
- Panel de administración (`public/reports.js`): muestra la versión de cada análisis y los textos de regeneración avisan de que se conservan las anteriores.

**Verificación**: `node --check` en todos los ficheros tocados; migración aplicada; endpoint de versiones y listado de filings probados contra la BD local; pipeline completo con proveedor `mock` comprobado (subsector `cerveceras` → `version=0.2`, `sector_version=0.2`, fila y PDF de prueba limpiados); escenarios de decisión de versión (solo sector, subsector, misma versión de subsector con sector mayor, versión inferior, sin versión) correctos. Servidor de desarrollo relanzado en `:3000`.

**Pendiente/duda**: hoy la versión de los 10-Q/10-K de filings se resuelve con el subsector que reciba el análisis; ninguna ruta pasa todavía un subsector/empresa concreta, por lo que los análisis nuevos registran la versión general del sector. Si se quiere aplicar reglas por subsector o por empresa (p. ej. `cerveceras` o un ticker concreto), basta con pasar `subsector` en las opciones del análisis y mantener `SUBSECTOR_VERSIONS`.

## 12:04 — Resumen: Versionado jerárquico general.sector.subsector.empresa

Se pidió cambiar el esquema de versiones a `general.sector.subsector.empresa`, donde cada nivel con agente propio declara su número al principio de su `.md`; la versión del análisis es la composición de los niveles aplicables y se regenera si la nueva es mayor que la guardada. Valores de partida: general = 0, sector = 1.

### Resultado

**Registro de versiones** (`src/agents/versionRegistry.js`):
- `parseVersionMarker()` lee `Versión: N` / `version: N` al principio de cada `.md` (admite `>` o frontmatter) y `resolveAnalysisVersionInfo()` compone `general.sector[.subsector][.empresa]`:
  - General: `knowledge/general.md` (10-Q) o `knowledge/anual/general.md` (10-K).
  - Sector: `knowledge/<sector>/sector.md`.
  - Subsector: `knowledge/<sector>/subsectores/<slug>/subsector.md` (solo si existe agente de subsector).
  - Empresa: `knowledge/<sector>/empresas/<ticker>/empresa.md` o `.../<ticker>.md` (solo si existe agente de empresa).
- Caché por `mtime`; `compareVersions()` considera más nueva la versión con más niveles cuando el prefijo coincide (`0.1.0 > 0.1`); `isAnalysisOutdated()` compara la versión guardada con la compuesta vigente (versión ausente = regenerable).
- `sectorAgent` devuelve la versión compuesta (y el segmento de sector para `sector_version`); `analystAgent` carga además las reglas de empresa si existen y las añade al prompt.

**Marcadores añadidos**: `general.md` → `Versión: 0`; `anual/general.md` → `Versión: 0`; `consumo-defensivo/sector.md` → `Versión: 1`; subsector `cerveceras` → `version: 0` en el frontmatter. La versión vigente de un 10-Q/10-K sin subsector es `0.1`, con cerveceras `0.1.0` y con agente de empresa `0.1.<n>`.

**Rutas y SEO**: el listado de filings, el endpoint de versiones, `/api/analyses/:id` y la hidratación SEO calculan la versión compuesta de forma asíncrona y devuelven `currentVersion` y `versionOutdated` según la comparación completa.

**Migración** (`db/schema.sql`): los análisis anteriores al versionado jerárquico (`sector_version` NULL) se marcan con `version = NULL` para que no colisionen con el nuevo esquema y ofrezcan regeneración con la versión vigente. Idempotente (al quedar ambos campos NULL no vuelve a aplicarse). Aplicada: 55 filas locales quedan «sin versión».

**Verificación**: 16 comprobaciones del registro de versiones (composición real desde los `.md`, nivel empresa temporal creado/eliminado, comparador con niveles, decisiones de regeneración), `node --check` de todos los ficheros, migración aplicada dos veces (idempotente), pipeline completo con proveedor `mock` (`version=0.1.0`, `sector_version=1`, fila y PDF de prueba limpiados) y endpoints reales probados en `:3000` (filings, versiones, `/api/analyses/:id`, hidratación SEO) con `currentVersion=0.1`.

**Nota**: los análisis antiguos guardados como `0.1` con el esquema anterior quedan «sin versión» tras la migración y por tanto muestran el botón de actualización; a partir de ahora, para forzar la regeneración de todos los análisis de un nivel basta con subir su número al principio del `.md` correspondiente.

## 12:20 — Resumen: Acceso directo al histórico de versiones desde la tabla de informes

Se preguntó cómo ver las versiones anteriores de un análisis. El histórico ya existía dentro de la vista de análisis (botón «Versiones (N) ▾» en las acciones del informe, con ver y descargar cada versión), pero no era evidente desde la tabla de informes de la empresa.

### Resultado

- `public/empresa.js`: nuevo botón **«Versiones (N)»** en la fila del filing cuando el análisis tiene más de una versión guardada. Al pulsarlo abre la vista de análisis y despliega automáticamente el menú de versiones.
- `public/analisis.js`: `runFilingAnalysis(ticker, accession, { openVersions: true })` abre el menú de versiones al terminar de cargar el análisis (espera a que llegue el listado si aún no está en caché).
- `public/styles.css`: estilo del botón; versiones de caché subidas a `styles.css?v=107`, `analisis.js?v=31` y `empresa.js?v=68` en `index.html` y `empresa.html`.

**Nota**: hasta ahora las regeneraciones antiguas borraban las versiones previas, por lo que muchos filings solo tienen 1 versión y no muestran el botón en la tabla (en el visor aparece igualmente «Versiones (1)»). A partir de este cambio, cada actualización conserva las anteriores: tras «Actualizar», la fila mostrará «Versiones (2)».

## 12:35 — Resumen: Menú de versiones unificado con hamburguesa junto a «Ver análisis con IA»

Se pidió sustituir los botones sueltos de versión/actualización/versiones de la tabla de informes por una pequeña hamburguesa integrada en el propio botón de análisis: al pulsarla debe mostrar la versión actual, la opción de actualizar (si procede) y las versiones anteriores.

### Resultado

- `public/empresa.js`: el botón «Ver análisis con IA ✨» pasa a ser un grupo partido (`filing-analyze-group`) con una hamburguesa «☰» (`.filing-analyze-menu`) que abre un desplegable flotante (`filing-version-popover`, posicionado con `fixed` para que no lo recorte la tabla) con:
  - **Versión actual** (vX o «Sin versión»).
  - **Actualizar a vX ✨** con subtítulo explicativo si la versión está desfasada; si no, «✓ Análisis en la versión vigente».
  - **Versiones anteriores (N)** con fecha, modelo y botones **Ver / PDF / DOCX / ODT** para cada una (descargas directas contra `/api/reports/...`).
  - Punto naranja en la hamburguesa (`has-update`) cuando hay actualización disponible.
- Caché de versiones por ticker+accession con invalidación al recargar los filings y al terminar un análisis; el desplegable se cierra al hacer clic fuera, al hacer scroll, al redimensionar o al recargar la tabla, y la hamburguesa actúa como toggle.
- `public/analisis.js` se mantiene igual (sigue existiendo el desplegable «Versiones (N)» dentro del informe; el `openVersions` ya no se usa desde la tabla).
- `public/styles.css`: estilos del grupo, hamburguesa, punto de actualización y del desplegable; se retiran las reglas de los botones eliminados. Versiones de caché subidas a `styles.css?v=108` y `empresa.js?v=69`.

**Verificación**: `node --check`; endpoints reales con filing de 9 versiones (TAP) y filings de 1 versión; servidor de desarrollo reiniciado para refrescar la plantilla SEO cacheada y comprobado que sirve `empresa.js?v=69` y `styles.css?v=108`.

## 12:27 — Resumen: Caja y deuda medidas por la variación del balance, con nota obligatoria y reconciliación

Se detectó que en la asignación de capital la caja podía quedar explicada con el cambio neto del estado de flujos (que puede diferir del balance por efectivo restringido o efecto divisa). Se pidió que el balance y la caja se miren siempre por cuánto se ha cambiado en el balance y que quede reflejado en las notas.

### Resultado

- **Reglas** (sin subir números de versión, por indicación expresa del usuario):
  - `knowledge/general.md` (10-Q) y `knowledge/anual/general.md` (10-K): la fila `Caja` y la `Deuda` se calculan SIEMPRE por la variación de saldos del balance (prohibido usar el cambio neto de efectivo del estado de flujos como valor de la fila). Nota obligatoria ampliada: `Deuda balance`, `Deuda neta` y `Caja balance`, indicando signo/significado y el valor de la fila; si el estado de flujos presenta un neto distinto, se explica la diferencia (efectivo restringido, efecto divisa u otras partidas no monetarias) citando las notas del informe.
  - `knowledge/consumo-defensivo/sector.md` y `prompts/consumo-defensivo.md`: nota obligatoria de deuda + caja y misma prohibición; la vinculación cromática `Caja*N`/`Deuda*N` pasa a referenciar conjuntamente esa nota.
- **Pipeline** (`analystAgent.js`):
  - La ruta con EDGAR (Q2/Q3) usa ahora la nota construida desde el balance (`buildCashMovementDetails`: saldos anterior/actual, significado y fila) en lugar de la cadena simple que devolvía EDGAR, tanto en 3M como en YTD.
  - En Q1 la nota de 3M incluye la reconciliación con el cambio neto de caja cuando está disponible.
  - Prompts internos (10-Q y 10-K) y ejemplos de los esquemas de salida actualizados con el formato exacto exigido.
- **Verificación**: `node --check` correcto en `analystAgent.js` y `edgar.service.js`.
- **Nota**: al no subir versiones, los análisis ya guardados no se marcan como desactualizados; cuando se quiera forzar la regeneración habrá que subir la versión explícitamente. Se añadió a `AGENTS.md` la regla de no tocar versiones sin petición explícita del usuario.

## 12:33 — Resumen: Máximo 2 decimales en cifras de Cash Flow y Asignación de Capital

Se detectó que la fila `Libre` del bloque de Cash Flow mostraba artefactos de coma flotante (ej. `1827.1000000000004` en lugar de `1827,1`). Se pidió que ninguna cifra tenga más de 2 decimales tras la coma.

### Resultado

- **Origen corregido** (`analystAgent.js`): `buildWorkingCapitalDataFallback` construía `Libre` con `String(fcf - dividendo)` sin redondear; ahora redondea a 2 decimales antes de formatear.
- **Normalización final**: nueva función `normalizeNumericCell()` que redondea a 2 decimales y usa coma decimal, respetando celdas con símbolos (`0,95 $`), vacías (`—`) y valores de miles. Se aplica a todos los valores de las tablas de Cash Flow y de Asignación de Capital antes de guardar el informe, de modo que cualquier artefacto que provenga del modelo también queda corregido.
- **Reglas** (sin subir versiones): añadida la regla de máximo 2 decimales y prohibición de artefactos de coma flotante en `knowledge/general.md`, `knowledge/anual/general.md` y `prompts/consumo-defensivo.md`.
- **Verificación**: `node --check`; prueba de `normalizeNumericCell` con `1827.1000000000004 → 1827,1`, `4108,1`, `-526`, `2281`, `0,95 $`, `—` y `1.724 → 1724`.

## 12:44 — Resumen: Corrección de errores detectados en el histórico de análisis (unidades, decimales, cuadre y notas)

Otro agente revisó los análisis guardados y detectó: valores ×1000 en asignación de capital, floats sin redondear, deuda con años futuros, exceso de «No cuadra», notas y filas incoherentes, ambigüedad de coma decimal, `Libre ≠ FCF − Dividendo` y dividendos con ceros.

### Resultado

- **Valores ×1000** (`analystAgent.js`): nueva `normalizeExtractedUnits()` que detecta importes desproporcionados para el tamaño de la compañía (referencia: mayor cifra de la cuenta de resultados) y los convierte de miles a millones (÷1000). Se aplica a `facts`, `balance`, `cashFlow` y capital circulante antes de construir la asignación de capital (cubre BF.B, KDP, MNST, GIS, FIZZ, SAM, CELH…). El prompt de extracción añade la regla de tablas «(In thousands)».
- **Decimales**: `parseLooseReportNumber` ya no interpreta como miles valores como `330,356` o `1114,201` (si la lectura como miles supera 100.000 y hay un solo separador, usa la lectura decimal) y no une miles si la parte entera es `0` (`0,955`). La normalización a 2 decimales se aplica **antes** de sumar y se usa `formatCellNumber()` al crear las filas.
- **Cuadre**: umbral relativo `|En total| ≤ máx(50M, 20 % del Libre, 10 % de la suma bruta de movimientos)` en código, prompts internos y `knowledge/general.md`; el bloque de capital sin cifras válidas se omite en vez de publicar todo `—`.
- **Filas derivadas**: se fuerza en código `FCF = Cash Flow − CAPEX` y `Libre = FCF − Dividendo` en cada escenario.
- **Deuda anual**: el histórico de EDGAR y el de la IA se filtran a `year ≤ fiscalYear`.
- **Notas**: las llamadas `*N` de las filas de Ventas se reasignan por concepto o se retiran si la nota no existe; las notas de desinversión/adquisición que decían «no hubo» se sustituyen por una nota factual con el importe de la fila; las llamadas del bloque de capital ya se sincronizaban.
- **Dividendos**: se descartan ejercicios con dividendo y pago total a 0.
- **Verificación**: `node --check`, import del módulo y pruebas de `normalizeExtractedUnits` (BF.B 1.000.000→1.000; CELH -60.005,5→-60; KO 39.065 intacto) y del parser (`330,356→330,356`, `1,724→1724`, `0,955→0,955`).
- **Pendiente**: los análisis ya guardados conservan los errores (no se migran datos); los duplicados worker/caché de usuario quedan como comportamiento de producto.

## 12:54 — Resumen: Verificación real con opencode (5 empresas) y arreglo de Libre sin dividendo

Se pidió probar el pipeline con la API de opencode (thinking off) en 5 empresas de resultados, un análisis por empresa, para poder comparar.

### Resultado

- Ejecutado el worker con `--provider=opencode --max-quarters=1 --once` sobre **KDP (Q2 2026), BF-B (Q1 2027), CELH (Q2 2026), MNST (Q2 2026) y GIS (10-K FY 2026)**. 5/5 completados (40–63 s por análisis), guardados como públicos.
- Comprobación automática de los bugs corregidos: KDP #119, BF.B #120, CELH #121 y GIS #123 limpios. MNST #122 destapó un caso real: cuando no hay dividendo (`—`) y se aplica ajuste fiscal, `Libre` quedaba sin recalcular (`Libre ≠ FCF − Dividendo`).
- **Corrección** (`analystAgent.js`): en la normalización fiscal, dividendo ausente = 0; en el cuadre final de filas, dividendo `—` implica `Libre = FCF`. Regenerado MNST como **#124** (OK), conservando #122 para comparar antes/después.
- URLs locales de comparación: `http://localhost:3000/empresa?ticker=KDP`, `...?ticker=BF-B`, `...?ticker=CELH`, `...?ticker=MNST`, `...?ticker=GIS`.

## 13:02 — Resumen: KDP no detectaba la adquisición de JDE Peet's (-16.615M)

En el análisis de KDP Q2 2026 no aparecía la fila `Adquisiciones` pese a que el estado de flujos muestra «Acquisitions of businesses, net of cash acquired (16,615)».

### Resultado

Cuatro causas encadenadas, todas corregidas en `analystAgent.js` y `edgar.service.js`:
- **Unidades SEC**: `parseFinancialValue` leía `(16,615)` como 16,615 (decimal) en lugar de 16.615M. Ahora delega en `parseLooseReportNumber`, que interpreta el grupo de 3 dígitos como miles y mantiene la coma decimal para cifras tipo `1784,4`.
- **Plural**: `extractCapitalCashFlowFacts` solo buscaba «Acquisition of business…»; ahora acepta «Acquisitions of businesses, net of cash acquired» y «Payments to acquire businesses». Se actualizaron los prompts y las reglas (`general.md`, `sector.md`, `prompts/consumo-defensivo.md`).
- **Signo XBRL**: EDGAR guardaba las adquisiciones en negativo y la comprobación `>= 50` las convertía a 0; ahora se compara en valor absoluto.
- **Prioridad**: un 0 de EDGAR ya no pisa el valor extraído del filing; el trimestre se deduce como `YTD Qn − YTD Qn-1` cuando el XBRL no lo desglosa (KDP: Q2 = 16.615 − 0). Además, las notas ya no mencionan desinversiones inmateriales (< 50M) sin fila.
- **Verificación**: KDP regenerado como **#128**; ambos horizontes muestran `Adquisiciones*1 = -16.615` con la nota de JDE Peet's, adquirida el 1 de abril de 2026. Comprobación automática final de los 5 análisis (BF.B #120, CELH #121, GIS #123, KDP #128, MNST #124): todos OK.

## 13:17 — Resumen: Financiación de capital y deuda asumida (no-cash) en la asignación de capital

Se pidió añadir a la tabla la emisión de preferentes, la venta de participaciones no controladoras y la deuda asumida no-cash (4.819M en KDP), aclarando si eso duplicaba la deuda.

### Resultado

- **Extracción** (`analystAgent.js`): nuevas funciones `extractEquityIssuance()` (preferentes y venta de participaciones no controladoras) y `extractDebtCashFlow()` (flujo neto de deuda de la sección de financiación, leyendo la columna actual por celdas y descartando comparativos cuando el periodo actual es `—`). Nuevos campos `preferredIssuance*`, `nonControllingSale*` y `debtCashFlowYtd` con su regla en el prompt y el esquema de extracción.
- **Filas nuevas**: `Emisión de preferentes` (+) y `Venta de participaciones` (+, ambas fuentes de capital, no deuda ni desinversión) y `Deuda asumida (no-cash)` (−), calculada como `Δdeuda del balance − flujo neto de deuda por caja` cuando hay adquisición material. Cada una con su nota y llamada `*N`; deduplicación y filtros actualizados.
- **Reglas**: documentadas en `knowledge/general.md`, `knowledge/consumo-defensivo/sector.md` y `prompts/consumo-defensivo.md`.
- **Aclaración de doble conteo**: no lo hay. La tabla suma algebraicamente `Deuda` (+13.839) y `Deuda asumida` (−4.819) → efecto neto +9.020, que es exactamente la deuda que aportó caja; sin la fila negativa se contaría como fuente un dinero que nunca entró.
- **Verificación**: KDP regenerado como **#129**: YTD con Libre 255, Adquisiciones −16.615, Emisión de preferentes +4.395, Venta de participaciones +3.899, Caja −491, Deuda +13.839, Deuda asumida −4.819 → En total **463** ("Más o menos cuadra"). Comprobación automática de BF.B #120, CELH #121, GIS #123, MNST #124 y KDP #129: todos OK.
- **Nota**: en el bloque de 3 meses de KDP no aparecen estas filas porque la financiación se levantó en Q1 (fuera de la ventana) y el 10-Q solo publica columnas acumuladas; el desglose trimestral de deuda en efectivo no está disponible en el filing.

## 13:26 — Resumen: La nota de deuda asumida desglosa el aumento de deuda (caja vs. heredada)

Se pidió que las notas expliquen claramente que parte del aumento de deuda del balance no es entrada de caja.

### Resultado

- **Nota mejorada** (`analystAgent.js`): la nota de `Deuda asumida (no-cash)` ahora desglosa el aumento de deuda en sus dos componentes. Ejemplo real de KDP #130: `De los 13.839M que sube la deuda del balance, 9.020M son deuda emitida/amortizada con caja y 4.819M son deuda que ya existía en la empresa adquirida y se asume con la compra; esa parte no supone entrada de caja y se resta en el cuadre.`
- **Reglas**: `knowledge/general.md`, `knowledge/consumo-defensivo/sector.md` y `prompts/consumo-defensivo.md` exigen ese desglose en la nota.
- **Verificación**: KDP regenerado como **#130** (YTD: En total 463, "Más o menos cuadra"); comprobación automática de BF.B #120, CELH #121, GIS #123, MNST #124 y KDP #130: todos OK.

## 13:28 — Resumen: Regeneración de KDP y TAP Q2 2026 con opencode

Se pidió regenerar el informe de KDP y generar el Q2 de TAP.

### Resultado

- **KDP #131**: YTD con Libre 255, Adquisiciones −16.615, Emisión de preferentes +4.395, Venta de participaciones +3.899, Caja −491, Deuda +13.839, Deuda asumida (no-cash) −4.819 → En total 463 ("Más o menos cuadra"); 3M con la adquisición de JDE Peet's detectada (−16.615).
- **TAP #132** (Molson Coors Q2 2026): adquisición de Atomic Brands (Monaco Cocktails, 271M pagados, abril 2026) detectada en ambos horizontes. 3M: Libre 623,8 − 271 (adq.) − 1.745,5 (caja) + 1.447 (deuda) = **54,3** ("Más o menos cuadra"). YTD: Libre 301,5 − 271 − 211 (recompras) − 1.231,6 (caja) + 1.419,4 (deuda) = **7,3** ("Más o menos cuadra"). Sin filas de financiación de capital ni deuda asumida (no aplican en este caso).
- **Verificación automática**: KDP #131 y TAP #132 → OK (sin ×1000, sin decimales largos, sin notas huérfanas y Libre = FCF − Dividendo).

## 13:33 — Resumen: Nota explicativa del descuadre trimestral cuando la compra se financió antes del trimestre

En KDP, el bloque de los últimos 3 meses seguía marcando "No cuadra" (−12.559M) porque la compra de JDE Peet's se pagó en Q2 con el efectivo (restringido/escrow) y la deuda/equity levantados en Q1, y el 10-Q solo publica el estado de flujos acumulado.

### Resultado

- **Dato verificado en XBRL**: KDP Q1 2026 tuvo un cambio neto de efectivo (incluido restringido) de +17.672M, mientras el balance solo muestra 898M de caja no restringida: la financiación se aparcó como efectivo restringido/escrow y en Q2 se usó para pagar la adquisición.
- **Nota automática** (`analystAgent.js`): cuando en `ÚLTIMOS 3 MESES` hay una adquisición material y existe financiación de capital acumulada (preferentes/participaciones) levantada antes del trimestre, se añade la nota: «la adquisición se financió en parte con recursos levantados antes del trimestre (emisión de preferentes, venta de participaciones y efectivo restringido/escrow de trimestres anteriores); el 10-Q solo publica el estado de flujos acumulado, por lo que la suma de los últimos 3 meses no puede cerrar exactamente».
- **Reglas**: documentada en `knowledge/general.md`, `knowledge/consumo-defensivo/sector.md` y `prompts/consumo-defensivo.md`.
- **Verificación**: KDP regenerado como **#133**; el 3M incluye la nota *3 y el YTD sigue en 463 ("Más o menos cuadra").

## 13:41 — Resumen: Fila de efectivo restringido y deuda asumida también en el trimestre (KDP cuadra en 3M)

El 3M de KDP seguía sin cuadrar aunque la nota lo explicaba. Se investigó el XBRL: en Q1 KDP levantó 17.672M y los aparcó como efectivo restringido/escrow; en Q2 liberó 17.782M (17.818M → 36M) para pagar la compra.

### Resultado

- **Nueva fila `Efectivo restringido`** (mismo signo que Caja: + si se libera, − si se consigna): se calcula con el efectivo restringido del balance (nuevo concepto XBRL `restrictedCash` y campos de extracción `restrictedCash`, `restrictedCashPreviousQuarter`, `restrictedCashBeginningOfYear`), fila si la variación es >= 50M, con su nota propia.
- **Deuda asumida en 3M**: si la adquisición del trimestre es la misma que la del acumulado (se cerró en el trimestre), la deuda asumida no-cash se imputa también al horizonte de 3 meses. La nota se adapta cuando el reparto trimestral no da un flujo de deuda con caja positivo.
- **Reglas**: documentadas en `knowledge/general.md`, `knowledge/consumo-defensivo/sector.md` y `prompts/consumo-defensivo.md`. La nota estática de "financiación previa" queda como respaldo para cuando no se dispone del efectivo restringido.
- **Verificación**: KDP regenerado como **#136**. 3M: Libre 402 − Adq 16.615 + Efectivo restringido 17.782 + Deuda 4.273 − Deuda asumida 4.819 − Caja 619 = **404** ("Más o menos cuadra"); YTD sigue en **463** ("Más o menos cuadra"). Comprobación automática de KDP #136, TAP #132, BF.B #120, CELH #121, GIS #123 y MNST #124: todos OK.

## 13:48 — Resumen: Revertida la suspensión de la normalización de capital circulante

Se había suspendido la normalización de WC cuando el periodo incluía una adquisición material (para no añadir al FCF el consumo de circulante real). El usuario aclaró que el método anterior era el correcto: con su fórmula `(AP − Inv − AR) × (inflación + volumen)` y el ajuste `Cash Flow ajustado = Cash Flow normal − desviación`, el efecto caja está bien expresado (signo incluido) y la normalización se debe mantener también con adquisiciones.

### Resultado

- **Código revertido** (`analystAgent.js`): se eliminó la excepción por adquisición material en el bloque EDGAR de Q2+, en `buildWorkingCapitalDataFallback` y en el bloque anual; los escenarios y notas vuelven a mostrar la normalización de WC de siempre.
- **Reglas revertidas**: se retiraron los párrafos de excepción de `knowledge/general.md`, `knowledge/consumo-defensivo/sector.md` y `prompts/consumo-defensivo.md`.
- **Verificación**: `node --check`; KDP regenerado como **#138** con el comportamiento original (3M: 895 → 938,1 y Libre 402 → 445,1; YTD: 1.176 → 1.508,9 y Libre 255 → 587,9) y el bloque de capital igual (404 y 463). No se llegó a guardar ninguna regeneración de TAP (proceso abortado antes de guardar).

## 13:48 — Resumen: Blindaje de la API de resultados (10-Q / 10-K) — menos errores y más seguridad

Se solicitó buscar, mejorar y blindar la API de los resultados anuales y trimestrales para que tenga los menos errores posibles.

### Resultado

Se han aplicado mejoras de robustez y seguridad a toda la capa de API del análisis de resultados (subida, histórico, valoraciones, reportes de error, descargas y regeneración admin):

- **`src/middleware/errorHandler.js`**: ya no filtra mensajes internos en errores 5xx (mensaje genérico); gestiona centralmente `MulterError`, JSON malformado (400) y payloads demasiado grandes (413).
- **`src/services/aiQuota.service.js`**: nueva `reserveAiQuota()` que reserva el cupo diario de forma atómica (inserta y cuenta después); elimina la race condition que permitía a peticiones paralelas del mismo usuario superar el límite diario. Usada en `/api/upload` y en `/api/screener/.../analyze`.
- **`src/api/routes/analysis.routes.js`**:
  - IDs estrictos con `parseIdParam()` (rechaza `1e3`, `12abc`, negativos, 0...).
  - Subida: validación de firma real de PDF (`%PDF-`), límite `files: 2`, middleware propio para errores de multer (antes el `LIMIT_FILE_SIZE` se escapaba al handler global como 500).
  - Filtros de `/api/analyses`: fechas validadas contra `AAAA-MM-DD` (antes una fecha inválido provocaba un 500 de PostgreSQL), `limit` saneado.
  - `sendSourceError` escapa HTML (anti-XSS) y se comprueba que el filing encontrado tenga `documentUrl` antes de redirigir (evita redirect a `/undefined`).
  - Valoraciones: solo sobre análisis visibles (público o propio), rate limit 20/10 min, rating redondeado y acotado 1–5, IP normalizada (toma la primera de X-Forwarded-For).
  - Reportes de error de análisis: solo sobre análisis visibles, rate limit 10/h, categorías en lista blanca (`incorrect_numbers`, `wrong_period`, `missing_data`, `wrong_classification`, `bad_formatting`, `other`) e imágenes validadas (solo `data:image/` o `https://`, máx. 5, máx. ~5 MB por imagen).
  - Descargas `/api/reports/:file`: rate limit 60/min y nombre personalizado limitado a 120 caracteres.
- **`src/api/routes/adminReports.routes.js`**: IDs estrictos, estados de incidencia validados en ruta (400 claro en vez de 404 confuso), categorías en lista blanca, email validado con formato, imágenes validadas y rate limit en `POST /reports/general` (10/30 min).
- **`src/api/routes/reports.routes.js`**: eliminada la lógica JWT duplicada (usa `resolveUser` del middleware), misma validación de categorías/email/imágenes y rate limit.
- **`src/utils/validate.js`**: nuevos helpers compartidos `parseIdParam`, `pickCategory` y `sanitizeReportImages`.

Verificado con el servidor arrancado: health OK, 401 limpio sin sesión, 400 en IDs/fechas/JSON inválidos, rating acotado, categoría e imágenes/email normalizados en BD. Los datos de prueba se han eliminado.

## 14:05 — Resumen: Blindaje de los errores durante el análisis de resultados (pipeline IA)

Se solicitó revisar también los errores que ocurren mientras se analiza un informe (pipeline de IA).

### Resultado

Los fallos del pipeline ya no devuelven 500 genéricos ni se enmascaran entre sí; cada tipo de error tiene su mensaje claro en español y su código:

- **`src/services/ai/modelProvider.js`**: nueva clase `AiProviderError` (mensaje de usuario + `code` + `status`). `chatJson` además valida que la respuesta sea un objeto JSON (rechaza null/arrays) y reintenta (2 intentos).
- **`src/services/ai/providers/deepseek.provider.js`** y **`opencode-go.provider.js`**: errores clasificados — `AI_CONFIG_ERROR` (500, falta/rechazo de API key), `AI_TIMEOUT` (504), `AI_RATE_LIMIT` (503 saturada), `AI_UNAVAILABLE` (503 red/5xx), `AI_BAD_RESPONSE` / `AI_EMPTY_RESPONSE` (502). El detalle técnico (API error + respuesta del proveedor) se guarda en `error.detail` para el log, no para el usuario.
- **Agentes (`originAgent`, `sectorAgent`, `analystAgent`)**: si el fallo es del proveedor de IA se propaga tal cual (con su mensaje y estado); solo los fallos de estructura del modelo se enmascaran como `INVALID_MODEL_RESPONSE`.
- **Rutas (`/api/upload`, `/api/screener/.../analyze`, `/admin/reports/ai-analysis/:id/regenerate`)**: mapean `AiProviderError` respondiendo su propio estado HTTP con el mensaje amigable.
- **`src/services/pdf.service.js`**: errores claros por tipo — `PDF_PROTECTED` (contraseña), `PDF_CORRUPT` (dañado/no válido), `PDF_READ_FAILED` (otro); el fallo de `destroy()` ya no puede ocultar el error real; buffer vacío → `EMPTY_DOCUMENT`.
- **`src/services/analysis.service.js`**: PDF sin texto (escaneo/imágenes) se detecta antes de gastar IA con `PDF_NOT_READABLE` y mensaje que sugiere el PDF oficial de SEC EDGAR; `logAnalysis` envuelto en `safeLogAnalysis` para que un fallo de log (BD o fichero) no rompa un análisis correcto ni enmascare un error; fallo de generación del PDF del informe → `REPORT_PDF_FAILED` (422) en vez de 500 opaco.

Verificado: pipeline end-to-end OK con mock (agentes + PDF + guardado), 422 `PDF_CORRUPT` al subir un PDF truñado, cupo reembolsado tras el fallo (0 usos), 400 claro con campo multipart inesperado y clasificación de `AI_CONFIG_ERROR` sin API key. Datos de prueba eliminados.
