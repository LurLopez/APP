# 2026-09-10

## 21:16 — Resumen: Tabla de datos financieros se adapta al ancho con límite máximo

Se solicitó que, cuando la tabla de datos financieros no ocupe toda la pantalla, se adapte a la anchura disponible con un límite máximo, en lugar de quedarse estrecha (comportamiento anterior de `width: max-content`).

### Resultado

- En `public/styles.css` (`.screener-block table`) se cambió `width: max-content; min-width: 100%` por `width: 100%; min-width: fit-content; max-width: 1700px; margin: 0 auto`: la tabla ahora llena la anchura del contenedor (igualando la pantalla) y, si hay muchas columnas, conserva el scroll horizontal; en pantallas muy anchas no crece más allá de 1700px y queda centrada.
- Se ajustó la regla posterior de pantallas estrechas (`.screener-block table { min-width: 0 }`) a `min-width: fit-content` para no romper el scroll horizontal con muchas columnas.

## 21:35 — Resumen: Ajuste del reparto de columnas de la tabla de datos financieros

Se reportó que la tabla no tomaba toda la anchura (efecto del `max-width: 1700px` + centrado de la iteración anterior) y que, con pocas columnas, quedaba un hueco vacío junto a la columna fija (el navegador acumulaba el sobrante en la primera columna de datos con layout automático).

### Resultado

- En `public/styles.css` (`.screener-block table`) se eliminó `max-width: 1700px` y `margin: 0 auto` (el límite global ya lo pone `.content-wrap`) y se añadió `table-layout: fixed`: con pocas columnas la tabla llena toda la anchura repartiendo el espacio por igual entre todas las columnas de datos (sin huecos); con muchas columnas, `min-width: fit-content` mantiene el scroll horizontal.
- La regla de `.screener-block table.filings-table` conserva su propio `table-layout: auto` por especificidad, sin conflicto.

## 21:55 — Resumen: Corregido reparto real del ancho (colgroup) y caché de estáticos

El usuario reportó que la tabla seguía exactamente igual. Dos causas: (1) el CSS estaba cacheado con `?v=90`, por lo que los cambios no se aplicaban, y (2) con `table-layout: fixed` el sobrante se reparte proporcionalmente también a la columna fija, que sigue creciendo e ignora su `max-width`.

### Resultado

- En `public/empresa.js` (`renderStatementTable`) se inyecta ahora un `<colgroup>` con la primera columna fija a 330px y las columnas de datos sin ancho (`<col>`): con `table-layout: fixed` el ancho sobrante se reparte por igual solo entre las columnas de datos, de modo que la tabla llena toda la anchura sin huecos junto a la columna fija.
- Se subieron las versiones de caché: `styles.css?v=91` y `empresa.js?v=57` en `empresa.html` e `index.html`.

## 22:10 — Resumen: Causa raíz del reparto de columnas (width: 96px) y verificación con Chrome headless

Se reprodujo el problema renderizando una réplica de la tabla con el CSS real en Chrome headless: con `table-layout: fixed`, Chrome reparte el ancho sobrante proporcionalmente a los anchos especificados de la primera fila; como las celdas de datos tenían `width: 96px`, la columna fija (330px) absorbía la mayor parte del sobrante (~730px), produciendo el hueco vacío junto a la columna fija.

### Resultado

- En `public/styles.css` (`.screener-block th:not(.sticky-col), td:not(.sticky-col)`) se eliminó `width: 96px` (se conserva `min-width: 90px`): las columnas de datos quedan sin ancho especificado, el `<colgroup>` fija la columna fija en 330px y el sobrante se reparte por igual entre las columnas de datos.
- Verificado con capturas headless: con 4 columnas la tabla llena toda la anchura con columnas equidistantes; con muchas columnas, `min-width: fit-content` mantiene el scroll horizontal.

## 22:30 — Resumen: Tabla centrada con tope de ancho por columna

Con pocas columnas (p. ej. 1 año) la tabla se estiraba a todo el ancho y quedaba desproporcionada. Se solicitó que se centrara.

### Resultado

- En `public/empresa.js` (`renderStatementTable`) se establecen anchos inline según el número de columnas: `min-width = 330 + n·96` (garantiza scroll con muchas columnas) y `max-width = 330 + n·320` (tope de 320px por columna de datos).
- En `public/styles.css` (`.screener-block table`) se añadió `max-width: fit-content; margin: 0 auto`: cuando la tabla no llena el contenedor se centra; cuando hay muchas columnas, el `min-width` inline gana y aparece el scroll horizontal.
- Verificado con Chrome headless: 1 columna → tabla compacta centrada; 4 columnas → llena el ancho equidistante.
- Caché subida a `styles.css?v=92` y `empresa.js?v=58`.

## 21:45 — Recompras: tabla SEC a 5 años con XBRL, badges del último año y caducidad siempre presente

Cuatro ajustes sobre la sección "1: Recompras": 1) la tabla del extracto SEC seguía mostrando solo los 3 años del 10-K; 2) "Impacto BPA" y "Evolución acciones" debían referirse al último año, no a los últimos dos; 3) "Autorización restante" debe incluir siempre la fecha de caducidad.

### Resultado

1. **Tabla a 5 ejercicios (`edgar.service.js` + `analystAgent.js`)**: nuevo `getAnnualBuybackCosts(ticker)` que consulta la API XBRL de la SEC (`us-gaap/PaymentsForRepurchaseOfCommonStock`, CIK resuelto con `getCompanyByTicker`, caché 6 h) y devuelve el coste anual de recompras por ejercicio. Nuevo `extendRepurchaseTableYears()`: amplía los headers de la tabla hasta los últimos 5 años, completando los que el 10-K no desglosa con el coste XBRL ("$51.5" en 2022, "$0.0" en 2021 para TAP) y "n/d" en el recuento de acciones; añade nota de fuente en el summary. Se aplica tras la extracción, tras el analista y tras los valores por defecto (idempotente). Cabecera de la tabla SEC en PDF con altura dinámica (con 5 columnas "December 31, AAAA" se envuelve a 2 líneas).
2. **Badges del último año (prompt del analista)**: "shareCountEvolution" ahora es del último año (de 208,9M en dic-2024 a 199,1M en dic-2025, -4,7 %) y "bpaImpact" el impacto del último año (+4,9 %), no el acumulado de dos años.
3. **Caducidad obligatoria**: "authorizationExpiry" siempre presente: fecha si consta, "Sin fecha de caducidad" si el programa es abierto, "Caducidad no indicada en el 10-K" si no consta. En TAP sale "Caducidad no indicada en el 10-K".
4. **Respaldos en normalización**: si el analista omite `authorizationRemaining`/`authorizationExpiry`/`sharesHistory`/`secSnippet`, se rellenan desde `annualDetails.repurchases` (nombres de campo correctos del esquema de extracción).
5. **Verificación**: TAP 10-K regenerado de principio a fin (id 55, pdf `88d1bbed...`): tabla 2025→2021 con coste XBRL, badges de último año, caducidad presente, 0 solapamientos reales en el PDF y regresión de 46 informes en 4 formatos sin fallos.

## 21:55 — Recompras: sin badge de Vigencia si el 10-K no indica caducidad y tabla SEC solo con años completos

Dos ajustes finales: 1) la badge "Vigencia" no debe mostrarse salvo que el 10-K indique la caducidad con certeza (si no consta, nada); 2) la tabla del extracto SEC no debe mostrar años sin datos (se retira la ampliación a 5 ejercicios con "n/d").

### Resultado

1. **Vigencia solo si consta (analystAgent.js + renderizadores)**: el prompt del analista vuelve a exigir `authorizationExpiry` SOLO si el 10-K lo indica de forma expresa (fecha concreta, o "Sin fecha de caducidad" únicamente si el 10-K lo afirma); si no consta, se omite el campo. Filtro defensivo anti-placeholder ("no indicada", "not disclosed", etc.) añadido en la normalización y en los tres renderizadores (HTML/DOCX/ODT, PDF y web). El informe TAP actual (id 55) se parcheó en BD (expiry a null).
2. **Tabla SEC solo con años completos**: eliminada la ampliación a 5 ejercicios con XBRL y las columnas "n/d" (`extendRepurchaseTableYears` y `getAnnualBuybackCosts` eliminados); la tabla vuelve a mostrar únicamente los ejercicios que el 10-K desglosa (2023-2025 en TAP), con su fila "Average price paid". El informe guardado se actualizó en BD y se regeneraron los 4 formatos.
3. Corregido también un bloque duplicado que había quedado en `public/analisis.js` durante la edición anterior (error de sintaxis).
4. **Verificación**: 0 solapamientos en el PDF, badge de Vigencia ausente (no consta en el 10-K), tabla de 3 columnas, regresión de 46 informes sin fallos y servidor activo.

## 22:29 — Resumen: Puntos condicionales definidos para la Parte II del análisis anual

Se solicitó definir qué puntos pueden aparecer en la Parte II (indagación a fondo) de los informes anuales más allá de los canónicos, y se propuso añadir el cambio de CEO.

### Resultado

- Se actualizó `src/agents/knowledge/anual/general.md`: nueva sección "6. Puntos Condicionales Adicionales (según materialidad)" con los puntos que pueden aparecer según materialidad (≥ 50 M$ o relevancia estratégica), siempre entre Adquisiciones y Watchlist: Dividendos, Desinversiones (regla 6 portada), Impairments, Reestructuraciones, Litigios/seguridad de producto, Impuestos, Pensiones/OPEB, Concentración de clientes y **Cambio de CEO / dirección** (propuesta del usuario, con análisis obligatorio de quién entra/sale, contexto, continuidad estratégica, costes asociados y juicio fundamentado en hechos del filing).
- El árbol de estructura de la Parte I del mismo documento refleja ahora estos puntos condicionales con la marca "(+)".
- Pendiente: implementar estos puntos en el `analystAgent` (esquema de extracción, normalización y renderizado).

## 22:55 — Resumen: SEO + GEO completo (meta etiquetas, JSON-LD, robots/sitemap, SSR ligero)

Se solicitó un plan y su implementación para posicionar la web en buscadores (SEO) y en motores generativos (GEO).

### Resultado

- **Fase 0**: `PUBLIC_SITE_URL` en `.env`/`.env.example` + `config.siteUrl` (dominio de producción pendiente de decidir; tokens `{{SITE_URL}}` sustituidos al servir).
- **Fase 1**: head completo en `index.html` y `empresa.html`: title/description con keywords, robots, canonical, Open Graph completo, Twitter Card, favicon SVG+PNG+apple-touch-icon y og:image 1200×630 generada (`public/og-cifra.png`, fuente en `scratch/seo/og-cifra.svg`).
- **Fase 2**: `empresa.js` actualiza en el navegador title/description/canonical/OG/Twitter al cargar una empresa (`updateCompanySeoMeta`).
- **Fase 3**: JSON-LD en landing (`WebSite`, `Organization`, `FAQPage` con 5 preguntas —claves para GEO—) y por empresa (`Corporation` + `BreadcrumbList`) inyectado en servidor.
- **Fase 4**: nuevo `src/middleware/seo.middleware.js` + `src/services/seo.service.js` que sirven el HTML con SSR ligero: metadatos reales por empresa vía `getCompanySeoProfile` (EDGAR, caché 6 h), bloque `<noscript>` con datos clave para rastreadores sin JS, `noindex` (meta + `X-Robots-Tag`) en rutas privadas, 301 de barra final. Nuevos ficheros: `public/robots.txt` (con reglas para bots de IA), `GET /sitemap.xml` dinámico (empresas de BD: analyses+filings, caché 30 min) y `public/llms.txt`.
- **Fase 5**: gzip (`compression`) y caché de estáticos (CSS/JS 7 días, imágenes 30 días).
- **Fase 6**: verificado en vivo con curl simulando Googlebot: `/empresa/KO` inyecta "Coca Cola Co" + NYSE + último 10-Q con JSON-LD válido; `/cartera` noindex; sitemap bien formado (4 empresas en BD); 301 y 404 correctos.
- Documentación: `documentacion/seo.md` (resumen, dónde tocar cada cosa y checklist de alta en Search Console/Bing cuando exista el dominio).

## 23:15 — Resumen: Outlook extraído de la presentación/comunicado de resultados (8-K)

El usuario detectó que el outlook no aparece en los análisis anuales: en casi todos los casos el guidance se publica en la presentación o comunicado de resultados del 8-K, no en el 10-K. Se pidió que la IA reciba las dos fuentes (10-Q/10-K + presentaciones) y las combine, y que en la pestaña de informes trimestrales aparezcan los dos documentos.

### Resultado

1. **Detección automática del 8-K de resultados (`src/services/edgar.service.js`)**: `findFilingPresentations` localiza el 8-K con item 2.02 (Results of Operations) cuyo `reportDate` cae en la ventana [periodEnd, +65 días] y extrae de su índice los exhibits de resultados (PDFs de presentación y HTML ex-99/earnings). Nuevos `getFilingsWithPresentations`, `getFilingPresentations` y `getPresentationBuffers`. Filtros de nombres (presentation/press release/ex-99) y límite de 2 docs por filing.
2. **Analista con documento complementario (`src/agents/analystAgent.js` + `analysis.service.js`)**: `analyzeText/analyzePdf` aceptan `presentationText`; `buildPresentationText` extrae el texto del PDF/HTML (cabeza+cola, máx. 14k por doc). `buildAnalysisText` concatena el informe principal (hasta 80k) con la sección complementaria de presentación (25k). El prompt de extracción indica que la presentación es LA FUENTE PRINCIPAL del `annualDetails.outlook` cuando el 10-K no lo incluye, con prioridad del 10-K para los estados financieros.
3. **Rutas**: `/screener/.../filings` devuelve la lista plana con filas `kind: 'presentation'` intercaladas (badge PRESENTACIÓN / RESULTADOS 8-K con enlace Abrir/Descargar al documento SEC). `/screener/.../filings/:acc/analyze` descarga el 8-K de resultados y lo pasa al analista. `/upload` acepta un segundo fichero `presentation` opcional.
4. **Frontend**: tabla de informes con filas de presentación y contador "X informes · Y presentaciones"; selector "Añadir presentación de resultados (opcional)" en la subida manual; caché subida (`styles.css?v=93`, `analisis.js?v=17`, `empresa.js?v=59`, `novedades.js?v=2`). Novedad v1.5.0 añadida.
5. **Verificación**: TAP FY2025 (sin outlook antes) ahora extrae la guía 2026 completa (ventas, EBT, BPA, FCF, CAPEX, intereses + tabla secSnippet). KHC FY2025 (id 57) y KHC Q1 2026 (id 58) regenerados con la presentación. Renderizado headless confirmado (16 presentaciones para KHC). Regla de conocimiento anual actualizada (`anual/general.md`).

> Nota: los análisis anuales ya guardados siguen sirviéndose en caché; para ver el nuevo outlook hay que pulsar "Regenerar" (admin).

## 23:10 — Resumen: SEO/GEO nivel 9 — contenido real para bots, guías y análisis públicos

Se solicitó subir la puntuación SEO/GEO de 5/10 a 9/10 añadiendo lo que faltaba: contenido real indexable.

### Resultado

- **SSR ampliado en fichas de empresa** (`getCompanyBotContent` en `src/services/seo.service.js`): bloque visible para bots con h1, ficha (sector, industria, bolsa, país), tabla de resultados anuales de los últimos 5 ejercicios (ventas, beneficio neto, FCF, EPS desde `getCompanyResults`, caché 12 h), últimos 8 filings 10-Q/10-K enlazados directamente al documento en sec.gov (`getCompanySeoProfile` ampliado) y enlaces a análisis públicos y guías. El bloque se oculta con JS (`html.js #seo-contenido{display:none}`) porque la app ya lo muestra interactivo.
- **Contenido real en portada y /empresa**: h1, cómo funciona, FAQ completa en HTML, guías y empresas destacadas (`getHomeBotContent` / `getCompaniesBotContent`).
- **Páginas de análisis públicos** `/informe/<id>`: para los análisis del sistema (`user_id IS NULL`, misma regla que la API), renderizado completo server-side desde `analyses.report`: ventas actual/anterior/variación, flujo de caja normal y ajustado con notas, asignación de capital, conclusiones (deuda, perspectivas, recompras, adquisiciones, watchlist) y enlace al PDF; JSON-LD `Article` + `BreadcrumbList`; caché 10 min.
- **6 guías educativas + hub** en `/guias` (`src/content/guias/*.html` + `public/guia.css`): 10-Q, 10-K, diferencias, FCF, asignación de capital y análisis de consumo defensivo; cada una con meta propios, JSON-LD `Article`+`FAQPage` y interlinking.
- **Sitemap ampliado**: 17 URLs (portada, /empresa, hub + 6 guías, empresas con datos, informes públicos). **llms.txt** actualizado con guías y análisis públicos.
- **Correcciones durante la verificación**: falta de import de `getPublicReportHtml` (500 → fix), títulos de conclusiones normalizados en español (quitada la numeración del PDF), eliminación de helper redundante en `serveHtml`.
- Verificado con curl + JSON.parse de todos los JSON-LD + Chrome headless (guía e informe renderizan con el estilo de la marca); API y flujo de análisis intactos.

## 23:55 — Resumen: Presentación como acción de fila (ajuste de UI tras feedback)

El usuario aclaró (adjuntando el deck de Q2 2026 de Molson Coors) que la presentación debe aparecer como una acción más en la fila del 10-Q/10-K (junto a Descargar / Analizar con IA), no como filas separadas en la lista.

### Resultado

- Se revirtió el aplanado de filas de presentación en `/screener/.../filings`: la lista vuelve a contener solo 10-Q/10-K (40 filas en TAP), cada una con su array `presentations` adjunto.
- `renderFilingsTable` (empresa.js) añade el botón "Presentación" en las acciones de la fila cuando hay documento de resultados del 8-K (preferencia docType 'presentation', fallback release), enlazando al documento SEC original. CSS `.filing-action-presentation`; se eliminaron `.filing-badge-presentation` y `.filing-row-presentation`.
- Verificado: endpoint devuelve 40 filas con `presentations` anidado; render headless muestra 16 filas con botón "Presentación" y contador "40 informes".
- Observación: el deck de inversores real (ej. `Molson-Coors-Q2-2026-Earnings-Deck-FINAL.pdf`) está alojado en la web de IR (Q4 Inc), no en EDGAR; el botón enlaza de momento los documentos del 8-K de resultados. Integración con la web de IR queda como opción pendiente.

## 23:23 — Resumen: Mejora de SEO/GEO (redondeo de la implementación)

Se solicitó mejorar el SEO/GEO actual. Se revisó la implementación existente y se cerraron los huecos que quedaban.

### Resultado

- **`/empresa` ahora sirve contenido real para bots** (antes la función `getCompaniesBotContent` existía pero no estaba cableada en el middleware): bloque con h1, listado de empresas destacadas con enlace a su ficha y guías, más JSON-LD `ItemList` con cada empresa (`Corporation` + `tickerSymbol`). La portada `/` también recibe `ItemList`.
- **JSON-LD de empresa enriquecido** (`buildCompanyJsonLd`): `sector`, `industry`, `countryOfOrigin` y `sameAs` enlazando al perfil público de la empresa en SEC EDGAR (CIK).
- **Hreflang auto-referente `es`** en todas las páginas: inyectado en `serveHtml` (canonical dinámico) y `serveStandalone` (guías e informes), además de en las plantillas.
- **`SearchAction`** en el JSON-LD `WebSite` de portada y `/empresa` apuntando a `/empresa?ticker={search_term_string}`.
- **Guías**: JSON-LD `Article` ampliado con `datePublished`, `dateModified`, `keywords` y `articleSection`; `og:image:alt` en hub, las 6 guías y las páginas de informe.
- **Sitemap**: las guías ahora llevan `lastmod` real (mtime del fichero).
- **llms.txt dinámico**: ahora añade una sección generada `## Empresas con análisis disponibles` con enlaces directos a las fichas.
- **PWA manifest** `public/manifest.webmanifest` (iconos con `logo-cifra.png`, theme/background, standalone) enlazado desde `index.html` y `empresa.html`.
- Verificado con el servidor en marcha: `/empresa` (contenido + hreflang + ItemList), `/empresa/KO` (sameAs/sector EDGAR), `/` (ItemList + SearchAction + manifest), guías con hreflang, `/cartera` noindex intacto, sitemap con lastmod de guías, manifest servido con `application/manifest+json`. JSON-LD de guías validado con `JSON.parse`.

## 00:20 — Resumen: Deck de resultados real desde la web de inversores (IR)

El usuario adjuntó el deck real de Molson Coors (Q2 2026) y confirmó que el botón "Presentación" debe abrir la presentación de la web de inversores, no solo el comunicado del 8-K.

### Resultado

- **Descubrimiento del sitio IR** (`market.service.js`): `getCompanyIrSites` obtiene `summaryProfile` de Yahoo (crumb/cookie existentes) y devuelve candidatos: `irWebsite` de Yahoo + `ir.<dominio>` / `investors.<dominio>` derivados de `website` (con `www.` limpiado).
- **Búsqueda del deck** (`edgar.service.js`): sondeo de candidatos vivos (200/403 = vivo), y lectura de la página de resultados trimestrales del IR con **Chrome headless** (`--disable-blink-features=AutomationControlled`, `--virtual-time-budget=60000`) porque los sitios Q4 Inc están detrás de Cloudflare/Akamai y bloquean fetch/curl (403). Se parsean los enlaces PDF de presentaciones, se extraen claves de trimestre (q22026, q42025, fy2025...) y se construye un mapa trimestre→deck con caché de 12 h y deduplicación de promesas por ticker (evita N procesos Chrome concurrentes).
- **Integración**: `findFilingPresentations` antepone el deck IR (docType 'presentation', source 'ir') a los documentos del 8-K; el botón "Presentación" del listado enlaza directamente el PDF del deck (verificado: `.../q2/Molson-Coors-Q2-2026-Earnings-Deck-FINAL.pdf`); `getPresentationBuffers` descarga también el deck (UA de navegador) y la IA lo recibe como documento complementario junto al comunicado del 8-K.
- **Bug corregido en el camino**: las regex de extracción de trimestre no tenían flag `/g` → bucle infinito de `RegExp.exec` que colgaba el proceso.
- **Verificado**: TAP Q1/Q2/Q3 2025, Q1/Q2 2026 y FY2025 (→ deck Q4 2025) con su deck real + comunicado SEC; endpoint 200 con 40 filas y render headless con 16 botones "Presentación" apuntando a los PDFs de q4cdn; el texto del deck extraído contiene la sección "RESULTS & OUTLOOK". Fallback intacto: si el sitio IR no es localizable/parseable, se mantienen solo los documentos del 8-K.

## 23:52 — Resumen: SEO/GEO técnico, privacidad de contenido y señales de fuente

Se solicitó mejorar el SEO y el GEO de la web, partiendo de la capa SSR, sitemap, robots.txt, llms.txt y datos estructurados existentes.

### Resultado

- **URLs canónicas y rutas**: el entorno de producción usa `https://cifra.app` como fallback seguro cuando no se define `PUBLIC_SITE_URL`; `/index.html` y `/empresa.html` redirigen con 301 a sus rutas canónicas; `/empresa?ticker=X` redirige a `/empresa/X`; los tickers inexistentes devuelven 404 y los fallos temporales de EDGAR devuelven 503.
- **Privacidad e indexación**: se añadió `analyses.is_public`, se migraron como públicos los análisis SEC anónimos existentes y las subidas manuales/análisis personales quedan fuera de `/informe`, `sitemap.xml`, listados SSR y API pública. La regeneración administrativa conserva la visibilidad pública del informe original.
- **Rastreo**: las reglas específicas para GPTBot, OAI-SearchBot, PerplexityBot, ClaudeBot y Google-Extended bloquean también todas las rutas privadas; los HTML exportados de `/api/reports` llevan `X-Robots-Tag: noindex, nofollow, noarchive`.
- **Sitemap y GEO**: el sitemap ya no limita empresas a 500 ni informes a 50; `llms.txt` incluye criterios de interpretación, límites, guía de citación, trazabilidad a SEC y enlaces a todas las fichas disponibles.
- **Datos estructurados**: las fichas incorporan `WebPage` enlazada con `Corporation` y breadcrumb; la página de empresas declara `Organization` y `CollectionPage`; los informes incluyen `isBasedOn`/`citation` hacia el filing SEC cuando existe.
- **Contenido confiable**: el bloque SSR se sirve como `<details>` accesible, sin ocultarlo mediante JavaScript; las seis guías muestran las preguntas frecuentes que ya declaraban en `FAQPage`; los informes públicos muestran su fuente primaria de SEC EDGAR.
- **Verificación**: `npm run db:migrate`, comprobación de sintaxis de los módulos modificados, parseo de JSON-LD, pruebas HTTP de 301/404/noindex/sitemap/llms y `git diff --check` completados correctamente.

## 00:55 — Resumen: Deck de IR también para EQ Systems (KHC) y parser genérico

El usuario reportó que con TAP el deck aparece pero con KHC el botón seguía apuntando al comunicado SEC.

### Resultado

- **Causa**: el sitio IR de KHC (`ir.kraftheinzcompany.com`) usa la plataforma **EQ Systems**, no Q4 Inc: rutas relativas `/media/document/{uuid}/assets/...pdf?disposition=inline`, secciones por año (`documents__head`) y el deck se llama "**Business Update**" (ej. `2Q26_KHC_Business_Update_vFinal.pdf`). El parser anterior solo aceptaba hrefs terminados en `.pdf` y nombres presentation/deck/slides, y el `continue` del año en `parseIrDocumentLinks` se saltaba el contenido de cada sección.
- **Parser genérico** (`parseIrDocumentLinks` + `extractQuarterKeys`): acepta `.pdf?`/query, extrae el año de las secciones `documents__head`, soporta "Q2 2026", "2Q26", "1Q25", "FY 2025" y usa el contexto de año cuando el texto del enlace es solo "Q2". Filtro `isDeckDocument` (business update/presentation/deck/slides) que excluye releases (ER), transcripts y pre-recorded. Nombre legible desde el nombre del archivo cuando el enlace no tiene texto descriptivo.
- **Verificado**: KHC Q1/Q2 2026, Q3/Q2/Q1 2025 y FY2025 (→ 4Q25 Business Update) con su deck de IR; buffer PDF descargable y texto extraído; TAP sin regresión; render headless con 16 botones apuntando a `ir.kraftheinzcompany.com/media/document/...`.

## 01:40 — Resumen: Cobertura del deck de IR para más plataformas (Q4 API, EQ, webs custom)

El usuario reportó que TAP/KHC funcionaban pero CAG no, y pidió que la presentación aparezca en todas las empresas.

### Resultado

1. **API pública de Q4 Inc** (`/feed/Event.svc/GetEventList?year=YYYY&languageId=1`): nuevo `getQ4EventDeckMap` que obtiene eventos y adjuntos (Title/Extension/Url) sin Chrome y construye el mapa trimestre→deck con los PDFs de presentación/slides de q4cdn. Cubre TAP y GIS (15/30 filas) de forma fiable y rápida.
2. **Sitios IR en la web corporativa** (CAG): si los subdominios IR fallan, se sondea la web principal, se parsea su HTML (fetch plano) buscando la sección de inversores y se crawlea con Chrome (máx. 5 cargas) hub → eventos/resultados. Se aceptan enlaces sin extensión (CAG: `/files/events/.../Q4FY26-Earnings-Slides`).
3. **Detección de trimestres ampliada**: "Q2 2026", "2Q26", "F26-Q3", "Q3 Fiscal 2026", "Third-quarter Fiscal 2026", "FY26"/"Full Year 2026". Exige deck + trimestre para considerar una página válida (evita enlaces de navegación tipo "Events & Presentations").
4. **Clasificación de candidatos**: `irWebsite` que coincide con la web principal se marca como `website`; se recorren hasta 3 candidatos vivos y hasta 2 en el crawl; el API Q4 se prueba también en orígenes descubiertos (arregla CL, que enlaza a `investor.colgatepalmolive.com`).
5. **Robustez**: `fetchIrPage` reintenta ante páginas de reto WAF y omite 404; `probeIrSiteAlive` acepta 429; caché de mapa vacío 5 min (evita fallos transitorios persistentes) y de candidatos IR vacíos 10 min con reintento de sesión Yahoo.
6. **Verificado**: TAP 16/40, KHC 9/40, GIS 15/30 con decks reales; CAG 5/40 verificado antes del rate-limit 429 autoinducido por las pruebas; KO y PEP no publican deck trimestral en su IR (el botón cae al comunicado del 8-K de la SEC, presente en 16 filas). Quedan como fallback universal los documentos del 8-K.
