## 11:45 — Resumen: Corregidos los errores de KDP (10-Q Q2 2026 y 10-K FY2025)

Se solicitó corregir los errores vistos en el análisis del Q2 de KDP (fila «Adquisiciones» ausente en el 3M, descuadre con «resto +21.784M») y la ausencia del calendario de vencimientos de deuda, sin romper los análisis ya verificados (CAG, TAP, PEP, HRL, KHC) ni el 10-K 2026 de PG.

### Resultado

- **Adquisición trimestral deducida**: en los 10-Q de Q2/Q3 el estado de flujos solo publica la columna acumulada, así que `deduceQuarterCashFlow`/`storePreviousQuarterCashFlow` (`analystRunSteps.js`) calculan adquisiciones del trimestre = acumulado actual − acumulado del trimestre previo (KDP Q2 = 16.615 − 0) y mandan sobre la IA y sobre el frame XBRL parcial de 3 meses (402M). Con ello reaparecen la fila `Adquisiciones` de JDE Peet's y la `Deuda asumida (no-cash)` en el 3M, y el cuadre queda en -17.432 con el escrow (+17.782) explicado bajo la tabla (resto +350M).
- **Tags EDGAR**: `PaymentsToAcquireProductiveAssets` (capex) eliminado de la lista de `acquisitions` (`statementConcepts.js`); el capex ya no infla adquisiciones (TAP Q1 tomaba 231,7M de capex y ahora la deducción da el 271 verificado; PEP deduce 14 < 50, sin fila, igual que antes).
- **Verificación de Asignación de Capital**: si los movimientos no monetarios no reducen el descuadre, el mensaje lo dice («no reducen el descuadre…») y no inventa un «resto»; la nota determinista de Deuda/Caja se garantiza antes de la deuda asumida y se limpian etiquetas duplicadas.
- **Calendario de vencimientos del 10-K**: `extractDebtFilingText` ya no cae en el índice de exhibiciones (filas «due 20XX» con fechas 8-K) y reconoce las tablas con columna «Maturity Date»; `buildMaturityScheduleFromFilingText` parsea las filas con fecha completa, cupón y doble columna y descarta narrativa, revólveres y comercial paper. KDP 10-K 688 genera 11 tramos (2026: 900 · 2027: 1.600 · 2028: 1.612 · 2029: 1.750 · 2030: 1.250; >2030: 6.952; tipo medio 4,27 %) y el gráfico del PDF.
- **Regeneración**: KDP 10-Q 687 y 10-K 688 (provider opencode). No-regresión determinista verificada: TAP 271, PEP 14, HRL/KHC sin adquisiciones, CAG 10-K con su tabla, PG 10-K 2026 con sus 16 tramos.
- **Tests**: 11 pruebas nuevas (`previousQuarterDebt.test.js`, `debtMaturityFallback.test.js`, `cashCapitalReconciliation.test.js`) y suite completa **206/206**. Traducciones EN de los nuevos textos y `_sources` regenerados.
- **Docs**: ficha de KDP en `documentacion/REVISION-ANALISIS.md` (empresa 6) con causas y verificación.

## 11:51 — Resumen: Acceso a la API de Search Console y diagnóstico real de tráfico en Google

Se solicitó revisar cómo van las visualizaciones de la web y si es normal el bajo tráfico, y comprobarlo directamente contra Search Console.

### Resultado

- **Acceso habilitado**: la cuenta de servicio `ga4-905@cifra-508021.iam.gserviceaccount.com` se añadió en Search Console con permiso Restringido y se habilitó la *Google Search Console API* en el proyecto `cifra-508021`. Con eso el servidor ya puede consultar consultas, páginas, países e indexación sin depender del panel web.
- **Script reutilizable**: `scripts/gsc-report.js` (mismo credential que GA4; `GSC_SERVICE_ACCOUNT_PATH | GA4_SERVICE_ACCOUNT_PATH`, `GSC_SITE` opcional, días por argumento). Uso: `node --env-file=.env scripts/gsc-report.js 28`. Sirve para local y VPS.
- **Datos 10–17 sep** (Google publica con 2-3 días de retraso): 3 clics, 102 impresiones, CTR 2,9 %, posición media 16,8. Los 3 clics son la portada desde Chile por búsqueda de marca; las impresiones crecen (7 → 27 → 34 → 34).
- **Indexación sana**: 4 URLs inspeccionadas (portada, ficha KO, guía 10-K, informe KO) → `PASS / Submitted and indexed`, robots `ALLOWED` y último crawl 12–14 sep. El `indexed=0` del sitemap (352 URLs enviadas) es el retraso conocido de esa métrica, no un problema real.
- **Primeras señales genéricas** (posición, 0 clics por volumen mínimo): fichas de empresa en top 10 (`/empresa/PM` 3,2 · `/empresa/SAM` 2,5 · `/empresa/COST` 5,3 · `/empresa/TAP` 8,7 · `/empresa/CL` 7,3) y guías (`que-es-un-informe-10-k` 8,2 · `diferencias-entre-10-k-y-10-q` 5,8). Pendiente de mejorar: `que-es-el-flujo-de-caja-libre` en posición 96,7 para «free cash flow»/«fcf yield».
- **Conclusión**: tráfico aún de marca y de volumen casi nulo, pero normal para 6 días; la web está indexada y ya asoma en genéricas. Palancas: contenidos y backlinks.
- **Pendiente**: alta en Bing Webmaster Tools.

## 11:59 — Resumen: Calendario de resultados (.ics) de las empresas del catálogo

Se solicitó un calendario de los próximos resultados de las empresas cubiertas para estar atento y publicar análisis en redes sociales.

### Resultado

- **Script nuevo** `scripts/earnings-calendar.js` (npm `calendar:earnings`): consulta Yahoo Finance (`calendarEvents`, vía `companyCalendar.service`) para las 28 empresas del catálogo benchmark de consumo defensivo, imprime los resultados y ex-dividendos ordenados por fecha y genera un `.ics` importable en Google Calendar/Outlook con eventos de todo el día, enlace a la ficha `/empresa/{ticker}` de Cifra y dos recordatorios (1 día antes y ~9 h antes).
- **Opciones**: `--tickers=KO,PEP`, `--days=200`, `--out=ruta.ics`, `--concurrency=4`. Salida por defecto: `scratch/calendario/resultados-cifra.ics` (24 resultados + 6 ex-dividendos en la primera generación).
- **Próximos hitos**: GIS 23-sep · COST 24-sep · CAG 30-sep · MKC 1-oct · STZ 6-oct · PEP 8-oct · KO 20-oct · PM 21-oct · PG 22-oct · MDLZ 27-oct · KHC 28-oct · MO 29-oct.
- **Uso previsto**: avisar de los días de resultados para preparar el análisis y publicarlo el mismo día en X/LinkedIn.

## 12:01 — Resumen: Listado de últimos filings (10-Q/10-K) del catálogo

Se preguntó por los últimos resultados ya publicados por las empresas cubiertas.

### Resultado

- **Script nuevo** `scripts/recent-filings.js` (npm `filings:recent`): consulta SEC EDGAR para las 28 empresas del benchmark (`getCompanyFilings`), filtra por fecha de presentación (`--days=90` por defecto, `--since=`, `--tickers=`, `--limit=`) y los imprime ordenados con enlace al documento original.
- **Datos (últimos 75 días, 9-jul → 28-ago)**: 22 presentaciones; las más recientes WMT (28-ago), DG/DLTR/HRL (27-ago), SJM (26-ago), EL 10-K (19-ago), CLX 10-K (7-ago), KVUE/TAP (6-ago), KHC (5-ago), ADM/PG 10-K (4-ago), TSN (3-ago), CHD/CL (31-jul), HSY/MO (30-jul), KO (29-jul), MDLZ (28-jul), PM (24-jul), CAG 10-K (15-jul) y PEP (9-jul). Sin presentaciones en el periodo: COST, GIS, STZ, KR, MKC, CPB.
- **Cruce con los análisis públicos de la app**: ya hay informe listo para compartir de KO, PEP, TAP, KHC, MDLZ, KDP, MNST, STZ y GIS; pendientes de analizar los filings recientes de PG (10-K FY2026), WMT, EL (10-K), CLX (10-K), MO, PM, CL, ADM, HSY, KVUE, TSN, CHD, DG, DLTR, SJM, HRL, CAG (10-K) y KR.

## 12:00 — Resumen: El veredicto del cuadre de capital se calcula sobre el resto tras los movimientos no monetarios

El usuario mostró el Q2 de KDP en producción: la tabla cuadraba en -17.378M, la verificación explicaba el escrow (+17.782M) y dejaba un resto de +404M, pero el veredicto seguía siendo «No cuadra: quedan -17.378M sin explicar». Resultaba contradictorio: el descuadre estaba prácticamente explicado y el resto entraba en el margen.

### Resultado

- **Veredicto corregido** (`analystCashCapitalProcessor.js`): con movimientos que no pasan por caja, el veredicto («Más o menos cuadra» / «No cuadra») se decide sobre `suma + movimientos no monetarios` comparado con el mismo margen (50M / 20 % del Libre / 10 % de los movimientos brutos). El texto de la rama del margen («dentro del margen razonable») usa ese mismo umbral en vez de 50M fijos.
- **KDP Q2 regenerado (699)**: «Más o menos cuadra… los siguientes no pasan por caja: efectivo restringido +17.782M. Con ellos, el resto sin explicar sería +350M, dentro del margen razonable» (con Libre 402 el resto es +404M y el veredicto es el mismo). El 10-K (700) mantiene los 11 tramos del calendario.
- **Caso que empeora** (movimientos no monetarios con el mismo signo que el descuadre): sigue mostrando «no reducen el descuadre… faltan movimientos de caja por mapear» y nunca afirma que lo explican.
- **Tests**: actualizados los dos casos afectados en `cashCapitalReconciliation.test.js`; suite **206/206**.
- **Docs**: ficha de KDP en `REVISION-ANALISIS.md` actualizada con el criterio del veredicto y los análisis 699/700.

## 12:15 — Resumen: El escrow que financia una adquisición se pinta como fila (KDP Q2)

El usuario señaló que el «ajuste de efectivo restringido +17.782M» era un parche contable y que faltaban las fuentes de financiación del trimestre (preferentes 4.395M y venta de participaciones 3.899M del acumulado).

### Resultado

- **Verificación en los filings**: el 10-Q Q1 de KDP (31-mar-2026) incluye 17.818M de efectivo restringido «legally segregated … for the completion of the JDE Peet's Acquisition»; las preferentes (4.489M en Q1) y la venta de participaciones (3.948M en Q1) se levantaron en el primer trimestre (4.395M/3.899M en el acumulado por costes de transacción), por lo que no son fuentes de caja de Q2. El vehículo del pago en Q2 es la liberación del escrow (17.782M).
- **Arreglo general** (`analystCashCapitalProcessor.js`): si la variación de efectivo restringido es material (≥ 100M) y comparable a la adquisición del periodo (≥ 25 %), se pinta la fila **«Efectivo restringido (escrow)»** con nota determinista: saldo del trimestre previo → actual, importe liberado que financió la compra y financiación que lo consignó (preferentes y participaciones del acumulado con sus importes). En el resto de casos sigue oculto y explicado bajo la tabla (KHC, P&G, movimientos pequeños).
- **KDP Q2 regenerado (713)**: Libre 402 · Adquisiciones −16.615 · Deuda 4.273 · Deuda asumida −4.819 · Caja −619 · **Efectivo restringido (escrow) +17.782** · En total **+404** → «Más o menos cuadra». La nota *4 enlaza con la financiación de Q1. YTD 463 con preferentes +4.395 y participaciones +3.899. 10-K 714 mantiene los 11 tramos del calendario (6.952M >2030; tipo 4,27 %).
- **Tests**: caso KDP actualizado (fila visible + nota); el resto de casos de escrow oculto (Pepsi 2018, KHC) siguen cubiertos. Suite **206/206**.
- **Docs**: `REVISION-ANALISIS.md` (empresa 6) e historial actualizados.

## 12:39 — Resumen: Pie de página con logo y enlace de Cifra Research en los informes (PDF, Word y ODT)

Se solicitó que el pie de cada página de los informes descargables incluya, de forma discreta y a la derecha, el logotipo de Cifra Research y el enlace de cifraresearch.com, también al descargar en PDF, Word u ODT.

### Resultado

- **Asset nuevo**: `src/assets/logo-cifra.png` (logotipo plano recortado, transparencia sobre el blanco de las páginas) y `src/services/reportExport/reportBranding.js` (`BRAND_URL`, `BRAND_LABEL`, `BRAND_LOGO_PATH`, `BRAND_LOGO_RATIO`, `readBrandLogo`, `hasBrandLogo`); si el asset faltara, los informes se siguen generando solo con el enlace (sin romper la descarga).
- **PDF** (`pdfReportBuilder.js`): pie de marca en todas las páginas vía evento `pageAdded` más la primera página: a la izquierda la nota «Generado por Cifra · beta 0.1…» y a la derecha el logo (9 pt) con «cifraresearch.com» en gris claro y anotación de enlace clicable (`doc.link`). Se eliminó el pie antiguo que solo aparecía en la última página.
- **Word** (`docxExporter.js`): `word/footer1.xml` con párrafo alineado a la derecha (logo 10 pt + hipervínculo externo), relaciones `footer1.xml.rels`, override en `[Content_Types].xml`, `footerReference` en el `sectPr` y `word/media/logo-cifra.png`.
- **ODT** (`odtExporter.js`): `<style:footer>` en la página maestra de `styles.xml` (párrafo alineado a la derecha con `draw:frame` del logo y `text:a` al enlace), entrada en `META-INF/manifest.xml` y `Pictures/logo-cifra.png`.
- **Verificación**: informes de prueba de 2 páginas convertidos con LibreOffice; el pie con logo y enlace aparece en cada página y el enlace es clicable en los tres formatos. Suite completa **208/208**.
- **Regeneración**: `reportExport.service.js` cambia de mtime, por lo que los informes ya compilados se regeneran automáticamente en la próxima descarga.

## 13:31 — Resumen: Novedades v0.1.0 y revisión de traducciones y calidad antes de producción

Se solicitó preparar el lanzamiento a producción: asegurar que todas las traducciones están hechas, revisar buenas prácticas del código y añadir en Novedades la versión 0.1.0 (UI y gráficos de cartera, bugs, optimización de recursos de IA, calidad de los análisis, multi-idioma, datos financieros, pestaña de ratios, versiones de análisis y calendario).

### Resultado

- **Novedades v0.1.0**: nueva entrada en `public/novedades.js` («Primera versión estable», 18-sep-2026) con los 9 cambios pedidos clasificados como `nuevo`/`mejora`/`correccion`; v0.0.1 queda como versión anterior. Badge por defecto de `index.html` y `empresa.html` actualizado a `v0.1.0 · Actual` y `novedades.js` con cache-busting `?v=7`.
- **Traducciones**: `extract.js` regenera las fuentes (4.576 estáticos y 250 plantillas) y `translate.js` traduce las 15 cadenas nuevas (título, explicación, cambios y fecha) → `en.json` con 4.882 entradas; el test de cobertura del diccionario pasa. Verificado con Chrome headless en `/en`: la v0.1.0 se pinta íntegra en inglés sin restos en español.
- **Auditoría i18n corregida** (`scripts/i18n/audit.js`): decodifica entidades HTML (`&amp;`, `&lt;`…) para no marcar falsos positivos, ignora los bloques SSR con `data-i18n-skip` (por diseño se sirven en el idioma de la ruta) y usa por defecto las rutas `/en`. Resultado: **0 candidatos** en `/en`, `/en/empresa/KHC`, `/en/guias`, `/en/guias/*` y legales; `_missing.json` queda vacío.
- **Calidad**: `node --check` OK en todo `src`, `public`, `scripts`, `tests` y `server.js`; sin `debugger`, `TODO/FIXME` ni `.only` en tests; los `console.log` de `src` son logs de servicio legítimos. Se sube `DICTIONARY_VERSION` a 10 y el `?v=` de `i18n.js` a 10 para invalidar la caché semanal del diccionario ampliado. Suite completa **209/209**.
- **Ampliación pedida después**: nuevo cambio en la v0.1.0 «Nueva pestaña de guías, para ver qué es cada cosa y cómo analiza la IA» (traducido y verificado en `/en`); `novedades.js` pasa a `?v=8`.
- **Fallo de traducción corregido en el badge**: el nodo `v0.1.0 · Actual` no coincidía con el diccionario y «Actual» quedaba sin traducir en `/en` (la auditoría no lo detectaba porque «actual» también es palabra inglesa); ahora el estado va en un `<span>` propio y se pinta `v0.1.0 · Current` (verificado con Chrome headless).
- **Pendiente para el usuario**: commit y despliegue (token de commit protegido).
