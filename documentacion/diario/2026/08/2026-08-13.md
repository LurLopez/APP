# Diario — 2026-08-13

## 20:59 — Resumen: definido el agente del sector consumo defensivo

Se solicitó definir el agente del sector consumo defensivo, usando como referencia el PDF de ejemplo `ejemplos/consumo defensivo/KHC 2025_Q3 ANÁLISIS_ES.pdf` (análisis manual de KHC Q3 2025). El PDF no pudo cargarse como adjunto (el modelo no admite PDFs), pero se extrajo su texto con `pdftotext`.

### Resultado

- Creado `src/agents/prompts/consumo-defensivo.md` con 7 reglas de análisis derivadas del PDF de referencia (doble visión Ajustado/Normal, intangibles a 0, impuestos normalizados al 23 %, fórmula de WC para el cash flow, cuadre de asignación de capital, valoración de desinversiones, BPA) y el formato del informe de referencia (cabecera, dos horizontes temporales, bloques de Ventas / Cash Flow / Asignación de Capital, estilo en español).
- Pendiente de validación del usuario: confirmar que las reglas derivadas reflejan correctamente su criterio y decidir si faltan reglas adicionales.

## 21:15 — Resumen: nuevo proveedor IA para OpenCode Go

Se solicitó dejar preparado el cambio de API de IA para poder usar la suscripción OpenCode Go en las pruebas (en lugar de la API directa de DeepSeek).

### Resultado

- Creado `src/services/ai/providers/opencode-go.provider.js`: llama a `https://opencode.ai/zen/go/v1/chat/completions` (formato OpenAI-compatible), modelo por defecto `deepseek-v4-flash` (configurable con `OPENCODE_GO_MODEL`), `temperature: 0`, `max_tokens` con `AI_MAX_TOKENS`, limpieza de respuestas en ```json```, y error visible si falta `OPENCODE_GO_API_KEY`.
- Registrado `opencode-go` en `modelProvider.js` (junto a `mock` y `deepseek`).
- `.env` y `.env.example`: añadidas `AI_PROVIDER` (deepseek | opencode-go), `OPENCODE_GO_API_KEY` y `OPENCODE_GO_MODEL` con comentarios. Para cambiar de API solo hay que editar esas líneas; los agentes no cambian.
- Verificado: `AI_PROVIDER=opencode-go` resuelve el proveedor correctamente en `modelProvider.js`.
- Configurado `.env` con la clave de OpenCode Go y `AI_PROVIDER=opencode-go`; verificado con una llamada real (respuesta correcta, ~2 s).
- Añadido el alias `opencode` en `modelProvider.js` (además de `opencode-go`): ahora basta cambiar una sola línea en `.env` (`AI_PROVIDER=deepseek` o `AI_PROVIDER=opencode`) para cambiar de API; ambas claves conviven en el archivo. Verificado que ambos valores resuelven su proveedor.

## 21:30 — Resumen: corregida la descarga del PDF del análisis

Se solicitó implementar la descarga del PDF generado, que no funcionaba.

### Resultado

- Causa: en `public/app.js`, `currentPdfUrl` nunca se asignaba con `data.pdfUrl`, por lo que el botón "Descargar PDF del análisis" siempre abortaba.
- Corregido: al terminar el análisis se guarda la URL y un nombre de archivo (`<ticker>-analisis-cifra.pdf`); el botón ahora crea un enlace temporal con `download` para forzar la descarga (antes abría una pestaña).
- Verificado: `GET /api/reports/<uuid>.pdf` responde 200 con `application/pdf`.

## 21:50 — Resumen: nuevo módulo "Cribador de resultados" con datos de la SEC (EDGAR)

Se solicitó implementar la parte screener de la web: buscar una empresa y ver todos sus resultados publicados (10-Q/10-K), como complemento del analizador con IA, usando la API gratuita de la SEC.

### Resultado

- Nuevo `src/services/edgar.service.js`: consulta la API oficial de la SEC. Carga y cachea la tabla ticker→CIK (`company_tickers.json`, 24 h en memoria), busca empresas por ticker o nombre, descarga los `companyfacts` XBRL (caché 6 h) y construye series anuales (últimos 5) y trimestrales (últimos 8) alineadas por frame. Incluye 12 conceptos con fallbacks (ingresos con varios tags, BPA, activos, pasivos, fondos propios, caja, CF operativo/inversión/financiación). Las partidas de balance (instantáneas, frame `...Q4I`) se copian al cierre Q4 para completar la tabla anual. Errores con código (`COMPANY_NOT_FOUND`, `EDGAR_UNAVAILABLE`).
- Nuevo `src/api/routes/screener.routes.js`: `GET /api/screener/search?q=` (búsqueda por ticker/nombre) y `GET /api/screener/company/:ticker` (series anual/trimestral). Registrado en `server.js`.
- Frontend: nueva sección `#screener` en `index.html` (buscador propio, cabecera de empresa, conmutador Anual/Trimestral y 3 tablas: Resultados, Balance y Cash flow, valores en millones $ y BPA en $). El buscador superior ahora usa la API real de EDGAR (antes datos estáticos) y al elegir una empresa carga el cribador. Estilos añadidos en `styles.css`.
- Verificado: búsqueda `ko`/`coca`, resultados de KO (ingresos 47.941 M$, B. neto 13.107 M$, BPA 3,04, activos 104.816 M$ en FY2025) y TAP (vía tag `RevenueFromContractWithCustomerExcludingAssessedTax`), y 404 para ticker inexistente.
- Pendiente: puente "Analizar" desde el cribador hacia el pipeline de IA (siguiente fase).

## 22:30 — Resumen: cribador ampliado a estados financieros completos (estilo TIKR)

Se solicitó que el cribador muestre la cuenta de resultados, el balance y el cash flow completos de la empresa (partidas como filas y periodos como columnas), similar a tick.com/TIKR.

### Resultado

- `edgar.service.js`: conceptos reorganizados en 3 estados (`income` 11 partidas, `balance` 13, `cashflow` 10) con tags us-gaap y fallbacks (ASC 606, `CostOfRevenue`/`CostOfGoodsSold`, cuentas por pagar con `AccountsPayableTradeCurrent`, deuda con `LongTermDebtAndCapitalLeaseObligations`, CAPEX con `PaymentsToAcquireProductiveAssets`, intangibles como combinación de finite+indefinite vía `combine`). El selector de tag elige el que tiene datos más recientes. Valores derivados cuando la empresa no los reporta: pasivo total = activo − fondos propios y beneficio bruto = ingresos − coste de ventas.
- Corrección clave de alineación: los balances del 10-K se asocian al año fiscal por la fecha de fin (`end`) de las partidas instantáneas con `fp=FY`, en vez del `fy` del filing (el balance del año anterior en el 10-K venía etiquetado con el fy del filing). Verificado con KO (10-K con balance actual sin frame) y PG (cierre fiscal en junio).
- La API devuelve ahora `statements` (definición de partidas con formato `money`/`perShare`) para que el frontend pinte las tablas de forma genérica.
- Frontend: las 3 tablas ahora son partidas × periodos (estilo TIKR), con valores en millones $ (BPA en $), columnas alineadas a la derecha y desplazamiento horizontal.
- Verificado con KO, PEP, TAP y PG (ingresos, márgenes, balance completo, CFO/CAPEX/dividendos correctos en todos).
- Documentación pendiente de actualizar tras este cambio (docs de screener).

## 23:10 — Resumen: serie anual del cribador ampliada a 10 años

Se solicitó poder ver 10 años en la vista Anual del cribador (antes 5).

### Resultado

- `src/services/edgar.service.js`: el límite de la serie anual pasa de 5 a 10 periodos (`.slice(0, 10)`); la trimestral se mantiene en 8.
- Verificado: KO devuelve 2016–2025.
- Docs de screener (backend/frontend), IMPLEMENTACION y diario actualizados con "últimos 10".

## 23:40 — Resumen: cribador rediseñado como clon de TIKR (3 subpáginas)

Se solicitó que la vista de empresa del cribador fuera un clon de las capturas del usuario (estilo TIKR): mismos parámetros, colores y dividida en 3 subpáginas (Cuenta de resultados / Balance / Cash flow). El modelo no admite imágenes, así que se extrajeron los colores de las capturas con PIL (paleta: blanco, texto #333333, naranja #FF9900, crema #FFF7E8, azul de enlace, negativos entre paréntesis) y se implementó siguiendo la descripción del usuario.

### Resultado

- `edgar.service.js`: partidas ampliadas a 53 (income 15, balance 24, cashflow 14) con las estándar de TIKR (resultado antes de impuestos, operaciones continuadas, acciones diluidas, inversiones a CP, gastos anticipados, activo/pasivo no corriente, gastos devengados, ingresos diferidos, deuda a CP, capital adicional, reservas, autocartera, minoritarios, adquisiciones, emisión/amortización de deuda, variación neta de caja). Nuevos fallbacks (`RepaymentsOfLongTermDebtAndCapitalSecurities`, `...DebtIssuanceCosts`) y valores derivados (activo/pasivo no corriente = total − corriente). Añadida la marca `emphasis` en los totales (beneficio bruto, operativo, neto, activo/pasivo/fondos propios totales, CFO/CFI/CFF, variación de caja).
- Frontend: la vista de empresa ahora tiene 3 pestañas (una tabla por subpágina), conmutador Anual/Trimestral y chip USD. Tabla estilo TIKR claro: filas de totales en crema y negrita, primera columna fija (sticky) al hacer scroll horizontal, valores en millones alineados a la derecha, negativos entre paréntesis, tipografía tabular. La partida "Acciones diluidas" usa su propio formato.
- Verificado con KO (pretax 15.998 M$, autocartera 56.423 M$, variación de caja −478 M$, activo no corriente derivado 73.772 M$) y TAP.
- Documentación pendiente de actualizar tras este cambio.

## 21:45 — Resumen: pipeline F1 completo (verificación + analista + PDF con 3 bloques)

Se solicitó que la app funcione de extremo a extremo: detectar 10-Q/10-K, detectar consumo defensivo y generar un PDF básico con los 3 subbloques (Ventas, Cash Flow, Asignación de Capital), aunque no sea perfecto.

### Resultado

- Creado `src/agents/analystAgent.js` (agente analista registrado en `agentRegistry`): extracción de cifras en dos fases (1. extracción de datos clave del 10-Q/10-K; 2. estructuración del informe con las reglas de `src/agents/prompts/consumo-defensivo.md`). Envía al modelo una ventana de texto que incluye los estados financieros (antes se quedaba fuera por el corte de 80K caracteres).
- Creado `src/services/report.service.js` (generador de PDF con pdfkit: cabecera, dos horizontes, bloques 1. VENTAS / 2. CASH FLOW / 3. ASIGNACIÓN DE CAPITAL con tablas y notas).
- `analysis.service.js` ejecuta origen → sector → analista → genera PDF; `analysis.routes.js` devuelve el informe JSON + `pdfUrl` y sirve los PDFs en `GET /api/reports/:file` (validado contra path traversal).
- Frontend (`public/app.js` + `index.html` + `styles.css`): el panel de resultado muestra los 3 bloques del informe real y botón "Descargar PDF del análisis".
- Corregido `deepseek.provider.js`: `max_tokens` 400 → 8000 (configurable con `AI_MAX_TOKENS`); antes el informe se truncaba.
- Ampliado `mock.provider.js` con respuesta mínima para el analista (modo sin API key).
- Probado end-to-end con el 10-Q real de Kraft Heinz (Q2 2026, descargado de SEC EDGAR): HTTP 200 en ~21s, formType 10-Q, sector defensive_consumer, informe con los 3 bloques en 2 horizontes y notas con los ajustes (deterioros 7.352M, impuestos normalizados al 23 %, venta de la unidad de alimentación infantil). Rechazos correctos: documento no financiero (NOT_FINANCIAL) y empresa tech (NOT_DEFENSIVE_CONSUMER).
- Pendiente: afinar reglas (p. ej. impuestos del año anterior, WC), validar el PDF en el navegador del usuario.

## 23:58 — Resumen: clon visual del screener según las capturas

Se solicitó corregir el error y clonar con mayor fidelidad las capturas de Cuenta de resultados, Balance y Cash flow.

### Resultado

- `index.html`: añadidas las pestañas visuales de Ratios y Segmentos, aviso de consejo, cabecera de tabla y controles de precisión/orientación.
- `app.js`: la vista trimestral pasa a ser la inicial, se muestran fechas reales de cierre, rangos de periodos, filas de sección, métricas interanuales y márgenes, y se habilita el clic de filas para el futuro gráfico.
- `edgar.service.js`: la API conserva los 10 años anuales y 8 trimestres, añade `periodEnd` y entrega definiciones de filas agrupadas para los tres estados financieros.
- `styles.css`: aplicado el lenguaje visual de las capturas: cabecera negra, filas crema de totales, tipografía compacta, primera columna fija, controles densos y aviso amarillo.
- Verificado: `node --check` para frontend y backend, `git diff --check` y respuesta HTTP 200 de `/api/screener/company/KO` con 10 periodos anuales, 8 trimestrales y 23/25/19 filas visuales.
- Ratios y Segmentos quedan visibles como placeholders porque todavía no existe su fuente de datos en EDGAR dentro del proyecto.

## 23:46 — Resumen: ajuste fino del clon financiero

Se solicitó continuar con el mismo modelo visual y completar la revisión del screener contra las capturas.

### Resultado

- Las columnas se muestran en orden cronológico ascendente, como en las capturas, manteniendo los datos descendentes de la API sin modificarlos.
- Las cuatro columnas más antiguas se muestran como celdas `PRO` y las cuatro más recientes conservan los valores de EDGAR.
- La cabecera negra ahora ocupa también la primera columna sticky; se eliminó el ancho mínimo heredado que impedía ver los ocho periodos en pantallas estrechas.
- Las métricas interanuales y márgenes usan la fila comparable correcta después de invertir el orden visual.
- Añadidos controles `.0`, `.00`, compactación y ocultación de filas vacías, además del cierre funcional del aviso de consejo.
- Verificado visualmente con Chrome headless y KO: cabecera sin solaparse con la barra fija, tabla de 9 columnas, pestañas `income`/`balance`/`cashflow`, vista anual de 10 periodos y bloqueo `PRO` recalculado.

## 23:58 — Resumen: sesión iniciada desbloquea el cribador y 5 años para invitados

Se solicitó que el bloqueo `PRO` desaparezca con la sesión iniciada y que sin sesión solo se puedan ver 5 años.

### Resultado

- `src/middleware/auth.middleware.js`: nueva función `resolveUser` (autenticación opcional: devuelve el usuario si la cookie es válida o `null` sin romper la petición).
- `src/api/routes/screener.routes.js`: el endpoint `/api/screener/company/:ticker` detecta la sesión y responde con `authenticated`.
- `src/services/edgar.service.js`: `getCompanyResults` limita la serie anual a los últimos 5 periodos cuando no hay sesión (la trimestral se mantiene en 8).
- `public/auth.js`: se emite el evento `auth:change` con el usuario al cargar sesión, entrar o salir.
- `public/app.js`: el bloqueo `PRO` solo se pinta en trimestrales antiguas y sin sesión; al cambiar la sesión se recarga en silencio la empresa cargada para aplicar el nuevo límite.
- Verificado con Chrome headless y un usuario real de la API: invitado ve 5 años anuales y 4 trimestres bloqueados con `PRO`; con sesión ve 10 años y los 8 trimestres completos, sin `PRO`; al cerrar sesión vuelve a 5 años.

## 00:10 — Resumen: sin sesión se muestran todas las columnas con `PRO` en las antiguas

Se solicitó aclarar que las columnas deben seguir apareciendo todas aunque pongan `PRO` (no recortar la tabla a 5 años).

### Resultado

- `src/services/edgar.service.js`: se elimina el recorte de la serie anual; la API vuelve a devolver siempre los 10 años y los 8 trimestres, manteniendo el campo `authenticated` en la respuesta.
- `public/app.js`: `isLockedPeriod` vuelve a bloquear con `PRO` las 6 columnas anuales y los 4 trimestres más antiguos cuando no hay sesión, en ambas vistas.
- Verificado con Chrome headless: sin sesión el anual muestra 11 columnas (2016–2025) con 6 `PRO` y el trimestral 9 columnas con 4 `PRO`; con sesión las mismas columnas pero con valores completos y sin `PRO`.

## 00:23 — Resumen: eliminadas las secciones de portada (banner, hero y bienvenida)

Se solicitó quitar de la pantalla las tres secciones de la portada: el banner con cuenta atrás, el hero de promoción y las tarjetas de bienvenida.

### Resultado

- `public/index.html`: eliminados `<section class="announcement">` (cuenta atrás), `<section class="promo-banner">` (hero "Datos claros. Mejores decisiones.") y `<section class="welcome-section">` (tarjetas de bienvenida). La primera sección visible es ahora Mercados de referencia (`#mercados`).
- `public/styles.css`: retirados los estilos muertos de esas secciones (announcement, countdown, promo, fake-terminal, info-card, copy-field) y limpiados los bloques responsive de 1120px, 900px y 700px. Se conservan `.close-button`, `.cifra-symbol` y los selectores compartidos que se usan en otras secciones.
- `public/app.js`: eliminado el listener del botón de copiar (`copy-field`, que ya no existe) y el manejador genérico de `.close-button` sin elementos.
- Verificado con Chrome headless: las tres secciones ya no existen en el DOM, `#mercados` es la primera sección y no hay errores de consola.