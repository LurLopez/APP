# Diario — 2026-08-16

## 16:05 — Resumen: mejora de la cartera (gráficos de distribución en Empresa + ticker obligatorio del buscador EDGAR)

Se solicitó mejorar la implementación de la cartera: (1) los gráficos circulares de distribución (cuánto pesa cada posición) solo estaban en la pantalla principal (Inicio) y deben aparecer también al abrir la sección "Cartera" desde la página de una empresa; (2) al añadir una operación, el ticker debe elegirse con el buscador como el de acciones (teclear "TA" → aparece TAP en la lista, con logos), y si la empresa no tiene ticker exacto en el buscador de EDGAR no debe permitirse guardar la operación.

### Resultado

**1. Donuts en la sección Cartera de Empresa** (`public/portfolio.js`, `renderCompanyPanel`):

- El panel de empresa ahora muestra, bajo la posición y el formulario, el panel "Distribución de la cartera" con los dos donuts SVG (por empresa y por sector) y sus leyendas, idénticos a los de Inicio (mismo `donutBlock`/`COLORS`), solo si hay posiciones.

**2. Buscador de ticker en el formulario de operación** (`public/portfolio.js`, `wireTransactionForm` + `styles.css`):

- El desplegable de resultados ahora replica el buscador principal: logo de la empresa (con fallback a inicial), nombre y ticker.
- Se guarda la selección (`data-selected-ticker`); si el usuario edita el ticker después de elegir, se descarta la selección.
- Al enviar: si el ticker no se eligió de la lista, se comprueba contra `/api/screener/search`; si no existe coincidencia exacta en EDGAR, se bloquea con el mensaje "«X» no existe en el buscador de EDGAR. Elige una empresa de la lista de resultados." (si la comprobación falla por red, valida el servidor).

**3. Validación en el servidor** (`src/api/routes/portfolio.routes.js`):

- `POST /api/portfolio/transactions` ahora siempre resuelve el ticker con `getCompanyByTicker` (EDGAR, cacheado) antes de guardar: el `companyName` canónico viene de EDGAR si el cliente no lo envía, y cualquier ticker inexistente se rechaza con `400 COMPANY_NOT_FOUND` (antes solo se validaba si el cliente no enviaba `companyName`, y el 404 se usaba para entradas inválidas).

**Pruebas:**

- E2E con usuario temporal: compra de TAP correcta (201, nombre canónico de EDGAR), `XYZFAKE` y `AAA-FAKE` rechazadas (400 `COMPANY_NOT_FOUND`); `GET /api/portfolio` devuelve `allocations` completos para los donuts. Usuario de prueba eliminado.
- `node --check` OK en `portfolio.js` y `portfolio.routes.js`; versiones subidas a `styles.css?v=27` y `portfolio.js?v=2` en Inicio y Empresa.
- El servidor local se reinició (PID anterior sin `--watch`) para aplicar el cambio de rutas; queda en `node server.js` con log en `/tmp/opencode/cifra-server.log`.

**Pendiente:** nada para esta petición. Nota: `portfolio.js` y `portfolio.routes.js` siguen sin commit inicial (archivos nuevos nunca commitados).

## 16:35 — Resumen: buscador de ticker también en Empresa + historial de operaciones con pantalla completa y filtros

Continuación de la mejora de la cartera: el usuario confirmó que "el buscador sigue igual" porque en la página de empresa el campo Ticker del formulario venía fijo (readonly) y no se podía buscar. Además pidió que en el historial aparezcan solo las 10 últimas operaciones con un botón de pantalla completa donde se vean todas y se pueda filtrar por compra/venta, cantidad y empresa.

### Resultado

**1. Buscador de ticker en la página de empresa** (`public/portfolio.js`, `transactionFormHtml` + `wireTransactionForm`):

- El campo Ticker ya no es readonly: queda precargado con el ticker de la empresa pero es editable y busca en EDGAR con el desplegable con logos (mismo componente que Inicio).
- Si el usuario modifica el texto y se aleja del ticker preseleccionado (o del elegido en la lista), se descarta la empresa oculta; al enviar, si no hay empresa asociada se comprueba la coincidencia exacta contra `/api/screener/search` y se bloquea si no existe (el servidor también valida contra EDGAR).

**2. Historial de operaciones con widget de 10 últimas + pantalla completa** (`public/portfolio.js` + `styles.css`):

- Nuevo panel "Historial de operaciones" en Inicio (sustituye a la tabla completa) y en la sección Cartera de Empresa: muestra las **10 últimas operaciones** (orden por fecha desc, empate por id) con botón "Pantalla completa ⛶".
- Modal `pf-history-modal` (z-index 80, cerrable con ×, clic fuera o Escape) con **todas** las operaciones y filtros: **Tipo** (Todas/Compra/Venta), **Empresa** (select con todas las empresas de la cartera) y **Cantidad mínima** (acciones). Contador de resultados y borrado de operaciones también dentro del modal (se refresca tras borrar).
- Refactor: tabla de operaciones compartida (`historyTableHtml`/`transactionRowHtml`/`transactionsSortedDesc`), borrado compartido (`wireDeleteButtons`) y cableado del widget (`wireHistoryWidget`).

**Pruebas:** E2E con usuario temporal y 12 operaciones en 5 empresas (KO, TAP, PG, PEP, MMM): orden desc correcto y filtros tipo+empresa+cantidad verificados; cleanup OK. `node --check` OK; versiones `styles.css?v=28` y `portfolio.js?v=3` en Inicio y Empresa.

## 12:14 — Resumen: rediseño de la cartera inspirado en DivvyDiary

Se solicitó que la cartera tuviera una presentación equivalente a la referencia visual de DivvyDiary.

### Resultado

- Se sustituyó la composición principal por un dashboard con tres métricas: valor de cartera, rentabilidad por dividendo y dividendos anuales brutos previstos.
- Se añadieron pestañas de Cartera, Dividendos, Análisis, Operaciones e Inspiración, manteniendo las acciones existentes de compra/venta e historial FIFO.
- La pestaña Cartera muestra un donut SVG grande de asignación por empresa, con cambio entre Valor y Coste, leyenda porcentual y enlaces a Tabla y Exportar CSV.
- La vista Tabla se adaptó a un formato tipo broker con logos, coste, mercado, ganancia, peso de mercado, dividendos y fila de totales.
- Se añadieron estilos responsive para escritorio y móvil, se normalizó el formato español de miles y se actualizaron las versiones cacheadas de CSS/JS.
- Se actualizó la documentación funcional del frontend. No se modificaron API, base de datos ni cálculos backend.

### Verificación

- `node --check` correcto para `portfolio.js`, `app.js` y `empresa.js`.
- Chrome headless validado en 1230 px y 390 px para dashboard, gráfico, tabla, operaciones y formulario.

## 12:18 — Resumen: Quitadas las empresas destacadas de la pantalla de inicio

Se solicitó eliminar de la pantalla de inicio la sección de empresas destacadas (AAPL, MSFT, JNJ, KO, NVDA, MMM), que mostraba cotizaciones estáticas.

### Resultado

- Eliminada la sección "Empresas destacadas" completa en `public/index.html` (6 tarjetas con cotizaciones y sparklines estáticos).
- El JS de `app.js` referente a `.home-card` queda inerte pero inofensivo (querySelectorAll sobre lista vacía).
- La pantalla de inicio ahora enlaza directamente el buscador con la sección de análisis.

## 12:30 — Resumen: Pantalla de inicio rediseñada con 3 secciones

Se solicitó reorganizar la pantalla de inicio en 3 secciones (Seguimiento, Cartera, Análisis): al pulsar una, el resto desaparece y solo quedan las 3 opciones y el buscador.

### Resultado

- Topbar con 3 pestañas persistentes: Seguimiento, Cartera, Análisis (sustituyen a Seguimiento/Filtros/Cartera; se eliminó "Filtros").
- Hub inicial con 3 tarjetas grandes (menú de inicio) + buscador; al pulsar una tarjeta o pestaña se abre solo esa sección y el menú desaparece.
- La sección "Análisis" agrupa subida de PDF (#nuevo) e historial (#historial) en un contenedor común.
- Pulsar la pestaña activa de nuevo vuelve al hub.
- Parámetros de URL `?cartera=1` y `?analizar=` abren su sección automáticamente.
- Limpieza: eliminado código muerto de `.home-card` (tarjetas destacadas) en `app.js` y `openWatchSection`/`updateWatchButtons`.
- Verificado en Chrome headless (1230 px y 390 px): conmutación de secciones, vuelta al hub y sin desbordamiento.

### Pendiente

- Ajustar el diseño de Cartera con más inspiración divvydiary cuando se revise la Fase de cartera.

## 12:42 — Resumen: Bug en sección Seguimiento (no mostraba nada)

Al probar la nueva pantalla de inicio, la sección Seguimiento aparecía vacía.

### Resultado

- Causa: en el refactor de secciones se eliminó por accidente la llamada `Watchlists.mountSection(...)` de `public/app.js`, por lo que `#watchlists-section` nunca se montaba.
- Corrección: restaurada la llamada (sin el viejo `onEmptyChange`, ahora la visibilidad la controla la pestaña). Bump de caché `app.js` a v=17.
- Verificado en Chrome headless con sesión real (usuario temporal creado y borrado tras la prueba): sin sesión muestra el estado vacío, con listas muestra la tabla con 3 filas.

## 12:50 — Resumen: Seguimiento como sección inicial por defecto

Se pidió que la pantalla principal abra directamente la sección Seguimiento (como si se hubiera pulsado su tarjeta), sin mostrar el menú de las 3 opciones.

### Resultado

- Al entrar en `/` se abre automáticamente Seguimiento (menú oculto, pestaña activa) si no hay parámetros.
- `?cartera=1` y `?analizar=` siguen abriendo su sección (prioridad sobre el valor por defecto).
- Bump de caché `app.js` a v=18. Verificado en Chrome headless: `/` abre Seguimiento; `?cartera=1` abre Cartera.

## 13:05 — Resumen: Cartera con gráfico y tabla visibles a la vez

Se pidió que en la sección Cartera el gráfico y la tabla de posiciones se vean juntos, sin necesidad de pulsar "Tabla".

### Resultado

- La pestaña Cartera ahora muestra el gráfico de asignación (dona + leyenda) y debajo la tabla de valores, ambos a la vez.
- Eliminado el estado `portfolioContent` (allocation/table) y los botones de alternancia "▦ Tabla" / "◔ Gráfico" / "← Asignación"; el exportador CSV se mantiene en ambos paneles.
- Pequeña pista en el footer del gráfico ("El detalle de cada posición está abajo.") con nuevo estilo `.pf-footer-hint`.
- Bump de caché `portfolio.js` a v=6. Verificado en Chrome headless con cartera de prueba (usuario temporal creado y borrado): dona visible, tabla con 3 posiciones debajo, sin errores JS.

## 12:50 — Resumen: Cartera: quitadas pestañas Análisis/Inspiración y arreglados dividendos

Se pidió eliminar los submenús "Análisis" e "Inspiración" de la cartera y arreglar la pestaña de dividendos, que daba valores incorrectos.

### Resultado

- Quedan 3 submenús: Cartera, Dividendos, Operaciones. Eliminadas las pestañas y sus paneles placeholder.
- **Bug de dividendos**: el historial se descargaba solo desde la primera compra de cada ticker, así que el TTM (12 meses) perdía pagos anteriores y PYPL/KHC daban 0. Ahora se descarga desde `min(primera compra, inicio TTM)` en `src/services/portfolio.service.js`.
- Resultado con los datos reales: TAP $22,80/año (antes $5,76), PYPL $11,34 (antes 0), KHC $16 (antes 0); rentabilidad por dividendo 2,06 % (antes 0,24 %).
- Nota de entorno: el servidor que servía en :3000 se había lanzado sin `--watch` y no recargaba código; se reinició (ahora el proceso `--watch` sirve el puerto).
- Bump de caché `portfolio.js` a v=7. Verificado en Chrome headless: 3 pestañas y panel de dividendos correcto.

## 13:05 — Resumen: Ocultar posiciones totalmente vendidas en Cartera

Se reportó que una empresa vendida por completo (CAG) seguía apareciendo como fila en la cartera.

### Resultado

- El backend ahora devuelve en `positions` solo posiciones con acciones en cartera (`shares > 0`), en `src/services/portfolio.service.js`.
- El resumen se sigue calculando con todas las posiciones, así que las ganancias realizadas de lo vendido permanecen en los totales.
- Eliminada la función muerta `renderPositionsTable` y su lista de cabeceras en `public/portfolio.js`.
- Bump de caché `portfolio.js` a v=8. Verificado en Chrome headless con caso real (compra+venta total de CAG): la fila desaparece de tabla y dividendos, y el total conserva la ganancia realizada.

## 13:25 — Resumen: Columnas Sector, Tipo, País y Región en la tabla de posiciones

Se pidió que junto a "Valor" la tabla de la cartera muestre el sector, el tipo de valor (acciones, ETF...), el país y la región.

### Resultado

- **Backend**:
  - `edgar.service.js`: nueva `getCompanyOrigin(ticker)` que devuelve sector (SIC) y país; `getCompanySector` delega en ella.
  - `market.service.js`: la cotización ahora incluye `instrumentType` (EQUITY/ETF) de Yahoo.
  - `portfolio.service.js`: cada posición lleva `sector`, `type` (Acción/ETF/Índice), `country` (EDGAR) y `region` (mapeo de país → América del Norte/Europa/Asia-Pacífico/Latinoamérica/Internacional).
- **Frontend**: tabla de posiciones con 4 columnas nuevas (Sector, Tipo, País, Región) tras Valor; fila de totales con "—" en esas celdas; CSV exportado incluye los campos nuevos. `min-width` de la tabla ampliado a 1380 px (scroll horizontal).
- Bump de caché `portfolio.js` a v=9. Verificado en Chrome headless: KO → Consumo defensivo/Acción/Estados Unidos/América del Norte; ETF SCHD → tipo ETF. Sin desbordamiento.

## 13:40 — Resumen: Gráfico de asignación agrupa por Valor/Sector/Tipo/País/Región

Se pidió que el desplegable del gráfico de asignación dejara de ofrecer "Coste" y permitiera agrupar por las dimensiones de la tabla (sector, tipo, país, región).

### Resultado

- El select del gráfico ahora tiene: Valor (por empresa), Sector, Tipo, País y Región.
- Al elegir una dimensión, la dona agrupa las posiciones (p. ej. "País" → Estados Unidos) y la leyenda/título se adaptan.
- El interruptor "Coste" se mantiene como control de base de cálculo (valor vs coste), independiente de la agrupación.
- Estado renombrado: `allocationGroup` (agrupación) + `allocationBasis` (base). Verificado en Chrome headless con KO/KHC/SCHD/PM: país → "Estados Unidos", tipo → Acción/ETF, sector → Consumo defensivo, etc.
- Bump de caché `portfolio.js` a v=10.

## 13:55 — Resumen: Tooltip al pasar el cursor sobre el gráfico de asignación

Se pidió que al situar el cursor sobre el gráfico aparezca cerca el porcentaje y el nombre de lo que representa ese segmento.

### Resultado

- Tooltip personalizado al hacer hover sobre los segmentos de la dona: muestra color, etiqueta (empresa o grupo), porcentaje e importe, siguiendo al cursor (con ajuste de bordes de pantalla).
- Funciona en el gráfico de la cartera y en las donas de distribución de la página de empresa.
- Los segmentos ahora llevan `data-label/data-pct/data-amount`; se eliminó el tooltip nativo `<title>`.
- CSS `.pf-chart-tooltip` (fijo, `pointer-events: none`, ocultable con `[hidden]`); se oculta al re-renderizar.
- Bump de caché `portfolio.js` v=11 y `styles.css` v=31. Verificado en Chrome headless: hover muestra "Schwab US Dividend Equity ETF 49,59 % · $863,00", se oculta al salir, y funciona también en modo agrupado por país.

## 14:20 — Resumen: Filas expandibles con el detalle de compras/ventas en la tabla de posiciones

Se pidió poder desplegar cada posición para ver sus compras y ventas por separado, con la rentabilidad exacta de cada compra/venta.

### Resultado

- **Backend** (`portfolio.service.js`): cada posición expone ahora `lots` — por lote de compra: fecha, precio, acciones, quedan, coste, valor actual, no realizada, dividendos cobrados y sus ventas (fecha, acciones, precio, ingresos, ganancia, dividendos).
- **Frontend**: nueva columna con botón "+/−" en las 3 vistas (Actual/Vendido/Todo). Al pulsar, aparece debajo una sub-fila por cada compra con su detalle y, dentro, las ventas de ese lote con su ganancia. Las sub-filas se ocultan/muestran en el DOM sin re-renderizar (botón cambia a −).
- Estilos nuevos: `.pf-expand-*`, `.pf-lot-row`, `.pf-lots`, `.pf-lot-*`.
- Bump de caché `portfolio.js` v=14 y `styles.css` v=33. Verificado en Chrome headless con compra→venta→compra→venta de CAG: cada lote muestra su ganancia exacta (+$216 y +$120), y KO con 2 compras muestra ambas por separado.

## 14:35 — Resumen: Expansión "+" filtrada por vista y rediseño como tabla limpia

Se pidió que el desplegable de cada posición muestre SOLO lo relevante según la vista (Actual = lo que tienes ahora; Todo = todo el historial) y que estéticamente sea una tabla con una fila por operación.

### Resultado

- **Actual**: al expandir solo muestra los lotes en cartera (compras con restantes): fecha, acciones restantes, precio, coste, valor actual, no realizada y dividendos. Las compras/ventas pasadas NO aparecen.
- **Vendido**: al expandir muestra las ventas (fecha, acciones, precio, ingresos, ganancia, dividendos).
- **Todo**: al expandir muestra TODAS las operaciones (compras y ventas) con fecha, operación, acciones, precio, importe, ganancia, dividendos y estado (Quedan X / Vendida).
- Rediseño visual: en lugar de textos sueltos que se rompían, ahora es una sub-tabla `.pf-lots-table` con cabeceras y columnas alineadas (una fila por operación); scroll horizontal propio en móvil.
- Bump de caché `portfolio.js` v=15 y `styles.css` v=34. Verificado en Chrome headless: TAP actual muestra solo 2 compras (sin la venta de marzo); CAG vendido muestra solo las 2 ventas; en Todo aparecen las 4 operaciones con su ganancia exacta (+$216, +$120).

## 14:50 — Resumen: Vista Todo con una línea por compra (ventas agregadas en la misma línea)

Se pidió que en el desplegable de la vista "Todo" cada compra ocupe una sola línea que combine lo vendido y lo que queda (p. ej. compro 10, vendo 5 → 1 línea: vendidas 5 y quedan 5).

### Resultado

- La sub-tabla de "Todo" ahora muestra una fila por lote de compra con: Compra, Compradas, Precio, Coste, Vendidas, Ingresos, Ganancia (agregadas si hubo varias ventas del mismo lote), Quedan (o etiqueta "Vendida"), No realizada y Div. cobrados.
- Corregido un bug: las posiciones 100 % vendidas no mostraban su lote en "Todo" (la guarda de filas vacías se aplicaba antes de seleccionar la vista).
- Limpieza de código muerto en `lotsRowsForView`.
- Bump de caché `portfolio.js` v=17. Verificado en Chrome headless: TAP (compra 10, venta 5) → 1 línea con vendidas 5/quedan 5; KO con 2 ventas del mismo lote → 1 línea agregada (5 vendidas, +$60); CAG vendida al 100 % → 1 línea con estado Vendida.

## 15:05 — Resumen: Vista Todo: lo vendido y lo que queda, en 2 líneas separadas

Se pidió que en "Todo" la parte vendida y la parte en cartera de una misma compra se muestren en 2 líneas distintas (antes iban fusionadas en una).

### Resultado

- Por cada lote se generan hasta 2 líneas: "Venta" (acciones vendidas, precio medio de venta, ingresos, ganancia, dividendos de venta) y "En cartera" (acciones restantes, precio de compra, coste de lo que queda, no realizada, dividendos de cartera).
- Lote sin vender → solo línea "En cartera"; lote 100 % vendido → solo línea "Venta"; venta parcial → 2 líneas con la misma fecha de compra.
- Columnas: Compra, Detalle, Acciones, Precio, Importe, Ganancia, Div. cobrados.
- Bump de caché `portfolio.js` v=18. Verificado en Chrome headless: TAP (compra 10, venta 5) → 2 líneas; KO con 2 ventas agregadas → 2 líneas; CAG vendida y PEP sin vender → 1 línea cada una.

## 22:28 — Resumen: Tabla de posiciones: columnas recortadas, fila expansible y botón de expandir nuevo

Se solicitó quitar de la tabla de posiciones las columnas Valor (valor actual), Sector, País y Región (manteniéndolas en el gráfico de asignación), rediseñar el botón "+" y hacer que el clic en la fila expanda las sublíneas en lugar de navegar a la empresa.

### Resultado

- Tabla de posiciones (vistas Actual / Vendido / Todo): eliminadas las columnas Sector, País, Región y la de valor actual ("Mercado" en Actual; "Valor" en Todo). Se conserva Tipo, la columna de empresa y el resto de métricas. El gráfico de donut y sus modos de agrupación no cambian.
- El botón "+" ahora es un chevron (▸) sin caja, que rota 90° al expandir; la fila expandida se marca con fondo suave y el botón con fondo naranja.
- El clic (o Enter/Espacio) en cualquier parte de la fila de posición alterna la expansión de las sublíneas (compras/ventas) en lugar de navegar a la empresa.
- La navegación a la ficha de empresa se conserva: el nombre/logo de la empresa ahora es un enlace a `/empresa/<ticker>`.
- Sin cambios de backend; solo frontend (`public/portfolio.js` + `public/styles.css`).

## 23:05 — Resumen: Tabla de posiciones sin columna "Valor" (empresa)

Se solicitó quitar también la columna "Valor" de la tabla de posiciones, la que mostraba el valor/acción (logo y nombre de empresa).

### Resultado

- Eliminada la columna "Valor" (empresa) de las tres vistas (Actual / Vendido / Todo), incluida su celda en la fila de totales y su cabecera.
- Ajustados los colspan de las filas de sublíneas (Actual 9, Vendido 8, Todo 8) y limpiado el código muerto: función `positionCompanyCell`, clase `pf-broker-first-head` (CSS) y el enlace de empresa.
- Nota: las filas ya no identifican la empresa por nombre; el ticker sigue disponible en el `data-ticker` de la fila y en las sublíneas. La tabla de dividendos mantiene la columna de empresa.
- Sin cambios de backend.

## 23:20 — Resumen: Corrección en tabla de posiciones: se quita "Tipo" y se restaura la columna "Valor" (empresa)

Se aclaró que la columna a quitar no era la de empresa sino la de "Tipo".

### Resultado

- Restaurada la columna "Valor" (logo + nombre de empresa, enlace a ficha) en las tres vistas (Actual / Vendido / Todo), incluida su celda en la fila de totales, el ancho mínimo de la primera cabecera (`pf-broker-first-head`), el CSS del enlace (`pf-company-link`) y los colspan de sublíneas (Actual 9, Vendido 8, Todo 8).
- Eliminada la columna "Tipo" de las tres vistas y limpiado el código asociado (`positionMetaCells` y `POSITION_META_TOTALS`).
- El gráfico de asignación conserva el modo "Tipo" como agrupación.
- Nota: la exportación CSV mantiene sus columnas completas (Sector, Tipo, País, Región, Mercado/Valor) como exportación de datos completa.

## 23:55 — Resumen: Bug de alineación de columnas en la tabla de posiciones

Se reportó que la columna "Valor" quedaba justo encima de la columna de acciones (contenido desfasado respecto a las cabeceras).

### Resultado

- Causa raíz: regla genérica global `td:first-child { display: flex; ... }` de `styles.css` (para iconos de otras tablas) convertía la primera celda de cada fila de la tabla de posiciones en flex; la celda de expandir se escapaba de su columna y se apilaba sobre la columna de empresa, desplazando todo el contenido una columna.
- Fix: override en `styles.css` que restaura `display: table-cell` para la primera celda de `.pf-broker-table`, `.pf-lots-table` y `.pf-transactions-table`.
- Verificado con Chrome headless + CDP midiendo posiciones: cabeceras y datos coinciden en Actual (9 col), Todo (8 col), Dividendos y Operaciones. Vendido sin filas (no hay posiciones 100 % vendidas en la cartera de prueba).
- Bump de caché `styles.css` v=35/v=30 y `portfolio.js` v=19/v=5.

## 00:20 — Resumen: Tabla Actual: ganancia con dividendos, "Peso cartera" y dividendos cobrados

Se pidió en la vista Actual: añadir la ganancia incluyendo dividendos, renombrar "Mercado %" a "Peso cartera" y cambiar la columna "Div." para que muestre los dividendos cobrados hasta hoy desde la fecha de compra.

### Resultado

- Nueva columna "Ganancia + div." (no realizada + dividendos cobrados de la posición, con su %), usando `unrealizedWithDividends` del backend, que ya calculaba los dividendos desde la fecha de compra hasta hoy (`dividendsHeld`).
- "Mercado %" renombrada a "Peso cartera" (mismo cálculo: peso sobre el valor total).
- "Div." (anual previsto + por acción) sustituida por "Div. cobrados": dividendos acumulados cobrados desde la compra hasta hoy (`dividendsTotal`). Totales: `totalUnrealized + Σ dividendsHeld` en "Ganancia + div." y `summary.totalDividends` en "Div. cobrados".
- 10 columnas en la vista Actual (colspan de sublíneas ajustado). Verificado en Chrome headless: cabeceras alineadas y valores correctos (CAG −$99,04 → −$40,91 con $96,89 de dividendos; totales +$706,01 y $102,65).
- Bump de caché `portfolio.js` v=20/v=6. Sin cambios de backend.

## 01:10 — Resumen: Cartera: clic en acción expande, sublíneas con columnas de la tabla y porcentajes con hover

Se pidió: 1) que al pulsar sobre la acción se abran las sublíneas (no navegar a la ficha), 2) que las sublíneas usen las columnas existentes en lugar de una sub-tabla con columnas propias, y 3) que en Ganancia, Ganancia + div., Div. % y Div. YoC se vean los porcentajes y al pasar el ratón se vea la cantidad (ganancia, ganancia+div. y dividendos esperados).

### Resultado

- Eliminado el enlace de la columna de empresa: pulsar en cualquier parte de la fila (incluida la acción) expande las sublíneas. La tabla de dividendos (sin sublíneas) conserva la navegación a la ficha.
- Las sublíneas ahora son filas de la propia tabla con las columnas de la vista: Actual muestra por lote fecha, acciones, coste+precio, ganancia % (hover importe), ganancia+div. % (hover importe), peso cartera, Div. %/YoC "—" y div. cobrados; Vendido muestra fecha, vendidas, coste, ingresos, ganancia, div. y total; Todo muestra las líneas "Venta"/"En cartera" con las columnas Realizada/No realizada. `toggleLotsRow` expande todas las sublíneas del ticker.
- Ganancia y Ganancia + div.: se muestra el % y el importe en el tooltip (title). Div. % y Div. YoC: % visible y tooltip con los dividendos anuales esperados. Aplicado también a la fila de totales.
- Limpiado el código de la antigua sub-tabla (`lotsTableHtml`, `lotsRowHtml`, `lotsRowsForView`, estilos `.pf-lots*`, `.pf-company-link`).
- Verificado en Chrome headless: expansión por clic en la fila, sublíneas alineadas con las cabeceras en Actual y Todo (3 sublíneas para CAG con sus badges), tooltips con importes correctos. Bump de caché `portfolio.js` v=21/v=7, `styles.css` v=36/v=31.

## 01:40 — Resumen: Sublíneas con Div. %/YoC y colores rojo/verde en ganancias

Se pidió: 1) que las sublíneas de la vista Actual también muestren Div. % y Div. YoC (con el valor al hover), y 2) que Ganancia y Ganancia + div. se coloreen en rojo/verde según el signo.

### Resultado

- Sublíneas de "Actual": Div. % y Div. YoC ahora muestran el % (dividendos cobrados del lote sobre valor y sobre coste, respectivamente) con el importe en el tooltip, igual que la fila principal.
- Colores: la regla genérica `td.positive/negative` era anulada por `.pf-broker-table td` (color fijo). Añadidas reglas específicas `.pf-broker-table td.positive/negative`, `.pf-lot-row td.positive/negative` y `.pf-broker-totals-row td.positive/negative` al final de la sección de cartera.
- Verificado en Chrome headless: CAG −44,21 %/−18,26 % en rojo, PYPL +82,55 % en verde, sublíneas y totales también coloreados. Bump de caché `styles.css` v=38/v=33.

## 02:15 — Resumen: Ordenación por columnas en la tabla de posiciones

Se pidió poder ordenar la tabla de posiciones por los valores de las columnas (coste, ganancia, ganancia + div., etc.), de mayor a menor y de menor a mayor.

### Resultado

- Clic en cualquier cabecera de las tres vistas (Actual / Vendido / Todo) ordena por esa columna: primer clic descendente (ascendente en "Valor", por nombre), segundo clic invierte el sentido.
- Marcadores en cabecera: ↕ sin ordenar, ↓/↑ con color naranja en la columna activa; cursor de clic en las cabeceras ordenables.
- Ordenadores por vista: Actual (valor, acciones, coste, ganancia, ganancia + div., peso cartera = valor, div. %, div. YoC, div. cobrados), Vendido (vendidas, coste, ingresos, ganancia, div. cobrados, total), Todo (acciones, invertido, realizada, no realizada, div. cobrados, total). Los valores nulos (sin precio) van al final.
- La fila de totales permanece al final. Verificado en Chrome headless: Ganancia desc (+82,55 % → −44,21 %), asc invertido, Coste desc ($912 → $224).
- Bump de caché `portfolio.js` v=23/v=9, `styles.css` v=39/v=34.

## 02:50 — Resumen: Conmutador %/importe por columna (Ganancia, Ganancia + div., Div. %, Div. YoC)

Se pidió poder elegir en cada una de esas columnas si se muestra el porcentaje (con la cantidad al hover) o la cantidad (con el % al hover), para poder ordenar por cantidad o por porcentaje.

### Resultado

- Botón conmutador en la cabecera de cada columna afectada (%, pasa a $): alterna el modo de visualización de toda la columna, incluida la fila de totales y las sublíneas.
- El ordenamiento de esas columnas respeta el modo: en modo % ordena por el porcentaje, en modo importe por la cantidad (ej. Ganancia desc en modo $: +$752,82 → −$99,04; en modo %: por el ratio sobre coste).
- Clic en el botón no dispara la ordenación (stopPropagation). Estado independiente por columna y persistente entre renders.
- Verificado en Chrome headless: toggle pct→amt con hover invertido (−44,21 % ↔ −$99,04), orden desc por cantidad y por porcentaje. Bump de caché `portfolio.js` v=24/v=10, `styles.css` v=40/v=35.

## 03:20 — Resumen: Conmutadores restaurados en Div. %/YoC y scroll preservado

Se aclaró que sí se quieren los conmutadores %/$ en Div. % y Div. YoC (error previo), y se reportó que al pulsar el icono la tabla saltaba a la izquierda.

### Resultado

- Restaurados los botones conmutadores en las 4 columnas: Ganancia, Ganancia + div., Div. % y Div. YoC (filas, sublíneas y totales).
- Bug de scroll: el re-render tras el clic reiniciaba el scroll horizontal de la tabla y el vertical de la página. Nueva función `rerenderKeepingScroll` que guarda `scrollLeft` del contenedor y `scrollY` de la ventana antes de renderizar y los restaura en el siguiente frame. Aplicada al clic de ordenación y al conmutador.
- Verificado en Chrome headless: 4 toggles con su estado independiente (divpct % → $ con hover invertido) y scroll horizontal (202 px) y vertical (300 px) intactos tras el clic.
- Bump de caché `portfolio.js` v=25/v=11.

## 03:50 — Resumen: Sublíneas: Div. % vacío y Div. YoC con rendimiento sobre el precio de compra

Se pidió que en las sublíneas de la vista Actual la columna Div. % no muestre nada y que Div. YoC muestre el % de dividendo obtenido sobre el precio de compra.

### Resultado

- Sublínea de "Actual": la celda de Div. % queda vacía (eliminado el cálculo sobre el valor actual, `lotYieldOnValue`).
- Div. YoC de la sublínea muestra el % de dividendos cobrados sobre el coste de la compra (`heldDividends / heldCost`), con el importe al hover (sigue el conmutador de la columna).
- Verificado en Chrome headless: sublínea de CAG con Div. % vacío y Div. YoC +32,29 % ($58,13 de dividendos sobre $180 de coste).
- Bump de caché `portfolio.js` v=26/v=12.

## 04:15 — Resumen: Div. YoC de sublíneas como rendimiento anual previsto sobre el coste

El +32,29 % de la sublínea de CAG (dividendos acumulados desde 2018 sobre el coste) resultaba engañoso: se pidió que el Div. YoC de las sublíneas sea el rendimiento anual previsto sobre el coste del lote, como en la fila principal.

### Resultado

- Sublínea de "Actual": Div. YoC = dividendos anuales previstos proporcionales al lote (proyección por acción × acciones del lote) sobre el coste del lote; el hover/importe muestra ese importe anual esperado.
- Verificado en Chrome headless: fila CAG Div. YoC +4,38 %, sublínea +4,08 % (antes +32,29 %); Div. % de sublínea sigue vacío.
- Bump de caché `portfolio.js` v=27/v=13.

## 04:45 — Resumen: Columna Coste renombrada a "Precio medio" con conmutador coste/precio

Se pidió renombrar la columna de coste a "Precio medio", que al pasar el ratón muestre el coste total, y un botón para alternar entre el coste total y el precio medio.

### Resultado

- Vista Actual: la columna pasa a llamarse "Precio medio". Botón conmutador en la cabecera ("Medio" ↔ "Coste"): en modo "Medio" muestra el precio medio por acción y el hover el coste total; en modo "Coste" muestra el coste total. Aplicado también a sublíneas (precio de compra del lote ↔ coste del lote) y a la fila de totales (precio medio global ↔ coste total).
- La ordenación por esa columna respeta el modo: ordena por precio medio o por coste total según lo mostrado.
- El conmutador reutiliza el mecanismo existente de `displayMode`/`rerenderKeepingScroll` (el scroll se conserva). Verificado en Chrome headless: CAG muestra $28,00 (hover $224,00) y al conmutar $224,00; sublínea $45,00 ↔ $540,00.
- Bump de caché `portfolio.js` v=28/v=14, `styles.css` v=41/v=36.

## 05:40 — Resumen: Tabla de posiciones sin scroll horizontal y columna Coste renombrada

Se pidió 1) que el hover de la columna en modo "Coste" muestre el precio medio (y al revés), 2) que el nombre de la columna vuelva a "Coste", y 3) reducir tamaños para no necesitar scroll horizontal.

### Resultado

- Hover de la columna Coste: muestra siempre el valor contrario al visible (modo "Medio" → coste total al hover; modo "Coste" → precio medio al hover).
- Cabecera renombrada de "Precio medio" a "Coste" (el conmutador "Medio"/"Coste" y la ordenación por modo se mantienen). "Ganancia + div." abreviada a "Gan. + div." para ganar espacio.
- Sin scroll horizontal: `min-width` de la tabla bajado de 1380 a 760 px, padding de celdas y cabeceras reducido, columna de empresa y logo más estrechos, fuente del nombre a 10 px. Verificado con Chrome headless a 1400/1100/1000 px: sin scroll; 950/900 px conservan scroll horizontal (10 columnas).
- Bump de caché `portfolio.js` v=31/v=17, `styles.css` v=46/v=41.
