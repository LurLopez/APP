# Diario — 2026-08-14

## 14:30 — Resumen: recuperación de contraseña ("olvidé mi contraseña")

Se solicitó implementar la opción de recuperar la contraseña: el usuario pide un código a su correo, lo introduce junto con la nueva contraseña (repetida para confirmar) y al terminar vuelve a la pantalla de inicio de sesión.

### Resultado

- **Backend** (`src/services/email.service.js`): `sendCode` genérico (asunto/cuerpo por tipo) con `sendVerificationCode` y nuevo `sendPasswordResetCode` (asunto "Restablece tu contraseña — Cifra"); respeta `MAIL_TO_OVERRIDE`.
- **Backend** (`src/services/auth.service.js`): `requestPasswordReset({email})` — si la cuenta existe, guarda código (mismo esquema: 6 dígitos, SHA-256, 15 min) y lo envía; si no existe responde igual (no filtra cuentas); `resetPassword({email, code, newPassword})` — valida código (6 dígitos, 5 intentos máx., consumido al acertar), contraseña (≥8), actualiza el hash y marca el correo como verificado (el usuario demostró acceso al correo).
- **Backend** (`db/repositories/userRepository.js`): `updatePassword(userId, passwordHash)`. Reutilizada la tabla `verification_codes` existente (sin migración).
- **API** (`auth.controller.js`, `auth.routes.js`): `POST /api/auth/forgot-password` y `POST /api/auth/reset-password`.
- **Frontend** (`public/auth.js`, `index.html`, `empresa.html`, `styles.css`): enlace "¿Olvidaste tu contraseña?" solo en la pestaña de login (`.forgot-row`); dos pasos nuevos en el modal — `auth-reset-request` (correo → "Enviar código") y `auth-reset-code` (código + nueva contraseña + repetir, botón "Cambiar contraseña", reenviar código y volver). Al completar: cierra el modal, toast de confirmación y reabre el login. Assets subidos a `?v=9` (CSS) y `?v=6` (auth.js).
- **Pruebas**: flujo completo contra la API con usuario temporal (registro → código insertado a propósito → reset → login con la nueva contraseña OK → login con la antigua 401 → código reutilizado rechazado); casos de borde (email inexistente sin filtrar, código mal formado, contraseña corta); correo real enviado a lurlopez13@gmail.com (llegó al SMTP de Gmail sin errores). Chrome headless vía CDP: 13/13 comprobaciones del flujo del modal (enlace visible solo en login, paso a paso, pantallas ocultas, reenvío, vuelta atrás, cierre) sin errores de consola en index y empresa.html.
- **Nota técnica**: el envío SMTP a Gmail tarda ~2 s; la petición responde tras el envío real (no es un cuelgue del frontend).

## 13:30 — Resumen: diagnóstico del código de verificación que no llegaba y preparación de envío real

Se reportó que al registrarse el código de verificación no llegaba al correo.

### Resultado

- **Causa raíz**: las variables `SMTP_HOST`, `SMTP_USER` y `SMTP_PASS` estaban vacías en `.env`, así que `email.service.js` caía al modo desarrollo y solo imprimía el código por consola del servidor; nunca se enviaba ningún correo.
- **Configuración para pruebas** (`.env`, `.env.example`): SMTP de Gmail (`smtp.gmail.com:587`, `SMTP_USER=lurlopez13@gmail.com`, `MAIL_FROM=Cifra <lurlopez13@gmail.com>`) y nueva variable `MAIL_TO_OVERRIDE=lurlopez13@gmail.com` que redirige todos los correos salientes a esa dirección aunque el registro sea con otro email (solo para pruebas; vacía en producción).
- **Código** (`src/services/email.service.js`): soporte de `MAIL_TO_OVERRIDE` en `sendVerificationCode` (con aviso en consola cuando se redirige); si no hay SMTP, se mantiene el fallback por consola.
- **Script de prueba** (`scripts/test-email.js`, nuevo, `npm run test:email`): envía un correo de prueba a `MAIL_TO_OVERRIDE` (o a la dirección pasada por argumento) para verificar el envío sin registrarse.
- **Pendiente**: el usuario debe generar una contraseña de aplicación de Gmail (https://myaccount.google.com/apppasswords, requiere verificación en 2 pasos) y ponerla en `SMTP_PASS` del `.env`; después `npm run test:email` para comprobar la llegada.

### Actualización (13:40)

- El usuario generó y aportó la contraseña de aplicación de Gmail; se puso en `SMTP_PASS` (`.env`, ignorado por git). `npm run test:email` envió correctamente un código de prueba (sin errores de Gmail), confirmado el envío real por SMTP. Pendiente solo de que el usuario confirme la recepción en su bandeja.

## 00:45 — Resumen: verificación por correo en el registro

Se solicitó que al registrarse un usuario con correo y contraseña se le envíe un código de verificación por email y deba introducirlo para validar su cuenta.

### Resultado

- **Base de datos** (`db/schema.sql`): columna `email_verified` (BOOLEAN, por defecto `false`) en `users` (con `ADD COLUMN IF NOT EXISTS` idempotente para bases existentes) y nueva tabla `verification_codes` (user_id, code_hash, attempts, expires_at, created_at) con índice por usuario.
- **Servicio de email** (`src/services/email.service.js`): nuevo, con nodemailer (SMTP configurable vía `SMTP_HOST/PORT/SECURE/USER/PASS` y `MAIL_FROM`); si no hay SMTP configurado (desarrollo), el código se imprime por consola.
- **auth.service**: `register` crea la cuenta sin sesión y envía el código (6 dígitos, SHA-256, 15 min, máx. 5 intentos); nuevos `verifyEmail` (marca `email_verified`, emite la sesión) y `resendVerificationCode`; `login` bloquea con 403 `EMAIL_NOT_VERIFIED` si la cuenta no está verificada.
- **API**: `POST /api/auth/register` (ya no inicia sesión), `POST /api/auth/verify`, `POST /api/auth/resend-code`; el errorHandler propaga un campo `code` para que el frontend distinga `EMAIL_NOT_VERIFIED` y `CODE_EXPIRED`.
- **Frontend**: el modal de autenticación tiene ahora dos pasos — credenciales y verificación (código, reenviar, cambiar correo); al hacer login con una cuenta sin verificar salta al mismo paso.
- **Pruebas**: flujo completo verificado contra la API (registro → login bloqueado 403 → código erróneo 400 → verificación 200 + sesión → login 200; reenvío de código; código caducado).
- **Pendiente**: configurar credenciales SMTP reales para enviar correos de verdad (hasta entonces el código sale por consola).

## 00:40 — Resumen: pestaña "Resultados" con histórico de 10-Q/10-K en el cribador

Se solicitó que al elegir una empresa (ej. TAP) en el cribador aparezca una nueva pestaña "Resultados" con todos los 10-Q y 10-K en orden, cada uno con opciones de descarga y vista previa.

### Resultado

- **Backend** (`src/services/edgar.service.js`): nuevo `getCompanyFilings(ticker)` que consulta el índice de *submissions* de EDGAR (`data.sec.gov/submissions/CIK{CIK}.json`, caché 6 h, límite 40). Se normaliza su formato columnar (cada campo es un array paralelo). Devuelve por filing: formType (10-Q/10-K), periodo, etiqueta (Q2 2026 / FY 2025), fecha de presentación, accession y URL del documento. `getFilingDocumentStream` sirve el documento (con resolución previa del PDF homónimo vía HEAD; si no existe, usa el documento primario HTML/XBRL).
- **Backend** (`src/api/routes/screener.routes.js`): `GET /api/screener/company/:ticker/filings` (lista) y `GET /api/screener/company/:ticker/filings/:accession/document` (proxy del PDF con `?download=1` para descarga forzada, `inline` para vista previa; valida accession contra el patrón y contra la lista del ticker).
- **Frontend** (`public/index.html`, `app.js`, `styles.css`): pestaña "Resultados" habilitada tras Cash flow; tabla con Formulario (badge 10-Q/10-K), Periodo, Periodo que cubre, Fecha de presentación y acciones "Vista previa"/"Descargar"; vista previa en modal con iframe (cierre por botón, clic fuera y Escape) y enlace "Abrir en pestaña nueva". La carga de filings es perezosa (solo al abrir la pestaña) y se resetea al cambiar de empresa.
- **Pruebas**: endpoints verificados con TAP (40 filings ordenados por fecha, 404/400 en casos inválidos, descarga e inline correctos).

### Nota

- Los filings actuales de la SEC usan el documento primario HTML/XBRL (no PDF); la vista previa lo renderiza igualmente en el iframe.
- El servidor `--watch` del usuario estaba colgado (no escuchaba en 3000); se reinició con `npm start`.

## 00:50 — Resumen: corrección de crash del servidor y modal de vista previa

El usuario reportó que "no aparece nada" al pulsar Vista previa o Descargar en la pestaña Resultados.

### Resultado

- **Causa raíz**: el `AbortSignal.timeout(60000)` del fetch del documento en EDGAR abortaba a mitad del streaming; el error emitido en el `Readable` no estaba manejado y **derrumbaba todo el proceso Node** (unhandled 'error' event), dejando la web sin servidor.
- **Correcciones** (`src/services/edgar.service.js`, `src/api/routes/screener.routes.js`): el timeout ahora se cancela al recibir las cabeceras (`AbortController` + `clearTimeout`), y el stream tiene manejador de error (502 si no se han enviado cabeceras; corte limpio del socket si ya se enviaron), por lo que un fallo de EDGAR ya no puede tumbar el servidor.
- **Frontend** (`styles.css`): el modal de vista previa tenía `display: grid` sobre el atributo `hidden` (la regla de autor anula la del navegador), así que podía quedarse siempre visible y no cerrarse; añadido `.filings-preview-backdrop[hidden] { display: none }`.
- **Pruebas**: servidor sirve el documento (200) y permanece vivo tras la descarga; flujo de preview y download verificado con curl.

## 01:10 — Resumen: verificación con navegador automatizado y blindaje contra caché

El usuario seguía viendo "no aparece nada" tras las correcciones del crash.

### Resultado

- **Verificación automatizada** (Chrome headless vía CDP, script en `/tmp/opencode/cdp-test.mjs`): se reprodujo el flujo completo — cargar TAP, pestaña Resultados (40 filas), clic en Vista previa (modal abre, iframe carga el documento XBRL), cierre del modal, y clic en Descargar (el archivo `tap-q-2026.htm` se descarga). Sin errores de consola. **El código funciona correctamente**; el problema del usuario era de caché del navegador o página abierta desde antes.
- **Cache-busting** (`public/index.html`): `styles.css`, `app.js` y `auth.js` ahora se cargan con `?v=3` para forzar la versión nueva.
- **Feedback en el modal** (`app.js`, `styles.css`, `index.html`): indicador "Cargando documento…" con spinner sobre el iframe (se oculta al cargar) y aviso a los 20 s si el documento tarda, para que nunca quede un modal en blanco sin explicación.
- **Dato técnico**: en Chrome, `hidden` sí oculta elementos con `display: grid` (la regla UA gana), así que el fallo del modal no era el CSS sino el crash del servidor.

## 11:00 — Resumen: descargas en PDF real y diagnóstico del problema del usuario

El usuario seguía sin ver nada en Vista previa/Descargar (usa Chrome) y pidió que la descarga sea en PDF.

### Resultado

- **Diagnóstico**: con Chrome headless vía CDP se reprodujo el flujo completo contra el servidor en marcha (cargar TAP → pestaña Resultados con 40 filas → Vista previa abre el modal y carga el documento → cierre correcto; Descargar guarda el archivo). Sin errores de consola. El servidor del usuario (PID 6962, arrancado a las 10:46) estaba en marcha con código correcto; el problema persistente es de la pestaña/Chrome del usuario (caché o sesión antigua), no del código.
- **Descarga en PDF real** (`edgar.service.js`): el flujo de documento ahora genera PDF de verdad:
  1. Busca un PDF real en el filing vía `index.json` de EDGAR (caché 24 h, empareja por nombre del documento primario, fallback al PDF más largo).
  2. Si no existe (los 10-Q/10-K modernos son solo HTML/XBRL), convierte el HTML a PDF con `google-chrome --headless --print-to-pdf` (binario configurable con `CHROME_BIN`), guardado en `uploads/generated/filings/` con caché permanente.
  3. Fallback: sirve el HTML original.
- El nombre de descarga es ahora siempre `tap-q-2026.pdf` (y similares) con `Content-Type: application/pdf`.
- **Nota de entorno**: el servidor del usuario no recarga el código al cambiar (usa `npm start` sin --watch); se reinició con el código nuevo (PID 8540).

## 11:07 — Resumen: perfil de empresa antes de los estados financieros

Se solicitó que al elegir una empresa en el cribador aparezca primero un perfil con métricas como dividendo y capitalización bursátil, con una estética coherente con la aplicación y sin el bloque ESG de la referencia.

### Resultado

- **Backend** (`src/services/market.service.js`, `src/services/edgar.service.js`): la respuesta existente de la empresa incluye ahora un perfil construido con datos SEC EDGAR y una consulta cacheada de Yahoo Finance para cotización, variación diaria y anual, rango de 52 semanas, volumen, dividendos, beta, fecha de OPV y serie del gráfico. La capitalización se calcula con el precio y las acciones reportadas más recientes.
- **Frontend** (`public/index.html`, `public/app.js`, `public/styles.css`): se añadió el perfil inmediatamente después de la cabecera de la empresa y antes de las pestañas de cuenta de resultados, balance, cash flow y resultados. Incluye cotización con gráfico, tarjetas de Informe e Información y descripción corporativa. No se añadió ningún bloque ESG.
- **Verificación**: endpoint de `TAP` comprobado con cotización, capitalización, dividendo y sector; Chrome headless comprobado en escritorio y móvil, sin errores de consola ni desbordamiento horizontal.

## 11:05 — Resumen: la SEC bloqueaba a Chrome headless; PDFs ahora con User-Agent declarado

El usuario reportó que el PDF descargado contenía la página de bloqueo de la SEC ("Your Request Originates from an Undeclared Automated Tool").

### Resultado

- **Causa**: Chrome headless usa el User-Agent `HeadlessChrome/...`, que la SEC identifica como herramienta automática no declarada y responde con una página de bloqueo; el PDF generado era esa página (46 KB, 2 páginas).
- **Corrección** (`edgar.service.js`, `generateFilingPdf`): se añade `--user-agent="Cifra contacto@cifra.local"` a la invocación de Chrome (mismo UA declarado que ya usan los `fetch` directos). El PDF generado ahora contiene el informe real (2,9 MB, 8 páginas, "FORM 10-Q" / "MOLSON COORS").
- **Caché**: borrados los PDFs mal generados en `uploads/generated/filings/`; la caché permanente funciona (segunda descarga en 0,015 s).
- **Pruebas**: descarga y preview devuelven `application/pdf` con el contenido correcto; verificado con `pdftotext`.

## 11:20 — Resumen: vista previa a prueba de fallos (visores PDF en iframes)

El usuario confirmó que la descarga en PDF ya se ve, pero la vista previa seguía sin mostrar nada (Chrome, PDF dentro de iframe).

### Resultado

- **Causa probable**: el visor PDF interno de Chrome dentro de un iframe puede no disparar el evento `load` o no renderizar según configuración/extensiones (p. ej. Adobe Acrobat); el indicador "Cargando documento…" se quedaba tapando el contenido.
- **Cambios** (`app.js`, `index.html`, `styles.css`):
  - El indicador de carga se oculta al disparar `load` **o** a los 6 s como máximo (nunca queda bloqueando).
  - Si a los 6 s no se ha confirmado la carga, aparece una barra inferior "¿No ves el documento? Abrir en pestaña nueva ↗" (enlace directo al PDF, funciona siempre).
  - El botón "Abrir en pestaña nueva" del encabezado del modal ya existía y se mantiene.
- **Pruebas**: flujo verificado con Chrome headless (modal abre, indicador se oculta, sin errores de consola). No se puede verificar el render del visor PDF en headless (no incluye el visor); se confió en el fallback.
- **Nota**: los assets están en `?v=4` (cambio realizado por el usuario para forzar la recarga).

## 11:40 — Resumen: vista previa por imágenes de páginas (sin iframes ni visor PDF)

El usuario confirmó que el visor PDF de Chrome no renderiza dentro del iframe (en pestaña nueva sí). Se sustituyó la vista previa por imágenes de las páginas del PDF, que funcionan en cualquier navegador.

### Resultado

- **Backend** (`edgar.service.js`): refactor — `getFilingPdfPath()` unifica la obtención del PDF en disco (PDF real de la SEC vía `index.json` si existe, o generación con Chrome), con caché en `uploads/generated/filings/`; `getFilingDocumentStream()` sirve ahora desde el archivo (más robusto que streamear desde la SEC); nuevo `getFilingPreview()` que renderiza el PDF a PNG por página con `pdftoppm` (100 DPI, caché permanente en `uploads/generated/filings/previews/{accession}/`).
- **API** (`screener.routes.js`): `GET .../filings/:accession/preview` (JSON con nº de páginas) y `.../preview/pages/:page` (PNG; comparación numérica para soportar el padding `page-01.png` de `pdftoppm`; 404 si no existe).
- **Frontend** (`index.html`, `app.js`, `styles.css`): el modal de vista previa muestra las páginas como imágenes (fondo gris, ancho máx. 860 px, scroll, carga perezosa); se eliminó el iframe y el aviso anterior; el título muestra "· 74 páginas"; "Abrir en pestaña nueva" se mantiene; errores de generación muestran mensaje con enlace.
- **Pruebas**: Chrome headless verifica el flujo completo (modal, 74 imágenes cargadas, cierre limpio, sin errores de consola); páginas 1/2/74 sirven 200 `image/png`.

## 11:55 — Resumen: rediseño en dos páginas (Inicio y Empresa) clonando la estructura de referencia

Se solicitó clonar la estructura de dos imágenes de referencia manteniendo el diseño actual de Cifra: una página de Inicio (logo centrado, buscador y cuadrícula de tarjetas de empresas) y una página de Empresa (menú lateral, cabecera de empresa y perfil con gráfico), con dos URLs distintas. Al pulsar una tarjeta o un resultado de búsqueda se navega a `/empresa/:ticker` abriendo directamente el apartado **Perfil**.

### Resultado

- **Rutas** (`server.js`): nueva ruta `GET /empresa/:ticker` que sirve `public/empresa.html`; `/` sigue sirviendo el nuevo `public/index.html`.
- **Inicio** (`public/index.html`, `public/app.js`, `styles.css`): sin menú lateral; barra superior con accesos Favoritos/Filtros/Cartera (placeholders con toast), logo + insignia Beta centrados, buscador con acceso "Ctrl K" y cuadrícula de 6 tarjetas de demostración (Apple, Microsoft, J&J, Coca-Cola, NVIDIA, 3M) que navegan a `/empresa/:ticker`. Debajo se conservan la subida manual de PDF, el análisis con los 3 agentes y el histórico.
- **Empresa** (`public/empresa.html`, `public/empresa.js`): menú lateral reducido a Favoritos, Alertas de precio, Cartera, Perfil, Informes trimestrales, Valoración, Datos financieros y Accionariado (sin Resumen, Referidos, Filtros, Super inversores, Resultados, Análisis y noticias, ni los bloques Quality/Crecimiento/Value/Dividendo); banner de aviso de prueba; cabecera de empresa (logo, nombre, bolsa · sector, chip de ticker, favorito, informes y alerta); tarjeta de cotización con sparkline. El apartado **Perfil** reúne todo: tarjeta Informe (izquierda), gráfico de cotización grande (derecha, donde antes estaba el ESG, que se elimina), Información y Descripción debajo (sin bloques Valoración/Factores). Informes trimestrales y Datos financieros reutilizan las tablas de filings y estados existentes; el resto de apartados muestran placeholder.
- **Gráfico de cotización** (`market.service.js`, `screener.routes.js`): nuevo endpoint `GET /api/screener/company/:ticker/chart?range=3m|6m|1y|3y|5y|10y|all` (Yahoo Finance, intervalos según rango, caché 5 min); render SVG propio con área, etiquetas de años, eje de precios y etiqueta del último precio.
- **Autenticación** (`public/auth.js`): se hizo defensiva (null-checks) para funcionar en ambas páginas, con o sin tarjeta de cuenta en el menú lateral.
- **Verificación**: `node --check` correcto en todos los JS; servidor arrancado y comprobado — `/`, `/empresa/TAP`, `/api/screener/search`, `/api/screener/company/KO` y el gráfico devuelven 200; 6 tarjetas en Inicio, 8 enlaces de navegación y 4 secciones en Empresa. Se detectó y evitó un servidor `--watch` antiguo que seguía escuchando en el puerto 3000.
- **Pendiente**: favoritos/alertas/cartera son placeholders (decisión del usuario: cuadrícula de demostración mientras no existan favoritos).

## 12:30 — Resumen: corrección de la página Empresa sin estilos ni scripts (rutas de assets)

El usuario reportó que al elegir una empresa la página se veía "muy muy mal".

### Resultado

- **Causa raíz**: en `public/empresa.html` los assets se referenciaban con rutas relativas (`styles.css?v=5`, `empresa.js?v=5`, `auth.js?v=5`). Al abrir la página en `/empresa/:ticker`, el navegador pedía `/empresa/styles.css` y `/empresa/empresa.js`, y la nueva ruta dinámica `GET /empresa/:ticker` capturaba esas peticiones devolviendo el propio `empresa.html` con `Content-Type: text/html` (SyntaxError "Unexpected token '<'" en los scripts; CSS inexistente). Resultado: página desestructurada sin estilos y sin lógica.
- **Correcciones**: rutas absolutas (`/styles.css?v=5`, `/empresa.js?v=5`, `/auth.js?v=5`) en `empresa.html` e `index.html`; la ruta `GET /empresa/:ticker` de `server.js` ahora valida el ticker con el patrón `^[A-Z0-9.-]{1,10}$` y redirige a `/` si no cumple, para no tragarse subrutas arbitrarias.
- **Verificación** (Chrome headless vía CDP): `/empresa/KO` en escritorio (1440 px) carga los datos reales (COCA COLA CO, NYSE, cotización 87,84 $, gráfico con 18 elementos SVG), sin errores de consola, sin desbordamiento horizontal, con la cabecera+cotización en fila, Informe y gráfico lado a lado e Información/Descripción debajo. También comprobado en ventana estrecha (780 px) donde el layout apila correctamente.

## 12:45 — Resumen: menú lateral de Empresa dividido en fijos y de empresa

Se solicitó dividir el menú lateral de la página de empresa en dos partes: arriba los apartados fijos (Favoritos, Alertas de precio, Cartera y Perfil — Cartera confirmada como fija) y, un poco más abajo, una cabecera con el logo y nombre de la empresa consultada, seguida del resto de apartados (Informes trimestrales, Valoración, Datos financieros y Accionariado).

### Resultado

- **Frontend** (`public/empresa.html`, `public/empresa.js`, `public/styles.css`): el menú lateral se reorganizó en dos bloques `.nav-section` separados por una cabecera `.company-nav-head` que muestra logo (inicial), nombre y ticker de la empresa actual, rellenada por `renderCompany` al cargar los datos. En modo contraído la cabecera se oculta (y se restaura en móvil). Assets subidos a `?v=6` para forzar la recarga.
- **Verificación** (Chrome headless): 2 secciones de navegación (4 enlaces fijos + 4 de empresa), cabecera de empresa visible con "C" / "COCA COLA CO" / "KO", perfil activo por defecto, sin errores de consola ni desbordamiento.

## 12:50 — Resumen: Perfil pasa al bloque de empresa del menú lateral

Se solicitó que «Perfil» aparezca también en el apartado de la empresa (debajo de la cabecera con logo y nombre), en lugar de entre los fijos.

### Resultado

- **Frontend** (`public/empresa.html`): Perfil se movió del bloque fijo al bloque de empresa como primer enlace tras la cabecera `.company-nav-head`. El menú queda: fijos (Favoritos, Alertas de precio, Cartera) → cabecera de empresa → de empresa (Perfil activo por defecto, Informes trimestrales, Valoración, Datos financieros, Accionariado).
- **Verificación** (Chrome headless): 2 secciones de navegación (3 + 5 enlaces), perfil activo, cabecera de empresa visible, sin errores ni desbordamiento.

## 13:00 — Resumen: logos de empresas en los resultados del buscador

Se solicitó que al buscar una empresa en el buscador (tanto en Inicio como en Empresa) aparezcan los logos de las empresas junto a los resultados.

### Resultado

- **Fuente de logos**: `https://companiesmarketcap.com/img/company-logos/64/{TICKER}.webp` (por ticker, verificado 200; la alternativa IEX Cloud devuelve 403).
- **Frontend** (`public/app.js` y `public/empresa.js`): cada resultado del dropdown ahora incluye `<img class="search-result-logo">` con el logo de la empresa; si falla la carga (empresa sin logo en la fuente), se sustituye por un cuadrado con la inicial del nombre (fallback, mismo estilo que el logo de la página de empresa).
- **CSS** (`public/styles.css`): logo de 24 px redondeado con `object-fit: cover`; el nombre queda a la izquierda junto al logo y el ticker anclado a la derecha (`margin-left: auto`); estilo del fallback con inicial.
- **Verificación**: `node --check` correcto en ambos scripts. Cobertura completa de empresas de EE. UU. de la fuente; los logos sin cargar caen al fallback con la inicial.

## 12:26 — Resumen: mejoras del gráfico (media 100 sesiones, pantalla completa y precio bajo el cursor)

Se solicitó tres mejoras para el gráfico de cotización de la página de empresa: una media de 100 sesiones, modo pantalla completa y que al pasar el cursor muestre el precio del día.

### Resultado

- **Media móvil 100 sesiones** (`empresa.html`, `empresa.js`, `styles.css`): botón "MA 100" en la cabecera del gráfico (a la derecha de los rangos) que activa/desactiva la línea (azul, `computeMovingAverage` con ventana de 100 puntos sobre los cierres). Solo se dibuja si la serie tiene al menos 100 puntos (los rangos 3M/6M no llegan y la línea no aparece).
- **Pantalla completa** (`empresa.html`, `empresa.js`, `styles.css`): botón con icono de expandir junto a "MA 100" que hace `requestFullscreen` sobre la tarjeta `.chart-block`; en fullscreen la tarjeta ocupa toda la pantalla, el gráfico se re-renderiza a la altura disponible (vía `fullscreenchange`) y el botón vuelve a salir del modo. Si el navegador no lo soporta, avisa con toast.
- **Precio bajo el cursor** (`empresa.js`, `styles.css`): al mover el ratón sobre el SVG se localiza el punto más cercano a la posición (escalas guardadas en `chartScale`), se dibuja una línea vertical discontinua con un círculo en el precio y un tooltip (fecha en español + precio en 2 decimales) que se voltea cerca del borde derecho. Se oculta al salir del gráfico o al re-renderizar.
- **Verificación**: `node --check` correcto; assets subidos a `?v=7` para forzar recarga. No comprobado visualmente en navegador.

## 12:40 — Resumen: corrección de la media de 100 sesiones (ahora sobre cierres diarios)

El usuario reportó que la línea de media de 100 sesiones aparecía pero no era correcta.

### Resultado

- **Causa**: la MA100 se calculaba en el frontend sobre los puntos mostrados, y en los rangos 3Y/5Y esos puntos son semanales y en 10Y/ALL mensuales; la "media de 100 sesiones" resultante promediaba 100 semanas o 100 meses (2-8 años de retraso) en lugar de 100 sesiones reales.
- **Backend** (`src/services/market.service.js`): `getChartSeries(ticker, range, withMovingAverage)` ahora, cuando se pide (parámetro `ma=1`), obtiene la serie diaria de Yahoo para el mismo rango (verificado: Yahoo sí devuelve diario para 3mo…max) y calcula la MA de 100 cierres diarios (ventana deslizante, primer punto en la sesión 100). Si el intervalo mostrado ya es diario (3M/6M/1Y) la calcula sin fetch adicional. Caché propia `chart-ma:` (TTL 5 min). Ante fallo del fetch diario devuelve `maPoints: []` sin romper el gráfico.
- **API** (`src/api/routes/screener.routes.js`): `GET .../chart?range=X&ma=1` devuelve `maPoints`.
- **Frontend** (`public/empresa.js`): `loadChart` pide siempre `&ma=1` y guarda `chartMaPoints`; `renderPriceChart` dibuja la línea con esos puntos (resolución diaria, uno por sesión) en lugar de recalcular, e incluye los valores de la MA en la escala Y para que nunca se recorte. Eliminado el cálculo local.
- **Verificación**: MA del endpoint validada numéricamente contra Yahoo — el último valor (80,640017) coincide exactamente con la media de los últimos 100 cierres diarios (diferencia 0,0). En navegador headless: 5Y dibuja la línea con 1.156 segmentos (uno por sesión, antes eran ~162 semanales), 1Y con 153, y en 3M no aparece (64 sesiones < 100, correcto). Assets a `?v=8`; servidor reiniciado (el que servía en 3000 era un proceso sin `--watch` con código anterior; quedó uno solo con `--watch`).

## 13:15 — Resumen: logo real de la empresa en el perfil y en el menú lateral

Se solicitó que las imágenes (logos) que ya aparecen en el buscador también se muestren en el perfil de la empresa y en la barra lateral izquierda, en lugar de la inicial.

### Resultado

- **Frontend** (`public/empresa.js`): nuevo helper `renderCompanyLogo(el, ticker, fallbackLetter)` que pinta un `<img>` con el logo de `companiesmarketcap.com` y, si falla la carga, vuelve a mostrar la inicial como fallback (mismo comportamiento que el buscador). Se usa en la cabecera del perfil (`#company-logo`) y en la cabecera de empresa del menú lateral (`#sidebar-company-logo`).
- **CSS** (`public/styles.css`): las imágenes rellenan el cuadrado del logo (`object-fit: cover`, `border-radius: inherit`) tanto en 46 px (cabecera) como en 30 px (lateral).
- **Verificación**: `node --check` correcto; assets subidos a `?v=8` para forzar la recarga.
